---
phase: visual-rebuild
task: 0
total_tasks: 5
status: not_started
last_updated: 2026-03-18T22:12:07.345Z
---

<current_state>
The game has 196K+ LOC with all systems built (vocabulary, quests, factions, skill trees, achievements, etc.) but the VISUAL LAYER is completely broken. The Phaser canvas shows black squares everywhere because the terrain auto-tiling uses frame indices that don't match the actual Kenmi spritesheet dimensions. Three patch attempts failed.

The user wants EVERYTHING visual nuked and rebuilt from scratch using ONLY Kenmi pixel art. Keep React overlays (HUD, menus, dialogue) untouched. Keep the loading screen (koi fish) untouched.
</current_state>

<completed_work>

This session shipped:
- v8.0 Visual Overhaul (Phases 38-43) — attempted but terrain rendering is broken
- v9.0 Content Depth (Phases 44-46) — NPC dialogue, quests, vocabulary
- Tier 5 Learning Systems — skill trees, grammar, quizzes, achievements
- Tier 6 Narrative — factions, lore codex, gifts, journal
- Tier 7 Infrastructure — code splitting, save system, content pipeline
- Tier 8 AAA Polish — accessibility, game feel, endgame
- All systems WIRED into game menus and gameplay triggers
- 3 failed attempts to fix black squares by patching MapLoader frame indices
</completed_work>

<remaining_work>

## Task 1: Strip WorldScene Visual Layer
- Read `src/game/scenes/WorldScene.js` create() method completely
- Remove ALL old sprite creation (ground tiles, buildings, objects, player, NPCs)
- Keep: camera setup, collision, EventBus listeners, subsystem initialization

## Task 2: New Clean Terrain Renderer
- DO NOT use frame indices from spritesheets — they're unreliable
- Option A: Use `scene.add.tileSprite()` with a single Kenmi sand texture to fill the ground
- Option B: Load individual tile PNGs as `image` type (not spritesheet) and place them
- Verified tile files: desert-beach-tiles-1.png (80x48), desert-water-tiles-1.png (96x48), desert-grass.png (48x80)
- For water areas: use a solid blue tint or load water as a separate full image

## Task 3: Kenmi Player Sprite
- Current: old 128x128 silhouette still showing (Player.js)
- Replace with: `kenmi-char-knights-swordman` (already in KENMI_CATALOG as spritesheet)
- Need to verify frame layout and set correct walk animation frames

## Task 4: Kenmi NPC Sprites
- Current: mix of old silhouettes and Kenmi sprites
- Replace ALL with Kenmi character spritesheets from KENMI_CATALOG
- NPC_KEY_MAP in spriteKeyMap.js already maps NPCs to Kenmi keys

## Task 5: Remove Old Assets
- Delete old placeholder PNGs from public/assets/sprites/
- Remove old load calls from BootScene.js
- Remove old DOM overlay rendering for in-game elements

## Also requested:
- Add 4th learning path "Polymath" (المتعلم) to PathChoice.jsx + playerSlice.js
</remaining_work>

<decisions_made>

- NUKE AND REBUILD — don't patch the old system, start visual layer from scratch
- KEEP OVERLAYS — React DOM overlays (HUD, quest tracker, menus) stay untouched
- KEEP LOADING SCREEN — koi fish + Gogo Arabic + Yala Arabic must NOT be touched
- KENMI ONLY — everything visual must come from Kenmi pixel art assets
- NO FRAME INDICES — the spritesheet frame math is fundamentally broken, find alternative approach
- 4TH PATH — add "Path of the Polymath" combining all three learning focuses
</decisions_made>

<blockers>
- The core issue: KENMI_CATALOG loads tiles as spritesheets with frameWidth:16/frameHeight:16, but MapLoader uses incorrect frame indices that exceed actual frame counts or reference wrong positions. 3 fix attempts failed.
- Root cause: beach-tiles has 5 cols, water-tiles has 6 cols, grass has 3 cols — the auto-tiling code mixes constants across sheets.
- Solution: don't use the spritesheet frame approach at all. Use tileSprite or load as individual images.
</blockers>

<context>
The user has been extremely patient. They just want to see a clean Kenmi pixel art world with no black squares. The approach should be the SIMPLEST possible thing that works — even a solid sand fill with water rectangles and grass patches would be better than the current broken mess. Build incrementally: first get a clean ground, then add buildings, then player, then NPCs. Test visually at each step.

Key Kenmi assets verified to exist:
- public/assets/kenmi/desert/tiles/desert-beach-tiles-1.png (80x48 — sand)
- public/assets/kenmi/desert/tiles/desert-water-tiles-1.png (96x48 — water)
- public/assets/kenmi/desert/tiles/desert-grass.png (48x80 — grass)
- public/assets/kenmi/characters/ (knight, goblin, orc spritesheets)
- public/assets/kenmi/desert/buildings/ (houses, temples, pergolas)
</context>

<next_action>
Start with: Read WorldScene.js create() completely. Then write a NEW _renderKenmiTiles that uses `this.scene.add.image(px, py, 'desert-beach-tiles-1')` (the PLAIN IMAGE key loaded in BootScene line 224, NOT the spritesheet key) scaled to fill each 64x64 tile. This bypasses the broken spritesheet frame system entirely. Test with just sand fill first — confirm zero black squares — then layer water and grass on top.
</next_action>
