---
phase: 103-mobile-cloud-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md
  - .planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A dead-code audit verdict for src/services/sync.js + /api/game/* exists with human checkpoint approval"
    - "A bundle-size baseline number for the current build exists, committed before any rex install"
  artifacts:
    - path: ".planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md"
      provides: "Documented audit verdict and chosen disposition (DELETE / DEPRECATE / KEEP) for sync.js + game routes"
      contains: "## Verdict"
    - path: ".planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md"
      provides: "dist/ chunk sizes captured before any new dependency lands"
      contains: "Baseline"
  key_links:
    - from: ".planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md"
      to: "later plans (06, 07)"
      via: "verdict determines whether new /api/v1/cloudsave/* coexists with /api/game/* or replaces it"
      pattern: "Verdict|DELETE|DEPRECATE|KEEP"
---

<objective>
Audit and baseline before any code lands. Two outputs:
1. A verdict on the existing dead-code candidates (`src/services/sync.js` + `/api/game/save|load|resolve` server routes + `src/components/HUD/SyncIndicator.jsx`), gated by a human checkpoint.
2. A pre-install bundle-size baseline so the rex VirtualJoyStick install gate in Plan 03 has a number to compare against.

Purpose: We do not write `/api/v1/cloudsave/*` until we know whether the prior sync attempt is dead, deprecated, or live. We do not install `phaser3-rex-plugins` until we know what the cost is.

Output: Two markdown files in the phase directory recording verdicts + numbers. No source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-mobile-cloud-sync/103-CONTEXT.md
@.planning/phases/103-mobile-cloud-sync/103-RESEARCH.md

<interfaces>
<!-- Files implicated by the audit. Auditor MUST read these and grep callers. -->

src/services/sync.js — imports `* as api from './api.js'` and calls `api.saveGame()`, `api.loadGame()`, `api.resolveConflict()`. Exposes `syncGameState()` and presumably listener init.

src/services/api.js — exports `saveGame`, `loadGame`, `resolveConflict` pointing at `/game/save`, `/game/load`, `/game/resolve` (string concat with `/api/` prefix in `request()`).

src/components/HUD/SyncIndicator.jsx — imports `syncGameState` from `services/sync.js`. This is the ONLY known consumer found in research; auditor must confirm.

server/src/routes/game.js — Express router mounted at `${API_VERSION}/game` and ALSO at `/api/game` (legacy unversioned). Owns `/save|/load|/resolve`.

src/services/saveManager.js — separate 3-slot localStorage save system (`gogo_save_<n>` keys). Confirmed AUTHORITATIVE for cloud sync per RESEARCH Open Q #2. NOT to be touched in this audit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit src/services/sync.js + /api/game/* + SyncIndicator usage</name>
  <files>.planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/services/sync.js (full file)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/services/api.js (full file)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/components/HUD/SyncIndicator.jsx (full file)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/server/src/routes/game.js (full file)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/server/src/controllers/ — grep for game controller
  </read_first>
  <action>
    Create `.planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md`. The file must contain the following sections in this order:

    1. **Consumer Graph** — Run these exact greps and paste raw output:
       - `grep -rn "from.*services/sync\|require.*services/sync" src/ --include="*.js" --include="*.jsx"`
       - `grep -rn "syncGameState\|api\.saveGame\|api\.loadGame\|api\.resolveConflict" src/ --include="*.js" --include="*.jsx"`
       - `grep -rn "SyncIndicator" src/ --include="*.js" --include="*.jsx"`
       - `grep -rn "/game/save\|/game/load\|/game/resolve" src/ server/src/ --include="*.js" --include="*.jsx"`

    2. **Mount Status** — Document whether `server/src/app.js` still mounts `/api/v1/game` and `/api/game` (legacy). Quote the exact `app.use(...)` lines.

    3. **Last Touched** — `git log -1 --format='%ci %s' -- src/services/sync.js` and same for `server/src/routes/game.js` and `src/components/HUD/SyncIndicator.jsx`. Paste raw output.

    4. **Runtime Reachability** — Is `SyncIndicator` rendered? Grep `<SyncIndicator` in src/. If unmounted in the React tree, sync.js is unreachable at runtime regardless of imports.

    5. **Verdict** — Pick ONE of three and justify in <=5 sentences:
       - `DELETE` — sync.js, game routes, SyncIndicator are dead code; remove in a Plan-06 subtask before /api/v1/cloudsave/* lands.
       - `DEPRECATE` — keep mounted but mark deprecated; new /api/v1/cloudsave/* coexists; remove in a future phase.
       - `KEEP` — actively reachable; new /api/v1/cloudsave/* must coexist; do not touch sync.js.

    6. **Impact on later plans** — One bullet per affected plan (Plan 06 cloud save endpoints, Plan 07 conflict UI). Reference plan number explicitly.

    No subjective language. Every claim cites file:line or grep output. Use `## Verdict` as the section heading verbatim so the grep gate in Task 3 matches.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md && grep -c "^## Verdict$" .planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md | grep -q "^1$" && grep -E "^(DELETE|DEPRECATE|KEEP)" .planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md` exists
    - Contains exactly one line matching `^## Verdict$`
    - Verdict body contains one of the literals `DELETE`, `DEPRECATE`, or `KEEP` at the start of a line
    - Six numbered sections present (Consumer Graph, Mount Status, Last Touched, Runtime Reachability, Verdict, Impact on later plans)
    - Every grep result section contains raw grep output (not summarised prose)
  </acceptance_criteria>
  <done>Audit file written with verdict and grep evidence; ready for human checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-install bundle baseline</name>
  <files>.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/package.json — confirm `build:analyze` or `build` script exists
    - /Users/theshumba/Documents/GitHub/gogo-arabic/vite.config.js — confirm rollupOptions.output settings
  </read_first>
  <action>
    Run `npm run build` (or `npm run build:analyze` if it exists in package.json). Capture the dist/ chunk sizes. Write `.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md` with the following structure:

    1. **Build command run** — exact command + exit code.
    2. **Total dist size** — `du -sh dist/` output.
    3. **Per-chunk sizes** — `find dist/assets -name "*.js" -exec ls -la {} \; | sort -k5 -n -r` (top 20 chunks by size).
    4. **Vendor chunk total** — `find dist/assets -name "*vendor*" -exec du -ch {} + | tail -1` (or grep equivalent for Phaser/React chunks).
    5. **Gzipped sizes** — for top-5 chunks: `gzip -c <file> | wc -c` for each, label rows.
    6. **Date + commit SHA** — `git rev-parse HEAD` + `date -u`.

    No source code changed. If `npm run build` fails (lockfile mismatch, etc.), STOP and surface error verbatim in the file under `## Build Failure` heading. Do not invent numbers.

    The Plan 03 install gate compares against the JS chunk total and vendor chunk total. Specifically: a `phaser3-rex-plugins/plugins/virtualjoystick-plugin.js` standalone import should add <100KB to a vendor chunk. Plan 03 will repeat this measurement post-install and diff.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md && grep -E "Total dist size|Per-chunk sizes|commit SHA" .planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md | grep -v '^#' | wc -l | awk '{exit ($1>=3)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the path above
    - Contains literal substrings "Total dist size", "Per-chunk sizes", "commit SHA"
    - Build command exit code recorded (0 OR documented failure under `## Build Failure`)
    - At least 5 chunk size rows captured (or documented build failure)
  </acceptance_criteria>
  <done>Baseline numbers committed to phase dir; comparable to Plan 03's post-install measurement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies dead-code verdict</name>
  <what-built>
    Two audit artifacts:
    - `.planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md` — verdict on src/services/sync.js + /api/game/* routes + SyncIndicator
    - `.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md` — pre-install chunk sizes
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/103-mobile-cloud-sync/103-01-AUDIT.md`.
    2. Confirm the Consumer Graph grep output matches what you expect (one consumer: SyncIndicator.jsx).
    3. Confirm the Verdict line is one of DELETE / DEPRECATE / KEEP and the 5-sentence justification is sound.
    4. Open `.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md`. Confirm numbers exist (not "build failed" — if build failed, you may approve to proceed but Plan 03 install gate will reject).
    5. Approve the verdict OR override with a different choice (typing "DELETE" / "DEPRECATE" / "KEEP" + reason).
  </how-to-verify>
  <resume-signal>Type "approved" to lock the AUDIT.md verdict, OR type one of "DELETE" / "DEPRECATE" / "KEEP" + 1-line reason to override.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (audit-only plan) | No untrusted input crosses boundaries; this plan writes documentation files only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Information Disclosure | Audit document content | accept | Audit documents committed to repo contain no secrets — grep results + git log output only |
</threat_model>

<verification>
- AUDIT.md exists with `^## Verdict$` heading and one of DELETE/DEPRECATE/KEEP at start of line
- BUNDLE-BASELINE.md exists with chunk sizes OR documented build failure
- Human checkpoint approved verdict
- No source code modified (`git diff src/ server/src/ public/` returns no changes)
</verification>

<success_criteria>
- Plan 06 (cloud save endpoints) has a clear directive: delete dead sync.js, deprecate alongside, or keep
- Plan 03 (rex install) has a quantitative pre-install baseline to compare against
- No new dependencies installed in this plan
</success_criteria>

<output>
Create `.planning/phases/103-mobile-cloud-sync/103-01-SUMMARY.md` when done — record the verdict literal, the bundle total in KB, and any blockers surfaced.
</output>
