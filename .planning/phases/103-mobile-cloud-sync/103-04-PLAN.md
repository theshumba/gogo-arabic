---
phase: 103-mobile-cloud-sync
plan: 04
type: execute
wave: 2
depends_on:
  - 103-02
files_modified:
  - public/sw.js
  - public/manifest.json
  - public/__tests__/sw.test.js
  - src/services/swRegistration.js
autonomous: true
requirements:
  - MOB-06
  - MOB-07
must_haves:
  truths:
    - "Service worker uses NetworkOnly (not networkFirst) for /api/ — cross-user data leak on shared devices fixed"
    - "PWA manifest validates against W3C schema and includes 192/512 icons + maskable purpose"
    - "Service worker caches app shell (HTML, JS/CSS, fonts, sprites, audio) for offline boot"
    - "Last-zone restoration on offline boot continues to work via existing IndexedDB Redux state"
  artifacts:
    - path: "public/sw.js"
      provides: "Service worker with /api/ network-only strategy and stale-while-revalidate for JS/CSS"
      contains: "NetworkOnly"
    - path: "public/manifest.json"
      provides: "PWA install manifest validated against W3C"
      contains: "standalone"
    - path: "public/__tests__/sw.test.js"
      provides: "SW strategy assertions and regression guard for /api/ caching"
      contains: "networkFirst.*\\/api"
  key_links:
    - from: "public/sw.js"
      to: "browser fetch handler"
      via: "/api/ requests must never write to a cache; network failures must NOT return cached auth-bound data"
      pattern: "API_PREFIX|NetworkOnly|networkOnly"
---

<objective>
[BLOCKING] Fix the cross-user data-leak bug in `public/sw.js:64` — the current `networkFirst(request, API_CACHE)` writes /api/ responses to a cache, which means a logged-out user can briefly see another user's `/api/user/profile` on a shared device. Switch /api/ handling to `NetworkOnly` (no cache read, no cache write) and add a regression-guard test.

Also: confirm the existing manifest.json is W3C-valid (already complete per RESEARCH) and validate the offline boot path. Do NOT migrate to `vite-plugin-pwa` — RESEARCH locks the decision to keep the hand-rolled SW because vite-plugin-pwa's 2 MiB precache cap would silently drop Phaser assets.

Purpose: MOB-06 (PWA installable), MOB-07 (service worker offline cache). The SW /api/ caching bug is BLOCKING — it is a security fix, not an enhancement.

Output: SW patched, manifest validated, regression test ensures /api/ is never cached.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-mobile-cloud-sync/103-CONTEXT.md
@.planning/phases/103-mobile-cloud-sync/103-RESEARCH.md

<interfaces>
<!-- Current public/sw.js handlers (from earlier inspection) -->

```javascript
const CACHE_VERSION = 'gogo-arabic-v1';
const STATIC_CACHE = `${CACHE_VERSION}-static`;
const API_CACHE = `${CACHE_VERSION}-api`;
const API_PREFIX = '/api/';

// (line ~60) BUG: caches /api/ responses
if (url.pathname.startsWith(API_PREFIX)) {
  event.respondWith(networkFirst(request, API_CACHE));  // ← FIX: network-only, no cache
}

// (line ~94) cacheFirst — fine for static assets
async function cacheFirst(request, cacheName) { ... }

// (line ~113) networkFirst — used for /api/; KEEP for non-/api/ navigations
async function networkFirst(request, cacheName) { ... }
```

Current public/manifest.json (already complete):
- name, short_name, start_url, display: standalone, theme_color, background_color
- 3 icons (192x192 SVG, 512x512 SVG, 512x512 maskable SVG)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix SW /api/ cache leak (BLOCKING) + regression test</name>
  <files>public/sw.js, public/__tests__/sw.test.js</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/public/sw.js (full file — currently ~250 lines)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/services/swRegistration.js (full — confirm SW registration path)
  </read_first>
  <behavior>
    - Test 1: A `fetch` event targeting `/api/v1/cloudsave/push` is handled WITHOUT calling `cache.put` — i.e. the response body is never written to a cache
    - Test 2: A successful `/api/...` response is returned to the page from the network
    - Test 3: A failing `/api/...` request (network error) returns the network error, NOT a cached fallback
    - Test 4: A static asset (`/assets/sprites/foo.png`) STILL uses `cache-first` strategy (regression guard — we only changed /api/ behavior)
    - Test 5: Source-code grep: `grep -E "networkFirst.*[\"']?\/api\/" public/sw.js` returns no matches (gate against accidental revert)
  </behavior>
  <action>
    1. Edit `public/sw.js`. Locate the fetch handler block that calls `networkFirst(request, API_CACHE)` for paths starting with `API_PREFIX`. Replace with a call to a new helper `networkOnly(request)`:
       ```
       async function networkOnly(request) {
         // Auth-bound /api/ responses must NEVER write to a cache.
         // No cache read either — network errors return the error verbatim.
         return fetch(request);
       }
       ```
       Update the dispatch:
       ```
       if (url.pathname.startsWith(API_PREFIX)) {
         event.respondWith(networkOnly(request));
       }
       ```

    2. Remove the now-unused `API_CACHE` constant IF and only if no other code path references it. Use `grep -c "API_CACHE" public/sw.js` to confirm zero remaining references before removing.

    3. Bump `CACHE_VERSION` from `'gogo-arabic-v1'` to `'gogo-arabic-v2'` so existing clients invalidate any cached /api/ responses from before this fix. The existing `activate` event in sw.js already deletes old cache names matching the version pattern — verify this and rely on it.

    4. Create `public/__tests__/sw.test.js`. Use vitest with a synthetic `fetch` mock and the technique from `fake-indexeddb`-style mocks already in the project (see RESEARCH section: fake-indexeddb is in devDeps). For each test, import the sw.js module via the file path — if sw.js is not directly importable as ESM (it's a worker script), test the contracts via grep assertions in Test 5 plus a parsed-AST check.

    Concrete test approach:
    - Tests 1-3: Use `vi.stubGlobal('fetch', ...)` + a small harness that requires the sw via `await import('../sw.js')`. If sw.js declares top-level `self.addEventListener('fetch', handler)`, instantiate a fake `self` mock first.
    - Test 4: Same harness, send a fetch event for `/assets/sprites/foo.png`; assert the cacheFirst path runs (asserted via mocked cache.put being called).
    - Test 5: Pure grep assertion via `expect(readFileSync('public/sw.js', 'utf8')).not.toMatch(/networkFirst\([^)]*[\"']?\/api/)`.

    If sw.js cannot be cleanly imported in vitest, fall back to Test 5 (grep gate) only — but still write Tests 1-4 as `it.todo` to keep the contract visible.
  </action>
  <verify>
    <automated>grep -E "networkFirst\([^)]*API_CACHE" public/sw.js ; test $? -ne 0 && grep -q "networkOnly" public/sw.js && grep -q "gogo-arabic-v2" public/sw.js && npm test -- --run public/__tests__/sw.test.js 2>&1 | tail -10 | grep -E "pass"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "networkFirst\([^)]*API_CACHE" public/sw.js` returns exit code 1 (no matches)
    - `grep -c "networkOnly" public/sw.js` returns at least 1
    - `grep -c "gogo-arabic-v2" public/sw.js` returns at least 1
    - `grep -vE '^\s*(//|\*|#)' public/sw.js | grep -c "networkFirst(request, API_CACHE)"` returns 0 (filters out comments to avoid header-prose self-invalidation per Nyquist rule)
    - `npm test -- --run public/__tests__/sw.test.js` exits 0 with at least 1 passing test (Test 5 grep gate at minimum)
    - `npm run test:run` exits 0
  </acceptance_criteria>
  <done>SW no longer caches /api/, regression test guards revert.</done>
</task>

<task type="auto">
  <name>Task 2: Validate PWA manifest + offline boot audit</name>
  <files>public/manifest.json, public/__tests__/manifest.test.js</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/public/manifest.json (full file)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/public/sw.js — confirm install handler precaches `/` and `/index.html`
    - /Users/theshumba/Documents/GitHub/gogo-arabic/index.html — confirm `<link rel="manifest" href="/manifest.json" />` and `<link rel="apple-touch-icon" href="/assets/icons/icon-192.svg" />`
  </read_first>
  <action>
    1. Create `public/__tests__/manifest.test.js` that:
       - Reads `public/manifest.json` via `fs.readFileSync`.
       - JSON.parse — must not throw.
       - Asserts presence of: `name`, `short_name`, `start_url === '/'`, `display === 'standalone'`, `theme_color`, `background_color`, `icons` array length >= 2.
       - Asserts at least one icon has size `192x192` and at least one has `512x512`.
       - Asserts at least one icon has `purpose: 'maskable'`.
       - Asserts that for each icon, the `src` path starts with `/` (absolute path).

    2. Confirm `public/manifest.json` already meets all assertions (RESEARCH says it does). If any assertion fails, add the missing field to manifest.json — do NOT change existing fields. Specifically:
       - If `purpose: 'maskable'` is missing on any icon, add it to the 512×512 maskable icon entry (RESEARCH confirms this entry exists).

    3. Verify offline boot path manually via code-read (NOT a checkpoint, just inspection):
       - `public/sw.js` install handler precaches `/` and `/index.html` (PRECACHE_URLS array).
       - On a navigation fetch (request.mode === 'navigate'), sw.js falls back to cached `/index.html` if network fails.
       - The Redux+IDB Phase 27.1 layer holds last-visited zone; on app boot from cached HTML, the existing rehydration restores the zone.

       If the navigate fallback is missing, add it: in the fetch handler, after the /api/ branch, add:
       ```
       if (request.mode === 'navigate') {
         event.respondWith(
           fetch(request).catch(() => caches.match('/index.html'))
         );
         return;
       }
       ```

    Do not introduce vite-plugin-pwa. CONTEXT and RESEARCH both lock the decision to stay with the hand-rolled SW.
  </action>
  <verify>
    <automated>npm test -- --run public/__tests__/manifest.test.js 2>&1 | tail -5 | grep -E "pass" && grep -q "maskable" public/manifest.json && grep -E "request.mode.*navigate|navigate.*request.mode" public/sw.js</automated>
  </verify>
  <acceptance_criteria>
    - `public/__tests__/manifest.test.js` exists and all its assertions pass
    - `grep -c "maskable" public/manifest.json` returns at least 1
    - `public/sw.js` contains a `request.mode === 'navigate'` fallback to `caches.match('/index.html')` (or equivalent)
    - `npm run test:run` exits 0
    - `npm run build` exits 0 (no SW syntax errors)
  </acceptance_criteria>
  <done>Manifest validated; offline navigate fallback confirmed in sw.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service worker ↔ /api/ requests | Auth-bound API responses must not be cached client-side; SW must not be a confused deputy |
| Browser cache ↔ multiple users on shared device | Cache key isolation must hold; logout must invalidate per-user cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-06 | Information Disclosure | SW /api/ cache (BLOCKING) | mitigate | networkOnly strategy in Task 1; CACHE_VERSION bumped to invalidate any existing v1 entries; regression test guards revert |
| T-103-07 | Spoofing | SW serves stale /api/ data post-logout | mitigate | networkOnly means no SW cache layer; logout path elsewhere already destroys JWT cookie |
| T-103-08 | Tampering | manifest.json icon swap | accept | Manifest icons are static SVGs served from same origin; CSP and SRI policies elsewhere |
</threat_model>

<verification>
- `grep -E "networkFirst\([^)]*API_CACHE" public/sw.js` returns no matches
- `grep -c "networkOnly" public/sw.js` returns at least 1
- `grep -c "maskable" public/manifest.json` returns at least 1
- Navigate fallback in sw.js present
- `npm run test:run` exits 0
- `npm run build` exits 0
</verification>

<success_criteria>
- Cross-user /api/ cache leak fixed
- PWA installable on iOS Safari + Android Chrome (manifest valid)
- Offline boot serves cached app shell; existing IDB restores last zone
</success_criteria>

<output>
Create `.planning/phases/103-mobile-cloud-sync/103-04-SUMMARY.md` recording: lines changed in sw.js, CACHE_VERSION before→after, manifest validation pass, regression test count.
</output>
