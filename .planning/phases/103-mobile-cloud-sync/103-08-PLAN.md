---
phase: 103-mobile-cloud-sync
plan: 08
type: execute
wave: 4
depends_on:
  - 103-02
  - 103-03
  - 103-04
  - 103-06
  - 103-07
files_modified:
  - tests/e2e/mobile-sync.spec.ts
  - playwright.config.js
  - .planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Full client vitest suite passes (5623+ tests, zero regressions vs Phase 97 baseline)"
    - "Full server vitest suite passes"
    - "Playwright mobile-viewport E2E spec exercises: boot → joystick mount → tap-to-interact → save slot 1 → enable cloud sync → magic-link OTP (mocked SMTP) → push → simulated second device pulls → conflict UI renders if applicable"
    - "Offline-play smoke: airplane-mode boot loads cached app shell + restores last zone"
    - "Lighthouse PWA score in CI ≥ 90"
    - "Cross-device manual smoke checkpoint approved (real iPhone OR Chrome mobile emulation walkthrough)"
  artifacts:
    - path: "tests/e2e/mobile-sync.spec.ts"
      provides: "End-to-end Playwright spec covering mobile + sync paths"
      contains: "magic-link"
    - path: ".planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md"
      provides: "Final test report: vitest counts, Playwright results, Lighthouse score, bundle delta vs Plan 01 baseline"
      contains: "Lighthouse PWA"
  key_links:
    - from: "tests/e2e/mobile-sync.spec.ts"
      to: "playwright.config.js"
      via: "uses devices['iPhone 13'] context for mobile viewport"
      pattern: "iPhone|mobile"
---

<objective>
Regression sweep + end-to-end verification. This is the final wave: nothing new ships here, but every prior wave is exercised end-to-end on a mobile viewport, and a human signs off on a cross-device walkthrough before the phase closes.

Per RESEARCH validation architecture: full vitest (client + server) must remain green, a new Playwright spec exercises the mobile + sync golden path, Lighthouse PWA score must be ≥ 90, and offline boot must work.

Purpose: Phase-level verification. Catches integration drift between waves 1–3.

Output: New Playwright spec, regression report, human-approved cross-device smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-mobile-cloud-sync/103-CONTEXT.md
@.planning/phases/103-mobile-cloud-sync/103-RESEARCH.md
@.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md
@.planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md
@.planning/phases/103-mobile-cloud-sync/103-02-SUMMARY.md
@.planning/phases/103-mobile-cloud-sync/103-03-SUMMARY.md
@.planning/phases/103-mobile-cloud-sync/103-04-SUMMARY.md
@.planning/phases/103-mobile-cloud-sync/103-05-SUMMARY.md
@.planning/phases/103-mobile-cloud-sync/103-06-SUMMARY.md
@.planning/phases/103-mobile-cloud-sync/103-07-SUMMARY.md

<interfaces>
Existing Playwright infrastructure:
- /Users/theshumba/Documents/GitHub/gogo-arabic/playwright.config.js — already exists per RESEARCH
- Playwright version 1.58.2 in devDeps
- Existing E2E specs (if any) under tests/e2e/ — find via `ls tests/e2e/`. RESEARCH mentions Phase 98 was building a 15-spec smoke suite.

Lighthouse: prefer lighthouse-ci npm package OR `npx lighthouse <url> --only-categories=pwa --output=json`. If neither is available, document the gap in REGRESSION.md and treat the PWA score gate as a manual check on the checkpoint.

Cross-device smoke: requires real iPhone OR Chrome DevTools device emulation. Checkpoint task documents the steps; user runs them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright mobile-sync E2E spec + config check</name>
  <files>tests/e2e/mobile-sync.spec.ts, playwright.config.js</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/playwright.config.js (full — confirm projects, devices, baseURL)
    - `ls /Users/theshumba/Documents/GitHub/gogo-arabic/tests/e2e/` — see if any existing specs use device emulation patterns to copy
    - /Users/theshumba/Documents/GitHub/gogo-arabic/.planning/phases/103-mobile-cloud-sync/103-05-SUMMARY.md — confirm Decision B (B1 = OTP-in-app path) so the spec can mock the OTP flow if applicable
  </read_first>
  <action>
    Create `tests/e2e/mobile-sync.spec.ts` covering one happy path and two failure paths.

    Use Playwright `devices['iPhone 13']` device context for mobile viewport (375x812 with pointer:coarse media).

    Mock SMTP: intercept the `/api/v1/auth/magic-link/request` POST response so the server still creates the token row, then read the rawToken from a test-only response header OR query the test DB directly (mongodb-memory-server pattern). The test should not actually send email — instead, perform the verify+confirm POST directly with the captured token.

    Happy path test:
    1. Boot localhost (vite dev server started by Playwright globalSetup).
    2. Assert pointer:coarse media matches (mobile viewport context).
    3. Assert virtual joystick base element is visible (rex creates a circle with the data-touch-control attribute — locate it).
    4. Simulate joystick drag → assert player position changes (read scene.player.x via window.__phaserDebug if available; otherwise assert via Phaser canvas snapshot pixel-diff).
    5. Tap on an interactable in the world → assert the same dialogue/quest overlay that keyboard 'E' triggers opens.
    6. Save to slot 1 via existing pause-menu Save Slot UI.
    7. Open Settings → toggle Cloud sync ON → enter test@example.com → submit magic-link request.
    8. Server-side: directly fetch the unconsumed MagicLinkToken row; extract rawToken (test fixture endpoint OR direct Mongo). POST to /api/v1/auth/magic-link/confirm with that token. Confirm cookie is set in the page context.
    9. Force a sync push → assert outbox row created in IDB then drained (poll IDB via page.evaluate).
    10. Simulate second device by opening a fresh browser context with the same cookies and a clean local IDB. Call pullOnBoot. Assert slot 1 payload is restored locally.

    Failure paths:
    - Conflict test: pre-seed both local IDB and Mongo CloudSave with different lastModifiedAt > lastSyncedAt. Boot. Assert ConflictResolutionModal renders with role="dialog". Pick 'cloud' for slot 1. Apply. Assert modal dismisses and slot 1 matches cloud payload.
    - Desktop hide test: spawn a second browser context with `devices['Desktop Chrome']` (pointer:fine). Assert the joystick is NOT visible (display:none via @media (pointer: coarse) gate).

    Check `playwright.config.js` — if it does not already define a mobile project, add one:
    ```
    { name: 'mobile-iphone13', use: { ...devices['iPhone 13'] } }
    ```
    Place alongside existing projects; do not remove or rename any.

    Keep the spec under 3 minutes runtime per RESEARCH Validation Architecture target. Use `test.describe.serial` if tests share state.
  </action>
  <verify>
    <automated>test -f tests/e2e/mobile-sync.spec.ts &amp;&amp; grep -q "iPhone\|mobile" tests/e2e/mobile-sync.spec.ts &amp;&amp; grep -q "magic-link\|cloudsave\|joystick" tests/e2e/mobile-sync.spec.ts &amp;&amp; npx playwright test tests/e2e/mobile-sync.spec.ts --project=mobile-iphone13 --reporter=line 2>&amp;1 | tail -10 | grep -E "passed|✓"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/mobile-sync.spec.ts` exists
    - Spec uses `devices['iPhone 13']` or equivalent mobile context
    - Spec contains at least 3 test() blocks (happy path, conflict, desktop-hide)
    - `playwright.config.js` defines a mobile project (mobile-iphone13 or similar)
    - All tests pass; runtime under 3 minutes
  </acceptance_criteria>
  <done>E2E spec covers mobile + sync golden path and key failure modes.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression sweep + Lighthouse + write REGRESSION.md</name>
  <files>.planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md (pre-phase baseline)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/.planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md (post-rex-install delta)
  </read_first>
  <action>
    Run the full regression sweep and write `.planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md` with these sections in order:

    1. **Client vitest** — `npm run test:run` — paste exit code + last 5 lines of output (test counts). Confirm count ≥ 5623 (Phase 97 baseline from STATE.md). If lower, list the now-failing files.
    2. **Server vitest** — `cd server && npm run test:run` — same format.
    3. **Build** — `npm run build` — paste exit code + dist size.
    4. **Final bundle delta** — vs the 103-01-BUNDLE-BASELINE.md numbers. Per-chunk delta in KB. Note the rex-plugins chunk size specifically.
    5. **Lighthouse PWA score**:
       - Start the production build: `npm run build && npx serve dist -p 4173 &`
       - Run `npx lighthouse http://localhost:4173 --only-categories=pwa --output=json --output-path=./lh.json --chrome-flags="--headless"`
       - Extract the PWA category score. Must be ≥ 0.9.
       - If lighthouse CLI is unavailable, document the gap and switch to a manual checklist (manifest valid, SW registered, HTTPS in prod, viewport meta present, icon set complete).
    6. **Playwright E2E** — `npx playwright test --project=mobile-iphone13` — paste exit code + pass count.
    7. **Offline smoke** — describe steps verified manually:
       - npm run build, npm run preview, open in Chrome mobile-emulation mode
       - DevTools Network tab → throttle to Offline
       - Reload — assert app shell loads from cache, last zone restores
    8. **Anonymous mode regression** — wipe localStorage + IDB (the `?reset` URL helper at index.html line 81), boot, confirm app works WITHOUT enabling cloud sync (no /api/v1/cloudsave/* requests fire).
    9. **Coverage Summary Table** — for each requirement MOB-01..07, SYNC-01..06, the file(s) addressing it and the test(s) verifying it.

    If ANY section fails, do not mark the plan complete — flag the failing items at the top of REGRESSION.md under `## ⚠ Failures` and stop. The phase checkpoint depends on this report.
  </action>
  <verify>
    <automated>test -f .planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md &amp;&amp; grep -q "Client vitest" .planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md &amp;&amp; grep -q "Lighthouse PWA" .planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md &amp;&amp; grep -q "Coverage Summary Table" .planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md &amp;&amp; ! grep -qE "^##\s+⚠\s*Failures" .planning/phases/103-mobile-cloud-sync/103-08-REGRESSION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 9 sections
    - Client vitest section shows exit code 0 AND test count ≥ 5623
    - Server vitest section shows exit code 0
    - Lighthouse PWA score ≥ 0.9 OR documented fallback checklist with all items passing
    - No `## ⚠ Failures` section present (i.e., everything green)
    - Coverage table maps each of the 13 requirement IDs to file(s) + test(s)
  </acceptance_criteria>
  <done>Regression sweep clean; phase ready for human cross-device smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human cross-device smoke walkthrough</name>
  <what-built>
    Phase 103 is fully built. Final gate before phase-close: a human runs a 10-minute manual smoke on at least two device profiles.

    Available artifacts:
    - 103-08-REGRESSION.md — automated test pass results
    - tests/e2e/mobile-sync.spec.ts — Playwright spec output
    - All Plan 02-07 SUMMARY files

    Build is ready to be served locally via `npm run build && npx serve dist -p 4173`.
  </what-built>
  <how-to-verify>
    Profile A — Desktop (Chrome, pointer:fine):
    1. Visit http://localhost:4173 (or staging if deployed).
    2. Confirm NO virtual joystick visible. Keyboard arrows + 'E' work as before.
    3. Open Settings → enable Cloud sync → enter your email → click "Send link".
    4. Open the inbox (or check the dev console for the test-mode token if Resend domain not verified). Click the link — confirm it opens a "Confirm sign-in" page (NOT direct redirect). Click Confirm.
    5. Confirm Settings now shows the authenticated email.
    6. Save to slot 1.
    7. Confirm SyncStatusIndicator in HUD shows "Synced HH:MM".

    Profile B — Mobile (real iPhone OR Chrome DevTools device emulation as iPhone 13):
    1. Visit the same URL.
    2. Confirm virtual joystick visible bottom-left.
    3. Confirm safe-area-inset padding renders correctly (notch area is dark, not under content). If real iPhone, look at the top status bar / bottom home-indicator.
    4. Drag joystick → player moves in 8 directions.
    5. Tap on an NPC/sign → interaction opens (same as keyboard 'E' on desktop).
    6. Pinch to confirm the canvas does NOT scroll/zoom (touch-action: none in CSS).
    7. Sign in with magic link OR (if Decision B was B1) sign in with the 6-digit OTP code that arrived in the email.
    8. Sign-in succeeds inside the PWA context.
    9. Visit Settings → confirm slot 1 from Profile A is available via cloud pull.

    Profile C — Offline (any profile, throttle to Offline in DevTools):
    1. Reload the page.
    2. Confirm the app boots from cached shell.
    3. Confirm last zone restores from existing IDB.
    4. Confirm no /api/* requests fire (the new SW networkOnly strategy means failed /api/ requests do NOT return cached data — instead they fail gracefully).

    Type "approved" if all three profiles work, OR list specific failures (e.g., "Profile B step 3 — notch padding missing on real iPhone 14 Pro").
  </how-to-verify>
  <resume-signal>Type "approved" or list specific failures with profile + step number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test → live server | Tests interact with a real dev server + memory Mongo; no production data touched |
| Lighthouse audit → production build | Audit runs against served dist/; no secret env vars exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-26 | Information Disclosure | E2E spec captures token in test fixture | accept | Tokens generated inside test scope, never persisted; mongodb-memory-server torn down after run |
| T-103-27 | Tampering | Lighthouse run modifies prod build artifacts | accept | Lighthouse is read-only; serves but doesn't write |
</threat_model>

<verification>
- REGRESSION.md exists with all 9 sections green
- Client + server vitest suites both exit 0
- Playwright mobile spec passes
- Lighthouse PWA score ≥ 0.9 OR documented fallback checklist clean
- Human checkpoint approved
</verification>

<success_criteria>
- Zero regressions vs Phase 97 baseline (5623+ tests pass)
- Mobile + sync golden path verified end-to-end automated
- Cross-device manual smoke approved
- Phase ready to be closed; v17.5 progress advances
</success_criteria>

<output>
Create `.planning/phases/103-mobile-cloud-sync/103-08-SUMMARY.md` with: final vitest counts, Playwright pass count, Lighthouse PWA score, bundle delta vs baseline, smoke checkpoint status.
</output>
