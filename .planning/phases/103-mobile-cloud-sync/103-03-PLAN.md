---
phase: 103-mobile-cloud-sync
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/game/config.js
  - src/game/systems/TouchInputAdapter.js
  - src/game/systems/TapToInteract.js
  - src/game/scenes/WorldScene.js
  - src/game/sprites/Player.js
  - src/components/Mobile/TouchControls.jsx
  - src/components/Mobile/TouchControls.module.css
  - src/components/Mobile/__tests__/TouchControls.test.jsx
  - src/game/systems/__tests__/TapToInteract.test.js
  - .planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md
autonomous: false
requirements:
  - MOB-01
  - MOB-02
  - MOB-05
must_haves:
  truths:
    - "Virtual joystick renders on touch devices (pointer: coarse) and feeds cursor-key input to the existing Player update loop"
    - "Tap-to-interact casts a 64px radius around pointerdown, picks the nearest interactable, and fires the same code path as keyboard 'E'"
    - "Touch controls do not render on desktop (pointer: fine) — keyboard/mouse path untouched"
    - "phaser3-rex-plugins standalone joystick import adds <100KB to the JS bundle"
  artifacts:
    - path: "src/game/systems/TouchInputAdapter.js"
      provides: "rex VirtualJoyStick wired into WorldScene; exports a hook that Player consumes"
      contains: "VirtualJoystickPlugin"
    - path: "src/game/systems/TapToInteract.js"
      provides: "Pointerdown → InteractableManager.findNearest(x, y, 64) → activate()"
      contains: "RADIUS"
    - path: "src/components/Mobile/TouchControls.jsx"
      provides: "React overlay wrapper for joystick + action button containers"
      contains: "pointer: coarse"
    - path: "src/components/Mobile/TouchControls.module.css"
      provides: "@media (pointer: coarse) gate, data-touch-control attributes for hit-test exclusion"
      contains: "@media (pointer: coarse)"
    - path: ".planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md"
      provides: "Pre vs post-install bundle size delta with verdict ACCEPT or REJECT"
      contains: "Delta"
  key_links:
    - from: "src/game/systems/TouchInputAdapter.js"
      to: "src/game/sprites/Player.js"
      via: "scene.touchCursors object consumed in Player.update movement reads"
      pattern: "touchCursors"
    - from: "src/game/systems/TapToInteract.js"
      to: "src/game/systems/InteractableManager.js"
      via: "findNearest(x, y, RADIUS) → activate()"
      pattern: "findNearest"
    - from: "src/components/Mobile/TouchControls.module.css"
      to: "src/game/systems/TapToInteract.js"
      via: "data-touch-control attribute lets TapToInteract ignore joystick taps"
      pattern: "data-touch-control"
---

<objective>
Install `phaser3-rex-plugins` (standalone VirtualJoyStick plugin path), wire the joystick into WorldScene, add tap-to-interact pointer hit-testing, and gate touch UI to `pointer: coarse` devices only. Per CONTEXT decision: touch is additive — desktop keyboard/mouse path must remain identical.

Purpose: MOB-01 (joystick), MOB-02 (tap-to-interact), MOB-05 (no touch UI on desktop). Per RESEARCH Pitfall 7: import the per-plugin path (`phaser3-rex-plugins/plugins/virtualjoystick-plugin.js`), NOT the package barrel — barrel import inflates bundle by 7+ MB.

Output: Touch input layer working on mobile-emulated viewport, bundle delta measured and approved, three new tests covering joystick mount + hit-test + desktop hide behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-mobile-cloud-sync/103-CONTEXT.md
@.planning/phases/103-mobile-cloud-sync/103-RESEARCH.md
@.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md

<interfaces>
<!-- Key existing files this plan extends -->

src/game/sprites/Player.js (lines 260-263 per RESEARCH): movement reads `this.cursors.left.isDown || this.wasd.left.isDown`. Extend chain with `(this.scene.touchCursors?.left?.isDown ?? false)`.

src/game/systems/InteractableManager.js — exposes a manager keyed by world coords. RESEARCH says `findNearest(x, y, radius)` exists or needs to be added. Verify before assuming.

src/game/scenes/WorldScene.js — has `this.input.on('pointerdown')` precedent per RESEARCH (cite: CalligraphyScene.js:147). Add a sibling listener gated on `matchMedia('(pointer: coarse)')`.

rex VirtualJoyStick API (from `phaser3-rex-plugins/plugins/virtualjoystick-plugin.js`):
```javascript
plugins.global: [{ key: 'rexVirtualJoystick', plugin: VirtualJoystickPlugin, mapping: 'rexVirtualJoystick' }]
const joystick = this.plugins.get('rexVirtualJoystick').add(scene, {
  x, y, radius, base, thumb, dir: '8dir', forceMin: 16, fixed: true
});
const cursors = joystick.createCursorKeys();  // { left, right, up, down } with .isDown
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve phaser3-rex-plugins install (slopcheck unavailable)</name>
  <what-built>
    Not yet installed. About to run `npm install phaser3-rex-plugins`. Per RESEARCH Package Legitimacy Audit, slopcheck CLI is unavailable in this dev env, so the package is tagged [ASSUMED] and requires a blocking human-verify gate.

    Package details from RESEARCH:
    - Name: `phaser3-rex-plugins`
    - Version: 1.80.20 (latest 2026-03-31)
    - Author: rexrainbow
    - Repo: github.com/rexrainbow/phaser3-rex-notes
    - License: MIT
    - Whole package: 7.37 MB. We import ONLY the standalone path `phaser3-rex-plugins/plugins/virtualjoystick-plugin.js` to keep bundle small.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/phaser3-rex-plugins in a browser.
    2. Confirm the package author shows as `rexrainbow` (NOT a typosquat — `phaser3-rex-plugin` without the `-s` would be slop).
    3. Confirm the repo link goes to `github.com/rexrainbow/phaser3-rex-notes` and that the repo has 1K+ stars + recent commits.
    4. Confirm version 1.80.20 was published 2026-03-31.
    5. Type "approved" to allow `npm install phaser3-rex-plugins` in Task 2, OR type "rejected: <reason>" to halt the plan and fall back to a hand-rolled joystick (which Plan 03 would then re-scope).
  </how-to-verify>
  <resume-signal>Type "approved" to install, or "rejected: <reason>" to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install rex-plugins and measure bundle delta</name>
  <files>package.json, package-lock.json, .planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/.planning/phases/103-mobile-cloud-sync/103-01-BUNDLE-BASELINE.md (the pre-install numbers from Plan 01 Task 2)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/package.json (confirm phaser version, current devDeps)
  </read_first>
  <action>
    Run `npm install phaser3-rex-plugins@1.80.20`. Verify it lands in `dependencies` (not devDependencies).

    Run `npm run build`. Capture new dist sizes with the same commands used in Plan 01 Task 2:
    - `du -sh dist/`
    - `find dist/assets -name "*.js" -exec ls -la {} \; | sort -k5 -n -r | head -20`
    - `find dist/assets -name "*vendor*" -exec du -ch {} + | tail -1`
    - Gzipped totals for the top 5 chunks.

    Write `.planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md` with these sections in this order:
    1. **Baseline** — pasted from 103-01-BUNDLE-BASELINE.md
    2. **Post-install** — new measurements
    3. **Delta table** — per-chunk before/after/delta in KB, rounded to 1 decimal
    4. **Largest delta chunk** — name + KB increase + gzipped delta
    5. **Verdict** — One of:
       - `ACCEPT` if total JS delta is <100 KB (gzipped <30 KB) per RESEARCH guidance
       - `REJECT` if delta exceeds threshold; in that case Task 3 onward MUST not run and the planner re-scopes (see Resume Signal in next task)

    Stop the plan here if the verdict is REJECT. The plan re-scopes to a hand-rolled joystick or alternate library.
  </action>
  <verify>
    <automated>test -f node_modules/phaser3-rex-plugins/plugins/virtualjoystick-plugin.js && grep -q "\"phaser3-rex-plugins\"" package.json && test -f .planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md && grep -E "^(ACCEPT|REJECT)" .planning/phases/103-mobile-cloud-sync/103-03-BUNDLE-DELTA.md</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/phaser3-rex-plugins/plugins/virtualjoystick-plugin.js` exists (standalone plugin file present)
    - `package.json` contains `"phaser3-rex-plugins"` in dependencies
    - BUNDLE-DELTA.md exists with `ACCEPT` or `REJECT` at start of a line
    - If REJECT: subsequent tasks halted and re-planning triggered
  </acceptance_criteria>
  <done>Package installed, delta measured, verdict recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire VirtualJoyStick + TouchInputAdapter + Player consumer</name>
  <files>src/game/config.js, src/game/systems/TouchInputAdapter.js, src/game/scenes/WorldScene.js, src/game/sprites/Player.js, src/game/systems/__tests__/TouchInputAdapter.test.js</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/game/config.js (full)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/game/scenes/WorldScene.js (read create() method and any input setup — grep `create()` and `input.on` first to find regions)
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/game/sprites/Player.js (read lines 250-290 to confirm cursor-key consumption pattern)
  </read_first>
  <behavior>
    - Test 1: When `matchMedia('(pointer: coarse)').matches === true`, `TouchInputAdapter.init(scene)` mounts a joystick and assigns `scene.touchCursors` to its cursor-keys object
    - Test 2: When `matchMedia('(pointer: coarse)').matches === false`, `TouchInputAdapter.init(scene)` is a no-op and `scene.touchCursors` remains undefined
    - Test 3: The cursor-keys object exposes `.left.isDown`, `.right.isDown`, `.up.isDown`, `.down.isDown` boolean readers (shape match with Phaser keyboard cursor keys)
  </behavior>
  <action>
    1. Edit `src/game/config.js` — add a `plugins` block. Import `VirtualJoystickPlugin` from `phaser3-rex-plugins/plugins/virtualjoystick-plugin.js` (this exact path — per RESEARCH Pitfall 7, the barrel import inflates bundle 7MB+). Add:
       ```
       plugins: {
         global: [
           { key: 'rexVirtualJoystick', plugin: VirtualJoystickPlugin, mapping: 'rexVirtualJoystick' }
         ]
       }
       ```

    2. Create `src/game/systems/TouchInputAdapter.js` exporting a class with `init(scene)` method. Behavior:
       - Read `window.matchMedia('(pointer: coarse)').matches`. If false, return immediately.
       - Use `scene.plugins.get('rexVirtualJoystick').add(scene, {...})` with: x=150, y=`scene.cameras.main.height - 150`, radius=100, base=`scene.add.circle(0,0,100,0x888888,0.3)`, thumb=`scene.add.circle(0,0,50,0xcccccc,0.6)`, dir='8dir', forceMin=16, fixed=true.
       - Assign `scene.touchCursors = joystick.createCursorKeys();`
       - Mark the base/thumb game objects with `setData('touchControl', true)` (Plan 04/03 hit-test uses this to ignore joystick taps).

    3. Edit `src/game/scenes/WorldScene.js` — in `create()`, after the existing input setup, instantiate and call `new TouchInputAdapter().init(this)`. Place the call inside `create()` AFTER `this.cameras.main` is set so `cameras.main.height` is defined.

    4. Edit `src/game/sprites/Player.js` lines ~260-263 (current pattern: `this.cursors.left.isDown || this.wasd.left.isDown`). Append the touch chain to each of the 4 directional reads:
       ```
       const left  = this.cursors.left.isDown  || this.wasd.left.isDown  || (this.scene.touchCursors?.left?.isDown  ?? false);
       const right = this.cursors.right.isDown || this.wasd.right.isDown || (this.scene.touchCursors?.right?.isDown ?? false);
       const up    = this.cursors.up.isDown    || this.wasd.up.isDown    || (this.scene.touchCursors?.up?.isDown    ?? false);
       const down  = this.cursors.down.isDown  || this.wasd.down.isDown  || (this.scene.touchCursors?.down?.isDown  ?? false);
       ```
       Find the EXACT current lines via grep before editing — line numbers in RESEARCH may have drifted.

    5. Create `src/game/systems/__tests__/TouchInputAdapter.test.js`. Use `vi.stubGlobal('matchMedia', ...)` to control the coarse/fine branch. Mock `scene.plugins.get` to return an object with `add()` returning `{ createCursorKeys: () => ({ left:{isDown:false}, right:{isDown:false}, up:{isDown:false}, down:{isDown:false} }) }`. Mock `scene.add.circle` to return a stub with `setData()`. Mock `scene.cameras.main.height = 600`. Run the 3 tests in `<behavior>`.
  </action>
  <verify>
    <automated>grep -q "VirtualJoystickPlugin" src/game/config.js && grep -q "touchCursors" src/game/sprites/Player.js && grep -q "phaser3-rex-plugins/plugins/virtualjoystick-plugin" src/game/config.js && npm test -- --run src/game/systems/__tests__/TouchInputAdapter.test.js 2>&1 | tail -5 | grep -E "3 pass|passed"</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/config.js` imports from the literal path `phaser3-rex-plugins/plugins/virtualjoystick-plugin.js`
    - `src/game/config.js` registers a global plugin with key `rexVirtualJoystick`
    - `src/game/systems/TouchInputAdapter.js` exists and exports a class/function with an `init` method
    - `src/game/sprites/Player.js` contains the substring `touchCursors` and 4 occurrences of `?.isDown ?? false`
    - 3 TouchInputAdapter tests pass
    - `npm run test:run` exits 0 (no Player regression)
  </acceptance_criteria>
  <done>Joystick mounts on coarse pointer, drops into existing Player cursor reads, desktop path untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: TapToInteract + TouchControls overlay + pointer:coarse gate</name>
  <files>src/game/systems/TapToInteract.js, src/game/scenes/WorldScene.js, src/components/Mobile/TouchControls.jsx, src/components/Mobile/TouchControls.module.css, src/components/Router/GameLayout.jsx, src/game/systems/__tests__/TapToInteract.test.js, src/components/Mobile/__tests__/TouchControls.test.jsx</files>
  <read_first>
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/game/systems/ — list dir; grep for `InteractableManager` to find its file
    - /Users/theshumba/Documents/GitHub/gogo-arabic/src/components/Router/GameLayout.jsx (full file — must wire TouchControls in)
    - Verify `findNearest` exists: `grep -n "findNearest\|activate" src/game/systems/InteractableManager.js`. If absent, the action below ADDS this method as part of the change.
  </read_first>
  <behavior>
    TapToInteract tests:
    - Test 1: pointerdown event at (worldX=100, worldY=200) when an interactable exists within 64px → InteractableManager.activate is called once
    - Test 2: pointerdown when no interactable in 64px radius → activate NOT called
    - Test 3: pointerdown whose DOM event target has `[data-touch-control]` ancestor → activate NOT called (joystick swallow guard)

    TouchControls tests:
    - Test 1: When `matchMedia('(pointer: coarse)').matches === true` the root container is rendered with the touch-controls class
    - Test 2: When `matchMedia('(pointer: coarse)').matches === false` the root container is rendered but the CSS module hides it (the test asserts the `data-touch-control` data attribute is present on the rendered DOM regardless — the visibility gate is a CSS contract; visibility is asserted by the snapshot of computed-style mock if available, otherwise by class application)
  </behavior>
  <action>
    1. Create `src/game/systems/TapToInteract.js` per RESEARCH Pattern 2:
       - Class with `constructor(scene)` storing `this.RADIUS = 64`.
       - `init()` registers `this.scene.input.on('pointerdown', (pointer) => {...})`.
       - Inside the handler: early-return if `pointer.event?.target?.closest('[data-touch-control]')` truthy.
       - Read `wx = pointer.worldX, wy = pointer.worldY`.
       - Call `this.scene.interactableManager.findNearest(wx, wy, this.RADIUS)` — if `findNearest` does not yet exist on `InteractableManager`, ADD it as a method that iterates the manager's registered interactables and returns the closest within `radius` or null.
       - If a hit is returned, call `this.scene.interactableManager.activate(interactable)`.

    2. Edit `src/game/scenes/WorldScene.js` create() — instantiate `new TapToInteract(this).init()` after TouchInputAdapter. Place AFTER InteractableManager is constructed so `scene.interactableManager` exists.

    3. Create `src/components/Mobile/TouchControls.jsx`:
       ```
       export function TouchControls() {
         return <div className={styles.touchControlsRoot} data-touch-control="root" />;
       }
       ```
       The actual joystick/button DOM is rendered by Phaser via TouchInputAdapter — this React component is just the gating layer + a marker for tap-to-interact exclusion. Keep it lazy/lightweight; no rex import here.

    4. Create `src/components/Mobile/TouchControls.module.css` with the @media (pointer: coarse) rule from RESEARCH Code Examples:
       ```
       .touchControlsRoot {
         display: none;
         position: fixed;
         inset: 0;
         pointer-events: none;
         z-index: 100;
       }
       @media (pointer: coarse) {
         .touchControlsRoot { display: block; }
         .touchControlsRoot > * { pointer-events: auto; }
       }
       ```

    5. Edit `src/components/Router/GameLayout.jsx` — add `<TouchControls />` near the existing overlays root (lazy-load if other overlays are lazy-loaded; otherwise eager). The component should mount UNCONDITIONALLY; the CSS module's `@media (pointer: coarse)` does the gating per MOB-05. Do NOT JS-gate with matchMedia in React (would re-render on orientation change unnecessarily).

    6. Tests as in `<behavior>` block.
  </action>
  <verify>
    <automated>test -f src/game/systems/TapToInteract.js && grep -q "findNearest" src/game/systems/TapToInteract.js && grep -q "data-touch-control" src/game/systems/TapToInteract.js && test -f src/components/Mobile/TouchControls.jsx && grep -q "@media (pointer: coarse)" src/components/Mobile/TouchControls.module.css && grep -q "TouchControls" src/components/Router/GameLayout.jsx && npm test -- --run src/game/systems/__tests__/TapToInteract.test.js src/components/Mobile/__tests__/TouchControls.test.jsx 2>&1 | tail -10 | grep -E "pass"</automated>
  </verify>
  <acceptance_criteria>
    - `src/game/systems/TapToInteract.js` exists; contains literal `data-touch-control` and `RADIUS = 64`
    - `src/components/Mobile/TouchControls.module.css` exists; contains literal `@media (pointer: coarse)`
    - `src/components/Router/GameLayout.jsx` imports and renders `<TouchControls />`
    - At least 5 tests pass between TapToInteract and TouchControls test files
    - `npm run test:run` exits 0 (no regression)
  </acceptance_criteria>
  <done>Tap-to-interact + touch overlay live; desktop path verified untouched by full test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dev env | New dependency `phaser3-rex-plugins` enters the supply chain |
| browser→Phaser pointer event | Tap input coordinates supplied by user; trusted as input device but bounded to world coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-SC-01 | Tampering | phaser3-rex-plugins install | mitigate | Blocking human checkpoint (Task 1) verifies npm page + repo + author before install; package tagged [ASSUMED] per slopcheck unavailability |
| T-103-04 | Tampering | rex barrel-import path | mitigate | Import strictly the per-plugin path `phaser3-rex-plugins/plugins/virtualjoystick-plugin.js`; bundle-delta gate in Task 2 enforces <100KB JS budget |
| T-103-05 | Denial of Service | pointerdown handler | accept | Hit-test is O(n) over InteractableManager registry; bounded by zone object count (<200 per zone per WORLD-AUDIT) |
| T-103-SC | Tampering | npm install gate | mitigate | Blocking human checkpoint before any package-manager install (Task 1) — see T-103-SC-01 |
</threat_model>

<verification>
- `grep -c "phaser3-rex-plugins/plugins/virtualjoystick-plugin.js" src/game/config.js` returns 1
- `grep -c "phaser3-rex-plugins\"" package.json` returns 1
- `grep -c "touchCursors" src/game/sprites/Player.js` returns at least 4 (4 directions)
- BUNDLE-DELTA.md verdict is `ACCEPT`
- `npm run test:run` exits 0
- `npm run build` exits 0 and dist JS delta vs Plan 01 baseline is <100KB
</verification>

<success_criteria>
- Touch joystick functional on coarse pointer devices
- Tap-to-interact reuses InteractableManager activation path (same as keyboard E)
- Desktop pointer:fine path untouched (full test suite still green)
- Bundle budget kept
</success_criteria>

<output>
Create `.planning/phases/103-mobile-cloud-sync/103-03-SUMMARY.md` with: bundle delta (KB), new files added, lines changed in Player.js, test pass count.
</output>
