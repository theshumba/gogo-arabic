---
phase: 58-grammar-a1-a2-lesson-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/grammar.js
  - src/data/__tests__/grammarChecker.test.js
  - src/components/Grammar/stages/ExerciseStage.jsx
  - src/components/Grammar/GrammarLesson.jsx
autonomous: true
requirements: [GRAM-02]

must_haves:
  truths:
    - "Every A1-A2 grammar lesson presents at least 12 exercises"
    - "Every A1-A2 grammar lesson uses at least 4 distinct exercise types"
    - "There are at least 20 A1-A2 lessons total in grammar.js"
    - "All 12 exercise types render correctly in ExerciseStage.jsx"
    - "ExerciseStage keyboard shortcuts (1-4) work for all new option-based types"
  artifacts:
    - path: "src/data/grammar.js"
      provides: "20+ A1-A2 grammar lessons with 12+ exercises and 4+ quiz questions each"
      contains: "cefrLevel: 'A1'"
    - path: "src/data/__tests__/grammarChecker.test.js"
      provides: "Data integrity validation for grammar lessons"
      contains: "grammarChecker"
    - path: "src/components/Grammar/stages/ExerciseStage.jsx"
      provides: "Renderers for all 12 exercise types"
      contains: "conjugation-drill"
  key_links:
    - from: "src/data/grammar.js"
      to: "src/components/Grammar/stages/ExerciseStage.jsx"
      via: "exercise.type discriminated union"
      pattern: "exercise\\.type === "
    - from: "src/data/__tests__/grammarChecker.test.js"
      to: "src/data/grammar.js"
      via: "vitest import"
      pattern: "import.*grammarLessons.*from.*grammar"
---

<objective>
Expand the grammar lesson content from stub-quality A1-A2 lessons to a full 20-lesson A1-A2 curriculum with 12 exercise types per lesson, add 9 new exercise type renderers to ExerciseStage.jsx, and create a grammarChecker vitest for data integrity validation.

Purpose: Satisfies GRAM-02 — each grammar lesson has 12 exercise types with Arabic examples, not just fill-in-blank.
Output: 20+ fully-populated A1-A2 grammar lessons, 12 working exercise type renderers, passing grammarChecker test suite.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-grammar-a1-a2-lesson-wiring/58-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grammarChecker test + expand grammar.js with 20 A1-A2 lessons using 12 exercise types</name>
  <files>
    src/data/__tests__/grammarChecker.test.js
    src/data/grammar.js
  </files>
  <read_first>
    src/data/grammar.js
    src/data/__tests__/skillTreeRewards.test.js
    .planning/phases/58-grammar-a1-a2-lesson-wiring/58-RESEARCH.md
  </read_first>
  <action>
**Step 1: Create grammarChecker.test.js** at `src/data/__tests__/grammarChecker.test.js`:

Import `grammarLessons` from `../../data/grammar.js`. Write tests that validate:
- All lessons with `cefrLevel === 'A1'` or `cefrLevel === 'A2'` have `exercises.length >= 12`
- All A1-A2 lessons have `new Set(lesson.exercises.map(e => e.type)).size >= 4`
- All A1-A2 lessons have `quiz.length >= 4`
- Total A1-A2 lessons count `>= 20`
- Every `conjugation-drill` exercise has `answer` (string), `options` (array length 4), and `paradigm` (one of 'present', 'past', 'future')
- Every `sentence-transformation` exercise has `prompt` and `answer` strings
- Every `word-order` exercise has `answer` (string) and `options` (array length 4) — implemented as 4-option multiple-choice
- Every `error-identification` exercise has `sentence`, `error`, `correction`, and `options` (array)
- Every `multiple-select` exercise has `prompt`, `correctAnswers` (array), and `options` (array)
- Every `true-false` exercise has `statement` (string) and `answer` (boolean)
- Every `cloze` exercise has `text` (string with `___` blanks), `blanks` (array of `{answer, options}` objects)
- Every `classify` exercise has `prompt`, `categories` (array of strings), and `items` (array of `{text, category}` objects)
- Every `build-sentence` exercise has `prompt`, `words` (array of strings), and `answer` (string)
- No duplicate lesson IDs across all grammarLessons
- All lesson `order` values are unique

Run `npx vitest run src/data/__tests__/grammarChecker.test.js` — it MUST FAIL (RED phase — tests exist before data does).

**Step 2: Backfill `cefrLevel: 'A1'` on the first 7 lessons** (orders 1-7: al-definite through prepositions). These are genuinely A1 content. Add the field right after the `order` field on each lesson.

**Step 3: Expand the first 7 lessons to 12+ exercises each.** They currently have 5 exercises and 4 quiz questions. Add 7+ more exercises to each using a mix of the new types. Ensure at least 4 distinct `type` values per lesson. The existing 3 exercise types (`fill-blank`, `translate`, `match`) stay. Add exercises using these 9 new types appropriate to each lesson's topic:

- `conjugation-drill`: `{ type: 'conjugation-drill', prompt: 'Conjugate كَتَبَ for أنا', verb: 'كَتَبَ', root: 'ك-ت-ب', pronoun: 'أنا', paradigm: 'past', answer: 'كَتَبْتُ', options: ['كَتَبْتُ', 'كَتَبَ', 'كَتَبْتِ', 'كَتَبْنا'] }`
- `sentence-transformation`: `{ type: 'sentence-transformation', prompt: 'Change to feminine: الولد طويل', answer: 'البنت طويلة', hint: 'masculine → feminine' }`
- `word-order`: `{ type: 'word-order', prompt: 'Which sentence is correct?', answer: 'الكتاب على الطاولة', options: ['الكتاب على الطاولة', 'على الكتاب الطاولة', 'الطاولة الكتاب على', 'على الطاولة الكتاب'] }` (multiple-choice — no drag-and-drop)
- `error-identification`: `{ type: 'error-identification', sentence: 'هذا بنت جميلة', error: 'هذا', correction: 'هذه', options: ['هذا', 'بنت', 'جميلة', 'No error'] }`
- `multiple-select`: `{ type: 'multiple-select', prompt: 'Select all sun letters:', correctAnswers: ['ت', 'ش', 'ر'], options: ['ت', 'ب', 'ش', 'ر', 'ك'] }`
- `true-false`: `{ type: 'true-false', statement: 'الشمس is pronounced "al-shams" (the ل is pronounced)', answer: false }`
- `cloze`: `{ type: 'cloze', text: '___ كتاب على ___ طاولة', blanks: [{ answer: 'ال', options: ['ال', 'إل', 'أل'] }, { answer: 'ال', options: ['ال', 'في', 'من'] }] }`
- `classify`: `{ type: 'classify', prompt: 'Classify as sun or moon letter', categories: ['Sun', 'Moon'], items: [{ text: 'ش', category: 'Sun' }, { text: 'ب', category: 'Moon' }] }`
- `build-sentence`: `{ type: 'build-sentence', prompt: 'Build: "The book is on the table"', words: ['الطاولة', 'الكتاب', 'على'], answer: 'الكتاب على الطاولة' }`

**Step 4: Expand the 5 existing A1 stub lessons** (orders 13-17: numbers-1-10, basic-adjectives, demonstratives, possessive-pronouns, basic-negation). Each currently has 1-3 exercises and 0 quiz questions. Expand each to 12+ exercises (4+ distinct types) and 4 quiz questions. Write real Arabic content appropriate to each topic.

**Step 5: Expand the 7 existing A2 stub lessons** (orders 18-24: present-tense through active-participle). Each currently has 1 exercise and 0 quiz questions. Expand each to 12+ exercises (4+ distinct types) and 4 quiz questions. Write real Arabic content appropriate to each topic. Use conjugation-drill extensively for verb-related A2 lessons (present-tense, future-tense, active-participle).

**Step 6: Author 1 new A1 lesson to fill order 8** — topic: `colors-and-shapes` (id: `colors-and-shapes`, category: `vocabulary`, cefrLevel: `A1`, order: 8, difficulty: 1). Write full lesson with explanation, 4 examples, 3 rules, 12+ exercises (4+ types), 4 quiz questions. Arabic color words: احمر، ازرق، اخضر، اصفر، ابيض، اسود.

The total A1-A2 lesson count after this: first 7 (backfilled A1) + 1 new (order 8) + 5 existing A1 (13-17) + 7 existing A2 (18-24) = **20 lessons**.

**Step 7: Run `npx vitest run src/data/__tests__/grammarChecker.test.js`** — all tests MUST pass (GREEN phase).

**Content quality rules:**
- All Arabic text uses single-quoted strings (matching the 7 well-built lessons)
- Every exercise has plausible distractors (not random characters)
- Conjugation-drill exercises have exactly 4 options including the correct answer
- Word-order exercises are multiple-choice with 4 sentence options (no drag-and-drop)
- sentence-transformation hint field explains the transformation direction
- All quiz questions have `explanation` field with educational content
  </action>
  <verify>
Run `npx vitest run src/data/__tests__/grammarChecker.test.js` — all tests pass.
Verify: `grep -c "cefrLevel: 'A1'" src/data/grammar.js` returns >= 13 (7 backfilled + 5 existing + 1 new).
Verify: `grep -c "cefrLevel: 'A2'" src/data/grammar.js` returns >= 7.
Verify: `grep -c "type: 'conjugation-drill'" src/data/grammar.js` returns > 0.
Verify: `grep -c "type: 'word-order'" src/data/grammar.js` returns > 0.
Verify: `grep -c "type: 'error-identification'" src/data/grammar.js` returns > 0.
Verify: `grep -c "type: 'sentence-transformation'" src/data/grammar.js` returns > 0.
  </verify>
  <acceptance_criteria>
- `npx vitest run src/data/__tests__/grammarChecker.test.js` exits 0
- `grep -c "cefrLevel: 'A1'" src/data/grammar.js` outputs a number >= 13
- `grep -c "cefrLevel: 'A2'" src/data/grammar.js` outputs a number >= 7
- `grep "type: 'conjugation-drill'" src/data/grammar.js` finds matches
- `grep "type: 'word-order'" src/data/grammar.js` finds matches
- `grep "type: 'error-identification'" src/data/grammar.js` finds matches
- `grep "type: 'sentence-transformation'" src/data/grammar.js` finds matches
- `grep "type: 'multiple-select'" src/data/grammar.js` finds matches
- `grep "type: 'true-false'" src/data/grammar.js` finds matches
- `grep "type: 'cloze'" src/data/grammar.js` finds matches
- `grep "type: 'classify'" src/data/grammar.js` finds matches
- `grep "type: 'build-sentence'" src/data/grammar.js` finds matches
- grammarChecker.test.js contains `describe('grammar.js data integrity'`
  </acceptance_criteria>
  <done>20+ A1-A2 grammar lessons in grammar.js, each with 12+ exercises across 4+ distinct types from the 12-type taxonomy. grammarChecker vitest validates all lessons pass data integrity checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add 9 new exercise type renderers to ExerciseStage.jsx + quiz skip guard in GrammarLesson.jsx</name>
  <files>
    src/components/Grammar/stages/ExerciseStage.jsx
    src/components/Grammar/GrammarLesson.jsx
  </files>
  <read_first>
    src/components/Grammar/stages/ExerciseStage.jsx
    src/components/Grammar/GrammarLesson.jsx
    src/components/Grammar/grammarStyles.js
    src/data/grammar.js
  </read_first>
  <action>
**Step 1: Add 9 new exercise type renderers to ExerciseStage.jsx.**

The existing renderer handles `fill-blank`, `translate`, and `match` via `{exercise.type === 'xxx' && (...)}` conditionals inside the `bodyStyle` div. Add 9 more conditionals following the same pattern. All new renderers use the existing style imports: `choiceStyle`, `choiceCorrectStyle`, `choiceWrongStyle`, `textStyle`, `FONTS`, `COLORS`.

**1. conjugation-drill** — Shows verb, root, and pronoun in the prompt area. Renders 4 options as buttons (same layout as `fill-blank`). Uses `onAnswerSelect(option)`. Correct answer matched via `exercise.answer`. Prompt text: `"Conjugate: {exercise.verb} ({exercise.paradigm}) for {exercise.pronoun}"`. Options formatted with `formatArabic()`.

**2. sentence-transformation** — Shows `exercise.prompt` and `exercise.hint` in the prompt area (hint in smaller italic text below prompt). This is a free-form type rendered as multiple-choice: compare input to `exercise.answer`. To keep consistent with the keyboard-shortcut pattern, render `exercise.options` as 4 buttons if present, or render the `answer` plus 3 distractors if `options` is not present. Use `onAnswerSelect(option)`.

NOTE: For consistency, ensure all sentence-transformation exercises in grammar.js include an `options` array with 4 items (answer + 3 distractors). This way the renderer always shows 4 buttons.

**3. word-order** — Shows `exercise.prompt` in the prompt area. Renders `exercise.options` (4 possible sentences) as buttons. Uses `onAnswerSelect(option)`. Correct answer matched via `exercise.answer`. Each option rendered with `formatArabic()`.

**4. error-identification** — Shows `exercise.sentence` with label "Find the error:" in the prompt area. Renders `exercise.options` as buttons. Uses `onAnswerSelect(option)`. Correct answer matched via `exercise.error`. Feedback shows: `"Correct! {exercise.error} → {exercise.correction}"` or the wrong feedback from the existing pattern. NOTE: The `handleAnswerSelect` in GrammarLesson.jsx checks `answer === item.answer || answer === item.correct`. For error-identification, set `exercise.answer = exercise.error` in the data so the existing check works (OR add `exercise.answer` field equal to `exercise.error` in grammar.js data).

IMPORTANT: To avoid modifying handleAnswerSelect logic, ensure every error-identification exercise in grammar.js has an `answer` field set equal to the `error` field value. The renderer shows `exercise.sentence` as the prompt and uses `exercise.options` as buttons.

**5. multiple-select** — Shows `exercise.prompt` in the prompt area. Renders `exercise.options` as toggle buttons (click to select/deselect). Track selected items in a local array via the existing `answers` state or a new internal state. This type is more complex because multiple answers are correct. Implementation approach:
- Add a `selectedMultiple` state tracking (use the `answers[currentIndex]` as an array instead of a single value).
- HOWEVER, to avoid complex state changes in GrammarLesson.jsx, simplify: render as standard buttons where the player must select ALL correct answers. When a correct answer is clicked, highlight it green. When all `correctAnswers` are selected, auto-advance.
- SIMPLEST approach: Convert to a sequential selection where the player picks correct answers one at a time. Or just use `onAnswerSelect` and check if the selected option is in `correctAnswers` — mark correct for each individual selection.
- RECOMMENDED: Treat multiple-select as a modified match-style interaction. Use the `onMatchSelect` handler pattern. Each click toggles selection. Add a "Check" button that validates when ready.

ACTUALLY — to maintain simplicity and avoid modifying GrammarLesson.jsx state management extensively, implement multiple-select as: show the prompt, show all options as buttons. When player clicks a correct option, it highlights green and is disabled. When player clicks a wrong option, it highlights red. After all `correctAnswers.length` correct options are selected, show feedback "All correct!" and auto-advance via `onAnswerSelect` with a special value.

SIMPLEST VIABLE: Add a special case in ExerciseStage for `multiple-select` that tracks its own local React state for selected items. When all correct items are selected, call `onAnswerSelect(exercise.correctAnswers.join(','))` — and in the data, set `exercise.answer = exercise.correctAnswers.join(',')` so the existing correctness check passes.

**6. true-false** — Shows `exercise.statement` in the prompt area. Renders exactly 2 buttons: "True" and "False". Uses `onAnswerSelect('true')` or `onAnswerSelect('false')`. In grammar.js data, set `exercise.answer = exercise.answer === true ? 'true' : 'false'` — actually store answer as string `'true'` or `'false'` to match the existing string comparison in handleAnswerSelect. Also add `exercise.options = ['true', 'false']` so keyboard shortcuts 1/2 work.

**7. cloze** — Shows `exercise.text` with blanks highlighted. For each blank sequentially, show `exercise.blanks[currentBlankIndex].options` as buttons. Track which blank the player is filling (internal state in ExerciseStage). When all blanks are filled correctly, call `onAnswerSelect` with a success signal. Set `exercise.answer` to a combined string of all blank answers joined by `|` and set `exercise.options` to the first blank's options for keyboard compatibility. This is the most complex renderer — keep it simple by only showing one blank at a time with its options.

**8. classify** — Shows `exercise.prompt` and the category labels. Renders each `exercise.items` as a draggable item. SIMPLIFY: Show items as buttons — player clicks an item, then clicks a category. Track matches. When all items are classified, auto-advance. SIMPLEST: Show each item one at a time with category options as buttons. Set `exercise.answer` to the correct category for the current item. Use `exercise.options = exercise.categories` so keyboard shortcuts work. But classify has multiple items...

FOR BOTH cloze and classify and multiple-select: These are inherently multi-step exercises. To keep the renderer simple and compatible with the existing single-answer `onAnswerSelect` flow:
- Manage internal state within ExerciseStage for these types
- Show one sub-question at a time
- On the final correct sub-answer, call `onAnswerSelect(exercise.answer)` to signal completion to GrammarLesson

For classify specifically: show one item at a time, options are the categories. `exercise.answer` = comma-joined correct categories for all items. Internal state tracks progress through items.

**9. build-sentence** — Shows `exercise.prompt` and the `exercise.words` as a word bank. Player clicks words in sequence to build a sentence. Track the built sequence internally. When sequence matches `exercise.answer`, auto-advance. SIMPLIFY: Show the scrambled words and 4 pre-composed sentence options as buttons (same as word-order). ACTUALLY: build-sentence is distinct from word-order — the player constructs rather than selects. But to keep it keyboard-friendly and simple: render the word bank as clickable tokens. As each token is clicked, it moves to an "answer" area. A "Check" button validates against `exercise.answer`. On correct, call `onAnswerSelect(exercise.answer)`.

Alternatively, keep build-sentence as multiple-choice with 4 options (the correct sentence and 3 wrong orderings) — this is functionally identical to word-order but with different prompt text. Add `exercise.options` with 4 sentence options. This is the SIMPLEST approach and maintains keyboard shortcut compatibility.

**FINAL DECISION ON COMPLEX TYPES:** To maintain the existing `onAnswerSelect` single-answer flow and keyboard shortcuts:
- `multiple-select`: manage internal toggle state; "Submit" button calls onAnswerSelect with joined answer
- `true-false`: 2 buttons, `options: ['true', 'false']`, `answer: 'true'` or `'false'`
- `cloze`: show one blank at a time with its options; on final blank, call onAnswerSelect
- `classify`: show one item at a time with category buttons; on final item, call onAnswerSelect
- `build-sentence`: render as 4-option multiple-choice (like word-order but prompt says "Build the sentence")

Ensure ExerciseStage.jsx receives `formatArabic` prop (already does) and uses it for all Arabic text in new renderers.

**Step 2: Add quiz-skip guard in GrammarLesson.jsx.**

In `handleNext` function, after the exercises stage completes (line 151-157 area), add a guard: if `lesson.quiz.length === 0`, skip the quiz stage and go directly to the complete stage. Specifically, change the transition at the end of the exercises stage:

```javascript
// After exercises complete:
if (lesson.quiz.length === 0) {
  // No quiz questions — skip to completion
  const exerciseScorePercent = Math.round((exerciseScore / lesson.exercises.length) * 100);
  dispatch(completeLesson({
    lessonId: lesson.id,
    exerciseScore: exerciseScorePercent,
    quizScore: 100, // No quiz = perfect quiz score
  }));
  dispatch(addXP(exerciseScore === lesson.exercises.length ? XP_REWARDS.PERFECT_LESSON : XP_REWARDS.LESSON_COMPLETE));
  setStage('complete');
} else {
  setStage('quiz');
}
```

This handles the edge case where stub lessons that were not fully expanded still have `quiz: []`. Though all 20 A1-A2 lessons SHOULD have 4 quiz questions after Task 1, this guard is defensive.

**Step 3: Run `npx vitest run` to confirm no regressions.**
  </action>
  <verify>
Run `npx vitest run` — full suite passes (no regressions from ExerciseStage or GrammarLesson changes).
Verify: `grep -c "exercise.type === " src/components/Grammar/stages/ExerciseStage.jsx` returns >= 12 (3 existing + 9 new).
Verify: `grep "conjugation-drill" src/components/Grammar/stages/ExerciseStage.jsx` finds a match.
Verify: `grep "word-order" src/components/Grammar/stages/ExerciseStage.jsx` finds a match.
Verify: `grep "quiz.length === 0" src/components/Grammar/GrammarLesson.jsx` finds a match.
  </verify>
  <acceptance_criteria>
- `npx vitest run` exits 0 (full suite green)
- `grep "exercise.type === 'conjugation-drill'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'sentence-transformation'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'word-order'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'error-identification'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'multiple-select'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'true-false'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'cloze'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'classify'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "exercise.type === 'build-sentence'" src/components/Grammar/stages/ExerciseStage.jsx` finds a match
- `grep "lesson.quiz.length === 0" src/components/Grammar/GrammarLesson.jsx` finds a match
  </acceptance_criteria>
  <done>ExerciseStage.jsx renders all 12 exercise types (3 existing + 9 new). GrammarLesson.jsx has a quiz-skip guard for lessons with no quiz questions. Full test suite passes without regressions.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/data/__tests__/grammarChecker.test.js` — all grammar data integrity tests pass
2. `npx vitest run` — full suite passes (no regressions)
3. `grep -c "cefrLevel:" src/data/grammar.js` returns value showing CEFR tags on all A1-A2 lessons
4. All 12 exercise type strings appear in ExerciseStage.jsx
</verification>

<success_criteria>
- grammarChecker.test.js validates 20+ A1-A2 lessons with 12+ exercises and 4+ types each
- ExerciseStage.jsx handles all 12 exercise types without silent rendering skips
- GrammarLesson.jsx safely handles lessons with 0 quiz questions
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/58-grammar-a1-a2-lesson-wiring/58-01-SUMMARY.md`
</output>
