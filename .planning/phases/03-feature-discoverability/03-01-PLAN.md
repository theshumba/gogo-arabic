---
phase: 03-feature-discoverability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Router/GameLayout.jsx
  - src/components/Router/GameLayout.module.css
  - src/components/World/WorldMap.jsx
  - src/components/World/WorldMap.module.css
autonomous: true

must_haves:
  truths:
    - "Player can open Activities menu from pause menu and navigate to Grammar, Roots, Reading, or Mini-Games"
    - "Player can see boss challenge nodes on the world map for unlocked zones"
    - "Player can click a boss node on world map to start a Word Duel battle"
    - "Player can click the review badge in HUD to start a review session when words are due"
  artifacts:
    - path: "src/components/Router/GameLayout.jsx"
      provides: "PauseMenu with Activities button, ActivitiesMenu sub-view with 4 feature navigation buttons"
      contains: "Activities"
    - path: "src/components/Router/GameLayout.module.css"
      provides: "Styles for Activities menu (grid layout, feature cards, responsive)"
      contains: "activitiesMenu"
    - path: "src/components/World/WorldMap.jsx"
      provides: "Boss node rendering per unlocked zone"
      contains: "bossNode"
    - path: "src/components/World/WorldMap.module.css"
      provides: "Boss node styles (positioning, sprite, label)"
      contains: "bossNode"
  key_links:
    - from: "src/components/Router/GameLayout.jsx"
      to: "/grammar, /roots, /mini-games, /mini-games/reading"
      via: "useNavigate() in PauseMenu/ActivitiesMenu"
      pattern: "navigate\\('/grammar'\\)|navigate\\('/roots'\\)|navigate\\('/mini-games'\\)|navigate\\('/mini-games/reading'\\)"
    - from: "src/components/World/WorldMap.jsx"
      to: "/battle?boss="
      via: "useNavigate() with boss query param"
      pattern: "navigate.*\\/battle\\?boss="
---

<objective>
Add Activities menu to pause menu (Grammar, Roots, Reading, Mini-Games) and boss challenge nodes to the world map. Verify existing review badge works.

Purpose: Surface hidden learning features so players discover them during gameplay without leaving the game world. DISC-01, DISC-02, DISC-03.
Output: Extended PauseMenu with Activities sub-view, WorldMap with boss nodes per zone.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Router/GameLayout.jsx
@src/components/Router/GameLayout.module.css
@src/components/World/WorldMap.jsx
@src/components/World/WorldMap.module.css
@src/data/bosses.js
@src/routes.jsx
@src/hooks/useGameNavigation.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Activities menu to PauseMenu</name>
  <files>
    src/components/Router/GameLayout.jsx
    src/components/Router/GameLayout.module.css
  </files>
  <action>
    Extend the PauseMenu component in GameLayout.jsx to add an Activities button and sub-view:

    1. Add `useState` for `showActivities` to PauseMenu component.
    2. Add an "Activities" button between Resume and Main Menu buttons.
    3. When Activities is clicked, show an ActivitiesMenu sub-view (inline component in same file) that replaces the pause menu content.
    4. ActivitiesMenu renders a 2x2 grid of feature cards:
       - **Grammar** (icon: text "قواعد", route: '/grammar') — "Learn Arabic grammar rules"
       - **Word Roots** (icon: text "جذور", route: '/roots') — "Explore Arabic root patterns"
       - **Reading** (icon: text "قراءة", route: '/mini-games/reading') — "Practice reading Arabic passages"
       - **Mini-Games** (icon: text "ألعاب", route: '/mini-games') — "Fun vocabulary practice games"
    5. Each card is a button that:
       - Calls `dispatch(toggleMenu())` to close pause menu
       - Then calls `navigate(route)` to go to the feature
    6. ActivitiesMenu has a "Back" button that returns to normal pause menu (`setShowActivities(false)`).
    7. Pass `navigate` (from `useNavigate()`) and `dispatch` as props to PauseMenu. The PauseMenu already lives inside GameLayout which has access to both. Update the PauseMenu function signature: `PauseMenu({ onResume, onMainMenu, onNavigate })` where `onNavigate` is a callback accepting a path string that closes menu and navigates.

    CSS additions to GameLayout.module.css:
    - `.pauseMenuBtnActivities` — same style as `.pauseMenuBtnResume` but with a different background (use `var(--color-cyan)` or similar accent color to differentiate)
    - `.activitiesMenu` — centered container, max-width 400px, padding 20px
    - `.activitiesGrid` — CSS grid, 2 columns, gap 12px
    - `.activityCard` — pixel-art button card style, background dark semi-transparent, border `2px solid var(--color-xp-gold)`, padding 16px, text-align center, cursor pointer, hover state with brightness increase
    - `.activityCardArabic` — Arabic text, font-family Amiri, font-size 20px, color gold
    - `.activityCardLabel` — English label, font-family pixel, font-size 9px, color white, margin-top 8px
    - `.activityCardDesc` — description text, font-family pixel, font-size 7px, color gray, margin-top 4px
    - `.activitiesBackBtn` — back button, same style as `.pauseMenuBtnMenu`
    - Add responsive breakpoints: at 480px, switch grid to 1 column, reduce padding

    IMPORTANT: The PauseMenu component currently receives `onResume` and `onMainMenu` props. Add `onNavigate` prop. In GameLayout's render, pass `onNavigate` as:
    ```
    onNavigate={(path) => {
      dispatch(toggleMenu());
      navigate(path);
    }}
    ```

    Do NOT use EventBus for this — it's a React-only navigation flow (pause menu is a React component using React Router).
  </action>
  <verify>
    `npx vite build` succeeds with no errors. Visually inspect that PauseMenu has 3 buttons (Resume, Activities, Main Menu). Activities opens the 4-card grid. Each card navigates to the correct route.
  </verify>
  <done>
    Pause menu shows Activities button. Clicking it shows 4 feature cards (Grammar, Roots, Reading, Mini-Games). Clicking a card closes the menu and navigates to the feature route. Back button returns to normal pause menu.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add boss challenge nodes to WorldMap</name>
  <files>
    src/components/World/WorldMap.jsx
    src/components/World/WorldMap.module.css
  </files>
  <action>
    Extend WorldMap.jsx to show boss challenge nodes for each zone:

    1. Import `BOSSES, getBossesByZone` from `../../data/bosses.js` and `useNavigate` from `react-router-dom`.
    2. Get `navigate` from `useNavigate()`.
    3. Create `BOSS_OFFSETS` constant — offset boss nodes from their zone position to avoid overlap:
       ```
       { x: 6, y: -3 }  // Place boss slightly right and above zone dot
       ```
    4. Create `renderBossNode(boss)` function:
       - Calculate position: `{ x: ZONE_POSITIONS[boss.zone].x + BOSS_OFFSETS.x, y: ZONE_POSITIONS[boss.zone].y + BOSS_OFFSETS.y }`
       - Only render if boss's zone is unlocked (`unlockedZones.includes(boss.zone)`)
       - Render a button with:
         - `className={styles.bossNode}`
         - Positioned absolutely via inline `left`/`top` percent
         - onClick: `navigate(\`/battle?boss=${boss.id}\`)` then call `onBack()` to close map
         - Boss emoji sprite in `styles.bossSprite`
         - Boss name in `styles.bossLabel`
         - Boss nameArabic in `styles.bossLabelArabic`
         - Difficulty shown as stars (e.g., "★★" for difficulty 2)
         - aria-label: `"Challenge ${boss.name} — Difficulty ${boss.difficulty}"`
    5. In the `mapInner` div, after `{ZONE_ORDER.map(renderZoneNode)}`, add:
       ```
       {BOSSES.filter(b => unlockedZones.includes(b.zone)).map(renderBossNode)}
       ```
    6. Add boss nodes to the legend:
       - New legend item with a sword icon (⚔️) and text "Boss Challenge"

    CSS additions to WorldMap.module.css:
    - `.bossNode` — position absolute, transform translate(-50%, -50%), cursor pointer, z-index 3 (above connections z-index 1, below zone nodes z-index 2 if any), background none, border none, padding 4px, display flex, flex-direction column, align-items center, transition transform 0.1s
    - `.bossNode:hover` — transform translate(-50%, -50%) scale(1.15)
    - `.bossSprite` — font-size 20px, filter drop-shadow for glow effect, line-height 1
    - `.bossLabel` — font-family pixel, font-size 6px, color white, margin-top 2px, white-space nowrap
    - `.bossLabelArabic` — font-family Amiri, font-size 10px, color var(--color-xp-gold), direction rtl
    - `.bossDifficulty` — font-size 8px, color var(--color-xp-gold)
    - At 480px breakpoint: hide bossLabel and bossLabelArabic (keep just sprite + difficulty stars for mobile)
  </action>
  <verify>
    `npx vite build` succeeds. WorldMap renders boss emoji nodes near unlocked zones. Clicking a boss node navigates to `/battle?boss={bossId}`. Locked zone bosses are not shown.
  </verify>
  <done>
    WorldMap shows boss challenge nodes (emoji + name + difficulty) for each unlocked zone. Clicking a boss navigates to the Word Duel battle. Boss nodes are hidden for locked zones. Legend includes boss entry.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify review badge functionality (DISC-03)</name>
  <files></files>
  <action>
    DISC-03 states "Daily review sessions can be started from the game HUD." Phase 1 (CRIT-04) already implemented this.

    Verify by reading the code (no changes needed):

    1. Confirm HUD.jsx lines 240-252 render a Review button when `reviewDueCount > 0`.
    2. Confirm the button's `onClick` calls `openReviewSession` which emits `EventBus.emit('open-review-session')`.
    3. Confirm GameLayout.jsx has a listener: `EventBus.on('open-review-session', handleOpenReviewSession)` that calls `navigate('/review')`.
    4. Confirm cleanup exists: `EventBus.off('open-review-session', handleOpenReviewSession)`.

    If all 4 checks pass, DISC-03 is satisfied. No code changes needed.

    If any check fails, fix the broken link.
  </action>
  <verify>
    Read HUD.jsx and GameLayout.jsx to confirm the review button -> EventBus -> navigate chain is intact. `npx vite build` succeeds (build already passes from prior tasks).
  </verify>
  <done>
    DISC-03 verified: Review badge in HUD is clickable, emits 'open-review-session' event, GameLayout navigates to /review. No code changes needed.
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` passes with no errors
2. PauseMenu has Activities button → opens 4-card grid → each navigates to correct feature route
3. WorldMap shows boss nodes for unlocked zones → clicking navigates to /battle?boss={id}
4. HUD Review badge exists and navigates to /review when clicked
5. No z-index regressions (Activities menu uses --z-pause-menu from parent)
6. Mobile responsive: Activities grid collapses to 1 column at 480px, boss labels hidden at 480px
</verification>

<success_criteria>
- Grammar, Roots, Reading, Mini-Games all accessible from in-game pause menu Activities button (DISC-01)
- Word Duel bosses accessible through world map interface with per-zone nodes (DISC-02)
- Review sessions startable from game HUD review badge (DISC-03, verify only)
- Build passes, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-discoverability/03-01-SUMMARY.md`
</output>
