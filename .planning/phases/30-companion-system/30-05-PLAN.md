---
phase: 30-companion-system
plan: 05
type: execute
wave: 4
depends_on: ["30-01", "30-02", "30-03", "30-04"]
files_modified:
  - src/store/slices/__tests__/companionSlice.test.js
  - src/game/systems/companions/__tests__/CompanionBattleAI.test.js
  - src/game/systems/companions/__tests__/CompanionManager.test.js
  - src/game/systems/companions/__tests__/CompanionContext.test.js
  - src/utils/__tests__/dialogueComplexity.test.js
  - src/utils/__tests__/companionRelationship.test.js
  - src/data/__tests__/companions.test.js
autonomous: true

must_haves:
  truths:
    - "companionSlice reducers correctly manage recruitment, relationships, party, and gifts"
    - "CompanionBattleAI selects correct actions for all 4 roles at various HP/MP thresholds"
    - "CompanionManager creates and destroys companion sprites correctly"
    - "CompanionContext respects 10-second cooldown between comments"
    - "dialogueComplexity returns correct Arabic ratio for each CEFR level"
    - "companionRelationship maps tiers correctly and calculates gift bonuses"
    - "Companion data has 12 entries with complete fields covering all zones, roles, and specialties"
    - "All 874 existing tests still pass (zero regressions)"
  artifacts:
    - path: "src/store/slices/__tests__/companionSlice.test.js"
      provides: "Redux slice tests for companion state management"
      contains: "recruitCompanion"
    - path: "src/game/systems/companions/__tests__/CompanionBattleAI.test.js"
      provides: "Battle AI behavior tree tests for all 4 roles"
      contains: "selectAction"
    - path: "src/utils/__tests__/dialogueComplexity.test.js"
      provides: "CEFR dialogue scaling tests"
      contains: "scaleDialogueComplexity"
    - path: "src/utils/__tests__/companionRelationship.test.js"
      provides: "Relationship tier and gift bonus tests"
      contains: "getRelationshipTier"
    - path: "src/data/__tests__/companions.test.js"
      provides: "Data integrity tests for companion definitions"
      contains: "COMPANIONS"
  key_links:
    - from: "src/store/slices/__tests__/companionSlice.test.js"
      to: "src/store/slices/companionSlice.js"
      via: "direct reducer testing"
      pattern: "reducer.*initialState.*action"
    - from: "src/game/systems/companions/__tests__/CompanionBattleAI.test.js"
      to: "src/game/systems/companions/CompanionBattleAI.js"
      via: "selectAction method testing"
      pattern: "new CompanionBattleAI"
---

<objective>
Create comprehensive tests for the companion system: companionSlice reducers and selectors, CompanionBattleAI behavior trees for all 4 roles, CompanionManager lifecycle, CompanionContext cooldown logic, utility function tests, and data integrity verification. Then run full regression to confirm 874+ tests pass.

Purpose: This ensures INTG-02 (all existing tests pass — zero regressions) and validates all companion requirements work correctly.

Output: 7 test files with 100+ new tests, full regression passing
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-companion-system/30-01-SUMMARY.md
@.planning/phases/30-companion-system/30-02-SUMMARY.md
@.planning/phases/30-companion-system/30-03-SUMMARY.md
@.planning/phases/30-companion-system/30-04-SUMMARY.md
@src/store/slices/companionSlice.js
@src/game/systems/companions/CompanionBattleAI.js
@src/game/systems/companions/CompanionManager.js
@src/game/systems/companions/CompanionContext.js
@src/utils/dialogueComplexity.js
@src/utils/companionRelationship.js
@src/data/companions.js
@src/data/companionDialogue.js
@src/game/systems/__tests__/mocks/sceneMock.js
@src/test/testUtils.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create companionSlice, utility, and data integrity tests</name>
  <files>
    src/store/slices/__tests__/companionSlice.test.js
    src/utils/__tests__/dialogueComplexity.test.js
    src/utils/__tests__/companionRelationship.test.js
    src/data/__tests__/companions.test.js
  </files>
  <action>
**companionSlice.test.js** — Test all 9 reducers and 6 selectors:

Use direct reducer testing pattern (same as magicSlice tests, inventorySlice tests):
```javascript
import companionReducer, { recruitCompanion, setActiveCompanion, ... } from '../companionSlice.js';

// Import initial state for baseline
const initialState = companionReducer(undefined, { type: '@@INIT' });
```

Test cases (minimum 30):

Reducers:
1. `recruitCompanion` — recruits existing companion, increments recruitedCount
2. `recruitCompanion` — skips duplicate recruitment (idempotent)
3. `recruitCompanion` — skips unknown companion ID
4. `setActiveCompanion` — sets battle companion
5. `setActiveCompanion` — sets exploration companion
6. `setActiveCompanion` — clears other slot if same companion (no duplicate)
7. `setActiveCompanion` — rejects unrecruited companion
8. `removeActiveCompanion` — clears battle slot
9. `removeActiveCompanion` — clears exploration slot
10. `giveGift` — increases relationship and mood, records gift
11. `giveGift` — clamps relationship at 100
12. `giveGift` — clamps mood at 100
13. `updateRelationship` — positive increment
14. `updateRelationship` — negative decrement (clamps at 0)
15. `setCompanionMood` — sets mood within bounds
16. `recordDialogueLine` — adds to dialogue history
17. `recordDialogueLine` — caps at 50 entries (FIFO)
18. `updateCompanionLevel` — updates level (min 1)
19. `clearBattleCompanionState` — resets all battle stats

Selectors:
20. `selectAllCompanions` — returns companions object
21. `selectCompanion` — returns specific companion
22. `selectActiveParty` — returns active party object
23. `selectRecruitedCompanions` — filters to recruited only
24. `selectCompanionRelationship` — returns relationship value
25. `selectCompanionsByZone` — filters by zone

**dialogueComplexity.test.js** — Test CEFR scaling:

Test cases (minimum 10):
1. `getArabicRatio('A1')` returns 0.2
2. `getArabicRatio('C2')` returns 0.95
3. `getArabicRatio(undefined)` returns 0.5 (fallback)
4. `scaleDialogueComplexity` at A1 → primary is English, showTransliteration = true
5. `scaleDialogueComplexity` at B2 → primary is Arabic, showTransliteration = false
6. `scaleDialogueComplexity` at C1 → primary is Arabic, secondary is null (hidden)
7. `scaleDialogueComplexity` with missing arabic field → handles gracefully
8. `scaleDialogueComplexity` is deterministic (same input = same output, no Math.random)
9. `CEFR_ARABIC_RATIOS` has all 6 levels
10. `scaleDialogueComplexity` at A2 → primary is English (40% < 50% threshold)

**companionRelationship.test.js** — Test tier mapping and gift bonuses:

Test cases (minimum 12):
1. `getRelationshipTier(0)` returns stranger tier
2. `getRelationshipTier(25)` returns acquaintance tier
3. `getRelationshipTier(50)` returns friend tier
4. `getRelationshipTier(70)` returns closeFriend tier
5. `getRelationshipTier(90)` returns bestFriend tier
6. `getRelationshipTier(100)` returns bestFriend tier (boundary)
7. `getRelationshipTier(-5)` handles negative (returns stranger)
8. `getGiftBonus('books', ['books', 'scrolls'])` returns base * 1.5 (preferred match)
9. `getGiftBonus('food', ['books', 'scrolls'])` returns base * 1.0 (no match)
10. `getGiftBonus('unknown_category', [])` handles gracefully (returns 0 or default)
11. `getRelationshipMultiplier(0)` returns 1.0 (stranger)
12. `getRelationshipMultiplier(90)` returns 1.2 (best friend)

**companions.test.js** — Data integrity tests:

Test cases (minimum 15):
1. COMPANIONS has exactly 12 entries
2. All companion IDs start with 'companion_'
3. All companions have required fields (id, name, nameArabic, zone, battleRole, teachingSpecialty, baseStats, spriteKey, colorPalette, recruitCondition)
4. All 4 battle roles have at least 2 companions (distribution)
5. All 4 teaching specialties have at least 2 companions (distribution)
6. All 6 zones have at least 2 companions
7. No duplicate companion IDs
8. All baseStats have hp, mp, damage, defense > 0
9. All colorPalette have primary, secondary, accent colors
10. All recruitCondition have valid type ('quest' | 'storyFlag' | 'relationship' | 'level')
11. `getCompanion(validId)` returns companion data
12. `getCompanion(invalidId)` returns null/undefined
13. `getCompanionsByZone('sacred_library')` returns 2+ companions
14. All preferredGifts reference valid GIFT_CATEGORIES keys
15. COMPANION_DIALOGUE has entries for all 12 companion IDs

Also validate COMPANION_DIALOGUE:
16. Each companion has at minimum 10 greetings
17. Each companion has zone_comments, object_comments, battle_comments, teaching, relationship, idle
18. All dialogue lines have arabic, english, transliteration fields
19. Total dialogue lines across all companions >= 2,400
  </action>
  <verify>
    - `npx vitest run src/store/slices/__tests__/companionSlice.test.js` — all tests pass
    - `npx vitest run src/utils/__tests__/dialogueComplexity.test.js` — all tests pass
    - `npx vitest run src/utils/__tests__/companionRelationship.test.js` — all tests pass
    - `npx vitest run src/data/__tests__/companions.test.js` — all tests pass
  </verify>
  <done>
    - companionSlice tests cover all 9 reducers and 6 selectors (30+ tests)
    - dialogueComplexity tests verify CEFR scaling (10+ tests)
    - companionRelationship tests verify tier mapping and gift bonuses (12+ tests)
    - companions data integrity tests verify 12 entries, role/specialty/zone distribution (15+ tests)
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CompanionBattleAI and CompanionManager/Context tests, run full regression</name>
  <files>
    src/game/systems/companions/__tests__/CompanionBattleAI.test.js
    src/game/systems/companions/__tests__/CompanionManager.test.js
    src/game/systems/companions/__tests__/CompanionContext.test.js
  </files>
  <action>
**CompanionBattleAI.test.js** — Test all 4 behavior trees:

Setup: Mock `store.getState()` to return companion state with controllable relationship values. Import CompanionBattleAI directly.

```javascript
import { vi, describe, it, expect, beforeEach } from 'vitest';

// Mock store
vi.mock('../../../../store/store.js', () => ({
  store: {
    getState: vi.fn(() => ({
      companions: {
        companions: {
          companion_test: { relationship: 50 },
        },
      },
    })),
  },
}));
```

Test cases (minimum 25):

Healer:
1. Healer heals player when player HP < 40% and MP >= 15
2. Healer heals self when companion HP < 40% and MP >= 15
3. Healer heals player when player HP < 70% and MP >= 10
4. Healer attacks when all HP above threshold
5. Healer attacks when MP too low to heal

Attacker:
6. Attacker uses skill when enemy HP > 50% and MP >= 20
7. Attacker power attacks when enemy HP < 25% (finish off)
8. Attacker basic attacks as fallback
9. Attacker skill has higher damage than basic attack

Defender:
10. Defender defends player when player HP < 50%
11. Defender dispels enemy buff when MP >= 10
12. Defender attacks as fallback
13. Defender does not defend when player HP >= 50%

Support:
14. Support buffs strength when no existing strength buff
15. Support buffs defense when no existing defense_up buff
16. Support buffs accuracy as third priority
17. Support attacks when MP depleted
18. Support does not apply duplicate buff

General:
19. Relationship at 0 (stranger) gives multiplier 1.0
20. Relationship at 50 (friend) gives multiplier 1.1
21. Relationship at 90 (best friend) gives multiplier 1.2
22. Damage values are integers (Math.floor applied)
23. Heal amounts are integers
24. Unknown role defaults to basic attack
25. selectAction returns object with 'action' and 'target' fields

**CompanionManager.test.js** — Test lifecycle:

Setup: Use sceneMock from `src/game/systems/__tests__/mocks/sceneMock.js`. Mock window.matchMedia for Phaser compatibility.

```javascript
import { vi, describe, it, expect, beforeEach } from 'vitest';
import { CompanionManager } from '../../companions/CompanionManager.js';
import { createSceneMock } from '../mocks/sceneMock.js';

// Mock store, EventBus, Companion sprite
```

Test cases (minimum 10):
1. Constructor creates CompanionManager instance
2. Spawns companion when exploration party slot is set
3. Despawns companion when exploration party slot cleared
4. Handles party change event (swap companion)
5. Does not spawn if companion ID not in COMPANIONS
6. Uses fallback texture if companion sprite not loaded
7. getActiveCompanion returns current companion
8. getActiveCompanionId returns current ID
9. destroy cleans up EventBus listener and companion sprite
10. update calls companion.update and context.evaluateTriggers

**CompanionContext.test.js** — Test trigger evaluation:

Test cases (minimum 8):
1. emits COMPANION_CONTEXTUAL_COMMENT on zone change
2. does not emit within 10-second cooldown
3. does not emit same comment twice (shownComments tracking)
4. emits battle comment for victory outcome
5. emits battle comment for defeat outcome
6. reset clears shownComments and cooldown
7. handles missing companion dialogue gracefully
8. handles null zone gracefully

After all tests pass individually, run full regression:
```bash
npx vitest run
```

Verify total test count is 874 + new tests (expect ~970-1000+ total). Zero failures.

Also verify build:
```bash
npx vite build
```

Report final test count and build status.
  </action>
  <verify>
    - `npx vitest run src/game/systems/companions/__tests__/CompanionBattleAI.test.js` — all tests pass
    - `npx vitest run src/game/systems/companions/__tests__/CompanionManager.test.js` — all tests pass
    - `npx vitest run src/game/systems/companions/__tests__/CompanionContext.test.js` — all tests pass
    - `npx vitest run` — ALL tests pass (874 existing + new companion tests), zero failures
    - `npx vite build` — build succeeds
  </verify>
  <done>
    - CompanionBattleAI tests verify all 4 role behavior trees with 25+ test cases
    - CompanionManager tests verify lifecycle (spawn/despawn/swap) with 10+ test cases
    - CompanionContext tests verify cooldown and trigger evaluation with 8+ test cases
    - Full regression: 874 existing + ~100+ new = 970+ total tests, zero failures
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. All new test files pass individually
2. Full regression passes (874 existing + ~100 new companion tests, zero failures)
3. `npx vite build` succeeds
4. companionSlice reducers and selectors fully tested
5. CompanionBattleAI behavior trees tested for all 4 roles at edge thresholds
6. CompanionManager lifecycle tested (spawn, despawn, swap, destroy)
7. CompanionContext cooldown and trigger logic tested
8. Utility functions (dialogueComplexity, companionRelationship) tested
9. Data integrity verified (12 companions, 2,400+ dialogue lines, complete fields)
</verification>

<success_criteria>
- Zero test failures across entire suite
- 100+ new tests added
- Build succeeds
- All existing 874 tests still pass (INTG-02 satisfied)
- Every companion requirement (COMP-01 through COMP-12) has at least one test covering its core behavior
</success_criteria>

<output>
After completion, create `.planning/phases/30-companion-system/30-05-SUMMARY.md`
</output>
