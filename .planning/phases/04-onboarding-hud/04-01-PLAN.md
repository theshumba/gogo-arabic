---
phase: 04-onboarding-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.js
  - src/styles/variables.css
  - src/components/HUD/HUD.module.css
  - src/components/Onboarding/OnboardingFlow.module.css
  - src/components/Battle/WordDuel.module.css
  - src/components/UI/LevelUpModal.module.css
  - src/components/Goals/DailyGoalsPanel.module.css
  - src/components/Goals/StreakRewardToast.module.css
  - src/components/HUD/QuestTracker.module.css
  - src/components/HUD/MiniMap.module.css
  - src/components/World/WorldMap.module.css
  - src/components/Quest/QuestLog.module.css
  - src/components/NPC/DialogueOverlay.module.css
  - src/components/Quiz/QuizOverlay.module.css
autonomous: true

must_haves:
  truths:
    - "Main JavaScript bundle (index chunk) is under 500KB after build"
    - "Phaser, React, and Redux are in separate vendor chunks"
    - "All top-level component CSS files use z-index token variables instead of literal integers"
    - "Build succeeds with no regressions"
  artifacts:
    - path: "vite.config.js"
      provides: "manualChunks configuration for vendor splitting"
      contains: "manualChunks"
    - path: "src/styles/variables.css"
      provides: "Z-index token definitions"
      contains: "--z-"
  key_links:
    - from: "vite.config.js"
      to: "node_modules/phaser"
      via: "manualChunks function"
      pattern: "includes.*phaser"
    - from: "src/components/HUD/HUD.module.css"
      to: "src/styles/variables.css"
      via: "CSS custom property reference"
      pattern: "var\\(--z-"
---

<objective>
Configure Vite bundle splitting to bring main JS bundle under 500KB, and standardize all top-level z-index values to use CSS custom property tokens from variables.css.

Purpose: PERF-01 ensures fast initial load by splitting 2.9MB monolithic bundle into cacheable vendor chunks. HUD-03 ensures consistent z-index layering across all overlays and HUD elements by eliminating magic numbers.

Output: Updated vite.config.js with manualChunks, all top-level CSS files using z-index tokens.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/variables.css
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vite manualChunks for vendor bundle splitting</name>
  <files>vite.config.js</files>
  <action>
Add `build.rollupOptions.output.manualChunks` function to vite.config.js that splits vendor dependencies into separate chunks:

1. `phaser` chunk: modules containing `node_modules/phaser` (~1.2MB)
2. `react-vendor` chunk: modules containing `node_modules/react`, `node_modules/react-dom`, `node_modules/framer-motion` (~400KB)
3. `redux-vendor` chunk: modules containing `node_modules/redux`, `node_modules/@reduxjs`, `node_modules/react-redux` (~200KB)
4. `router-vendor` chunk: modules containing `node_modules/react-router` (~100KB)
5. All other `node_modules` go to `misc-vendor` chunk

Keep existing config (plugins, publicDir, server, build.outDir, build.emptyOutDir) intact. Add the rollupOptions.output section under build.

After adding config, run `npx vite build` and verify:
- Build succeeds
- Output shows multiple chunk files (phaser, react-vendor, redux-vendor, etc.)
- The main index chunk is under 500KB (raw size)

If index chunk exceeds 500KB, also add `ts-fsrs` as a separate chunk and check if vocabularyAll.js data needs lazy-loading (dynamic import). The target is index.js raw size < 500KB.

Do NOT add rollup-plugin-visualizer or any new dev dependencies.
  </action>
  <verify>
Run `npx vite build` and confirm:
1. Build exits 0 (success)
2. Output lists separate chunk files for phaser, react-vendor, redux-vendor
3. Index/main chunk raw size is under 500KB
  </verify>
  <done>
Vite build produces separate vendor chunks. Main application bundle (index chunk) is under 500KB raw size. No build errors or warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Standardize z-index values to CSS custom property tokens</name>
  <files>
src/styles/variables.css
src/components/HUD/HUD.module.css
src/components/Onboarding/OnboardingFlow.module.css
src/components/Battle/WordDuel.module.css
src/components/UI/LevelUpModal.module.css
src/components/Goals/DailyGoalsPanel.module.css
src/components/Goals/StreakRewardToast.module.css
src/components/HUD/QuestTracker.module.css
src/components/HUD/MiniMap.module.css
src/components/World/WorldMap.module.css
src/components/Quest/QuestLog.module.css
src/components/NPC/DialogueOverlay.module.css
src/components/Quiz/QuizOverlay.module.css
  </files>
  <action>
Convert all top-level component z-index literal values to use existing CSS custom property tokens from variables.css. The tokens are already defined:

```
--z-phaser-overlay: 10;
--z-minimap: 90;
--z-hud: 100;
--z-toast: 150;
--z-overlay: 200;
--z-pause-menu: 200;
--z-battle: 250;
--z-level-up: 300;
--z-error: 9999;
--z-onboarding: 10000;
```

Mapping for each file (top-level z-index only, leave internal component z-index like `z-index: 1`, `z-index: 2` as literals):

| File | Current | Token |
|------|---------|-------|
| HUD.module.css `.container` | `z-index: 100` | `z-index: var(--z-hud)` |
| OnboardingFlow.module.css `.overlay` | `z-index: 10000` | `z-index: var(--z-onboarding)` |
| WordDuel.module.css (top-level) | `z-index: 250` | `z-index: var(--z-battle)` |
| LevelUpModal.module.css (top-level) | `z-index: 300` | `z-index: var(--z-level-up)` |
| DailyGoalsPanel.module.css (top-level) | `z-index: 200` | `z-index: var(--z-overlay)` |
| StreakRewardToast.module.css (top-level) | `z-index: 150` | `z-index: var(--z-toast)` |
| QuestTracker.module.css (top-level) | `z-index: 99` | Keep literal 99 — it's intentionally 1 below HUD, not a separate layer token |
| MiniMap.module.css (top-level) | `z-index: 90` | `z-index: var(--z-minimap)` |
| WorldMap.module.css (top-level, first `z-index: 200`) | `z-index: 200` | `z-index: var(--z-overlay)` |
| QuestLog.module.css (top-level) | `z-index: 200` | `z-index: var(--z-overlay)` |
| DialogueOverlay.module.css (top-level) | `z-index: 200` | `z-index: var(--z-overlay)` |
| QuizOverlay.module.css (top-level) | `z-index: 200` | `z-index: var(--z-overlay)` |

**Important:** Leave internal z-index values (e.g., `z-index: 1` inside WorldMap for internal layering, `z-index: 1` inside LevelUpModal and WordDuel) as literal numbers. Only convert top-level component layer positioning.

Each CSS Module file that doesn't already import variables.css needs `@import '../../styles/variables.css';` (or correct relative path) at the top. Check each file before editing — some already have this import.

Also add `@import` for variables.css in any file that currently doesn't import it but will now use `var(--z-*)` tokens.
  </action>
  <verify>
1. Run `grep -r "z-index: [0-9]" src/components/ --include="*.css"` — remaining literal z-index values should only be internal (1, 2, 3, 10, 11, 20, 99 for QuestTracker)
2. Run `grep -r "var(--z-" src/components/ --include="*.css"` — should show token usage in all top-level component CSS files
3. Run `npx vite build` — build succeeds with no CSS errors
  </verify>
  <done>
All top-level component z-index values use CSS custom property tokens. Internal component z-index stays as literals. Build passes. Only `GameLayout.module.css` (already done) and all overlay/HUD CSS files use `var(--z-*)` tokens.
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds — no errors, no regressions
2. Build output shows separate vendor chunks (phaser, react-vendor, redux-vendor)
3. Main index chunk is under 500KB raw size
4. `grep -c "var(--z-" src/components/**/*.module.css` shows token usage across 10+ files
5. No top-level z-index literals remain in overlay/HUD CSS files (only internal component z-index)
</verification>

<success_criteria>
- PERF-01: Main JavaScript bundle is under 500KB with vendor code in separate chunks
- HUD-03: All top-level z-index values use CSS custom property tokens from variables.css
- Build passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-onboarding-hud/04-01-SUMMARY.md`
</output>
