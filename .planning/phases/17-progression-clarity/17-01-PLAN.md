---
phase: 17-progression-clarity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LearningPath/LearningPath.jsx
  - src/components/LearningPath/LearningPath.module.css
  - src/components/Dashboard/DailyDashboard.jsx
  - src/components/Dashboard/DailyDashboard.module.css
  - src/routes.jsx
  - src/hooks/useGameNavigation.js
autonomous: true

must_haves:
  truths:
    - "User can access a Learning Path menu showing alphabet -> vocabulary -> grammar progression"
    - "New players see a 'Start Here' indicator on the alphabet section of Learning Path"
    - "User sees a Learning Dashboard with review queue count, available lessons, and next-step indicators"
    - "Learning Path is accessible from DailyDashboard and from the pause menu"
  artifacts:
    - path: "src/components/LearningPath/LearningPath.jsx"
      provides: "Learning Path menu component with 3-stage progression"
      min_lines: 80
    - path: "src/components/LearningPath/LearningPath.module.css"
      provides: "Pixel-art styled Learning Path layout"
      min_lines: 60
  key_links:
    - from: "src/components/LearningPath/LearningPath.jsx"
      to: "src/store/slices/alphabetSlice.js"
      via: "useSelector(selectAlphabetProgress)"
      pattern: "selectAlphabetProgress"
    - from: "src/components/LearningPath/LearningPath.jsx"
      to: "src/store/slices/vocabularySlice.js"
      via: "useSelector(selectLearnedWordCount)"
      pattern: "selectLearnedWordCount"
    - from: "src/components/LearningPath/LearningPath.jsx"
      to: "src/store/slices/grammarSlice.js"
      via: "useSelector(selectGrammarProgress)"
      pattern: "selectGrammarProgress"
    - from: "src/components/Dashboard/DailyDashboard.jsx"
      to: "/learning-path"
      via: "navigation link to Learning Path"
      pattern: "goToLearningPath|learning-path"
---

<objective>
Create a Learning Path menu showing alphabet -> vocabulary -> grammar progression with "Start Here" for new players, and enhance the DailyDashboard with a learning progress section and next-step guidance.

Purpose: Addresses the core user complaint "no sense of direction" and "can't find letter learning" by creating a clear, visual progression path that shows what to learn and in what order. The DailyDashboard becomes the natural hub for learning guidance.

Output: LearningPath component (new route `/learning-path`), enhanced DailyDashboard with learning progress section
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/routes.jsx
@src/hooks/useGameNavigation.js
@src/components/Dashboard/DailyDashboard.jsx
@src/components/Dashboard/DailyDashboard.module.css
@src/store/slices/alphabetSlice.js
@src/store/slices/vocabularySlice.js
@src/store/slices/grammarSlice.js
@src/store/slices/questSlice.js
@src/components/Router/GameLayout.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LearningPath component and route</name>
  <files>
    src/components/LearningPath/LearningPath.jsx
    src/components/LearningPath/LearningPath.module.css
    src/routes.jsx
    src/hooks/useGameNavigation.js
  </files>
  <action>
    Create a new LearningPath component that displays a vertical 3-stage progression path:

    **Stage 1: Alphabet (Letters)**
    - Show progress: X/7 groups completed (each group = 4 letters, 28 total)
    - Use `selectAlphabetProgress` from alphabetSlice for completion data
    - Use `selectCompletedGroups` to count completed groups
    - Button: "Learn Letters" -> navigates to `/alphabet`
    - If completedGroups.length === 0, show a pulsing "START HERE" badge with `styles.startHere` class (bright green background, pixel-art border, CSS pulse animation). This is the key new-player guidance.

    **Stage 2: Vocabulary (Words)**
    - Show progress: X/1220 words learned
    - Use `selectLearnedWordCount` from vocabularySlice for count
    - Use `selectReviewQueueCount` for due reviews
    - Button: "Review Words" -> navigates to `/review`
    - If reviews due > 0, show badge with count
    - If letters completed < 2 groups, show lock icon and muted styling with text "Complete 2 letter groups first" (soft-lock, not hard-lock -- button still works)

    **Stage 3: Grammar**
    - Show progress: X% completed
    - Use `selectGrammarProgress` from grammarSlice
    - Button: "Study Grammar" -> navigates to `/grammar`
    - If letters completed < 4 groups, show lock icon and muted text "Complete 4 letter groups first" (soft-lock, button still works)

    **Layout:**
    - Full-screen page layout matching DailyDashboard style (centered, max-width 600px, `var(--color-beige)` background)
    - Vertical progression with connecting line/arrow between stages (CSS border-left or pseudo-element)
    - Each stage is a card with: icon, title, progress bar, progress text, action button
    - Back button at top -> `onBack` prop (navigates back)
    - Use `useGameNavigation()` for all navigation (goToAlphabet, goToReview, goToGrammar)
    - Import `useSelector` from react-redux, `motion` from framer-motion
    - Use CSS Modules (LearningPath.module.css)
    - Follow pixel-art styling: `var(--font-pixel)`, `var(--color-dark)` borders, box-shadow patterns from DailyDashboard.module.css
    - Respect reduced motion: `window.matchMedia('(prefers-reduced-motion: reduce)').matches`

    **Route setup:**
    - Add lazy import for LearningPath in `src/routes.jsx`: `const LearningPath = lazy(() => import('./components/LearningPath/LearningPath.jsx'));`
    - Create `LearningPathRoute` function component following the same pattern as other routes (PageTransition > ErrorBoundary > Suspense > LearningPath with onBack={goBack})
    - Add route entry: `{ path: '/learning-path', element: <LearningPathRoute />, errorElement: <RouteErrorBoundary /> }`
    - Add `goToLearningPath` to `useGameNavigation.js`: `const goToLearningPath = useCallback(() => { navigate('/learning-path'); }, [navigate]);` and export it

    **CSS structure (LearningPath.module.css):**
    - `.container` — full viewport, centered, beige background (match DailyDashboard.container)
    - `.content` — max-width 600px, flex column, gap 0 (stages connect visually)
    - `.header` — flex row, back button + title "Learning Path"
    - `.backBtn` — pixel-art button style matching existing `.btn` from HUD
    - `.stage` — card with 4px solid border, white background, padding 20px, position relative
    - `.stageConnector` — pseudo-element or div, 4px wide vertical line connecting stages
    - `.stageIcon` — 32px font-size, the stage emoji/icon
    - `.stageTitle` — 14px, uppercase, pixel font
    - `.progressBar` — reuse same pattern as DailyDashboard progress bars
    - `.progressText` — 10px, brown color
    - `.stageBtn` — action button per stage, pixel-art styled
    - `.startHere` — bright green pulsing badge, positioned at top-right of Stage 1 card
    - `.softLock` — muted opacity (0.7), lock icon prefix
    - Mobile responsive: stack naturally (already column), ensure 44px min touch targets
  </action>
  <verify>
    1. `npx vite build` completes without errors
    2. Navigate to `/learning-path` in browser — page loads with 3 stages
    3. Verify "START HERE" badge appears when completedGroups is empty
    4. Verify stage buttons navigate to correct routes (/alphabet, /review, /grammar)
    5. Verify back button works
  </verify>
  <done>
    LearningPath page renders at `/learning-path` showing 3-stage progression (alphabet -> vocabulary -> grammar) with real Redux data, "Start Here" indicator for new players, and working navigation to each learning module
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance DailyDashboard with learning progress and Learning Path link</name>
  <files>
    src/components/Dashboard/DailyDashboard.jsx
    src/components/Dashboard/DailyDashboard.module.css
    src/components/Router/GameLayout.jsx
  </files>
  <action>
    **DailyDashboard enhancements:**

    1. Add a new "Learning Progress" section ABOVE the existing "Reviews Due" section. This section shows at-a-glance progress for all 3 learning domains:
       - Import `selectAlphabetProgress` from alphabetSlice, `selectLearnedWordCount` from vocabularySlice, `selectGrammarProgress` from grammarSlice, `selectCompletedQuests` from questSlice
       - Add `useGameNavigation()` import of `goToLearningPath` (add to existing destructuring)
       - Create a `learningProgress` section with 3 mini stat cards in a row (reuse `statsGrid` 3-column pattern):
         - Letters: `{completedGroups * 4}/28` with label "Letters"
         - Words: `{learnedWordCount}/1220` with label "Words"
         - Quests: `{Object.keys(completedQuests).length}/52` with label "Quests"
       - Below the stats grid, add a "View Learning Path" button that calls `goToLearningPath()`. Style it as a secondary action button — pixel-art border, `var(--color-cyan)` background, full width, 12px pixel font.

    2. Enhance the `suggestedActivity` logic to prioritize alphabet learning for new players:
       - Add a new condition at the TOP of the useMemo (before reviewsDue check):
         ```
         const completedGroupCount = alphabetProgress.completed;
         if (completedGroupCount === 0) {
           return {
             title: 'Start Learning Letters',
             description: 'Begin your Arabic journey with the alphabet!',
             action: goToAlphabet,
             icon: 'أ',
             color: 'var(--color-green)',
           };
         }
         ```
       - This ensures brand new players are immediately guided to alphabet learning from the dashboard
       - Import `goToAlphabet` from `useGameNavigation()`

    **DailyDashboard.module.css additions:**
    - `.learningSection` — same card styling as `.reviewsSection` (white bg, 4px border, box-shadow)
    - `.learningGrid` — display grid, 3 columns, gap 12px (same as `.statsGrid`)
    - `.learningCard` — same styling as `.statCard`
    - `.learningPathBtn` — full width, `var(--color-cyan)` background, white text, 4px solid dark border, pixel font, uppercase, box-shadow matching `.continueBtn` pattern, 44px min-height for touch targets
    - Mobile responsive: `.learningGrid` goes to `grid-template-columns: repeat(3, 1fr)` on all sizes (compact enough to stay 3-col)

    **GameLayout PauseMenu enhancement:**
    - In the `ActivitiesMenu` component inside `GameLayout.jsx`, add a new activity entry at the BEGINNING of the `activities` array:
      ```
      {
        id: 'learning-path',
        icon: 'مسار',
        label: 'Learning Path',
        description: 'See your learning progression',
        route: '/learning-path',
      },
      ```
    - This makes Learning Path accessible from the in-game pause menu
  </action>
  <verify>
    1. `npx vite build` completes without errors
    2. Navigate to `/dashboard` — "Learning Progress" section visible with Letters/Words/Quests counts
    3. "View Learning Path" button navigates to `/learning-path`
    4. New player (0 completed groups) sees "Start Learning Letters" as suggested activity
    5. Pause menu > Activities shows "Learning Path" option
  </verify>
  <done>
    DailyDashboard shows learning progress metrics (letters/words/quests counts), links to Learning Path, and suggests alphabet learning for new players. Learning Path is accessible from pause menu Activities.
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` — zero errors, zero warnings related to new files
2. Navigate to `/learning-path` — full 3-stage progression visible
3. Navigate to `/dashboard` — learning progress section with real data
4. New player flow: dashboard suggests "Start Learning Letters", Learning Path shows "Start Here" on alphabet
5. Pause menu > Activities includes "Learning Path" entry
6. All navigation buttons work (alphabet, review, grammar, learning path, back)
</verification>

<success_criteria>
- LearningPath component renders at `/learning-path` with 3 connected stages showing real progress data from Redux
- "Start Here" badge visible on alphabet stage when completedGroups is empty
- DailyDashboard has learning progress section with 3 metric cards and Learning Path link
- DailyDashboard suggests alphabet for players with 0 completed groups
- Learning Path accessible from pause menu Activities
- All styling follows pixel-art conventions (CSS Modules, CSS variables, box-shadows)
- Mobile responsive with WCAG touch targets
</success_criteria>

<output>
After completion, create `.planning/phases/17-progression-clarity/17-01-SUMMARY.md`
</output>
