---
phase: 11-architecture-cleanup
plan: 05
type: execute
wave: 2
depends_on: ["11-02", "11-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 9 overlay types render in correct z-index order on the game screen"
    - "EventBus events fire correctly: NPC interaction opens dialogue, chest opens with notification, zone transition works"
    - "Pause menu opens via HUD button, Activities sub-menu navigates correctly"
    - "Keyboard shortcuts M and L work when no overlay is open"
    - "Session time tracking starts on game load (visible in daily dashboard)"
    - "CSS Module-migrated components render with correct colors and layout"
  artifacts: []
  key_links: []
---

<objective>
Verify that GameLayout refactoring (plan 02) and CSS Modules migration (plan 04) did not introduce visual or functional regressions.

Purpose: The GameLayout god component extraction and inline style migration are the highest-risk changes in Phase 11. A human verification step ensures overlays stack correctly, EventBus communication works, and styles look right before marking the phase complete.

Output: Confirmation that refactored components work correctly in the browser.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-architecture-cleanup/11-02-SUMMARY.md
@.planning/phases/11-architecture-cleanup/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build to confirm no regressions</name>
  <files></files>
  <action>
    Run the complete verification battery:
    1. `npx vitest run` — all 548+ tests must pass
    2. `npx vite build` — production build must succeed
    3. Verify GameLayout line count: `wc -l src/components/Router/GameLayout.jsx` — must be under 200
    4. Verify no file in the modified set exceeds 300 lines
    5. Count migrated components: `grep -rl 'const styles = {' src/components/World/SignOverlay.jsx src/components/Shop/ShopOverlay.jsx src/components/Achievements/AchievementPanel.jsx src/components/Menu/SettingsMenu.jsx src/components/Roots/RootExplorer.jsx src/components/HUD/NotificationToast.jsx src/components/Achievements/AchievementToast.jsx 2>/dev/null | wc -l` — must be 0
    6. Verify ESLint runs: `npm run lint 2>&1 | head -5` — no config errors
    7. Verify Prettier runs: `npm run format:check 2>&1 | head -5` — no config errors
  </action>
  <verify>
    All 7 checks above pass.
  </verify>
  <done>
    Full test suite passes, build succeeds, GameLayout under 200 lines, no inline styles in migrated components, lint and format scripts work.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 11 Architecture Cleanup:
    1. **GameLayout refactored** from 607 to ~175 lines. EventBus listeners extracted to useEventBusListeners hook. Session tracking and keyboard shortcuts extracted to separate hooks.
    2. **ESLint 9 + Prettier 3** configured with 4 npm scripts (lint, lint:fix, format, format:check).
    3. **7 components migrated** from inline styles to CSS Modules (SignOverlay, NotificationToast, AchievementToast, SettingsMenu, ShopOverlay, AchievementPanel, RootExplorer).
    4. **5 Redux slices** updated with named selector exports and createSelector for transformations.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **1. Game screen overlays (verify z-index stacking):**
    - Load the game (click Play from main menu)
    - Walk to an NPC and press SPACE — dialogue overlay appears on top of game canvas
    - Close dialogue, walk to a sign — sign overlay appears correctly
    - Press ESC or click HUD menu button — pause menu appears over game
    - From pause menu, click Activities — activities sub-menu renders
    - Close pause menu, press M — world map opens

    **2. EventBus communication (verify hooks work):**
    - Walk to a chest and interact — notification toast shows "Found X dirhams!"
    - Walk to a bookshelf and interact — notification toast shows word learned
    - Walk to a zone exit — zone transition animation plays

    **3. Keyboard shortcuts:**
    - Press M on keyboard — world map opens
    - Press L on keyboard — alphabet module opens
    - Open an overlay (e.g., pause menu), press M — should NOT navigate (shortcuts suppressed)

    **4. Visual inspection of migrated components:**
    - Open Settings (pause menu > Main Menu > Settings) — check layout and colors
    - Open Shop (talk to a merchant NPC) — check item cards and buttons
    - Open Achievements (from Player Profile) — check panel styling
    - Check notification toasts appear with correct styling

    **5. Session tracking:**
    - Play for at least 1 minute, then go to Daily Dashboard — session time should show > 0

    If anything looks wrong, describe the issue and which component/area is affected.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 11 complete, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all overlay stacking, EventBus communication, keyboard shortcuts, migrated component styles, and session tracking work correctly in the browser.
</verification>

<success_criteria>
- Human approves that all game functionality works correctly after refactoring
- No visual regressions in migrated components
- All overlays render in correct z-index order
- EventBus events work through extracted hooks
</success_criteria>

<output>
After completion, create `.planning/phases/11-architecture-cleanup/11-05-SUMMARY.md`
</output>
