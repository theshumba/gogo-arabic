---
phase: 97
plan: 06
type: execute
wave: 2
depends_on: [01, 03, 04, 05]
files_modified:
  - src/data/zones.js
autonomous: true
requirements: [WORLD-01, WORLD-02, WORLD-06]
must_haves:
  truths:
    - "Every `objects` array in the 8 core zones in zones.js uses Kenmi keys that exist in KENMI_CATALOG (verified by zoneAssetIntegrity.test.js)"
    - "Every NPC id, interactable id, exit target, gatheringSpot, vocabCategory, spawnPoint, mapWidth, mapHeight, id, name, nameArabic, and tilesetTheme for the 8 core zones is preserved byte-identical to pre-Plan-06"
    - "CinematicIntroSequencer coordinate assumption (oasis_village spawn region at coords 14,17 / 14,18 / 14,20) is preserved"
    - "All 2535 pre-existing tests continue to pass"
    - "zones.js has consistent biome-palette building selection per BIOME_BUILDING_SETS (no ad-hoc keys)"
  artifacts:
    - path: "src/data/zones.js"
      provides: "8 core zones with rebuilt objects arrays, all game-logic fields preserved"
      contains: "oasis_village,ancient_library,desert_marketplace,farmland,bedouin_camp,mountain_village,coastal_port,royal_palace"
  key_links:
    - from: "src/data/zones.js"
      to: "src/data/kenmiCatalog.js"
      via: "every objects[].key is a kenmi-* entry from KENMI_CATALOG"
      pattern: "kenmi-"
    - from: "src/data/zones.js"
      to: "src/data/spriteKeyMap.js"
      via: "BIOME_BUILDING_SETS[biome] keys chosen for each zone's building objects"
      pattern: "BIOME_BUILDING_SETS"
---

<objective>
Rebuild the `objects` array for each of the 8 core zones in `src/data/zones.js` so every entry uses a valid Kenmi key from KENMI_CATALOG and the visual palette is consistent within each biome. This is where the actual visual rebuild happens: previous plans fixed the infrastructure; this plan fixes the data.

Purpose: Today the zones pass WORLD-AUDIT's "Missing / Dangling Kenmi References" section with some number of entries (identified by Plan 01) — Plan 06 eliminates those by rewriting the objects arrays. No game-logic fields (npcs, interactables, exits, spawnPoint, mapWidth, mapHeight, gatheringSpots, vocabCategories, id, name, nameArabic, tilesetTheme) may change.

Output: a reshaped objects array per zone. CinematicIntroSequencer still works. zoneAssetIntegrity.test.js goes GREEN. 2535 tests still pass.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-visual-world-layer-rebuild/97-CONTEXT.md
@.planning/phases/97-visual-world-layer-rebuild/97-RESEARCH.md
@.planning/phases/97-visual-world-layer-rebuild/WORLD-AUDIT.md
@.planning/phases/97-visual-world-layer-rebuild/97-03-SUMMARY.md
@.planning/phases/97-visual-world-layer-rebuild/97-04-SUMMARY.md
@.planning/phases/97-visual-world-layer-rebuild/97-05-SUMMARY.md
@src/data/zones.js
@src/data/kenmiCatalog.js
@src/data/spriteKeyMap.js
@src/data/__tests__/zoneAssetIntegrity.test.js
@WORLD-VS-LOGIC-CONCERN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild objects arrays for all 8 core zones with valid Kenmi keys and biome-consistent palette</name>
  <files>src/data/zones.js</files>
  <read_first>
    - .planning/phases/97-visual-world-layer-rebuild/WORLD-AUDIT.md (Missing / Dangling Kenmi References section gives the exact fix list per zone; Zone Kenmi Key Inventory gives current state)
    - src/data/zones.js FULL FILE (1602 LOC — read before any edit; 8 zones have `objects: [{ key, x, y, collide?, collideW?, collideH? }, ...]` plus `npcs`, `interactables`, `exits`, `spawnPoint`, `mapWidth`, `mapHeight`, `tilesetTheme`, `id`, `name`, `nameArabic`, `vocabCategories`, `gatheringSpots`, `buildMap()` function)
    - src/data/spriteKeyMap.js (BIOME_BUILDING_SETS[biome].small/large/temple/etc. — use these arrays exclusively when picking building keys)
    - src/data/kenmiCatalog.js (for any non-building object — palm trees, rocks, fences, statues, etc. — pick from kenmi-* keys in the catalog only)
    - WORLD-VS-LOGIC-CONCERN.md (CinematicIntroSequencer hardcoded coords 14,17 / 14,18 / 14,20 in oasis_village — preserve geometry)
    - .planning/phases/97-visual-world-layer-rebuild/97-RESEARCH.md (Pitfall 4 — do not touch npcs/interactables/exits; Assumption A6 — preserve cinematic geometry)
  </read_first>
  <action>
    This is the highest-risk task in Phase 97. It edits the 1602-LOC zones.js file and MUST NOT accidentally touch any game-logic field. Discipline: work one zone at a time, commit one zone at a time, run `npm run test:run` after each zone commit.

    **Prerequisites (verify before starting):**
    1. `.planning/phases/97-visual-world-layer-rebuild/WORLD-AUDIT.md` exists and has populated "Missing / Dangling Kenmi References" and "Zone Kenmi Key Inventory" sections (produced by Plan 01)
    2. Plans 02/03/04/05 have landed — kenmiFrameTables.js exists, NPC_KEY_MAP is faceless, DESERT_TILESETS is gone, BIOME_DECORATION_SETS exists
    3. `npm run test:run` exits 0 right now (baseline 2535 pass)

    **Per-zone rebuild procedure (repeat 8 times):**

    **A.** Open zones.js, locate the zone's export block (each zone is an object literal inside `ZONES = { ... }`).

    **B.** Identify the zone's tilesetTheme: desert (oasis_village, ancient_library, desert_marketplace, bedouin_camp, royal_palace), grass (farmland, coastal_port), snow (mountain_village).

    **C.** Rebuild the `objects` array using this algorithm:
      - **Buildings** (current entries with `collide: true` and keys containing `house`, `temple`, `inn`, `barn`, `stall`, `tower`, `shop`, etc.): replace with keys from `BIOME_BUILDING_SETS[biome]`. Use `.small` for small houses, `.large` for large houses, `.temple`/`.pergola`/`.fence` for those specific types. Preserve `x`, `y`, `collide`, `collideW`, `collideH` exactly — DO NOT reposition buildings. Preserve any `linkedInteractable` or similar metadata.
      - **Props** (non-colliding decorations already placed by hand — palm trees, rocks, statues, obelisks, fences, lanterns, fountains): if the current key exists in KENMI_CATALOG, KEEP IT. If the key is missing or legacy, replace with the closest equivalent from the catalog. Record the substitution in the commit message.
      - **Cinematic-bound props** in `oasis_village` at coordinates (14, 17), (14, 18), (14, 20): preserve the VISUAL appearance (treat like a building — use BIOME_BUILDING_SETS.desert.small or whatever matches the current visual). Do NOT move the coords; the CinematicIntroSequencer hardcodes them.

    **D.** Verify you did NOT modify any of these fields in the zone object:
      - `id`, `name`, `nameArabic`
      - `mapWidth`, `mapHeight`
      - `tilesetTheme`
      - `spawnPoint`
      - `npcs` (array of { id, x, y, ... })
      - `interactables` (array of { id, type, x, y, linkedObject?, ... })
      - `exits` (array of { to, x, y, ... })
      - `gatheringSpots` (if present)
      - `vocabCategories` (if present)
      - `buildMap()` function body

    **E.** After each zone commit, run:
      ```bash
      npm run test:run -- src/data/__tests__/zoneAssetIntegrity.test.js
      npm run test:run -- src/game/systems/__tests__/MapLoader.test.js
      npm run test:run -- src/game/systems/__tests__/MapLoader.frameValidity.test.js
      npm run test:run -- src/game/systems/__tests__/NPCManager.test.js
      npm run test:run -- src/game/systems/__tests__/InteractableManager.test.js
      npm run test:run -- src/game/systems/__tests__/ZoneTransition.test.js
      ```
      All must exit 0 after each zone. If any fails, revert that zone's commit and investigate before proceeding.

    **F.** After all 8 zones:
      ```bash
      npm run test:run
      ```
      Expect 2535 + new tests all pass. `zoneAssetIntegrity.test.js` is GREEN.

    **Zone order recommendation** (simpler first, riskier last):
      1. oasis_village — desert, has cinematic constraints, well-understood
      2. ancient_library — desert, scholar building set
      3. desert_marketplace — desert, stall-heavy
      4. bedouin_camp — desert, tent focus
      5. royal_palace — desert, temple/obelisk focus
      6. farmland — grass, new biome
      7. coastal_port — grass, has water edges — verify _renderSandTile edges still render
      8. mountain_village — snow, newly supported by Plan 05 biome expansion

    **G.** Do NOT touch `src/data/zones/realWorldZones.js` or `src/data/zones/fantasyZones.js`. They're out of scope per CONTEXT (8 core zones only). If they regress (zoneAssetIntegrity.test.js fails for a fantasy zone), that's a Plan 07 concern, not Plan 06's.
  </action>
  <verify>
    <automated>npm run test:run -- src/data/__tests__/zoneAssetIntegrity.test.js 2>&amp;1 | tail -10 | grep -qE "(passed|PASS)" &amp;&amp; npm run test:run -- src/game/systems/__tests__/MapLoader.test.js src/game/systems/__tests__/MapLoader.frameValidity.test.js src/game/systems/__tests__/NPCManager.test.js src/game/systems/__tests__/InteractableManager.test.js src/game/systems/__tests__/ZoneTransition.test.js 2>&amp;1 | tail -10 | grep -qE "(passed|PASS)" &amp;&amp; npm run test:run 2>&amp;1 | tail -10 | grep -qE "(passed|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- src/data/__tests__/zoneAssetIntegrity.test.js` exits 0 for at least all 8 core zones
    - `npm run test:run` full suite exits 0 (2535 pre-existing tests still pass)
    - `git diff HEAD~1 -- src/data/zones.js | grep -E "^[-+]\s+id:|^[-+]\s+npcs:|^[-+]\s+interactables:|^[-+]\s+exits:|^[-+]\s+spawnPoint:|^[-+]\s+mapWidth:|^[-+]\s+mapHeight:|^[-+]\s+tilesetTheme:|^[-+]\s+name:|^[-+]\s+nameArabic:|^[-+]\s+gatheringSpots:|^[-+]\s+vocabCategories:"` returns 0 lines (no game-logic field changed)
    - `grep -c "^  id: 'oasis_village'\|^    id: 'oasis_village'" src/data/zones.js` unchanged from pre-plan count
    - Every objects[].key in the 8 core zones starts with `kenmi-` (verify by grep; legacy fallback keys like `palm` from SPRITE_KEY_MAP should be gone from the 8 core zones — if they remain because SPRITE_KEY_MAP still resolves them, that's acceptable but flag)
    - CinematicIntroSequencer coordinate assumption holds: `grep -E "^\s*x: 14,\s*y: (17|18|20)" src/data/zones.js` still finds at least 3 matches in the oasis_village section (verify by inspecting the surrounding context is still inside ZONES.oasis_village)
    - No zone has more than 25% reduction in objects array length vs. pre-plan (spot check with `grep -c "key:" src/data/zones.js` before and after — the total count should be comparable, adjusted only for legitimate dedup)
  </acceptance_criteria>
  <done>
    All 8 core zones have rebuilt objects arrays using valid KENMI_CATALOG keys and consistent biome palettes.
    Every preserved game-logic field is byte-identical to pre-Plan-06.
    zoneAssetIntegrity.test.js GREEN.
    2535 tests still pass.
  </done>
</task>

</tasks>

<verification>
- `npm run test:run -- src/data/__tests__/zoneAssetIntegrity.test.js` exits 0
- `git diff HEAD~N -- src/data/zones.js` shows only `objects` arrays changed (no npcs/interactables/exits/... diffs)
- `npm run test:run` full suite exits 0
- CinematicIntroSequencer coords preserved
</verification>

<success_criteria>
WORLD-01 closed for 8 core zones. Kenmi art style is consistent within each biome via BIOME_BUILDING_SETS selection. The WORLD-AUDIT "Missing / Dangling Kenmi References" list is reduced to zero (or zero minus out-of-scope realWorld/fantasy zones).
</success_criteria>

<output>
Create `.planning/phases/97-visual-world-layer-rebuild/97-06-SUMMARY.md` with a per-zone table showing: pre-plan object count, post-plan object count, number of keys substituted, number of keys preserved, any zone-specific deviations from the standard procedure.
</output>
