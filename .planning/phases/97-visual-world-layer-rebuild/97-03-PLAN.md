---
phase: 97
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/data/spriteKeyMap.js
  - src/game/systems/NPCManager.js
autonomous: true
requirements: [WORLD-03, WORLD-06]
must_haves:
  truths:
    - "Every value in NPC_KEY_MAP points at a faceless sprite asset (from /assets/sprites/npcs/faceless/*.png or explicit npc-* registered in SHARED_ASSETS)"
    - "No value in NPC_KEY_MAP matches /^kenmi-desert-npc-(desert-person|pharaoh|traders)-/ or /^kenmi-base-npcs-premade-/"
    - "facelessNpcs.test.js goes from RED to GREEN"
    - "All 2535 pre-existing tests continue to pass"
    - "NPCManager still renders all 140 NPCs across all zones (no broken sprites)"
  artifacts:
    - path: "src/data/spriteKeyMap.js"
      provides: "NPC_KEY_MAP rewired to faceless-only sprite keys"
      contains: "NPC_KEY_MAP"
    - path: "src/game/systems/NPCManager.js"
      provides: "NPC rendering verified against new faceless keys (hijab overlay for FEMALE_NPC_IDS preserved)"
  key_links:
    - from: "src/data/spriteKeyMap.js"
      to: "src/data/zoneAssetManifests.js"
      via: "NPC_KEY_MAP values = 'npc-{id}' keys registered in SHARED_ASSETS via FACELESS_NPCS loop"
      pattern: "npc-scholar-yusuf|npc-merchant-fatima|npc-farmer-omar"
---

<objective>
Enforce the "faceless characters only" Islamic-art constraint (locked decision in CONTEXT.md, research Pitfall 5). Today's `NPC_KEY_MAP` in `src/data/spriteKeyMap.js` maps 24 logical NPC IDs to Kenmi sprites that have visible eyes (kenmi-desert-npc-desert-person-*, kenmi-desert-npc-pharaoh, kenmi-desert-npc-traders-*, kenmi-base-npcs-premade-*).

Purpose: Rewire every NPC_KEY_MAP entry to the existing 23 faceless PNGs at `/assets/sprites/npcs/faceless/*.png` — which are ALREADY registered in BootScene via `zoneAssetManifests.FACELESS_NPCS` under Phaser keys like `npc-scholar-yusuf`, `npc-merchant-fatima`, etc. Per CONTEXT: "use existing faceless PNGs, don't post-process Kenmi sprites."

Output: NPC_KEY_MAP rewired; NPCManager verified to still work with faceless-only keys; facelessNpcs regression test goes GREEN.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-visual-world-layer-rebuild/97-CONTEXT.md
@.planning/phases/97-visual-world-layer-rebuild/97-RESEARCH.md
@.planning/phases/97-visual-world-layer-rebuild/WORLD-AUDIT.md
@src/data/spriteKeyMap.js
@src/data/zoneAssetManifests.js
@src/game/systems/NPCManager.js
@src/game/systems/__tests__/NPCManager.test.js
@src/data/__tests__/facelessNpcs.test.js

<interfaces>
<!-- From src/data/zoneAssetManifests.js (existing FACELESS_NPCS array, currently 23 NPC name strings). -->
<!-- CORRECTED per plan-checker review: the faceless directory actually has 24 PNGs INCLUDING guide-amira.png. -->
<!-- Plan 03 adds 'guide-amira' to FACELESS_NPCS so all 24 NPC_KEY_MAP entries map 1:1 to dedicated faceless sprites. -->

```
Registered faceless Phaser keys (npc-{id}), each a 128×128 spritesheet — AFTER Plan 03 registration fix:
  npc-scholar-yusuf,      npc-merchant-fatima,    npc-student-khalid,
  npc-librarian-ibrahim,  npc-scribe-amina,       npc-trader-hassan,
  npc-spice-seller-layla, npc-farmer-omar,        npc-herbalist-maryam,
  npc-elder-tariq,        npc-storyteller-noor,   npc-guide-salim,
  npc-guide-amira,        npc-weaver-zahra,       npc-captain-rashid,
  npc-fishmonger-hana,    npc-vizier-abbas,       npc-princess-aisha,
  npc-guard-hamza,        npc-wanderer-ali,       npc-healer-khadija,
  npc-imam-muhammad,      npc-blacksmith-daud,    npc-poet-rumi
```

Verified via `ls public/assets/sprites/npcs/faceless/` — **24 PNGs** present on disk including `guide-amira.png`. FACELESS_NPCS in zoneAssetManifests.js (lines 19-30) currently registers only 23 entries. Plan 03 Task 1a adds `guide-amira` to FACELESS_NPCS so all 24 PNGs are loaded by BootScene via the existing npcSprites loop, giving true 1:1 mapping from the 24 NPC_KEY_MAP entries to 24 dedicated faceless sprites.

<!-- From src/data/spriteKeyMap.js (EXISTING NPC_KEY_MAP, 24 entries that must be rewired): -->
Current mappings that use face-bearing sprites (ALL must change):
  npc-scholar-yusuf         → kenmi-desert-npc-desert-person-1       (face-visible)
  npc-student-khalid        → kenmi-desert-npc-desert-person-2       (face-visible)
  npc-librarian-ibrahim     → kenmi-desert-npc-desert-person-3       (face-visible)
  npc-scribe-amina          → kenmi-desert-npc-desert-person-4       (face-visible)
  npc-imam-muhammad         → kenmi-desert-npc-desert-person-1       (face-visible)
  npc-poet-rumi             → kenmi-desert-npc-desert-person-3       (face-visible)
  npc-elder-tariq           → kenmi-desert-npc-pharaoh                (face-visible)
  npc-storyteller-noor      → kenmi-desert-npc-desert-person-4        (face-visible)
  npc-merchant-fatima       → kenmi-desert-npc-traders-desert-trader-1 (face-visible)
  npc-spice-seller-layla    → kenmi-desert-npc-traders-desert-trader-2 (face-visible)
  npc-trader-hassan         → kenmi-desert-npc-traders-desert-trader-3 (face-visible)
  npc-farmer-omar           → kenmi-base-npcs-premade-farmer-bob       (face-visible)
  npc-herbalist-maryam      → kenmi-base-npcs-premade-chef-chloe       (face-visible)
  npc-blacksmith-daud       → kenmi-base-npcs-premade-miner-mike       (face-visible)
  npc-weaver-zahra          → kenmi-base-npcs-premade-bartender-katy   (face-visible)
  npc-fishmonger-hana       → kenmi-base-npcs-premade-fisherman-fin    (face-visible)
  npc-captain-rashid        → kenmi-desert-npc-desert-person-2         (face-visible)
  npc-healer-khadija        → kenmi-desert-npc-desert-person-4         (face-visible)
  npc-vizier-abbas          → kenmi-desert-npc-pharaoh                 (face-visible)
  npc-princess-aisha        → kenmi-desert-npc-traders-desert-trader-1 (face-visible)
  npc-guard-hamza           → kenmi-desert-npc-desert-person-3         (face-visible)
  npc-wanderer-ali          → kenmi-desert-npc-desert-person-2         (face-visible)
  npc-guide-salim           → kenmi-desert-npc-desert-person-1         (face-visible)
  npc-guide-amira           → kenmi-desert-npc-traders-desert-trader-2 (face-visible) [faceless PNG EXISTS at /assets/sprites/npcs/faceless/guide-amira.png — just needs FACELESS_NPCS registration per Task 1a]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Register guide-amira in FACELESS_NPCS (enables 24/24 parity)</name>
  <files>src/data/zoneAssetManifests.js</files>
  <read_first>
    - src/data/zoneAssetManifests.js (FACELESS_NPCS array lines 19-30)
    - public/assets/sprites/npcs/faceless/guide-amira.png (verify file exists: `ls public/assets/sprites/npcs/faceless/guide-amira.png`)
    - .planning/phases/97-visual-world-layer-rebuild/97-CONTEXT.md (hard constraint: use existing faceless PNGs)
  </read_first>
  <action>
    The file `public/assets/sprites/npcs/faceless/guide-amira.png` exists on disk (plan-checker verification: 24 PNGs in the directory) but `FACELESS_NPCS` in `src/data/zoneAssetManifests.js` only has 23 entries, omitting `guide-amira`. This prevents `npc-guide-amira` from being registered in BootScene's `SHARED_ASSETS` via the `npcSprites` loop on lines 48-60.

    Append `'guide-amira'` to the `FACELESS_NPCS` array (currently ends line 29 with `'blacksmith-daud', 'poet-rumi',`). Target final state of the array:

    ```js
    const FACELESS_NPCS = [
      'scholar-yusuf', 'merchant-fatima', 'student-khalid',
      'librarian-ibrahim', 'scribe-amina',
      'trader-hassan', 'spice-seller-layla',
      'farmer-omar', 'herbalist-maryam',
      'elder-tariq', 'storyteller-noor',
      'guide-salim', 'weaver-zahra',
      'captain-rashid', 'fishmonger-hana',
      'vizier-abbas', 'princess-aisha', 'guard-hamza',
      'wanderer-ali', 'healer-khadija', 'imam-muhammad',
      'blacksmith-daud', 'poet-rumi', 'guide-amira',
    ];
    ```

    Do NOT touch any other function/constant in this file. Do NOT modify the `npcSprites` or `npcPortraits` map loops — they will automatically pick up the new entry and register `npc-guide-amira` (sprite) and `portrait-guide-amira` (portrait) Phaser keys in SHARED_ASSETS.

    Also verify `public/assets/portraits/guide-amira.png` exists. If it doesn't, flag in Plan 08 WORLD-AUDIT close as a portrait-asset gap (but don't block this task — portrait files are registered regardless; a 404 at runtime is surfaced by BootScene warnings, not by this data file).
  </action>
  <verify>
    <automated>grep -c "'guide-amira'" src/data/zoneAssetManifests.js | grep -q "^1$" && test -f public/assets/sprites/npcs/faceless/guide-amira.png && npm run test:run -- src/data/__tests__/zoneAssetIntegrity.test.js 2>&amp;1 | tail -5 | grep -qE "(passed|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'guide-amira'" src/data/zoneAssetManifests.js` returns 1 match (the new entry)
    - `public/assets/sprites/npcs/faceless/guide-amira.png` exists (already on disk — this is a no-op check)
    - `FACELESS_NPCS.length` is 24 at runtime (verify with a throwaway `node -e` or rely on the test)
    - `npm run test:run -- src/data/__tests__/zoneAssetIntegrity.test.js` exits 0
    - `npm run test:run` full suite exits 0 (2535 + new tests pass)
    - No other export in the file is modified (diff of file shows ONLY the 1-line addition inside FACELESS_NPCS)
  </acceptance_criteria>
  <done>
    `FACELESS_NPCS` has 24 entries including guide-amira.
    BootScene will now register `npc-guide-amira` and `portrait-guide-amira` keys automatically via the existing loops.
    Unblocks Task 1's 1:1 NPC_KEY_MAP mapping.
  </done>
</task>

<task type="auto">
  <name>Task 1: Rewire NPC_KEY_MAP to faceless sprite keys (1:1 mapping, 24/24)</name>
  <files>src/data/spriteKeyMap.js</files>
  <read_first>
    - src/data/spriteKeyMap.js (full file — 24 NPC_KEY_MAP entries, 10 FEMALE_NPC_IDS, preserve NPC_HIJAB_TINT and ENEMY_KENMI_MAP untouched)
    - src/data/zoneAssetManifests.js (FACELESS_NPCS array AFTER Task 1a — should have 24 entries including 'guide-amira')
    - .planning/phases/97-visual-world-layer-rebuild/WORLD-AUDIT.md (Face-Bearing NPC Sprites table — already enumerates the 24 entries and recommended fixes)
    - src/data/__tests__/facelessNpcs.test.js (the RED test that will go GREEN)
    - public/assets/sprites/npcs/faceless/ (confirm 24 PNGs including guide-amira.png; `ls public/assets/sprites/npcs/faceless/ | wc -l`)
  </read_first>
  <action>
    **Prerequisite:** Task 1a must complete first (adds 'guide-amira' to FACELESS_NPCS so `npc-guide-amira` is registered in BootScene).

    Replace NPC_KEY_MAP with faceless-only mappings. Every value becomes `npc-{id}` matching the Phaser key registered in BootScene via SHARED_ASSETS npcSprites loop. Each NPC maps to its OWN dedicated faceless asset (1-to-1 — no sprite sharing).

    Replace the entire `NPC_KEY_MAP` export (lines 124-158 of current file) with:

    ```js
    /**
     * Maps logical NPC IDs to FACELESS sprite keys registered in BootScene
     * via SHARED_ASSETS.npcSprites (zoneAssetManifests.js FACELESS_NPCS loop).
     *
     * Per Phase 97 Plan 03: every value must be a key from the FACELESS_NPCS list —
     * no kenmi-desert-npc-*, kenmi-desert-npc-pharaoh, kenmi-desert-npc-traders-*,
     * or kenmi-base-npcs-premade-* references permitted (those sprites have visible faces).
     *
     * All 24 existing faceless PNGs at /assets/sprites/npcs/faceless/*.png are used
     * with true 1:1 NPC → sprite mapping (no sprite sharing). Task 1a added 'guide-amira'
     * to FACELESS_NPCS so every NPC has its own dedicated faceless asset.
     */
    export const NPC_KEY_MAP = {
      // Desert scholars/elders
      'npc-scholar-yusuf':      'npc-scholar-yusuf',
      'npc-student-khalid':     'npc-student-khalid',
      'npc-librarian-ibrahim':  'npc-librarian-ibrahim',
      'npc-scribe-amina':       'npc-scribe-amina',
      'npc-imam-muhammad':      'npc-imam-muhammad',
      'npc-poet-rumi':          'npc-poet-rumi',
      'npc-elder-tariq':        'npc-elder-tariq',
      'npc-storyteller-noor':   'npc-storyteller-noor',

      // Merchants/traders
      'npc-merchant-fatima':    'npc-merchant-fatima',
      'npc-spice-seller-layla': 'npc-spice-seller-layla',
      'npc-trader-hassan':      'npc-trader-hassan',

      // Role-specific
      'npc-farmer-omar':        'npc-farmer-omar',
      'npc-herbalist-maryam':   'npc-herbalist-maryam',
      'npc-blacksmith-daud':    'npc-blacksmith-daud',
      'npc-weaver-zahra':       'npc-weaver-zahra',
      'npc-fishmonger-hana':    'npc-fishmonger-hana',
      'npc-captain-rashid':     'npc-captain-rashid',
      'npc-healer-khadija':     'npc-healer-khadija',

      // Authority figures
      'npc-vizier-abbas':       'npc-vizier-abbas',
      'npc-princess-aisha':     'npc-princess-aisha',
      'npc-guard-hamza':        'npc-guard-hamza',

      // Wanderers/guides
      'npc-wanderer-ali':       'npc-wanderer-ali',
      'npc-guide-salim':        'npc-guide-salim',
      'npc-guide-amira':        'npc-guide-amira',
    };
    ```

    Do NOT modify `SPRITE_KEY_MAP`, `FEMALE_NPC_IDS`, `NPC_HIJAB_TINT`, `ENEMY_KENMI_MAP`, or `BIOME_BUILDING_SETS`. Those export names and values must remain exactly as they are.

    **CRITICAL:** Do not add new PNG files. Per CONTEXT, "Use existing faceless NPC PNGs. Do NOT post-process Kenmi sprites." All 24 faceless PNGs already exist on disk — this task just rewires the key map to point at them.
  </action>
  <verify>
    <automated>npm run test:run -- src/data/__tests__/facelessNpcs.test.js 2>&amp;1 | tail -10 | grep -qE "(passed|PASS)" &amp;&amp; grep -cE "'kenmi-desert-npc-|'kenmi-base-npcs-premade-" src/data/spriteKeyMap.js | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Every value in NPC_KEY_MAP starts with `npc-` (verified by grep against the file)
    - `grep -cE "'kenmi-desert-npc-|'kenmi-base-npcs-premade-" src/data/spriteKeyMap.js` returns 0 (no face-bearing sprites)
    - `grep -c "^  'npc-" src/data/spriteKeyMap.js` returns at least 24 (NPC_KEY_MAP has 24 entries)
    - `npm run test:run -- src/data/__tests__/facelessNpcs.test.js` exits 0 (GREEN — no failing assertions)
    - `npm run test:run` full suite exits 0 (2535 + new tests pass)
    - `SPRITE_KEY_MAP`, `FEMALE_NPC_IDS`, `NPC_HIJAB_TINT`, `ENEMY_KENMI_MAP`, `BIOME_BUILDING_SETS` exports are unchanged (verified by diff)
  </acceptance_criteria>
  <done>
    NPC_KEY_MAP rewired to faceless-only keys.
    facelessNpcs.test.js GREEN.
    All 2535 existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify NPCManager renders all 140 NPCs with faceless keys (no code changes expected)</name>
  <files>src/game/systems/NPCManager.js</files>
  <read_first>
    - src/game/systems/NPCManager.js (full file — confirm NPC sprite creation uses NPC_KEY_MAP lookup + scene.textures.exists check)
    - src/game/systems/__tests__/NPCManager.test.js (existing test file — must still pass)
    - src/data/spriteKeyMap.js (Task 1 updated NPC_KEY_MAP; also FEMALE_NPC_IDS, NPC_HIJAB_TINT used by NPCManager for hijab overlay)
    - src/data/zoneAssetManifests.js (confirm SHARED_ASSETS npcSprites registers all 23 npc-{id} keys the new NPC_KEY_MAP points at)
  </read_first>
  <action>
    This is a READ-ONLY verification task. The working hypothesis: NPCManager.js already handles the `npc-{id}` key format correctly because:
      (a) those keys were already registered in SHARED_ASSETS for years,
      (b) the FACELESS_NPCS list predates Kenmi and was how NPCs rendered before v8.0,
      (c) NPC_KEY_MAP was the add-on for Kenmi sprites — fall-through to faceless-PNG rendering is already working code.

    **Step 1** — Read `src/game/systems/NPCManager.js`. Locate the sprite-creation call (likely `this.scene.add.sprite(x, y, key)` or `this.scene.physics.add.sprite(...)` where `key` is resolved via NPC_KEY_MAP).

    **Step 2** — Confirm three facts by direct grep/read:
      - NPCManager looks up NPC_KEY_MAP[npcId] OR falls back to the original id (e.g., `NPC_KEY_MAP[npcId] ?? npcId`)
      - If using `scene.textures.exists(key)` guard, the new faceless keys (`npc-scholar-yusuf` etc.) all return true (they're registered at boot)
      - FEMALE_NPC_IDS + NPC_HIJAB_TINT handling is unchanged

    **Step 3** — If NPCManager.js uses a hardcoded check like `if (key.startsWith('kenmi-'))` to gate Kenmi-specific behavior (e.g., 16×16 frame handling vs 128×128), that path may now dead-code itself because no NPC_KEY_MAP value starts with `kenmi-` anymore. In that case, the Kenmi-sizing branch becomes unreachable for NPCs (but still reachable for enemies via ENEMY_KENMI_MAP — which is unchanged). Document this but do NOT delete the dead branch in this plan — scope creep. File for Phase 98 Code Health.

    **Step 4** — No edits should be needed. If `npm run test:run -- src/game/systems/__tests__/NPCManager.test.js` fails after Task 1, the failing assertion names reveal whether NPCManager needs a minor tweak. ONLY at that point, make the minimal edit required to satisfy the existing test while keeping the faceless keys.

    **Step 5** — Document in the commit message exactly which lines you read in NPCManager.js to verify correctness. Code changes, if any, must be minimal (a handful of lines max) and justified by a failing test.
  </action>
  <verify>
    <automated>npm run test:run -- src/game/systems/__tests__/NPCManager.test.js 2>&1 | tail -10 | grep -qE "(Tests.*passed|PASS)" && npm run test:run 2>&1 | tail -10 | grep -qE "(Tests.*passed|PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run -- src/game/systems/__tests__/NPCManager.test.js` exits 0
    - `npm run test:run` full suite exits 0 (2535 tests still pass after Plan 03)
    - If NPCManager.js was modified, diff is ≤10 lines AND every change is justified by a failing test assertion
    - If NPCManager.js was NOT modified, a code comment or commit note describes which NPCManager code paths were read and verified to still work
    - `grep -cE "kenmi-(desert|base)-npc" src/game/systems/NPCManager.js` — any count is acceptable (NPCManager may still reference Kenmi keys for enemy rendering; that's ENEMY_KENMI_MAP, separate concern)
  </acceptance_criteria>
  <done>
    NPCManager confirmed to render faceless-only NPCs. 2535 tests still pass. The facelessNpcs.test.js is GREEN.
  </done>
</task>

</tasks>

<verification>
- `grep -cE "'kenmi-desert-npc-|'kenmi-base-npcs-premade-" src/data/spriteKeyMap.js` returns 0
- `npm run test:run -- src/data/__tests__/facelessNpcs.test.js` exits 0
- `npm run test:run` full suite exits 0
- No new dependencies on guide-amira.png or on post-processed Kenmi NPC sprites
</verification>

<success_criteria>
WORLD-03 closed. Every NPC renders from an eye-free sprite. The facelessNpcs.test.js is a permanent regression gate — future phases cannot accidentally reintroduce a face-bearing sprite to NPC_KEY_MAP.
</success_criteria>

<output>
Create `.planning/phases/97-visual-world-layer-rebuild/97-03-SUMMARY.md` noting: 24 NPC_KEY_MAP entries rewired, guide-amira exception documented, NPCManager diff (lines changed or "none"), test pass delta.
</output>
