---
phase: 60-quiz-expansion-core-3-types
plan: 02
type: execute
wave: 2
depends_on: [60-01]
files_modified:
  - src/components/Quiz/WordOrder.jsx
  - src/components/Quiz/ClozePassage.jsx
  - src/hooks/useQuiz.js
  - src/components/Quiz/QuizOverlay.jsx
  - src/data/quizTypes.js
  - src/data/__tests__/quizTypes.test.js
  - src/components/Quiz/__tests__/WordOrder.test.jsx
  - src/components/Quiz/__tests__/ClozePassage.test.jsx
autonomous: true
requirements: [QUIZ-01]

must_haves:
  truths:
    - "WordOrder renders a tile bank and drop zone; clicking tiles places them; Submit sends joined sentence via onAnswer"
    - "ClozePassage renders an Arabic paragraph with a blank and 4 Arabic word choices"
    - "ClozePassage falls back gracefully when word.exampleSentence is absent"
    - "QUIZ_TYPE_REGISTRY has GrammarFill at minLevel:4 cefrMin:'A2', WordOrder at minLevel:5 cefrMin:'B1', ClozePassage at minLevel:4 cefrMin:'A2'"
    - "Deferred types (DialectIdentify, RootExpand, CulturalContext) remain at minLevel:999 cefrMin:'B2'"
    - "selectQuizTypeForPlayer returns GrammarFill for A2+ level 4+ players and WordOrder for B1+ level 5+ players"
    - "All 18 quiz types exist in QUIZ_TYPE_REGISTRY"
  artifacts:
    - path: "src/components/Quiz/WordOrder.jsx"
      provides: "Sentence word-ordering quiz using tile click pattern from SentenceBuilder"
      min_lines: 80
    - path: "src/components/Quiz/ClozePassage.jsx"
      provides: "Paragraph cloze quiz with FSRS-sourced blank options"
      min_lines: 80
    - path: "src/components/Quiz/__tests__/WordOrder.test.jsx"
      provides: "Unit tests for WordOrder component"
      min_lines: 40
    - path: "src/components/Quiz/__tests__/ClozePassage.test.jsx"
      provides: "Unit tests for ClozePassage component"
      min_lines: 40
  key_links:
    - from: "src/components/Quiz/QuizOverlay.jsx"
      to: "src/components/Quiz/WordOrder.jsx"
      via: "import + conditional render on quiz.quizType === 'WordOrder'"
      pattern: "quiz\\.quizType === 'WordOrder'"
    - from: "src/components/Quiz/QuizOverlay.jsx"
      to: "src/components/Quiz/ClozePassage.jsx"
      via: "import + conditional render on quiz.quizType === 'ClozePassage'"
      pattern: "quiz\\.quizType === 'ClozePassage'"
    - from: "src/data/quizTypes.js"
      to: "src/hooks/useQuiz.js"
      via: "selectQuizTypeForPlayer now returns GrammarFill/WordOrder/ClozePassage at real gating values"
      pattern: "minLevel: [45]"
---

<objective>
Create WordOrder and ClozePassage quiz components, wire them into useQuiz.js and QuizOverlay.jsx, lower QUIZ_TYPE_REGISTRY minLevel values from 999 to real gating, update deferred types to cefrMin:'B2', and add component tests + registry tests confirming all 18 types are correctly configured.

Purpose: Completes QUIZ-01 — the quiz system now has 15 active types (12 existing + 3 new) and 3 deferred stubs. WordOrder is a near-clone of SentenceBuilder with different instruction text and B1 CEFR gating. ClozePassage is a FillInBlank variant that shows an Arabic paragraph with a blank instead of a single sentence.

Output: WordOrder.jsx, ClozePassage.jsx, extended useQuiz.js, extended QuizOverlay.jsx, updated quizTypes.js with real minLevel values, updated quizTypes.test.js, and component tests.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-quiz-expansion-core-3-types/60-RESEARCH.md
@.planning/phases/60-quiz-expansion-core-3-types/60-01-SUMMARY.md
@src/components/Quiz/SentenceBuilder.jsx
@src/components/Quiz/FillInBlank.jsx
@src/components/Quiz/QuizOverlay.jsx
@src/hooks/useQuiz.js
@src/data/quizTypes.js
@src/data/__tests__/quizTypes.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: WordOrder.jsx + ClozePassage.jsx + useQuiz.js wiring</name>
  <files>
    src/components/Quiz/WordOrder.jsx
    src/components/Quiz/ClozePassage.jsx
    src/hooks/useQuiz.js
  </files>
  <read_first>
    src/components/Quiz/SentenceBuilder.jsx
    src/components/Quiz/FillInBlank.jsx
    src/hooks/useQuiz.js (updated from 60-01)
    src/components/Quiz/QuizOverlay.jsx (updated from 60-01)
  </read_first>
  <action>
    **Create src/components/Quiz/WordOrder.jsx:**

    1. This is a near-clone of SentenceBuilder.jsx with these differences:
       - Instruction text: "Arrange the words in the correct Arabic word order:" (instead of "Arrange the tiles to form the Arabic sentence:")
       - Import useFormatArabic, COLORS, FONTS from same paths as SentenceBuilder
       - Import useState, useEffect from 'react'
       - Function signature: `export default function WordOrder({ word, options, onAnswer, feedback })`
       - The tiles variable: `const tiles = options || []`

    2. Copy SentenceBuilder's EXACT tile state management:
       - `const [placed, setPlaced] = useState([])` and `const [usedIndices, setUsedIndices] = useState([])`
       - useEffect reset on `word?.id` change
       - handleTileClick: adds tile to placed, adds idx to usedIndices (blocked by feedback or already used)
       - handlePlacedClick: removes tile from placed, removes idx from usedIndices (blocked by feedback)
       - handleSubmit: joins placed tiles and calls `onAnswer(placed.map(p => p.tile).join(' '))`

    3. Copy SentenceBuilder's EXACT inline styles object (styles.instruction, styles.prompt, styles.dropZone, styles.dropZoneCorrect, styles.dropZoneWrong, styles.tile, styles.tileUsed, styles.tileBank, styles.correctAnswer, styles.correctLabel, styles.clearBtn).

    4. Copy SentenceBuilder's EXACT JSX render structure: instruction -> prompt (word.english) -> drop zone -> tile bank -> clear/submit buttons -> feedback correct answer display.

    5. The ONLY differences from SentenceBuilder.jsx are:
       - Component name: `WordOrder` (not `SentenceBuilder`)
       - Instruction string: "Arrange the words in the correct Arabic word order:" (not "Arrange the tiles to form the Arabic sentence:")
       - Should be under 200 lines (identical structure to SentenceBuilder's ~199 lines)

    **Create src/components/Quiz/ClozePassage.jsx:**

    6. Based on FillInBlank.jsx pattern with passage display instead of single sentence.
       - Import useFormatArabic, COLORS, FONTS
       - Function signature: `export default function ClozePassage({ word, options, onAnswer, feedback })`

    7. Passage construction:
       - Primary: use `word.exampleSentence?.arabic` as the passage sentence
       - If exampleSentence exists, replace the word.arabic (or normalized version) with BLANK_PLACEHOLDER ('______') — same logic as FillInBlank
       - Add a context line above the passage: show word.english as a hint (same as FillInBlank's englishHint)
       - Fallback: if no exampleSentence, show `BLANK_PLACEHOLDER + ' (' + word.english + ')'` — same as FillInBlank's fallback

    8. Layout — follow FillInBlank.jsx styling exactly:
       - Instruction: "Read the passage and fill in the blank:" (uppercase, FONTS.pixel, 10px, COLORS.brown)
       - Passage box: COLORS.dark background, COLORS.gray border (same as FillInBlank's sentenceBox), but with slightly larger padding (18px 22px) and a label "Passage" in FONTS.pixel 9px COLORS.light above the Arabic text
       - Arabic text in FONTS.arabicDisplay, 20px (slightly smaller than FillInBlank's 22px to accommodate longer passages), COLORS.white, RTL, lineHeight 1.8
       - Blank in COLORS.gold, FONTS.pixel 14px
       - English hint below passage box: same as FillInBlank's englishHint style
       - 4 choice buttons in column layout (flexDirection: 'column', gap: '10px') — identical button style to FillInBlank (FONTS.arabicDisplay 22px, pixel-art border/shadow)
       - Feedback highlighting: identical to FillInBlank (choiceCorrect green, choiceWrong red)
       - Button click: `!feedback && onAnswer(c.value)` — disabled when feedback present

    9. The passage display logic uses the same normalize + replace pattern as FillInBlank to create the blank. Split on BLANK_PLACEHOLDER to render parts with formatArabic().

    **Modify src/hooks/useQuiz.js:**

    10. In buildChoices function, add two new blocks BEFORE the final `return []` (and after the GrammarFill block added in 60-01):

        For WordOrder — identical to sentence-build:
        ```
        if (type === 'WordOrder') {
          const sentence = word.exampleSentence?.arabic || word.arabic;
          const tiles = sentence.split(/\s+/).map(t => t.trim()).filter(Boolean);
          return tiles.map((t, i) => ({ label: t, value: t, correct: i === 0, tile: true }));
        }
        ```

        For ClozePassage — identical to fill-blank:
        ```
        if (type === 'ClozePassage') {
          return shuffle([
            { label: word.arabic, value: word.arabic, correct: true },
            ...distractors.map(d => ({ label: d.arabic, value: d.arabic, correct: false })),
          ]);
        }
        ```

    11. In answer() callback, add two new else-if blocks BEFORE the final else clause (after the GrammarFill block added in 60-01):
        ```
        } else if (quizState.quizType === 'WordOrder') {
          const expectedSentence = (word.exampleSentence?.arabic || word.arabic)
            .split(/\s+/).map(t => t.trim()).filter(Boolean).join(' ');
          correct = normalize(userAnswer) === normalize(expectedSentence);
        } else if (quizState.quizType === 'ClozePassage') {
          correct = normalize(userAnswer) === normalize(word.arabic);
        }
        ```

    12. In the correctAnswer assignment block, add two new cases BEFORE the final else clause (after GrammarFill block from 60-01):
        ```
        } else if (quizState.quizType === 'WordOrder') {
          correctAnswer = word.exampleSentence?.arabic || word.arabic;
        } else if (quizState.quizType === 'ClozePassage') {
          correctAnswer = word.arabic;
        }
        ```
  </action>
  <verify>
    Run: `npx vitest run src/hooks/__tests__/useQuiz.adaptive.test.js`
    All 17 adaptive tests must still pass.
    Run: `npx vitest run src/data/__tests__/quizTypes.test.js`
    All 16 quiz type tests must still pass.
  </verify>
  <acceptance_criteria>
    - grep -q "export default function WordOrder" src/components/Quiz/WordOrder.jsx
    - grep -q "Arrange the words in the correct Arabic word order" src/components/Quiz/WordOrder.jsx
    - grep -q "export default function ClozePassage" src/components/Quiz/ClozePassage.jsx
    - grep -q "Read the passage and fill in the blank" src/components/Quiz/ClozePassage.jsx
    - grep -q "WordOrder" src/hooks/useQuiz.js
    - grep -q "ClozePassage" src/hooks/useQuiz.js
    - WordOrder.jsx has at least 80 lines
    - ClozePassage.jsx has at least 80 lines
  </acceptance_criteria>
  <done>WordOrder.jsx renders tile bank + drop zone with SentenceBuilder click pattern and WordOrder instruction text. ClozePassage.jsx renders passage with blank and 4 Arabic choices with FillInBlank fallback. useQuiz.js buildChoices, answer(), and correctAnswer handle both new types. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: QuizOverlay wiring + quizTypes.js minLevel update + all tests</name>
  <files>
    src/components/Quiz/QuizOverlay.jsx
    src/data/quizTypes.js
    src/data/__tests__/quizTypes.test.js
    src/components/Quiz/__tests__/WordOrder.test.jsx
    src/components/Quiz/__tests__/ClozePassage.test.jsx
  </files>
  <read_first>
    src/components/Quiz/QuizOverlay.jsx (updated from 60-01)
    src/data/quizTypes.js
    src/data/__tests__/quizTypes.test.js
    src/components/Quiz/__tests__/QuizOverlay.test.jsx
    src/components/Quiz/WordOrder.jsx (just created in Task 1)
    src/components/Quiz/ClozePassage.jsx (just created in Task 1)
  </read_first>
  <action>
    **Modify src/components/Quiz/QuizOverlay.jsx:**

    1. Add imports at top (alongside GrammarFill import from 60-01):
       ```
       import WordOrder from './WordOrder.jsx';
       import ClozePassage from './ClozePassage.jsx';
       ```

    2. Add render blocks in the quiz type switch section (after GrammarFill block from 60-01, BEFORE the AnimatePresence feedback row):
       ```
       {quiz.quizType === 'WordOrder' && (
         <WordOrder
           word={quiz.currentWord}
           options={quiz.choices}
           feedback={combinedFeedback}
           onAnswer={handleAnswer}
         />
       )}
       {quiz.quizType === 'ClozePassage' && (
         <ClozePassage
           word={quiz.currentWord}
           options={quiz.choices}
           feedback={combinedFeedback}
           onAnswer={handleAnswer}
         />
       )}
       ```

    3. Add SFX correctness blocks in handleAnswer (inside the if/else chain, after GrammarFill block from 60-01, BEFORE the final else):
       ```
       } else if (quiz.quizType === 'WordOrder') {
         const expected = (word.exampleSentence?.arabic || word.arabic)
           .split(/\s+/).filter(Boolean).join(' ');
         isCorrect = normalize(userAnswer) === normalize(expected);
       } else if (quiz.quizType === 'ClozePassage') {
         isCorrect = normalize(userAnswer) === normalize(word.arabic);
       }
       ```

    **Modify src/data/quizTypes.js:**

    4. Update the three active Phase 60 types from minLevel:999 to real values:
       ```
       'GrammarFill':     { label: 'Grammar Fill',  cluster: 'grammar', minLevel: 4,   cefrMin: 'A2' },
       'ClozePassage':    { label: 'Cloze Passage', cluster: 'grammar', minLevel: 4,   cefrMin: 'A2' },
       'WordOrder':       { label: 'Word Order',    cluster: 'grammar', minLevel: 5,   cefrMin: 'B1' },
       ```

    5. Update the three deferred types to cefrMin: 'B2' (matching STATE.md decision and ROADMAP success criteria):
       ```
       'DialectIdentify': { label: 'Dialect Identify', cluster: 'listening', minLevel: 999, cefrMin: 'B2' },
       'RootExpand':      { label: 'Root Expand',      cluster: 'roots',    minLevel: 999, cefrMin: 'B2' },
       'CulturalContext': { label: 'Cultural Context', cluster: 'reading',  minLevel: 999, cefrMin: 'B2' },
       ```

    **Modify src/data/__tests__/quizTypes.test.js:**

    6. REPLACE the test `'Phase 60 types have minLevel: 999 (not yet active)'` with these new tests:

       ```
       it('active Phase 60 types (GrammarFill, WordOrder, ClozePassage) have real minLevel values', () => {
         expect(QUIZ_TYPE_REGISTRY['GrammarFill'].minLevel).toBe(4);
         expect(QUIZ_TYPE_REGISTRY['GrammarFill'].cefrMin).toBe('A2');
         expect(QUIZ_TYPE_REGISTRY['WordOrder'].minLevel).toBe(5);
         expect(QUIZ_TYPE_REGISTRY['WordOrder'].cefrMin).toBe('B1');
         expect(QUIZ_TYPE_REGISTRY['ClozePassage'].minLevel).toBe(4);
         expect(QUIZ_TYPE_REGISTRY['ClozePassage'].cefrMin).toBe('A2');
       });

       it('deferred types (DialectIdentify, RootExpand, CulturalContext) remain at minLevel:999 and cefrMin:B2', () => {
         const deferred = ['DialectIdentify', 'RootExpand', 'CulturalContext'];
         deferred.forEach(t => {
           expect(QUIZ_TYPE_REGISTRY[t].minLevel).toBe(999);
           expect(QUIZ_TYPE_REGISTRY[t].cefrMin).toBe('B2');
         });
       });
       ```

    7. Update the `'never returns Phase 60 types (minLevel 999)'` test — rename to `'never returns deferred types (minLevel 999)'` and change the phase60 array to only the 3 deferred types:
       ```
       it('never returns deferred types (minLevel 999)', () => {
         const deferred = ['DialectIdentify', 'RootExpand', 'CulturalContext'];
         const results = Array.from({ length: 200 }, () =>
           selectQuizTypeForPlayer({}, 50, 'B2')
         );
         results.forEach(t => {
           expect(deferred).not.toContain(t);
         });
       });
       ```

    8. ADD new tests for the active Phase 60 types appearing in rotation:
       ```
       it('GrammarFill appears in rotation for A2 player at level 4+', () => {
         const results = Array.from({ length: 300 }, () =>
           selectQuizTypeForPlayer({}, 4, 'A2')
         );
         expect(results).toContain('GrammarFill');
       });

       it('GrammarFill does not appear for A1 player', () => {
         const results = Array.from({ length: 200 }, () =>
           selectQuizTypeForPlayer({}, 10, 'A1')
         );
         expect(results).not.toContain('GrammarFill');
       });

       it('WordOrder appears in rotation for B1 player at level 5+', () => {
         const results = Array.from({ length: 300 }, () =>
           selectQuizTypeForPlayer({}, 5, 'B1')
         );
         expect(results).toContain('WordOrder');
       });

       it('WordOrder does not appear for A2 player', () => {
         const results = Array.from({ length: 300 }, () =>
           selectQuizTypeForPlayer({}, 10, 'A2')
         );
         expect(results).not.toContain('WordOrder');
       });

       it('ClozePassage appears in rotation for A2 player at level 4+', () => {
         const results = Array.from({ length: 300 }, () =>
           selectQuizTypeForPlayer({}, 4, 'A2')
         );
         expect(results).toContain('ClozePassage');
       });
       ```

    **Create src/components/Quiz/__tests__/WordOrder.test.jsx:**

    9. Mock useFormatArabic: `vi.mock('../../hooks/useFormatArabic.js', () => ({ useFormatArabic: () => (s) => s }))`

    10. Tests:
        a. `'renders tile bank from options array'` — Render with options: ['كلمة', 'في', 'البيت'].map((t, i) => ({ label: t, value: t, correct: i === 0, tile: true })). Assert 3 tile buttons visible in the bank area.

        b. `'clicking tile moves it to drop zone'` — Click first tile button, assert it appears in the drop zone area (use data-testid or role).

        c. `'submit calls onAnswer with joined sentence string'` — Place all tiles by clicking them in order, click Submit button, assert onAnswer called with the joined string (e.g., 'كلمة في البيت').

        d. `'placed tile click returns it to bank'` — Place a tile, click it in the drop zone, assert it reappears in the bank (is no longer disabled/dimmed).

        e. `'disables all interaction when feedback is present'` — Render with feedback = { correct: true, correctAnswer: 'sentence' }, assert Submit is not visible and tiles are disabled.

    **Create src/components/Quiz/__tests__/ClozePassage.test.jsx:**

    11. Mock useFormatArabic same as above.

    12. Tests:
        a. `'renders passage text with blank placeholder'` — Render with word: { id: 'test1', arabic: 'كتاب', english: 'book', exampleSentence: { arabic: 'أقرأ كتاب كل يوم', english: 'I read a book every day' } }, options: 4 choices. Assert '______' placeholder visible in the passage box.

        b. `'renders 4 Arabic word choices'` — Assert 4 button elements with Arabic text from options array.

        c. `'calls onAnswer with selected choice value'` — Click a choice, assert onAnswer called with that value.

        d. `'falls back gracefully when exampleSentence is absent'` — Render with word that has no exampleSentence field. Assert component renders without crash, shows blank placeholder.

        e. `'disables all choice buttons when feedback is present'` — Render with feedback, assert all buttons disabled.
  </action>
  <verify>
    Run: `npx vitest run src/components/Quiz/__tests__/WordOrder.test.jsx src/components/Quiz/__tests__/ClozePassage.test.jsx src/data/__tests__/quizTypes.test.js`
    All WordOrder (5), ClozePassage (5), and quizTypes tests pass.
    Run: `npx vitest run`
    Full test suite passes — no regressions.
  </verify>
  <acceptance_criteria>
    - grep -q "import WordOrder" src/components/Quiz/QuizOverlay.jsx
    - grep -q "import ClozePassage" src/components/Quiz/QuizOverlay.jsx
    - grep -q "quiz.quizType === 'WordOrder'" src/components/Quiz/QuizOverlay.jsx
    - grep -q "quiz.quizType === 'ClozePassage'" src/components/Quiz/QuizOverlay.jsx
    - grep -q "minLevel: 4" src/data/quizTypes.js
    - grep -q "minLevel: 5" src/data/quizTypes.js
    - grep -q "cefrMin: 'B2'" src/data/quizTypes.js
    - grep -q "GrammarFill does not appear for A1" src/data/__tests__/quizTypes.test.js
    - grep -q "WordOrder appears in rotation" src/data/__tests__/quizTypes.test.js
    - grep -q "deferred types.*remain at minLevel:999" src/data/__tests__/quizTypes.test.js
    - src/components/Quiz/__tests__/WordOrder.test.jsx exists with at least 40 lines
    - src/components/Quiz/__tests__/ClozePassage.test.jsx exists with at least 40 lines
  </acceptance_criteria>
  <done>QuizOverlay imports and renders WordOrder + ClozePassage with correct SFX. QUIZ_TYPE_REGISTRY has GrammarFill (minLevel:4, cefrMin:'A2'), WordOrder (minLevel:5, cefrMin:'B1'), ClozePassage (minLevel:4, cefrMin:'A2'). Deferred types at minLevel:999 cefrMin:'B2'. Registry tests updated: active types have real values, deferred stay gated, CEFR-level routing verified. WordOrder (5) and ClozePassage (5) component tests pass. Full suite green.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/Quiz/__tests__/WordOrder.test.jsx` — 5 tests pass
2. `npx vitest run src/components/Quiz/__tests__/ClozePassage.test.jsx` — 5 tests pass
3. `npx vitest run src/data/__tests__/quizTypes.test.js` — updated tests pass (active types at real minLevel, deferred at 999/B2)
4. `npx vitest run src/hooks/__tests__/useQuiz.adaptive.test.js` — 17 tests pass (no regression)
5. `npx vitest run src/components/Quiz/__tests__/GrammarFill.test.jsx` — 5 tests pass (no regression from 60-01)
6. `npx vitest run` — full suite green
7. `grep -c "minLevel: 999" src/data/quizTypes.js` returns exactly 3 (the deferred types only)
8. `grep -c "cefrMin: 'B2'" src/data/quizTypes.js` returns exactly 3 (the deferred types only)
</verification>

<success_criteria>
- WordOrder.jsx exists with tile click pattern cloned from SentenceBuilder and "word order" instruction
- ClozePassage.jsx exists with passage display + blank + 4 Arabic choices with fallback
- QuizOverlay.jsx imports and renders both new components with correct SFX wiring
- QUIZ_TYPE_REGISTRY: GrammarFill minLevel:4 cefrMin:'A2', WordOrder minLevel:5 cefrMin:'B1', ClozePassage minLevel:4 cefrMin:'A2'
- QUIZ_TYPE_REGISTRY: DialectIdentify/RootExpand/CulturalContext at minLevel:999 cefrMin:'B2'
- quizTypes.test.js updated: old minLevel:999 test replaced, new CEFR routing tests added
- 5 WordOrder + 5 ClozePassage component tests pass
- Full test suite passes with no regressions
- QUIZ-01 fully satisfied: 15 active + 3 deferred = 18 quiz types
</success_criteria>

<output>
After completion, create `.planning/phases/60-quiz-expansion-core-3-types/60-02-SUMMARY.md`
</output>
