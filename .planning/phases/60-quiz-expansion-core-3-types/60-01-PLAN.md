---
phase: 60-quiz-expansion-core-3-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Quiz/GrammarFill.jsx
  - src/hooks/useQuiz.js
  - src/components/Quiz/QuizOverlay.jsx
  - src/components/Quiz/__tests__/GrammarFill.test.jsx
autonomous: true
requirements: [QUIZ-01]

must_haves:
  truths:
    - "GrammarFill renders a verb root, paradigm context (pronoun + tense), and 4 conjugated form choices"
    - "Selecting a conjugated form calls onAnswer with the form string"
    - "Correct/wrong feedback highlights the correct choice green and wrong choice red"
    - "useQuiz buildChoices generates GrammarFill options from embedded VERB_PARADIGMS with correct:true on one"
    - "useQuiz answer() grades GrammarFill against quizState.choices correct value, NOT word.arabic"
    - "QuizOverlay renders GrammarFill when quiz.quizType === 'GrammarFill' and plays correct SFX"
  artifacts:
    - path: "src/components/Quiz/GrammarFill.jsx"
      provides: "Conjugation fill-in-blank quiz component"
      min_lines: 100
    - path: "src/components/Quiz/__tests__/GrammarFill.test.jsx"
      provides: "Unit tests for GrammarFill component"
      min_lines: 40
  key_links:
    - from: "src/components/Quiz/QuizOverlay.jsx"
      to: "src/components/Quiz/GrammarFill.jsx"
      via: "import + conditional render on quiz.quizType === 'GrammarFill'"
      pattern: "quiz\\.quizType === 'GrammarFill'"
    - from: "src/hooks/useQuiz.js"
      to: "src/components/Quiz/GrammarFill.jsx"
      via: "buildChoices generates options with paradigmContext, answer() grades via choices.find(c => c.correct)"
      pattern: "quizType === 'GrammarFill'"
---

<objective>
Create GrammarFill quiz component — a conjugation fill-in-blank that shows an Arabic verb root + paradigm context (pronoun + tense) and 4 conjugated form choices. Wire into useQuiz.js buildChoices/answer grading and QuizOverlay render switch + SFX block. Add component tests.

Purpose: GrammarFill is the most complex of the three new quiz types because it sources questions from an embedded VERB_PARADIGMS array (derived from grammar.js conjugation-drill exercises) rather than the vocabulary word's own Arabic field. Its grading path must compare against choices.find(c => c.correct)?.value, NOT word.arabic, because the correct answer is a conjugated form, not the dictionary form.

Output: GrammarFill.jsx component, extended useQuiz.js with buildChoices/answer/correctAnswer for GrammarFill, QuizOverlay import/render/SFX wiring, and passing tests.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-quiz-expansion-core-3-types/60-RESEARCH.md
@.planning/phases/59-adaptive-difficulty-engine/59-02-SUMMARY.md
@src/components/Quiz/ConjugationPick.jsx
@src/components/Quiz/FillInBlank.jsx
@src/components/Quiz/QuizOverlay.jsx
@src/hooks/useQuiz.js
@src/data/quizTypes.js
@src/data/grammar.js
@src/styles/theme.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: GrammarFill.jsx component + useQuiz.js wiring</name>
  <files>
    src/components/Quiz/GrammarFill.jsx
    src/hooks/useQuiz.js
  </files>
  <read_first>
    src/components/Quiz/ConjugationPick.jsx
    src/components/Quiz/FillInBlank.jsx
    src/hooks/useQuiz.js
    src/data/grammar.js (lines 250-260 for conjugation-drill exercise shape)
    src/styles/theme.js
  </read_first>
  <action>
    **Create src/components/Quiz/GrammarFill.jsx:**

    1. Import useFormatArabic from '../../hooks/useFormatArabic.js', COLORS and FONTS from '../../styles/theme.js', useState and useEffect from 'react'.

    2. Embed a VERB_PARADIGMS array (12-15 entries) sourced from the conjugation-drill exercises in grammar.js. Each entry has shape:
       ```
       { verb: 'كَتَبَ', root: 'ك-ت-ب', meaning: 'to write', paradigm: 'present',
         pronoun: { en: 'I (أنا)', ar: 'أنا' },
         correctForm: 'أكتب',
         distractors: ['يكتب', 'تكتب', 'نكتب'] }
       ```
       Source verbs from grammar.js conjugation-drills: كَتَبَ (write), ذَهَبَ (go), قَرَأَ (read), فَهِمَ (understand), سَكَنَ (live/dwell), سَافَرَ (travel), plus additional paradigms for present and future tenses. Include at least 12 entries covering 6+ verbs with varied pronouns.

    3. Component function signature: `export default function GrammarFill({ word, options, onAnswer, feedback })`
       - Uses standard 4-prop contract identical to ConjugationPick
       - `options` come from useQuiz buildChoices with `{ label, value, correct, paradigmContext }` shape

    4. Use a `paradigmIndex` state (useState) seeded from `word?.id` (reset via useEffect on word?.id change) to pick a deterministic paradigm entry. Derive index as: `word.id ? word.id.charCodeAt(0) % VERB_PARADIGMS.length : 0`

    5. Layout — follow ConjugationPick.jsx promptBox pattern exactly:
       - Instruction: "Complete the conjugation:" (uppercase, FONTS.pixel, 10px, COLORS.brown)
       - Prompt box (COLORS.dark background, COLORS.gray border):
         - Verb root in FONTS.arabicDisplay, 32px, COLORS.gold
         - Verb meaning in FONTS.pixel, 9px, COLORS.light, italic
         - Paradigm label (e.g., "present tense") in FONTS.pixel, 10px, COLORS.white
         - Pronoun Arabic in FONTS.arabicDisplay, 18px, COLORS.blue
         - Pronoun English in FONTS.pixel, 11px, COLORS.white
       - 4 choice buttons in 2x2 grid (gridTemplateColumns: '1fr 1fr', gap: '10px')
       - Each button: FONTS.arabicDisplay, 20px, pixel-art border/shadow identical to ConjugationPick
       - Feedback: correct = green highlight (choiceCorrect style), wrong = red (choiceWrong style)
       - Button click: `!feedback && onAnswer(c.value)` — disabled when feedback present

    6. The component does NOT read from VERB_PARADIGMS for display — it receives all display data via `options[0].paradigmContext` (set by buildChoices). The embedded VERB_PARADIGMS is only used by buildChoices in useQuiz.js. However, to keep the paradigm data co-located with the component for clarity, export it: `export const VERB_PARADIGMS = [...]`.

    **Modify src/hooks/useQuiz.js:**

    7. Add import at top: `import { VERB_PARADIGMS } from '../components/Quiz/GrammarFill.jsx';`

    8. In buildChoices function, add a new block BEFORE the final `return []`:
       ```
       if (type === 'GrammarFill') {
         const idx = word.id ? word.id.charCodeAt(0) % VERB_PARADIGMS.length : 0;
         const paradigm = VERB_PARADIGMS[idx];
         const paradigmContext = { verb: paradigm.verb, root: paradigm.root, meaning: paradigm.meaning, paradigm: paradigm.paradigm, pronoun: paradigm.pronoun };
         return shuffle([
           { label: paradigm.correctForm, value: paradigm.correctForm, correct: true, paradigmContext },
           ...paradigm.distractors.map(f => ({ label: f, value: f, correct: false, paradigmContext }))
         ]);
       }
       ```

    9. In answer() callback, add a new else-if block BEFORE the final else clause (before line ~274 `} else {`):
       ```
       } else if (quizState.quizType === 'GrammarFill') {
         const correctForm = quizState.choices.find(c => c.correct)?.value || '';
         correct = normalize(userAnswer) === normalize(correctForm);
       }
       ```
       CRITICAL: Do NOT grade against word.arabic — the correct answer is a conjugated form from VERB_PARADIGMS, not the dictionary word.

    10. In the correctAnswer assignment block (after the grading section, around lines 309-320), add:
        ```
        } else if (quizState.quizType === 'GrammarFill') {
          correctAnswer = quizState.choices.find(c => c.correct)?.value || word.arabic;
        }
        ```
        Place this BEFORE the final else clause.
  </action>
  <verify>
    Run: `npx vitest run src/hooks/__tests__/useQuiz.adaptive.test.js`
    Existing 17 adaptive tests must still pass (no regression).
    Run: `npx vitest run src/data/__tests__/quizTypes.test.js`
    Existing 16 quiz type tests must still pass.
  </verify>
  <acceptance_criteria>
    - grep -q "export default function GrammarFill" src/components/Quiz/GrammarFill.jsx
    - grep -q "export const VERB_PARADIGMS" src/components/Quiz/GrammarFill.jsx
    - grep -q "VERB_PARADIGMS" src/hooks/useQuiz.js
    - grep -q "GrammarFill" src/hooks/useQuiz.js
    - grep -q "quizState.choices.find" src/hooks/useQuiz.js
    - grep -q "paradigmContext" src/hooks/useQuiz.js
    - GrammarFill.jsx has at least 100 lines
    - VERB_PARADIGMS array has at least 12 entries
  </acceptance_criteria>
  <done>GrammarFill.jsx renders verb root + paradigm context + 4 conjugated form choices. useQuiz.js buildChoices generates GrammarFill options from VERB_PARADIGMS. useQuiz.js answer() grades GrammarFill against choices.find(c => c.correct)?.value. correctAnswer assignment uses the correct conjugated form. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: QuizOverlay wiring + GrammarFill tests</name>
  <files>
    src/components/Quiz/QuizOverlay.jsx
    src/components/Quiz/__tests__/GrammarFill.test.jsx
  </files>
  <read_first>
    src/components/Quiz/QuizOverlay.jsx
    src/components/Quiz/__tests__/QuizOverlay.test.jsx
    src/components/Quiz/GrammarFill.jsx (just created in Task 1)
  </read_first>
  <action>
    **Modify src/components/Quiz/QuizOverlay.jsx:**

    1. Add import at top (alongside other quiz component imports, around line 20):
       ```
       import GrammarFill from './GrammarFill.jsx';
       ```

    2. Add render block in the quiz type switch section (after the ConjugationPick block, around line 395, BEFORE the AnimatePresence feedback row):
       ```
       {quiz.quizType === 'GrammarFill' && (
         <GrammarFill
           word={quiz.currentWord}
           options={quiz.choices}
           feedback={combinedFeedback}
           onAnswer={handleAnswer}
         />
       )}
       ```

    3. Add SFX correctness block in handleAnswer (inside the if/else chain at lines 85-102, BEFORE the final else clause):
       ```
       } else if (quiz.quizType === 'GrammarFill') {
         isCorrect = quiz.choices.find(c => c.correct)?.value === userAnswer;
       }
       ```
       CRITICAL: This must match the grading logic in useQuiz.js — compare against choices.find(c => c.correct)?.value, NOT word.arabic.

    **Create src/components/Quiz/__tests__/GrammarFill.test.jsx:**

    4. Follow the test pattern from QuizOverlay.test.jsx for mocking. Tests:

       a. `'renders verb root, pronoun, and 4 choice buttons'` — Render GrammarFill with:
          - word: { id: 'test1', arabic: 'كلمة', english: 'word' }
          - options: 4 objects with { label, value, correct, paradigmContext: { verb: 'كَتَبَ', root: 'ك-ت-ب', meaning: 'to write', paradigm: 'present', pronoun: { en: 'I (أنا)', ar: 'أنا' } } }
          - Only one option has correct: true
          - Assert: 4 buttons rendered, verb root text visible, pronoun text visible

       b. `'calls onAnswer with selected conjugated form string'` — Click a choice button, assert onAnswer was called with that button's value string.

       c. `'shows green highlight on correct choice when feedback.correct is true'` — Render with feedback = { correct: true, selected: correctValue, correctAnswer: correctValue }. Assert the correct option has the green background style.

       d. `'shows red highlight on wrong selected choice when feedback.correct is false'` — Render with feedback = { correct: false, selected: wrongValue, correctAnswer: correctValue }. Assert the selected wrong option has red background.

       e. `'disables all buttons when feedback is present'` — Render with feedback, assert all buttons are disabled.

    5. Use vitest + @testing-library/react for rendering. Mock useFormatArabic to return identity function: `vi.mock('../../hooks/useFormatArabic.js', () => ({ useFormatArabic: () => (s) => s }))`.
  </action>
  <verify>
    Run: `npx vitest run src/components/Quiz/__tests__/GrammarFill.test.jsx`
    All 5 GrammarFill tests must pass.
    Run: `npx vitest run src/data/__tests__/quizTypes.test.js src/hooks/__tests__/useQuiz.adaptive.test.js`
    All existing tests must still pass (no regression from QuizOverlay changes).
  </verify>
  <acceptance_criteria>
    - grep -q "import GrammarFill" src/components/Quiz/QuizOverlay.jsx
    - grep -q "quiz.quizType === 'GrammarFill'" src/components/Quiz/QuizOverlay.jsx
    - grep -q "quiz.choices.find" src/components/Quiz/QuizOverlay.jsx
    - src/components/Quiz/__tests__/GrammarFill.test.jsx exists and has at least 40 lines
    - grep -q "onAnswer" src/components/Quiz/__tests__/GrammarFill.test.jsx
    - grep -q "feedback" src/components/Quiz/__tests__/GrammarFill.test.jsx
  </acceptance_criteria>
  <done>QuizOverlay imports GrammarFill, renders it when quiz.quizType === 'GrammarFill', and plays correct SFX using choices.find grading. GrammarFill.test.jsx has 5 passing tests covering render, answer submission, feedback highlighting, and button disabling.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/components/Quiz/__tests__/GrammarFill.test.jsx` — 5 tests pass
2. `npx vitest run src/hooks/__tests__/useQuiz.adaptive.test.js` — 17 tests pass (no regression)
3. `npx vitest run src/data/__tests__/quizTypes.test.js` — 16 tests pass (no regression)
4. `grep -q "GrammarFill" src/components/Quiz/QuizOverlay.jsx` — GrammarFill wired into QuizOverlay
5. `grep -q "VERB_PARADIGMS" src/components/Quiz/GrammarFill.jsx` — Paradigm data embedded
6. `grep -q "choices.find" src/hooks/useQuiz.js` — Custom grading path (not word.arabic)
</verification>

<success_criteria>
- GrammarFill.jsx component exists with verb root + paradigm display + 4 conjugated form choice buttons
- useQuiz.js buildChoices generates GrammarFill options from VERB_PARADIGMS with paradigmContext metadata
- useQuiz.js answer() grades GrammarFill using quizState.choices.find(c => c.correct)?.value (NOT word.arabic)
- QuizOverlay.jsx imports, renders, and plays correct SFX for GrammarFill
- 5 GrammarFill component tests pass
- All 33 pre-existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/60-quiz-expansion-core-3-types/60-01-SUMMARY.md`
</output>
