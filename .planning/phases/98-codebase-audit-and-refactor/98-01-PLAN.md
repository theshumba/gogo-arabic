---
phase: 98-codebase-audit-and-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md
  - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
  - .planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
  - .planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md
  - .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg
  - .planning/phases/98-codebase-audit-and-refactor/knip.txt
  - .planning/phases/98-codebase-audit-and-refactor/knip-raw.json
  - .planning/phases/98-codebase-audit-and-refactor/depcheck.txt
  - .planning/phases/98-codebase-audit-and-refactor/madge-circular.txt
  - knip.json
  - .dependency-cruiser.cjs
  - package.json
  - tests/e2e/smoke/phase98-smoke.spec.js
  - src/test/phase98-invariants.test.js
  - scripts/ralph/prd.json.paused-for-phase98
autonomous: false
requirements:
  - HEALTH-01
  - HEALTH-02
  - HEALTH-03
  - HEALTH-04
  - HEALTH-05
  - HEALTH-06
  - HEALTH-07
  - HEALTH-08
  - HEALTH-09
  - HEALTH-10
  - HEALTH-11
  - HEALTH-12
  - HEALTH-13
  - HEALTH-14
tags:
  - audit
  - refactor
  - code-health
  - guardrails

must_haves:
  truths:
    - "All 14 HEALTH-* requirements are registered in REQUIREMENTS.md under a new v17.0 Code Health section"
    - "A pre-phase baseline (LOC, test count, bundle bytes, test runtime) is captured in 98-01-BASELINE.json before any source file is modified"
    - "98-AUDIT.md exists covering all 10 audit dimensions from CONTEXT.md across src/, server/, scripts/, public/assets/manifests/, and .planning/phases/"
    - "98-DELETIONS.md exists with one approval checkbox per deletion candidate (files, exports, npm deps, phase stubs)"
    - "The dependency-cruiser dependency graph is rendered to SVG and committed to the phase directory"
    - "The Ralph pipeline is paused (prd.json renamed) for the duration of Phase 98"
    - "15 Playwright golden-path smoke tests exist under tests/e2e/smoke/ and pass against current main"
    - "Feature-count invariant tests exist in src/test/phase98-invariants.test.js and all pass"
    - "Phase 97 is verified merged before audit begins (hard gate)"
    - "No source file under src/ or server/ is modified by Plan 01 beyond additive test/config files"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Registered HEALTH-01 through HEALTH-14 under v17.0 section with traceability row mapping each ID to Phase 98"
      contains: "HEALTH-01"
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md"
      provides: "Full 10-dimension audit report with Executive Summary, Critical/High/Medium/Low tables, coverage heatmap link, dependency graph link, phase-stub disposition table, planning-doc drift table, open questions"
      min_lines: 300
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md"
      provides: "Sign-off ledger for deletion candidates with per-row checkboxes (Approved/Rejected) and 1M-context reasoning"
      min_lines: 50
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json"
      provides: "Numeric baselines for test count, LOC, bundle bytes, test runtime seconds, per-directory coverage summary"
      contains: "testCount"
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md"
      provides: "Per-directory coverage heatmap flagging directories under 50% as 'Wave-0 tests required' and under 70% on high-risk systems as 'needs additional coverage'"
      min_lines: 40
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg"
      provides: "Visual module dependency graph produced by dependency-cruiser + Graphviz"
    - path: "knip.json"
      provides: "Knip configuration with conservative entry points covering all 6 structural false-positive vectors"
      contains: "import.meta.glob"
    - path: "tests/e2e/smoke/phase98-smoke.spec.js"
      provides: "15 Playwright golden-path smoke tests covering boot, onboarding, zones, NPCs, quests, FSRS, quizzes, grammar, battle, inventory, companions, shop, crafting, save/reload, settings"
      min_lines: 200
    - path: "src/test/phase98-invariants.test.js"
      provides: "Feature-count invariants (zones=8, NPCs=140, quests=52, equipment=64, spells=50, companions=12, letters=28, achievements=263, grammar=50, quizTypes=18, tests>=2535)"
      min_lines: 80
  key_links:
    - from: ".planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md"
      to: ".planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md"
      via: "explicit cross-reference in Section 5 of AUDIT.md"
      pattern: "98-DELETIONS\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "Phase 98 traceability"
      via: "v17.0 Traceability table mapping HEALTH-01..14 to Phase 98"
      pattern: "HEALTH-0[1-9]|HEALTH-1[0-4]"
    - from: "knip.json"
      to: "src/store/store.js, src/store/slices/*.js, src/store/middleware/*.js, src/game/scenes/*.js, src/data/**/*.json, scripts/ralph/*.{js,sh,json}"
      via: "entry point list in knip.json"
      pattern: "scripts/ralph"
    - from: "98-01-BASELINE.json"
      to: "validation gate at phase end"
      via: "commit-time invariant script compares current metrics to baseline"
      pattern: "testCount|bundleBytes|testRuntimeSeconds"
---

<objective>
Plan 01 is the **audit-only, read-only, analysis-first** wave for Phase 98. It produces every artifact the subsequent refactor plans (02-07) need to do their work safely. Nothing in `src/` or `server/` is modified by Plan 01 beyond additive test/config files.

**Purpose:** The user's #1 fear is essential code being removed. This plan establishes the guardrails (baseline, smoke tests, invariants, dependency graph, deletion ledger, coverage heatmap) that protect against silent deletions and regressions across the entire Phase 98 sweep.

**Output:**
- REQUIREMENTS.md updated with HEALTH-01..14 under v17.0 Code Health
- 98-AUDIT.md (the findings document, ≥300 lines, 10 dimensions)
- 98-DELETIONS.md (the sign-off ledger — user ticks each row)
- 98-01-BASELINE.json (numeric invariants for validation)
- 98-01-COVERAGE-HEATMAP.md (per-directory coverage %)
- 98-01-DEPENDENCY-GRAPH.svg (module dependency visualisation)
- knip.json (false-positive-aware static analysis config)
- .dependency-cruiser.cjs (dep-graph tool config)
- tests/e2e/smoke/phase98-smoke.spec.js (15 golden-path tests)
- src/test/phase98-invariants.test.js (feature-count invariant tests)
- scripts/ralph/prd.json.paused-for-phase98 (Ralph paused)
- Dev tools installed: knip, depcheck, madge, dependency-cruiser

**Hard constraint:** No task in this plan may modify any file under `src/` or `server/` that existed before Plan 01 began, with the single exception of `package.json` (devDependencies addition). Additive test and config files are fine; edits to existing source code are FORBIDDEN in Plan 01.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md
@.planning/phases/98-codebase-audit-and-refactor/98-VALIDATION.md
@scripts/ralph/CLAUDE.md
@package.json
@vite.config.js
@vitest.config.js
@eslint.config.js

<interfaces>
Key contracts the executor must honour when producing artifacts:

Baseline JSON shape (produced by Task 2):
```json
{
  "commit": "<SHA>",
  "generatedAt": "<ISO-8601>",
  "loc": <number>,
  "testCount": <number, must be >= 2535>,
  "bundleBytes": <number>,
  "testRuntimeSeconds": <number>,
  "coverageByDirectory": {
    "src/components/Battle/": { "statements": <pct>, "branches": <pct>, "functions": <pct>, "lines": <pct> },
    "src/store/slices/": { ... },
    ...
  }
}
```

Knip config shape (produced by Task 3 — from RESEARCH.md Safety Rail section):
```json
{
  "$schema": "https://unpkg.com/knip@6/schema.json",
  "entry": [
    "src/main.jsx", "src/routes.jsx",
    "server/src/server.js",
    "scripts/*.{js,mjs,sh}",
    "vite.config.js", "vitest.config.js",
    "src/store/store.js",
    "src/store/slices/*.js",
    "src/store/middleware/*.js",
    "src/game/scenes/*.js",
    "src/data/npc-dialogue/*.json",
    "src/data/ink/*.ink.json",
    "src/data/**/*.json",
    "public/assets/manifests/**/*.json",
    "scripts/ralph/*.{js,sh,json}",
    "**/*.test.{js,jsx}",
    "**/__tests__/**"
  ],
  "project": ["src/**/*.{js,jsx}", "server/**/*.{js}", "scripts/**/*.{js,mjs}"],
  "ignore": ["dist", "node_modules", "e2e/**", "tests/**"],
  "ignoreDependencies": ["phaser", "inkjs", "howler", "ts-fsrs", "@reduxjs/toolkit"]
}
```

Feature count invariants (produced by Task 5):
- zones: 8
- NPCs: 140 (± tolerance, verified via `src/data/npcs*` counts)
- quests: 52
- equipment items: 64
- spells: 50
- companions: 12
- alphabet letters: 28
- achievements: 263
- grammar lessons: 50
- quiz types: 18
- vocabulary words active: 1220 (total 5029)

DELETIONS.md row format (for Task 7 output):
```markdown
### Row N
- **File:** `<path>`
- **LOC:** <n>
- **Last modified:** <date>
- **Why Claude thinks it's dead:** <evidence>
- **Risk if wrong:** <Low/Medium/High + reasoning>
- **1M-context reasoning:** <explanation>
- [ ] Approved — delete
- [ ] Rejected — keep
```
</interfaces>

<phase_97_gate>
This plan HARD-GATES on Phase 97 being merged to main. Task 1 verifies this first before any other work runs. If Phase 97 is not merged, Plan 01 aborts with a clear error message — no audit against an unstable visual layer.
</phase_97_gate>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 97 verification + Ralph pause + dev-tool install + REQUIREMENTS.md registration</name>
  <files>package.json, scripts/ralph/prd.json (renamed), .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md (hard guardrails)
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md sections: Phase Requirements, Safety Rail, Ralph Pipeline Safety
    - .planning/REQUIREMENTS.md (current contents + existing table of contents)
    - .planning/ROADMAP.md (Phase 97 status — must be COMPLETE before Plan 01 proceeds)
    - scripts/ralph/CLAUDE.md (Ralph contract — we must not violate it)
    - package.json (current devDependencies)
  </read_first>
  <action>
    1. **Verify Phase 97 merged:** Run `git log --oneline --grep="97-" -20` and `git log --oneline --grep="WORLD-" -20`. Cross-reference against ROADMAP.md Phase 97 plan list. If Phase 97 is NOT merged (plans 97-01 through 97-08 not all committed to main), STOP and write a blocking error to stdout: "ABORT: Phase 97 not merged. Phase 98 depends on Phase 97 per CONTEXT.md. Resolve Phase 97 before running Plan 01." Do not proceed with any other step.

    2. **Pause Ralph:** Run `test -f scripts/ralph/prd.json && mv scripts/ralph/prd.json scripts/ralph/prd.json.paused-for-phase98`. If `prd.json` doesn't exist (Ralph already paused), log "Ralph already paused" and continue. Verify the rename: `test -f scripts/ralph/prd.json.paused-for-phase98 && ! test -f scripts/ralph/prd.json` must both hold.

    3. **Install dev tools (with exact versions from RESEARCH.md):** Run `npm install --save-dev knip@^6.4.1 depcheck@^1.4.7 madge@^8.0.0 dependency-cruiser@^17.3.10`. Verify `node_modules/.bin/knip`, `node_modules/.bin/depcheck`, `node_modules/.bin/madge`, `node_modules/.bin/depcruise` all exist. Check Graphviz: `which dot || echo "MISSING"`. If missing, note in AUDIT.md later — use Mermaid fallback for the dependency graph.

    4. **Register HEALTH-01..14 in REQUIREMENTS.md:** Append a new top-level section `## v17.0 Requirements — Code Health (Phase 98)` verbatim from RESEARCH.md's "Proposed Requirements" section (lines 953-977 of 98-RESEARCH.md). The section MUST contain exactly 14 unchecked checkboxes, one per HEALTH-XX ID. Also append a `## v17.0 Traceability` table with 14 rows mapping HEALTH-01..14 → Phase 98 → `In progress`. Place this section AFTER the existing `## v15.0 Traceability` section and BEFORE any closing metadata footer.

    5. Verify REQUIREMENTS.md registration: `grep -c "^- \[ \] \*\*HEALTH-" .planning/REQUIREMENTS.md` must return exactly 14.
  </action>
  <verify>
    <automated>test -f scripts/ralph/prd.json.paused-for-phase98 && test ! -f scripts/ralph/prd.json && node -e "require('knip/package.json')" && node -e "require('depcheck/package.json')" && node -e "require('madge/package.json')" && node -e "require('dependency-cruiser/package.json')" && [ "$(grep -c '^- \[ \] \*\*HEALTH-' .planning/REQUIREMENTS.md)" = "14" ]</automated>
  </verify>
  <acceptance_criteria>
    - Phase 97 merged verification output present in task log (or abort triggered)
    - `scripts/ralph/prd.json` does NOT exist
    - `scripts/ralph/prd.json.paused-for-phase98` exists
    - `package.json` devDependencies include knip, depcheck, madge, dependency-cruiser
    - REQUIREMENTS.md contains exactly 14 `- [ ] **HEALTH-` unchecked checkboxes under `## v17.0 Requirements — Code Health (Phase 98)`
    - REQUIREMENTS.md contains `## v17.0 Traceability` table with 14 rows mapping HEALTH-01..14 → Phase 98
    - No file under `src/` has been modified (check: `git diff --name-only src/` is empty)
  </acceptance_criteria>
  <done>Phase 97 verified merged, Ralph paused, tools installed, 14 HEALTH-* requirements registered with traceability.</done>
</task>

<task type="auto">
  <name>Task 2: Capture pre-audit baseline (LOC, test count, bundle bytes, test runtime, coverage)</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md section: Validation Architecture, Regression Detection Artifacts (Example 5)
    - vitest.config.js (existing coverage config, thresholds, reporters)
    - package.json (existing npm scripts: test:run, test:coverage, build)
  </read_first>
  <action>
    Capture every numeric invariant for Phase 98 regression detection, in one JSON file, BEFORE any source modification lands.

    1. **LOC baseline:** `find src server scripts -type f \( -name '*.js' -o -name '*.jsx' \) ! -name '*.test.*' ! -name '*.spec.*' | xargs wc -l | tail -1 | awk '{print $1}'`. Store as `loc`.

    2. **Test count baseline:** Run `npm run test:run -- --reporter=verbose 2>&1 | tail -20 | grep -E "Tests.*passed" | grep -oE "[0-9]+ passed" | head -1 | grep -oE "[0-9]+"`. Cross-check against STATE.md claim of 2535. If it differs, record BOTH and flag the discrepancy in AUDIT.md section 9 (planning drift). Store actual count as `testCount`.

    3. **Test runtime baseline:** Run `time npm run test:run 2>&1` three times, record the real wall-clock time for each, store the median as `testRuntimeSeconds`.

    4. **Bundle bytes baseline:** Run `npm run build` (fresh, no cache if possible). Then `du -sb dist 2>/dev/null | awk '{print $1}'`. Store as `bundleBytes`. Also record per-chunk sizes from `dist/stats.html` if `ANALYZE=true npm run build` produces it — as `bundleChunks` (object).

    5. **Coverage baseline:** Run `npm run test:coverage`. Parse `coverage/coverage-summary.json`. For each directory under src/ (components, store, game, hooks, services, utils, data, world, test), aggregate to per-directory statements/branches/functions/lines %. Store as `coverageByDirectory` (object keyed by directory path).

    6. **Git commit SHA:** `git rev-parse HEAD`. Store as `commit`.

    7. **Generated at:** ISO-8601 UTC. Store as `generatedAt`.

    Write all seven fields to `.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json` as valid JSON (human-formatted, 2-space indent).

    **IMPORTANT:** If `npm run test:run` fails (any test red), STOP and write an error message to AUDIT.md's Section 1 Critical table: "Pre-phase baseline blocked — tests not green." Do not proceed to subsequent tasks.
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json && node -e "const b=require('./.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json'); if(!b.commit||!b.loc||!b.testCount||b.testCount<2535||!b.bundleBytes||!b.testRuntimeSeconds||!b.coverageByDirectory) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - 98-01-BASELINE.json exists and is valid JSON
    - `commit` field is a 40-character hex SHA
    - `loc` field is a positive integer
    - `testCount` field is >= 2535
    - `bundleBytes` field is a positive integer
    - `testRuntimeSeconds` field is a positive number
    - `coverageByDirectory` contains at least 6 directory entries, each with statements/branches/functions/lines fields
    - If tests are not green, task ABORTS rather than silently recording red baseline
  </acceptance_criteria>
  <done>Numeric baselines captured. Every subsequent plan can compare against these numbers.</done>
</task>

<task type="auto">
  <name>Task 3: Create knip.json + .dependency-cruiser.cjs + run dead-code and circular-dep tools</name>
  <files>knip.json, .dependency-cruiser.cjs, .planning/phases/98-codebase-audit-and-refactor/knip.txt, .planning/phases/98-codebase-audit-and-refactor/knip-raw.json, .planning/phases/98-codebase-audit-and-refactor/depcheck.txt, .planning/phases/98-codebase-audit-and-refactor/madge-circular.txt, .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md sections: Safety Rail (six false-positive vectors), Tool Selection, Code Examples
    - src/data/npcDialogueLoader.js (concrete evidence of import.meta.glob vector #1)
    - src/store/store.js (redux-persist whitelist, middleware chain order)
    - vite.config.js (manualChunks — treat as config-as-code, don't flag)
  </read_first>
  <action>
    Produce the static-analysis artifacts for Section 5 (Dead Code) and Section 7 (Dependency Graph) of AUDIT.md.

    1. **Write `knip.json` at repo root** with the exact content from RESEARCH.md Safety Rail section (Layer 1). The entry list MUST include every vector from the research:
       - `src/main.jsx`, `src/routes.jsx`, `server/src/server.js` (main entry points)
       - `scripts/*.{js,mjs,sh}`, `vite.config.js`, `vitest.config.js` (config)
       - `src/store/store.js`, `src/store/slices/*.js`, `src/store/middleware/*.js` (Redux chain)
       - `src/game/scenes/*.js` (Phaser scenes)
       - `src/data/npc-dialogue/*.json`, `src/data/ink/*.ink.json`, `src/data/**/*.json`, `public/assets/manifests/**/*.json` (string-keyed data)
       - `scripts/ralph/*.{js,sh,json}` (Ralph files — NEVER flag)
       - `**/*.test.{js,jsx}`, `**/__tests__/**` (test files — prevents Pitfall 8)
       - `project`: `src/**/*.{js,jsx}`, `server/**/*.{js}`, `scripts/**/*.{js,mjs}`
       - `ignore`: `dist`, `node_modules`, `e2e/**`, `tests/**`
       - `ignoreDependencies`: `phaser`, `inkjs`, `howler`, `ts-fsrs`, `@reduxjs/toolkit` (side-effect loaded)

    2. **Write `.dependency-cruiser.cjs`** at repo root with a minimal config: module systems `["cjs","es6","amd"]`, tsConfig absent, excludes `/node_modules/`, `/dist/`, `/coverage/`, `/e2e/`, `/tests/`. Output type `dot` for SVG generation.

    3. **Run Knip** (produces two outputs):
       ```bash
       npx knip --reporter text > .planning/phases/98-codebase-audit-and-refactor/knip.txt 2>&1 || true
       npx knip --reporter json > .planning/phases/98-codebase-audit-and-refactor/knip-raw.json 2>&1 || true
       ```
       Record exit code in the text file. Knip returning non-zero when it finds issues is EXPECTED.

    4. **Run depcheck:**
       ```bash
       npx depcheck --json > .planning/phases/98-codebase-audit-and-refactor/depcheck.txt 2>&1 || true
       ```
       Note: depcheck outputs JSON despite the .txt extension — that's for consistency of filename. Store as-is.

    5. **Run madge (circular dependencies):**
       ```bash
       npx madge --circular --extensions js,jsx src/ > .planning/phases/98-codebase-audit-and-refactor/madge-circular.txt 2>&1 || true
       ```

    6. **Generate dependency graph SVG:**
       ```bash
       # If Graphviz `dot` available:
       if which dot > /dev/null 2>&1; then
         npx depcruise --config .dependency-cruiser.cjs --output-type dot src/ | dot -Tsvg > .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg
       else
         # Mermaid fallback — generate .md and a placeholder SVG wrapping the Mermaid text
         npx depcruise --config .dependency-cruiser.cjs --output-type mermaid src/ > .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.md
         # Placeholder SVG so HEALTH-08 file-existence check passes
         cat > .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg <<'EOF'
<svg xmlns="http://www.w3.org/2000/svg" width="400" height="100">
<text x="10" y="50">Graphviz not installed. See 98-01-DEPENDENCY-GRAPH.md for Mermaid version.</text>
</svg>
EOF
       fi
       ```

    7. Verify every artifact exists. If `knip-raw.json` is not valid JSON (Knip error), record the stderr and proceed — Task 6 will handle degraded state.
  </action>
  <verify>
    <automated>test -f knip.json && test -f .dependency-cruiser.cjs && test -f .planning/phases/98-codebase-audit-and-refactor/knip.txt && test -f .planning/phases/98-codebase-audit-and-refactor/knip-raw.json && test -f .planning/phases/98-codebase-audit-and-refactor/depcheck.txt && test -f .planning/phases/98-codebase-audit-and-refactor/madge-circular.txt && test -f .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg && node -e "JSON.parse(require('fs').readFileSync('knip.json'))"</automated>
  </verify>
  <acceptance_criteria>
    - `knip.json` is valid JSON and contains `scripts/ralph` in entry array (grep)
    - `knip.json` contains `import.meta.glob`-friendly patterns (any of npc-dialogue/*.json, data/**/*.json, manifests/**/*.json)
    - `.dependency-cruiser.cjs` exists and exports a module
    - `knip.txt`, `knip-raw.json`, `depcheck.txt`, `madge-circular.txt` all exist
    - `98-01-DEPENDENCY-GRAPH.svg` exists (either real Graphviz or Mermaid-fallback placeholder)
    - No `src/` source files modified
  </acceptance_criteria>
  <done>Static analysis artifacts captured. Raw data ready for Task 6's three-layer safety filter.</done>
</task>

<task type="auto">
  <name>Task 4: Build per-directory coverage heatmap and feature-count invariant tests</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md, src/test/phase98-invariants.test.js</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md sections: Test Coverage Measurement, Feature Preservation Smoke Test (Feature inventory quantities table)
    - coverage/coverage-summary.json (produced by Task 2)
    - src/data/zonesRegistry.js or src/world/ZoneRegistry.js (zone count verification)
    - src/data/npcs/*.js (NPC count verification)
    - src/data/quests*.js (quest count)
    - src/data/equipment*.js (equipment count)
    - src/data/spells*.js (spell count)
    - src/data/companions*.js (companion count)
    - src/data/alphabet.js (letter count)
    - src/data/achievements.js (achievement count)
    - src/data/grammar.js (grammar lesson count)
    - src/data/quizTypes.js (quiz type registry length)
  </read_first>
  <action>
    Produce two artifacts: the human-readable coverage heatmap and a machine-checked invariant test suite.

    **Part A — Coverage heatmap (`98-01-COVERAGE-HEATMAP.md`):**

    1. Read `coverage/coverage-summary.json`.
    2. Group files by top-level directory under src/ (e.g., `src/components/Battle/`, `src/store/slices/`, `src/game/systems/`).
    3. For each group, compute weighted-average statements/branches/functions/lines %.
    4. Produce a markdown table:
       ```markdown
       | Directory | Files | Statements % | Branches % | Functions % | Lines % | Refactor-Safe? |
       |-----------|-------|--------------|------------|-------------|---------|----------------|
       | src/components/Battle/ | 14 | 18% | 42% | 21% | 19% | ❌ Wave-0 tests required |
       ```
    5. Mark each row:
       - ✅ Refactor-safe: statements AND branches both ≥ 50%
       - ⚠ Elevated-risk directory (FSRS, redux-persist, battle state machine, IndexedDB migration layer): requires ≥ 70% per user decision
       - ❌ Coverage gap: below threshold — "cannot-refactor-until-covered"
    6. Hard-code the elevated-risk list per user decision: `src/utils/fsrs*`, `src/store/migrations*`, `src/store/slices/battleSlice*`, `src/services/indexedDBService*` (or whatever the actual paths are — grep to confirm).
    7. List at the top: "Directories requiring Wave-0 tests before Plan 03 can proceed: <list>".

    **Part B — Feature-count invariant tests (`src/test/phase98-invariants.test.js`):**

    Create a vitest test file with ≥ 11 assertions, one per feature category. Example structure:
    ```javascript
    import { describe, it, expect } from 'vitest';

    describe('Phase 98 — Feature Count Invariants (must hold through refactor)', () => {
      it('zones: 8 active zones', () => {
        // Read src/world/ZoneRegistry.js (or whichever authoritative file)
        // Assert count === 8
      });
      it('NPCs: 140 NPCs across zones', () => { /* ... */ });
      it('quests: 52 quests', () => { /* ... */ });
      it('equipment: 64 items', () => { /* ... */ });
      it('spells: 50 spells', () => { /* ... */ });
      it('companions: 12 companions', () => { /* ... */ });
      it('alphabet: 28 Arabic letters', () => { /* ... */ });
      it('achievements: >= 263 entries', () => { /* ... */ });
      it('grammar: 50 lessons', () => { /* ... */ });
      it('quizTypes: 18 entries in QUIZ_TYPE_REGISTRY', () => { /* ... */ });
      it('vocabulary: >= 1220 active words', () => { /* ... */ });
    });
    ```

    For each test: read the authoritative data file, count entries, assert the expected count (using `>=` rather than `===` where appropriate so future additions don't break the invariant — only regressions do).

    **If a count doesn't match the research-claimed number (e.g., NPCs claimed as 140 but actual is 138):** Record the actual count in AUDIT.md section 9 (drift) AND use the actual count as the baseline in the test. The goal is "counts don't DROP," not "counts match the research guess." The test assertion should be `>= actual_current_count`.

    Run the test to confirm it passes: `npx vitest run src/test/phase98-invariants.test.js`.
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md && test -f src/test/phase98-invariants.test.js && npx vitest run src/test/phase98-invariants.test.js --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - 98-01-COVERAGE-HEATMAP.md contains a markdown table with at least 6 directory rows
    - Heatmap includes a "Directories requiring Wave-0 tests" header list
    - High-risk directories (FSRS, migrations, battleSlice, IndexedDB) are marked with the 70% threshold
    - `src/test/phase98-invariants.test.js` exists and contains at least 11 `it(` blocks
    - Running the invariant test suite passes (all assertions green)
    - No existing test file is modified
  </acceptance_criteria>
  <done>Coverage gaps identified and quantified; feature counts captured as permanent guard rails.</done>
</task>

<task type="auto">
  <name>Task 5: Create 15 Playwright golden-path smoke tests</name>
  <files>tests/e2e/smoke/phase98-smoke.spec.js</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md sections: Feature Preservation Smoke Test (Automated smoke tests list)
    - playwright.config.js (existing Playwright setup — baseURL, timeouts, browser)
    - e2e/ directory (existing Playwright tests for style reference)
    - src/main.jsx (app entry point — what renders on load)
    - src/components/MainMenu*.jsx (new game button)
    - src/components/HUD.jsx (HUD buttons for review, inventory, etc.)
  </read_first>
  <action>
    Create `tests/e2e/smoke/phase98-smoke.spec.js` with 15 golden-path tests. Each test is a minimal happy-path assertion — fast, deterministic, no deep feature testing. The point is to catch refactor regressions, not to exhaustively test features.

    Use Playwright's `test.describe.serial` if tests share state, or independent `test` blocks if not. Prefer independent.

    For each of the 15 paths below, write a test that does the minimum to verify the feature still functions. All 15 MUST be present and executable.

    1. **Boot** — `page.goto('/')`; no console errors; loading screen selector renders; main menu selector visible within 10s.
    2. **New player onboarding** — Click "New Game"; tutorial overlay visible; first step text rendered.
    3. **Zone travel** — Open world map; click zone card (or equivalent); zone transition completes; Phaser canvas visible.
    4. **NPC interaction** — Approach NPC (simulate keyboard or direct Redux dispatch); press E or Space; dialogue overlay opens; Arabic text visible.
    5. **Quest flow** — Accept a quest via dialogue; open quest journal; new quest appears; advance one objective.
    6. **FSRS review** — Open review from HUD; 3 words presented; submit answer; no error; next card loads or completion screen shows.
    7. **Quiz** — Launch any quiz type; answer 3 questions; quiz completion screen shows score.
    8. **Grammar lesson** — Open grammar section; select A1 lesson; complete one exercise; XP notification or bar update visible.
    9. **Battle** — Trigger battle; battle scene loads; cast one spell; no error. (Victory optional.)
    10. **Equipment** — Open inventory; equip an item; stat change reflected in stats panel.
    11. **Companion** — Open companion roster; click a companion card; companion detail visible.
    12. **Shop + haggling** — Open zone shop; attempt purchase; haggling prompt appears (Arabic numeral input or equivalent).
    13. **Crafting** — Open crafting panel; select a recipe; craft; result appears in inventory.
    14. **Save & reload** — Trigger autosave (wait or explicit action); reload page; assert XP/level/zone preserved.
    15. **Settings** — Open settings; toggle BGM off; close; reopen; setting persists.

    **Implementation pragmatics:**
    - Use `data-testid` attributes where components have them; fall back to role-based selectors.
    - For tests where navigation is deep (e.g., crafting), it's acceptable to use `page.evaluate()` to dispatch Redux actions directly, as long as the RESULT is asserted on the rendered DOM — the point is behavior preservation, not UI navigation coverage.
    - Set test timeout to 30s per test; phase smoke run should complete in < 10 min total.
    - If any test CANNOT be implemented (feature requires server state, etc.), stub it with `test.skip()` AND record the skip in AUDIT.md section 10 (Open Questions for User) with reason.

    Verify all 15 tests run: `npx playwright test tests/e2e/smoke/phase98-smoke.spec.js --reporter=list`.

    **If any test fails on current green main:** Do NOT mark the task complete. Either the test is wrong (fix it) OR there's a real pre-existing bug (flag in AUDIT.md section 1 Critical and stop the phase). Green smoke tests are the baseline for every subsequent refactor.
  </action>
  <verify>
    <automated>test -f tests/e2e/smoke/phase98-smoke.spec.js && [ "$(grep -cE "^\s*test\(" tests/e2e/smoke/phase98-smoke.spec.js)" -ge "15" ] && npx playwright test tests/e2e/smoke/phase98-smoke.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/smoke/phase98-smoke.spec.js` exists
    - File contains at least 15 `test(` blocks (grep count)
    - All 15 covered topics (boot, onboarding, zone, NPC, quest, FSRS, quiz, grammar, battle, equipment, companion, shop, crafting, save/reload, settings) appear as test names or descriptions
    - `npx playwright test tests/e2e/smoke/phase98-smoke.spec.js` exits 0 (or skipped tests documented in AUDIT.md with reason)
    - No existing Playwright test is modified
  </acceptance_criteria>
  <done>15 smoke tests exist, run, and pass against current main. Baseline for feature-preservation assertions in Plans 03-07.</done>
</task>

<task type="auto">
  <name>Task 6: Produce 98-AUDIT.md — the 10-dimension findings report (single 1M-context pass)</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md sections: Audit Structure (full template), Safety Rail (three-layer filter), Pre-existing Stubs Disposition
    - .planning/phases/98-codebase-audit-and-refactor/knip.txt (Task 3 output)
    - .planning/phases/98-codebase-audit-and-refactor/knip-raw.json (Task 3 output)
    - .planning/phases/98-codebase-audit-and-refactor/depcheck.txt (Task 3 output)
    - .planning/phases/98-codebase-audit-and-refactor/madge-circular.txt (Task 3 output)
    - .planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md (Task 4 output)
    - .planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json (Task 2 output)
    - .planning/AUDIT-REPORT.md (historical 2026-02-08 audit — reference only, do not delete)
    - src/ (ENTIRE source tree — this is the 1M-context pass)
    - server/src/ (entire backend)
    - scripts/ralph/ (read-only, audit but do not modify)
    - .planning/phases/86-*/ through .planning/phases/96-*/ (11 stub directories)
  </read_first>
  <action>
    This is the **core audit task**. Use Opus 4.7's 1M context to read the entire source tree, cross-reference it against the static analysis outputs from Task 3, apply the three-layer safety filter from RESEARCH.md Safety Rail section, and write `98-AUDIT.md`.

    **Structure (follow RESEARCH.md Audit Structure template EXACTLY):**

    ```markdown
    # Phase 98 Audit — Gogo Arabic Codebase (v16.0 baseline)

    **Generated:** <ISO date>
    **Scope:** src/, server/, scripts/, public/assets/manifests/, .planning/phases/
    **Baseline:** <commit SHA from 98-01-BASELINE.json>
    **Total files audited:** <count JS/JSX + JSON + MD>
    **Total LOC (pre-audit):** <from baseline.json>

    ## Executive Summary
    - (≤ 20 lines, top 5 findings by priority)
    - (what to sign off first — link to DELETIONS.md)
    - (out-of-scope acknowledgments)

    ## How To Use This Document
    1. Read the Executive Summary
    2. Review Section 1 (Critical — blocking bugs)
    3. Tick or reject each row in 98-DELETIONS.md
    4. Tell Claude "proceed with approved deletions"

    ## 1. Critical (must fix before any refactor)
    Table: Finding | File(s) | Severity | Fix effort | Recommendation

    ## 2. High Priority — Consolidations (behaviour-preserving)
    Table: Duplicate logic | Locations | Proposed consolidation | Risk

    ## 3. Medium Priority — Architectural Drift
    Table: Pattern | Violation sites | Canonical pattern | Refactor plan ref

    ## 4. Low Priority — Cosmetic / Comment Rot
    Table: Type | File | Line | Proposed edit

    ## 5. Dead Code Candidates — REQUIRES SIGN-OFF
    → See 98-DELETIONS.md for the full list. Summary stats: N files, M exports, K npm deps.

    ## 6. Test Coverage Heatmap
    → See 98-01-COVERAGE-HEATMAP.md. Summary: <N directories below 50%, K directories in elevated-risk list below 70%>

    ## 7. Dependency Graph — Module Relationships
    → See 98-01-DEPENDENCY-GRAPH.svg
    Narrative: circular imports found (list from madge), god modules (list), leaf-only modules (list).

    ## 8. Pre-Existing Phase Stubs (86-96) Disposition
    One row per phase 86, 87, 88, 89, 90, 91, 92, 93, 94, 95, 96 — 11 rows total.
    Column: Phase | Has implementation evidence? | Overlap with shipped? | Recommendation (keep/archive/delete) | User decision [ ]

    ## 9. Planning-Doc Drift (.planning/)
    Table: Doc | Claim | Reality | Proposed edit
    (e.g., if STATE.md claims 2535 tests but actual count is 2538, record it)

    ## 10. Open Questions for User
    Numbered list — must include LOC reduction confirmation, phase-stub disposition default, any deferred decisions.

    ## Appendix A — Tool Output Artifacts
    - knip.txt (Task 3)
    - depcheck.txt (Task 3)
    - madge-circular.txt (Task 3)
    - 98-01-DEPENDENCY-GRAPH.svg (Task 3)
    - coverage/coverage-summary.json (Task 2)
    - 98-01-BASELINE.json (Task 2)

    ## Appendix B — Historical Audit Context
    Link to .planning/AUDIT-REPORT.md (2026-02-08 baseline) — preserved as reference, NOT superseded. Phase 98's AUDIT.md is a fresh pass.
    ```

    **Filling each section:**

    - **Section 1 (Critical):** Outright bugs the 1M pass surfaced. Example types: unused async keywords hiding dangling promises, unhandled Redux error states, missing boundary error handlers. BE CONSERVATIVE — only things that are genuinely broken go here.

    - **Section 2 (Consolidations):** Duplicate algorithms/patterns detected during 1M read-through. Example: two shuffle functions, two debounce implementations, two zone-transition wrappers. Each row shows: locations, proposed merge, risk. Merging requires characterization tests first (per RESEARCH.md Pitfall 3).

    - **Section 3 (Architectural drift):** Places where the established pattern is violated. Example: component accesses `store.getState().slice.field` directly instead of via selector; middleware bypasses reducer and mutates state. Each row shows: pattern, violations, canonical form.

    - **Section 4 (Comment rot):** TODO/FIXME/HACK > 6 months old, stale comments contradicting code. Grep `TODO\|FIXME\|HACK\|XXX` + commit date.

    - **Section 5 (Dead code summary):** Reference DELETIONS.md (Task 7) for the full list. Here put only summary stats.

    - **Section 6 (Coverage):** Reference the heatmap file. List directories flagged as "cannot refactor until tested."

    - **Section 7 (Dependency graph):** Reference the SVG. Describe: circular imports found by madge, god modules (>30 importers — define precisely), leaf-only modules (no dependents = probably dead or entry-points).

    - **Section 8 (Phase stubs 86-96):** For each of the 11 phase directories, read the stub plan file, cross-reference against:
       - ROADMAP.md (is this phase claimed shipped under a different number?)
       - Git log `--all` for the phase's topic keywords
       - Existing slices, components, data files that match the stub's topic
       - If the stub topic IS implemented (e.g., seasonalEventSlice.js exists for phase 86), mark recommendation as `archive` (preserve evidence) with user-decision checkbox.
       - If clearly unrelated to shipped work, mark as `archive` by default (CONTEXT.md: preserve, don't delete).
       - NEVER recommend `delete` unless the user explicitly approved it in CONTEXT.md (they did NOT — default is archive per research open question 2).

    - **Section 9 (Drift):** Grep .planning/ for claims vs reality. Examples: STATE.md test count vs actual, ROADMAP.md phase status vs git history, REQUIREMENTS.md checkbox vs implementation evidence.

    - **Section 10 (Open questions):** Any decision the user must make. Include: LOC reduction 10% aspirational vs hard? (answer already: aspirational per orchestrator). Elevated-risk 70% threshold confirmed per orchestrator. DELETIONS.md rows waiting for tick. Phase 86-96 dispositions per row.

    **Minimum length:** 300 lines. If the audit finds less, extend narrative/evidence. This is the anchor document for the entire phase; brevity is the wrong optimization.

    **Anti-patterns (do NOT do):**
    - Do NOT mix dimensions in one table (dead code + drift + duplicates = unreadable).
    - Do NOT mark anything in Section 1 "Critical" unless it's a real bug. Critical must be genuinely actionable.
    - Do NOT recommend deletions in AUDIT.md itself — those go in DELETIONS.md (Task 7).
    - Do NOT propose dispositions for phases 86-96 stronger than `archive` unless the user has explicitly said so.
    - Do NOT include findings about `src/data/ink/*.ink.json` (out of scope per CONTEXT.md).
    - Do NOT include findings about Arabic content JSON (out of scope per CONTEXT.md — "JSON content is sacred").
    - Do NOT flag test files as dead (Pitfall 8 — knip.json should already handle this).

    **1M-context discipline:**
    - After reading the entire tree, the "reasoning" column for every flagged item must reference the ACTUAL code you read, not a guess.
    - If Knip flags a file that on manual 1M-context read has an EventBus string, manifest reference, or dynamic import match — RECLASSIFY it as "indirect-reference, keep" and do NOT put it in Section 5 or DELETIONS.md.
    - Every deletion candidate must explain WHY each false-positive vector (the six) does NOT apply.
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md && [ "$(wc -l < .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md)" -ge "300" ] && grep -q "^## 1. Critical" .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md && grep -q "^## 8. Pre-Existing Phase Stubs" .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md && [ "$(grep -cE "^### Phase 9[0-6]|^### Phase 8[6-9]" .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md)" -ge "11" ]</automated>
  </verify>
  <acceptance_criteria>
    - `98-AUDIT.md` exists, is at least 300 lines
    - Contains all 10 sections (1. Critical through 10. Open Questions) plus Appendices A and B
    - Section 8 contains at least 11 subsections (one per phase 86-96)
    - Section 5 references `98-DELETIONS.md` (cross-reference)
    - Section 6 references `98-01-COVERAGE-HEATMAP.md`
    - Section 7 references `98-01-DEPENDENCY-GRAPH.svg`
    - No recommendation in Section 8 stronger than `archive` unless explicitly approved by user (check: grep for `delete` recommendations requires sign-off)
    - No source file under `src/` or `server/` modified
  </acceptance_criteria>
  <done>Single canonical audit report produced. User can review and triage before any refactor action.</done>
</task>

<task type="auto">
  <name>Task 7: Produce 98-DELETIONS.md — the sign-off ledger with three-layer-filtered candidates</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md sections: Safety Rail (three-layer filter), Sign-Off Mechanism (DELETIONS.md structure)
    - .planning/phases/98-codebase-audit-and-refactor/knip-raw.json (Task 3)
    - .planning/phases/98-codebase-audit-and-refactor/depcheck.txt (Task 3)
    - src/utils/eventBusTypes.js (74 event constants — NEVER a deletion candidate)
    - src/store/migrations.js (if exists — migration refs are load-bearing)
    - src/store/store.js (redux-persist whitelist)
    - public/assets/manifests/ (string-key references)
    - scripts/ralph/ (EVERY file pre-flagged as unapprovable)
  </read_first>
  <action>
    Apply Layers 2 and 3 of the Safety Rail to produce the sign-off ledger. Every candidate in this file has already passed Layer 1 (Knip config). This task adds Layer 2 (1M-context cross-check) and prepares Layer 3 (user tick).

    **Structure:**

    ```markdown
    # Phase 98 — Deletion Approval Ledger

    **Generated:** <ISO date>
    **Total candidates:** Files=N, Exports=M, NPM deps=K, Phase stubs=11
    **How to review:** Read each row. Tick `- [x] Approved — delete` OR `- [x] Rejected — keep`. Untouched rows default to REJECTED.

    ## Section 0 — Do-Not-Delete Registry (pre-approved retention)
    These items are explicitly listed as NOT candidates, regardless of what static analysis says:
    - Every file in `scripts/ralph/` (Ralph pipeline — active)
    - Every file in `src/data/ink/` (ink engine — out of scope)
    - Every file in `src/data/*.json` that contains Arabic content (sacred content)
    - `src/utils/eventBusTypes.js` (string-keyed runtime wiring)
    - `src/store/migrations.js` and any `*migration*.js` (redux-persist history)
    - Every file in `server/src/middleware/` (security-sensitive)
    - Every file in `public/assets/manifests/` (Phaser string-key references)

    ## Section 1 — Full file deletion candidates
    ### Row 1
    - **File:** <path>
    - **LOC:** <n>
    - **Last modified:** <date from git log>
    - **Knip evidence:** <line from knip.txt>
    - **Layer 2 cross-check results:**
      - EventBus constant names (grep): <none / matches>
      - Redux action type strings (grep): <none / matches>
      - Asset manifest JSON (grep): <none / matches>
      - ink compiled output (grep): <none / matches>
      - scripts/ralph/prd-*.json (grep): <none / matches>
      - `// lazy-load:` or `import.meta.glob` comment (grep): <none / matches>
      - package.json scripts (grep): <none / matches>
    - **1M-context reasoning:** <why Claude believes it's safe to delete>
    - **Risk if wrong:** <Low / Medium / High + rationale>
    - [ ] Approved — delete
    - [ ] Rejected — keep

    ### Row 2
    ... (repeat per candidate)

    ## Section 2 — Unused export removal (file stays, export removed)
    ### Row N
    - **File:** <path>
    - **Export:** <symbol>
    - **Knip evidence:** <line>
    - **Layer 2 cross-check:** <same 7 checks>
    - **Risk if wrong:** Very low — tree-shaken anyway.
    - [ ] Approved — remove export
    - [ ] Rejected — keep export

    ## Section 3 — npm dependency removal
    ### Row M
    - **Package:** <name>
    - **depcheck evidence:** <line>
    - **Cross-check against vite manualChunks, side-effect imports:** <result>
    - **Risk if wrong:** Low — dev-time only (mostly) OR Medium — runtime (flag).
    - [ ] Approved — remove from package.json
    - [ ] Rejected — keep

    ## Section 4 — Phase-stub disposition (phases 86-96)
    One row per phase, checkbox for user decision: `[ ] keep [ ] archive [ ] delete`.
    Default recommendation for each row: `archive` (per research and orchestrator decision).

    ## Audit trail
    ### Execution log
    (Empty until user approves rows and Plan 03 executes.)
    ```

    **Procedure:**

    1. **Parse `knip-raw.json`** for the `files`, `exports`, `dependencies` categories.

    2. **For each candidate**, apply Layer 2 manually:
       - `grep -rn "<filename-stem>" src/ server/ scripts/ public/assets/manifests/ 2>/dev/null | grep -v "test" | head`
       - `grep -rn "<filename-stem>" src/utils/eventBusTypes.js 2>/dev/null`
       - `grep -rn "<filename-stem>" src/store/migrations*.js 2>/dev/null || true`
       - `grep -l "<filename-stem>" public/assets/manifests/**/*.json 2>/dev/null || true`
       - `grep -l "<filename-stem>" scripts/ralph/prd-*.json 2>/dev/null || true`
       - `grep -l "import.meta.glob" src/**/*.js 2>/dev/null | xargs grep "<filename-stem>" 2>/dev/null || true`
       - `grep "<filename-stem>" package.json || true`

       If ANY grep returns a match → **DO NOT** add to DELETIONS.md. Instead, record in AUDIT.md section 5 "Layer 2 rescues: <path> had indirect reference <evidence>".

    3. **For candidates that survive Layer 2**, write a row in DELETIONS.md. Mark risk conservatively — prefer `Medium` over `Low` when in doubt; user can downgrade after reading.

    4. **Auto-exclude the Do-Not-Delete Registry items** — even if Knip or depcheck flagged them, they do not enter Sections 1/2/3 at all.

    5. **Fill Section 4 (phase stubs)** using the same data Task 6 put in AUDIT.md Section 8. Each of the 11 phases gets an own row with the three-way checkbox.

    6. **Do NOT tick any checkbox.** DELETIONS.md ships empty (unticked) — the user ticks.

    **Minimum length:** 50 lines. If no candidates survive, that's a valid outcome — document it: "No deletion candidates survived the three-layer filter. Sections 1-3 are empty. Section 4 (phase stubs) remains for user review."
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md && [ "$(wc -l < .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md)" -ge "50" ] && grep -q "^## Section 0" .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md && grep -q "^## Section 4" .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md && [ "$(grep -c '^- \[ \] Approved\|^- \[ \] Rejected\|^- \[ \] keep\|^- \[ \] archive\|^- \[ \] delete' .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md)" -ge "11" ]</automated>
  </verify>
  <acceptance_criteria>
    - `98-DELETIONS.md` exists, is at least 50 lines
    - Contains Sections 0, 1, 2, 3, 4 all present
    - Section 0 lists the do-not-delete registry (scripts/ralph, ink, migrations, eventBusTypes, manifests, server middleware, Arabic JSON)
    - Section 4 has one row per phase 86-96 (11 rows)
    - Every candidate row in Sections 1-3 shows evidence for all 7 Layer-2 cross-checks
    - Zero `[x]` ticked boxes — user does the ticking
    - Candidates from Do-Not-Delete Registry are NOT present in Sections 1-3 (check: grep for `scripts/ralph` under Section 1-3 should return nothing)
  </acceptance_criteria>
  <done>Sign-off ledger ready for user review. No file is deleted until user ticks rows AND Plan 03 executes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: User reviews AUDIT.md + DELETIONS.md before Plan 02 onward</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md, .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md (user-modified by ticking checkboxes)</files>
  <action>CHECKPOINT — User reviews AUDIT.md and ticks DELETIONS.md rows. No Claude action until user responds with approval signal.</action>
  <verify>User types the resume signal, confirming review is complete.</verify>
  <done>User has ticked every row in DELETIONS.md Sections 1-4 (Approved or Rejected) AND responded with resume signal.</done>
  <what-built>
    Plan 01 has produced:
    - 98-AUDIT.md (the 10-dimension findings report)
    - 98-DELETIONS.md (the sign-off ledger for deletion candidates)
    - 98-01-BASELINE.json (numeric invariants)
    - 98-01-COVERAGE-HEATMAP.md (per-directory coverage)
    - 98-01-DEPENDENCY-GRAPH.svg (module graph)
    - knip.json + knip.txt + depcheck.txt + madge-circular.txt (tool outputs)
    - tests/e2e/smoke/phase98-smoke.spec.js (15 smoke tests — all green)
    - src/test/phase98-invariants.test.js (feature-count invariants — all green)
    - REQUIREMENTS.md now has v17.0 Code Health section with HEALTH-01..14
    - Ralph pipeline is PAUSED (prd.json renamed)
  </what-built>
  <how-to-verify>
    1. **Open `.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md`.** Read the Executive Summary and Section 1 (Critical). Any red flags?

    2. **Open `.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md`.** For each row in Sections 1, 2, 3, 4:
       - Read the file path / export / package name
       - Read the 1M-context reasoning and Layer-2 cross-check evidence
       - Tick either `- [x] Approved — delete/remove` OR `- [x] Rejected — keep`
       - For Section 4 (phase stubs), tick ONE of `keep / archive / delete` per phase
       - If unsure on a row, leave it unticked — default is REJECT (safer).

    3. **Review `.planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md`.** Confirm the list of directories requiring Wave-0 tests before refactor. These will drive Plan 02.

    4. **Review open questions in AUDIT.md Section 10.** Any decisions left to make? Answer them inline in the file OR tell Claude in chat.

    5. **Confirm Phase 98 can proceed** — respond with one of:
       - "DELETIONS.md reviewed, proceed" — Plan 02 starts
       - "Pause, need to think about <X>" — Plan 02 blocked, no refactor plans run
       - "Reject finding on row <N>" — Claude removes row, continues with remainder

    **Time expectation:** 30-90 minutes of focused review. The audit is designed to be readable in one sitting; DELETIONS.md is designed to be tickable without cross-referencing.

    **Nothing is deleted or refactored until this checkpoint clears.** Plans 02-07 ALL gate on this approval.
  </how-to-verify>
  <resume-signal>Type "audit approved, proceed" when DELETIONS.md ticks are complete and AUDIT.md open questions are answered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static analysis → deletion decision | Knip/depcheck output is untrusted input; the three-layer safety filter + user sign-off cross this boundary |
| Ralph agent → main branch | Ralph commits autonomously; Phase 98 must not race its commits (pause mechanism) |
| IndexedDB/localStorage (user browsers) → redux-persist migration | Refactor commits that touch slices could silently break old saves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | Static analysis tool output | mitigate | Three-layer filter: Knip config + 1M manual cross-check + user sign-off in DELETIONS.md |
| T-98-02 | Denial of Service | Ralph pipeline running during audit | mitigate | Pause Ralph before Plan 01 starts; restore at phase end (Plan 07) |
| T-98-03 | Tampering | Redux slice deletion breaks redux-persist migration | mitigate | Do-Not-Delete Registry includes migrations.js; any slice touch requires persistence round-trip test (per Plan 04+) |
| T-98-04 | Tampering | Accidental modification of Arabic JSON content | mitigate | Do-Not-Delete Registry + `src/data/**/*.json` never modified beyond format-normalization refactor |
| T-98-05 | Elevation | Removal of server security middleware | mitigate | Do-Not-Delete Registry includes server/src/middleware/ |
| T-98-06 | Information disclosure | Accidental commit of secrets during cleanup | accept | Server env vars referenced by string (JWT_SECRET etc.); audit never modifies env-referenced code |
| T-98-07 | Repudiation | Silent deletion without sign-off trail | mitigate | HEALTH-03 git invariant: every delete mode commit body references DELETIONS.md row number and user approval date |

</threat_model>

<verification>
**Per-task verification is defined inline above.**

**Plan-level verification (before Task 8 checkpoint opens):**
- `npm run test:run` exits 0 (no pre-existing failures)
- `git diff --name-only src/ server/` returns empty (no existing source files modified)
- All 9 artifact files exist:
  1. .planning/REQUIREMENTS.md contains 14 HEALTH-XX lines
  2. 98-AUDIT.md ≥ 300 lines
  3. 98-DELETIONS.md ≥ 50 lines
  4. 98-01-BASELINE.json valid JSON with all required fields
  5. 98-01-COVERAGE-HEATMAP.md exists
  6. 98-01-DEPENDENCY-GRAPH.svg exists
  7. knip.json valid JSON at repo root
  8. tests/e2e/smoke/phase98-smoke.spec.js exists (≥15 tests)
  9. src/test/phase98-invariants.test.js exists (≥11 tests, all green)
- `scripts/ralph/prd.json.paused-for-phase98` exists; `scripts/ralph/prd.json` does NOT
</verification>

<success_criteria>
Plan 01 succeeds when:
1. Phase 97 merge verified.
2. Ralph paused.
3. Dev tools installed (knip, depcheck, madge, dependency-cruiser).
4. REQUIREMENTS.md has 14 HEALTH-* requirements registered.
5. 98-01-BASELINE.json captures LOC, testCount ≥ 2535, bundleBytes, testRuntimeSeconds, coverageByDirectory.
6. knip.json + .dependency-cruiser.cjs written at repo root.
7. Static analysis artifacts produced: knip.txt, knip-raw.json, depcheck.txt, madge-circular.txt, dependency-graph.svg.
8. 98-01-COVERAGE-HEATMAP.md produced with refactor-safe flags per directory.
9. phase98-invariants.test.js passes with ≥ 11 feature-count assertions.
10. phase98-smoke.spec.js passes all 15 golden-path tests.
11. 98-AUDIT.md produced (≥ 300 lines, all 10 sections + 2 appendices).
12. 98-DELETIONS.md produced (≥ 50 lines, Sections 0-4, all rows unticked, Do-Not-Delete Registry honoured).
13. User reviews and approves via checkpoint (Task 8).
14. No file under src/ or server/ modified beyond additive tests and package.json devDependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/98-codebase-audit-and-refactor/98-01-SUMMARY.md` summarising:
- Counts (LOC baseline, test count, bundle bytes, coverage per directory, dead-code candidates, phase-stub dispositions)
- User review status (audit approved? rows ticked?)
- Next plan: 02 (Wave-0 test coverage uplift for high-risk directories)
</output>
