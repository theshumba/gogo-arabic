---
phase: 98-codebase-audit-and-refactor
plan: 05
type: execute
wave: 2
depends_on: ["98-01", "98-02", "98-03"]
files_modified:
  - "(Tangled-dependency targets identified in 98-AUDIT.md Sections 3 and 7 — concrete paths determined after audit review)"
autonomous: false
requirements:
  - HEALTH-05
  - HEALTH-07
  - HEALTH-11
tags:
  - dependencies
  - circular-imports
  - refactor
  - stub-plan

must_haves:
  truths:
    - "Every circular import reported by madge is either broken (by extraction) or explicitly documented as acceptable"
    - "Every god-module finding from dependency-cruiser is either decomposed or documented as load-bearing by design"
    - "No change to public API surface (Redux action types, API endpoints) — per CONTEXT.md"
    - "Middleware chain order in store.js is PRESERVED — Pitfall 4"
    - "All tests + invariants + smoke tests pass after each commit"
  artifacts:
    - path: "src/ — refactored imports"
      provides: "Circular imports eliminated through helper extraction; module boundaries respected"
  key_links:
    - from: "Dependency refactor commits"
      to: "98-AUDIT.md sections 3 and 7"
      via: "commit body references AUDIT.md finding"
      pattern: "AUDIT\\.md section [37]"
---

<objective>
**Stub plan — content populated AFTER user reviews 98-AUDIT.md.**

Purpose: Unwind tangled dependencies surfaced by the audit — circular imports (madge output) and god modules (dependency-cruiser output). Concrete targets cannot be specified until the audit exists.

**Why this is a stub:** Same reason as Plan 04 — the planner cannot pre-specify refactor targets before seeing audit findings without risking scope mis-calibration.

**Output after content is populated:** Per-dependency-finding atomic commit refactors.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md
@.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md
@.planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg
@.planning/phases/98-codebase-audit-and-refactor/madge-circular.txt
@.planning/phases/98-codebase-audit-and-refactor/98-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POPULATE PLACEHOLDER — Derive dependency-refactor tasks from AUDIT.md Sections 3 + 7</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-05-PLAN.md (this file, re-populated)</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md (Section 3 Architectural Drift + Section 7 Dependency Graph narrative)
    - .planning/phases/98-codebase-audit-and-refactor/madge-circular.txt (raw circular import list)
    - .planning/phases/98-codebase-audit-and-refactor/98-01-DEPENDENCY-GRAPH.svg (visual reference)
    - .planning/phases/98-codebase-audit-and-refactor/98-02-SUMMARY.md (test coverage status)
    - src/store/store.js (middleware chain — MUST NOT be reordered per Pitfall 4)
  </read_first>
  <action>
    This task is a **plan-population placeholder.**

    **Procedure:**

    1. Read AUDIT.md Section 3 (Architectural Drift) and Section 7 (Dependency Graph). Also read `madge-circular.txt` directly.

    2. For each finding:
       - Circular import: identify the two (or N) modules involved. Design a helper extraction that breaks the cycle — typically extract shared types/constants into a new lower-tier module both sides import.
       - God module: identify consumers. Design a split by responsibility. **CAUTION:** some god modules are load-bearing by design (e.g., `eventBusTypes.js` is intentionally a single registry). Flag these and leave alone.
       - Architectural drift: plan the shim/refactor. **CAUTION:** any Redux action type rename is FORBIDDEN by CONTEXT.md.

    3. Re-emit this PLAN.md with concrete tasks, each:
       - Lists affected files
       - Has before/after import structure
       - Runs the relevant test suite as verification
       - References AUDIT.md finding in acceptance criteria

    **Forbidden targets (do NOT include in any populated task):**
    - `src/store/store.js` middleware chain reorder (Pitfall 4)
    - `src/store/slices/*.js` action type rename (CONTEXT.md public contract)
    - Any file in `src/data/ink/` (out of scope)
    - Any file in `src/game/scenes/` (Phase 97 territory — out of scope per research Refactor Patterns)
    - Any file in `scripts/ralph/` (user-approval-only)
    - Server security middleware (`server/src/middleware/`)

    **If AUDIT.md surfaces no dependency findings:** Plan 05 becomes a no-op. Write SUMMARY.md accordingly.

    4. **MANDATORY plan-checker re-verification.** BEFORE calling `/gsd-execute-phase 98 --plan 05` against the populated file, run:
       ```
       /gsd-plan-phase 98 --check-only --plan 05
       ```
       This re-routes the newly-populated PLAN.md through `gsd-plan-checker` exactly as this initial stub went through. If the checker returns any BLOCKER on the populated tasks (e.g., a refactor that touches a forbidden path or lacks characterization-test backing), STOP and surface to user. Do NOT execute a populated plan that bypassed verification — this is the guardrail that prevents audit-derived tasks from drifting into essential-code territory.

    5. Only after Step 4 passes, run `/gsd-execute-phase 98 --plan 05`.
  </action>
  <verify>
    <automated>grep -q "AUDIT.md Section" .planning/phases/98-codebase-audit-and-refactor/98-05-PLAN.md || grep -q "No dependency" .planning/phases/98-codebase-audit-and-refactor/98-05-SUMMARY.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - This PLAN.md rewritten with concrete dependency-refactor tasks based on AUDIT.md findings
    - Forbidden targets NOT included
    - If no findings, no-op SUMMARY produced
  </acceptance_criteria>
  <done>Plan 05 content derived from audit. Ready for real execution.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-05-01 | Tampering | Middleware chain reordering (Pitfall 4) | mitigate | Explicit forbidden-target list in Task 1 action |
| T-98-05-02 | Tampering | Public action type rename | mitigate | Forbidden-target list |
| T-98-05-03 | Tampering | Circular import break hides real semantic dependency | mitigate | Per-module characterization tests (from Plan 02) pass before and after |

</threat_model>

<verification>
- All tests + invariants + smoke tests green after each commit
- `npx madge --circular src/` count decreases or documented as unchanged (god-modules-by-design case)
- `npm run build` succeeds
</verification>

<success_criteria>
- Every finding in AUDIT.md sections 3/7 is either refactored, documented as load-bearing, or explicitly deferred
- No forbidden-target modification
- All invariants preserved
</success_criteria>

<output>
`.planning/phases/98-codebase-audit-and-refactor/98-05-SUMMARY.md` listing dependency refactors, circular imports broken, god modules decomposed or documented.
</output>
