---
phase: 98-codebase-audit-and-refactor
plan: 04
type: execute
wave: 2
depends_on: ["98-01", "98-02", "98-03"]
files_modified:
  - "(Consolidation targets identified in 98-AUDIT.md Section 2 — concrete paths determined after audit review)"
autonomous: false
requirements:
  - HEALTH-02
  - HEALTH-03
  - HEALTH-05
  - HEALTH-07
  - HEALTH-11
  - HEALTH-14
tags:
  - consolidation
  - duplicate-logic
  - refactor
  - stub-plan
  - gated-on-user-signoff

must_haves:
  truths:
    - "Every consolidation is backed by characterization tests (from Plan 02) that prove behaviour is preserved"
    - "Every consolidation is an atomic commit with AUDIT.md section 2 row reference"
    - "All 2535 tests + feature invariants + smoke tests still pass after every commit"
    - "No consolidation reduces semantic functionality — deterministic vs random, seeded vs unseeded, etc. stay distinct (Pitfall 3)"
  artifacts:
    - path: "src/ — consolidated helpers"
      provides: "Shared utilities replace per-site duplicates; call sites updated"
  key_links:
    - from: "Consolidation commits"
      to: "98-AUDIT.md Section 2"
      via: "commit body references AUDIT.md finding row"
      pattern: "AUDIT\\.md section 2"
---

<objective>
**Stub plan — content populated AFTER user reviews 98-AUDIT.md.**

Purpose: Consolidate duplicate logic surfaced by the audit (Section 2 — High Priority Consolidations). Concrete targets cannot be specified until the audit exists.

**Why this is a stub:** The planner has no authority to pre-specify which modules to consolidate before seeing the audit findings. Attempting to do so would violate the "audit first, act second" guardrail from CONTEXT.md. The specific task content derives directly from AUDIT.md section 2 after the Plan 01 checkpoint is approved.

**Output after content is populated:** Per-consolidation atomic commit refactors, each preceded by characterization tests (already written in Plan 02 for elevated-risk directories).
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md
@.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md
@.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
@.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
@.planning/phases/98-codebase-audit-and-refactor/98-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POPULATE PLACEHOLDER — Derive consolidation tasks from AUDIT.md Section 2</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-04-PLAN.md (this file, re-populated)</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md (Section 2 — High Priority Consolidations)
    - .planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md
    - .planning/phases/98-codebase-audit-and-refactor/98-02-SUMMARY.md (which test coverage landed)
  </read_first>
  <action>
    This task is a **plan-population placeholder.** When Plan 04 begins execution, the first task is to PRODUCE A USER SIGN-OFF LEDGER, then rewrite this PLAN.md with concrete tasks derived from AUDIT.md section 2 findings AFTER user approval.

    ### FORBIDDEN TARGETS (consolidation cannot touch these — mirrors Plan 05)

    These paths are protected because consolidation here would silently remove essential code that the user has explicitly chosen to preserve:

    - `scripts/ralph/**` — Ralph autonomous agent pipeline (user-critical; pause/resume bracket the phase)
    - `src/data/ink/**` — ink dialogue story files (Arabic narrative content)
    - `src/data/**/*.json` containing Arabic vocabulary, grammar, or quest text (sacred content)
    - `src/store/slices/**/migrations/**` — redux-persist migrations (state-shape evolution; removal breaks saves)
    - `src/game/systems/fsrs/**` — FSRS algorithm implementation (deterministic learning-science core)
    - `src/game/systems/battle/BattleStateMachine.js` (state machine — FSM semantics must not be "simplified")
    - `src/game/systems/IndexedDBAdapter*` / migration layer (save-compat risk)
    - `server/src/middleware/**` — security middleware chain (order-sensitive)
    - `src/game/events/eventBusTypes.js` + EventBus constants (74 cross-file string references)
    - `public/assets/manifests/**` (Phaser texture-key contracts)
    - Any file where the "duplicate" is actually a domain-specific wrapper (e.g., two logging helpers where one has retry logic)

    Consolidation that would touch any forbidden path is ESCALATED to user via a new row in `98-CONSOLIDATIONS.md` and flagged `[ ] Forbidden Override Requested` — user must explicitly tick to override.

    ### PROCEDURE

    **Step 1: Produce `98-CONSOLIDATIONS.md` sign-off ledger (mirrors DELETIONS.md pattern).**

    For each duplicate-logic row in AUDIT.md Section 2, emit one ledger row:

    ```markdown
    ## Consolidation C-01

    - [ ] Approved by user
    - **Duplicate A:** `src/utils/formatArabicDate.js` (exports `formatDate`)
    - **Duplicate B:** `src/game/helpers/dateFormatter.js` (exports `formatArabicDate`)
    - **Proposed winner:** `src/utils/formatArabicDate.js` (older, more tests)
    - **Loser (to remove):** `src/game/helpers/dateFormatter.js`
    - **Call sites to update:** 14 files (listed below)
    - **Characterization tests:** Plan 02 Task N covered `src/utils/` with 9 tests; `src/game/helpers/` NOT covered → MUST add tests BEFORE consolidation
    - **Semantic divergence check:** Identical signatures, identical output on 20 sample inputs. SAFE to consolidate.
    - **Forbidden-target violation:** NONE
    - **AUDIT.md reference:** Section 2, row 3
    ```

    Write `98-CONSOLIDATIONS.md` to the phase directory. User reviews and ticks `[x] Approved by user` on rows they accept. Unticked rows are NOT processed.

    **Step 2: HUMAN-VERIFY CHECKPOINT** — STOP execution. Display:

    ```
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     GSD ► CHECKPOINT: 98-CONSOLIDATIONS.md SIGN-OFF REQUIRED
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    Review .planning/phases/98-codebase-audit-and-refactor/98-CONSOLIDATIONS.md
    Tick [x] Approved by user on rows you accept.
    Type "consolidations approved, proceed" when ready.
    Type "abort" to stop Plan 04 and leave duplicate code untouched.
    ```

    Wait for user response. Only `[x] Approved by user` rows proceed. User can also edit the Proposed winner/loser assignment before approving.

    **Step 3: Semantic divergence + coverage check (per approved row).**

    For each `[x] Approved by user` row:
    - Verify no forbidden-target path appears in "Winner" or "Loser" (re-check against the forbidden list above). If found and `[ ] Forbidden Override Requested` is NOT ticked, SKIP row and flag in SUMMARY.md.
    - Run characterization tests from Plan 02. If loser's directory is uncovered, ADD tests FIRST as a prefixed task in the populated plan.
    - **Pitfall 3 check:** Re-verify semantic identity. Run 20 sample inputs through both implementations. If any diverges, demote row to "semantic divergence documented; renamed for clarity only, no removal" in SUMMARY.md.

    **Step 4: Re-emit THIS file (`98-04-PLAN.md`) with concrete tasks.**

    - Frontmatter `files_modified` updated to list actual files
    - One `<task type="auto">` block per approved consolidation, each with:
      - Concrete `<files>`
      - `<read_first>` listing affected source + test files + `98-CONSOLIDATIONS.md`
      - `<action>` with exact before/after for the shared helper + call site updates
      - `<verify>` running the characterization tests from Plan 02 + phase98-invariants.test.js + smoke tests
      - Acceptance criteria referencing CONSOLIDATIONS.md row ID (C-01, C-02, etc.)
    - Task 1 (this placeholder) is REMOVED from the re-emitted file

    **Step 5: MANDATORY plan-checker re-verification on populated file.**

    BEFORE calling `/gsd-execute-phase 98 --plan 04`, run:
    ```
    /gsd-plan-phase 98 --check-only --plan 04
    ```
    This routes the newly-populated PLAN.md through `gsd-plan-checker` exactly as this initial version went through. If the checker returns any BLOCKER, STOP and surface to user with the blocker list. Do not execute a populated plan that bypassed verification.

    **Step 6: Execute.**

    Only after Step 5 passes, run `/gsd-execute-phase 98 --plan 04`.

    **If AUDIT.md section 2 is empty (no consolidations found):** Plan 04 becomes a no-op. Write a SUMMARY.md stating "No consolidation opportunities. Proceeding to Plan 05." Skip Steps 1-5.

    **User can also manually rewrite this file** after reviewing AUDIT.md; Step 5 re-verification still applies.
  </action>
  <verify>
    <automated>grep -q "AUDIT.md Section 2" .planning/phases/98-codebase-audit-and-refactor/98-04-PLAN.md || grep -q "No consolidation opportunities" .planning/phases/98-codebase-audit-and-refactor/98-04-SUMMARY.md 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - This PLAN.md is rewritten with concrete tasks based on AUDIT.md section 2
    - Each new task has concrete files, action, verify, acceptance criteria
    - If audit has no findings, Plan 04 is explicitly marked no-op
  </acceptance_criteria>
  <done>Plan 04 content derived from audit. Ready for executor to run the real consolidations.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04-01 | Tampering | Consolidation of non-identical algorithms (Pitfall 3) | mitigate | Per-module characterization tests from Plan 02 MUST pass before and after consolidation |
| T-98-04-02 | Tampering | Call-site update misses one location | mitigate | 10-file commit cap + full test run + smoke tests |

</threat_model>

<verification>
- Every concrete task (after Task 1 populates) runs test/lint/build green
- Feature invariants + smoke tests pass after every commit
- Each commit body references AUDIT.md section 2 finding
</verification>

<success_criteria>
- Every duplicate-logic finding in AUDIT.md section 2 is either: consolidated (with behaviour preservation proven), left-as-is (with semantic divergence documented), or explicitly deferred
- No regression in tests, invariants, or smoke tests
</success_criteria>

<output>
`.planning/phases/98-codebase-audit-and-refactor/98-04-SUMMARY.md` listing consolidations performed vs deferred, commit SHAs, LOC delta.
</output>
