---
phase: 98-codebase-audit-and-refactor
plan: 02
type: tdd
wave: 1
depends_on: ["98-01"]
files_modified:
  - src/utils/fsrs/__tests__/
  - src/store/__tests__/migrations.test.js
  - src/store/slices/__tests__/battleSlice.test.js
  - src/services/__tests__/indexedDBService.test.js
  - "(additional test files as determined by the coverage heatmap — see <behavior>)"
autonomous: true
requirements:
  - HEALTH-04
  - HEALTH-05
  - HEALTH-07
tags:
  - test-coverage
  - tdd
  - pre-refactor

must_haves:
  truths:
    - "Every directory flagged ❌ in 98-01-COVERAGE-HEATMAP.md has tests added until it clears the 50% (global) or 70% (elevated-risk) threshold"
    - "The elevated-risk list (FSRS, redux-persist migrations, battle state machine, IndexedDB migration layer) reaches ≥ 70% statements AND ≥ 70% branches"
    - "All existing 2535 tests continue to pass"
    - "Test count after Plan 02 is > baseline testCount from 98-01-BASELINE.json (additive only)"
    - "No production code under src/ is modified — this plan ADDS tests only, does not refactor"
    - "After Plan 02 merges, Plan 03 (approved dead-code removal) and Plan 04 (duplicate consolidation) have the coverage they need to prove behaviour preservation"
  artifacts:
    - path: "src/utils/fsrs/__tests__/ (or equivalent path)"
      provides: "Characterization tests pinning current FSRS algorithm behaviour — same input → same output"
    - path: "src/store/__tests__/migrations.test.js"
      provides: "redux-persist migration chain tests — old save (v0..v11) → current (v12+) round-trip"
    - path: "src/store/slices/__tests__/battleSlice.test.js (or expanded existing file)"
      provides: "Battle state machine transitions pinned — start → player turn → enemy turn → victory"
    - path: "src/services/__tests__/indexedDBService.test.js"
      provides: "IndexedDB service characterization tests using fake-indexeddb (already installed)"
  key_links:
    - from: "Plan 02 tests"
      to: "98-01-COVERAGE-HEATMAP.md"
      via: "one test file per ❌-flagged directory until threshold clears"
      pattern: "coverage.*[0-9]+%"
    - from: "Plan 02 tests"
      to: "Plans 03/04/05 safety"
      via: "Plans 03+ cannot touch uncovered systems; Plan 02 removes that blocker"
      pattern: "depends_on.*98-02"
---

<objective>
**Wave-0 test coverage uplift.** Every system flagged as coverage-deficient in 98-01-COVERAGE-HEATMAP.md gets tests added BEFORE any refactor plan (03+) touches it.

**Purpose:** CONTEXT.md hard guardrail #3 — "Test coverage before any refactor." A refactor without green tests cannot prove behaviour preservation. This plan produces the green tests that Plans 03-07 depend on.

**Output:** Characterization tests for FSRS algorithm, redux-persist migrations, battle state machine, IndexedDB migration layer, and any additional ❌-flagged directories.

**Concrete content cannot be fully specified until 98-01-COVERAGE-HEATMAP.md exists** (Plan 01 output). This plan's tasks define the PROCESS; specific test file names and assertions derive from the heatmap.

**This is a TDD plan (`type: tdd`):** Tests are written first and MUST pass against current behaviour before any refactor plan proceeds. The TDD cycle here is characterization-test style (RED = write test that captures current output → GREEN = verify current code produces that output → no REFACTOR needed in this plan).
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md
@.planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md
@.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
@.planning/phases/98-codebase-audit-and-refactor/98-VALIDATION.md
@vitest.config.js
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read coverage heatmap, enumerate ❌-flagged directories, plan test file additions</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-02-TEST-PLAN.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md (from Plan 01)
    - .planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md section: Test Coverage Measurement
    - vitest.config.js (current thresholds and exclude patterns)
  </read_first>
  <behavior>
    - For each ❌-flagged directory in the heatmap, produce a subsection in 98-02-TEST-PLAN.md listing:
      - Directory path
      - Current coverage
      - Target coverage (50% or 70% depending on elevated-risk status)
      - Files in the directory that need tests
      - Test filename to create (following existing convention: `<module>.test.js` sibling OR `__tests__/<module>.test.js`)
      - Rough test count estimate (3-10 characterization tests per non-trivial module)
    - Total test file count from this plan.
    - Estimated new test count (adds to baseline 2535).
  </behavior>
  <action>
    Parse 98-01-COVERAGE-HEATMAP.md. For every row marked ❌ or ⚠ below its threshold:
    1. List the directory.
    2. Read each file in the directory to understand its surface area (functions exported, components rendered).
    3. Decide test strategy per file type (using RESEARCH.md Refactor Patterns → each row's "safest refactor pattern"):
       - Pure utility: characterization tests (same input → same output)
       - Redux slice: reducer pinning tests (dispatch action → assert state delta)
       - Middleware: middleware unit test (next(action) → assert side effect)
       - React presentational: React Testing Library snapshot
       - Phaser system: extract pure helpers, test helpers; lifecycle untouched
       - Service: mock dependencies, assert resolved values
    4. Write `98-02-TEST-PLAN.md` with one subsection per ❌ directory. This file is a plan-within-a-plan — it drives Tasks 2-N.
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-02-TEST-PLAN.md && [ "$(wc -l < .planning/phases/98-codebase-audit-and-refactor/98-02-TEST-PLAN.md)" -ge "30" ]</automated>
  </verify>
  <acceptance_criteria>
    - 98-02-TEST-PLAN.md enumerates one subsection per ❌/⚠ row from the heatmap
    - Elevated-risk directories (FSRS, migrations, battleSlice, indexedDBService) are marked with 70% threshold
    - Estimated total new test count is recorded
  </acceptance_criteria>
  <done>Sub-plan enumerates every coverage gap to close in Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write characterization tests for elevated-risk systems (FSRS, migrations, battleSlice, IndexedDB)</name>
  <files>src/utils/fsrs/__tests__/ or src/utils/fsrs/*.test.js, src/store/__tests__/migrations.test.js, src/store/slices/__tests__/battleSlice.test.js, src/services/__tests__/indexedDBService.test.js</files>
  <read_first>
    - 98-02-TEST-PLAN.md (Task 1 output)
    - src/utils/fsrs/*.js (FSRS algorithm files — see RESEARCH.md Section Preserve)
    - src/store/migrations.js (if exists) — redux-persist migration chain
    - src/store/slices/battleSlice.js — battle state machine
    - src/services/*.js matching indexedDBService* — hybrid persistence layer
    - package.json — confirm fake-indexeddb, @testing-library/react, vitest installed
    - Existing sibling `.test.js` files in these directories for convention reference
  </read_first>
  <behavior>
    **Elevated-risk systems MUST reach ≥ 70% statements AND ≥ 70% branches.**

    **FSRS tests (characterization, not algorithm correctness):**
      - Given card state {s, d, r, reps, lapses, due}, review grade G ∈ {1,2,3,4}, → assert current output of `fsrs.review(card, grade)` matches golden value
      - Test ≥ 10 representative (state, grade) pairs
      - DO NOT test "is FSRS mathematically correct" — it's a third-party algorithm; test "we invoke it with the same shape we have today"

    **Migrations tests:**
      - For each known prior version v0..v11, construct a synthetic pre-migration state object
      - Run the migration function for that version
      - Assert the result shape matches expected current version
      - If migrations.js is empty or not present, this task may reduce to a single assertion: "no migrations defined" — confirm with AUDIT.md finding

    **battleSlice tests:**
      - Dispatch START_BATTLE → assert phase === 'player_turn'
      - Dispatch CAST_SPELL → assert enemy HP decremented
      - Dispatch END_TURN → assert phase === 'enemy_turn'
      - Dispatch VICTORY conditions → assert rewards dispatched
      - Cover ≥ 10 transitions

    **IndexedDB service tests (using fake-indexeddb already installed):**
      - Given a save payload, `put` → `get` → assert round-trip preserves shape
      - Version bump path: `put` at v11 → migrate → `get` at v12 → assert no data loss
      - Cover ≥ 8 cases
  </behavior>
  <action>
    For each elevated-risk system listed in 98-02-TEST-PLAN.md:
    1. Create or extend the sibling/__tests__ test file per convention.
    2. Import the module under test.
    3. Write characterization tests: capture current output, assert equality.
    4. Run `npx vitest run <test-file> --coverage.include=<module>` and verify coverage for that module reaches ≥ 70% statements AND ≥ 70% branches. If not, add more tests until threshold clears.
    5. Run full suite: `npm run test:run` must remain green. Test count must have INCREASED.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -5 | grep -E "Tests.*passed" && npm run test:coverage 2>&1 | tail -50 | grep -E "statements|branches"</automated>
  </verify>
  <acceptance_criteria>
    - All four elevated-risk test files exist (or skipped if module absent — documented in 98-02-TEST-PLAN.md)
    - `npm run test:run` passes, test count > baseline
    - `npm run test:coverage` reports ≥ 70% statements AND ≥ 70% branches for each elevated-risk directory
    - No production code under src/ modified (only test files added)
  </acceptance_criteria>
  <done>Elevated-risk systems covered. Plans 03+ can refactor them safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write characterization tests for remaining ❌-flagged directories (50% threshold)</name>
  <files>(Test files per 98-02-TEST-PLAN.md — typical paths: src/components/Battle/__tests__/, src/components/<Overlay>/*.test.jsx, src/game/systems/*.test.js)</files>
  <read_first>
    - 98-02-TEST-PLAN.md (Task 1 output) — the list of remaining ❌ rows
    - Source files for each directory
    - Existing test files in adjacent directories for convention reference
  </read_first>
  <behavior>
    **Remaining ❌ directories reach ≥ 50% statements AND ≥ 50% branches.**

    Lower bar than Task 2 because these are not single-point-of-failure systems. Target: enough tests that any refactor within the directory produces a failing test when behaviour changes unintentionally.

    Per-directory test count: 3-10 tests depending on surface area.

    Prioritise:
      - Pure-function utilities (easiest and cheapest)
      - Reducers/slices (per-action-type tests)
      - Presentational components (snapshot tests with RTL)
      - Middleware (next(action) unit tests)
      - Phaser systems — extract helpers, test helpers (leave scene lifecycle alone)

    Skip for Plan 02:
      - Phaser scenes (out of scope; Plan 97 handles)
      - Ink dialogue files (out of scope)
      - Arabic data JSON (out of scope)
  </behavior>
  <action>
    Work through 98-02-TEST-PLAN.md remaining rows. For each:
    1. Create test file following existing convention.
    2. Write 3-10 characterization tests.
    3. Re-run `npm run test:coverage`; confirm directory reaches ≥ 50% statements AND branches.
    4. Update 98-02-TEST-PLAN.md marking row as ✓ done with post-test coverage %.

    **If a directory cannot reach 50% without extensive new test infrastructure:**
    - STOP; record in 98-02-TEST-PLAN.md "Directory X blocked — requires Wave-0 infrastructure task"
    - Continue with remaining directories
    - Raise blocker in SUMMARY.md

    **Test-count invariant monitor:** After each test file added, run `npm run test:run | grep "Tests.*passed"`. Count must monotonically increase. A drop means a regression — stop and investigate.
  </action>
  <verify>
    <automated>npm run test:run && npm run test:coverage 2>&1 | grep -E "All files.*[0-9]+%"</automated>
  </verify>
  <acceptance_criteria>
    - Every ❌ row from 98-02-TEST-PLAN.md is either: (a) marked ✓ with coverage ≥ 50%, or (b) marked blocked with reason
    - Global `npm run test:run` passes
    - Global coverage for src/ has not decreased vs baseline (coverage/coverage-summary.json)
    - No production code modified
  </acceptance_criteria>
  <done>Coverage heatmap bottlenecks removed. Plans 03+ can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → production code | Tests import from src/; must not modify src/ while importing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-02-01 | Tampering | Test accidentally mutates shared module state | mitigate | Every test uses `beforeEach` reset; vitest isolate=true (default) |
| T-98-02-02 | Tampering | Characterization test pins incorrect behaviour as "expected" | accept | Pre-existing bugs become visible later; if found, capture in AUDIT.md, not in Plan 02 |

</threat_model>

<verification>
- `npm run test:run` passes at the end of each task
- `npm run test:coverage` shows threshold clearance per ❌-flagged directory
- `git diff --stat src/` shows ONLY test files added (no production code delta)
- Test count strictly > 98-01-BASELINE.json `testCount`
</verification>

<success_criteria>
1. 98-02-TEST-PLAN.md enumerates every coverage gap from heatmap.
2. Elevated-risk directories reach ≥ 70% statements + branches.
3. Remaining ❌ directories reach ≥ 50% statements + branches.
4. All 2535 existing tests still pass.
5. Test count is strictly greater than baseline.
6. No production code modified in Plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/98-codebase-audit-and-refactor/98-02-SUMMARY.md` listing:
- Directories lifted above threshold
- New test file count
- Updated test count (new baseline)
- Any blocked directories with reason
- Unlocks: Plans 03-05 now safe to proceed
</output>
