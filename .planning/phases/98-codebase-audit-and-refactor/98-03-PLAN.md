---
phase: 98-codebase-audit-and-refactor
plan: 03
type: execute
wave: 1
depends_on: ["98-01", "98-02"]
files_modified:
  - "(Files listed as Approved in 98-DELETIONS.md — concrete paths determined at execution time)"
  - package.json (if npm dep removals approved)
  - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md (append to execution log)
autonomous: true
requirements:
  - HEALTH-02
  - HEALTH-03
  - HEALTH-05
  - HEALTH-07
  - HEALTH-11
  - HEALTH-14
tags:
  - deletions
  - cleanup
  - approved-only

must_haves:
  truths:
    - "Every file/export/npm dep deleted has a corresponding `- [x] Approved` line in 98-DELETIONS.md"
    - "Every deletion is an atomic commit (one row = one commit = one revert target)"
    - "Every deletion commit message includes `DELETIONS.md row: N` and `User approval: <date>`"
    - "All 2535 existing tests still pass after each deletion commit"
    - "Feature-count invariants from phase98-invariants.test.js still pass after each commit"
    - "15 Playwright smoke tests still pass after the last commit"
    - "Zero silent deletions — git log invariant proves each `delete mode` maps to a ticked DELETIONS.md row"
    - "Ralph pipeline remains paused"
  artifacts:
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md"
      provides: "Execution log section appended with per-commit SHAs and approval dates for traceability"
    - path: "Git history"
      provides: "One atomic commit per approved DELETIONS.md row, each with DELETIONS.md reference in commit body"
  key_links:
    - from: "git log"
      to: "98-DELETIONS.md"
      via: "commit body `DELETIONS.md row: N` reference"
      pattern: "DELETIONS\\.md row: [0-9]+"
    - from: "Plan 03 commits"
      to: "HEALTH-02 + HEALTH-03 verification"
      via: "post-plan invariant check: every `delete mode` has DELETIONS.md reference"
      pattern: "delete mode"
---

<objective>
**Execute approved deletions from 98-DELETIONS.md.** Plan 03 is entirely driven by what the user ticks in DELETIONS.md during the Plan 01 checkpoint. Nothing happens here that wasn't explicitly approved.

**Purpose:** Translate user-approved deletion rows into atomic, reversible git commits with full audit trail.

**Output:** Git history with one commit per approved row, each commit:
- Touches only the files associated with that row
- Includes `DELETIONS.md row: N` and `User approval: <date>` in commit body
- Runs `npm run test:run && npm run lint && npm run build` green before commit

**If DELETIONS.md has zero `[x] Approved` rows:** Plan 03 is a no-op — run validation (tests green, feature invariants pass, smoke tests pass), append a "no approved deletions" entry to the execution log, and return success.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md
@.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md
@.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
@.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
@.planning/phases/98-codebase-audit-and-refactor/98-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse DELETIONS.md, extract approved rows, group into commit batches</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-03-COMMIT-PLAN.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md (current state with user ticks)
    - .planning/phases/98-codebase-audit-and-refactor/98-RESEARCH.md section: Sign-Off Mechanism
  </read_first>
  <action>
    1. Read `98-DELETIONS.md`.
    2. Extract every row where `- [x] Approved` is ticked (not `[ ]`).
    3. Group by section:
       - Section 1 (full file deletion) → one commit per row
       - Section 2 (unused export removal) → one commit per row (NOT batched — atomic reverts)
       - Section 3 (npm dep removal) → one commit per package
       - Section 4 (phase stub archive) → handled by Plan 06, skip here
    4. Write `98-03-COMMIT-PLAN.md` listing each approved row with:
       - Row N
       - Target file / export / package
       - Planned commit message (template per RESEARCH.md Example 6)
       - Pre-commit validation checklist
    5. If zero rows approved, write "No deletions approved. Plan 03 is a no-op." and terminate.
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-03-COMMIT-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - Every `[x] Approved` row in DELETIONS.md appears in 98-03-COMMIT-PLAN.md
    - Every Section 4 row is EXCLUDED (handled by Plan 06)
    - Each row has a planned commit message referencing the DELETIONS.md row number
  </acceptance_criteria>
  <done>Commit plan enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: Execute each approved deletion as an atomic commit with full validation</name>
  <files>(Per DELETIONS.md approved rows)</files>
  <read_first>
    - 98-03-COMMIT-PLAN.md (Task 1 output)
    - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
    - 98-01-BASELINE.json (test count, bundle bytes invariants)
  </read_first>
  <action>
    For each row in 98-03-COMMIT-PLAN.md, execute this atomic cycle:

    1. **Pre-check:** `npm run test:run` — must be green. If not, STOP and report.
    2. **Delete:** Remove the file / export / package per row type.
       - File: `git rm <path>`
       - Export: edit the file to remove the `export` line and any unused supporting code
       - npm package: `npm uninstall <package>`
    3. **Validate:**
       - `npm run test:run` — must pass (test count ≥ baseline)
       - `npm run lint` — must pass
       - `npm run build` — must succeed
       - `npx vitest run src/test/phase98-invariants.test.js` — feature counts must hold
       - Bundle size check: `du -sb dist | awk '{print $1}'` — must be ≤ baseline bundleBytes + 0 bytes (never grows)
    4. **Commit:**
       ```
       chore(cleanup): <short description> (DELETIONS.md #<N>)

       <longer description>

       Evidence:
       - Knip flagged: <line>
       - Zero grep hits in <scope>
       - No EventBus / manifest / migration reference (Layer 2 passed)

       DELETIONS.md row: <N>
       User approval: <YYYY-MM-DD>

       Refs: HEALTH-02, HEALTH-03, HEALTH-11
       ```
    5. **Rollback on failure:** If any validation fails, `git reset --hard HEAD` (the row stays unticked for retry; log the failure in 98-DELETIONS.md execution log).
    6. **Append to execution log:** In 98-DELETIONS.md under `## Audit trail → ### Execution log`, add: `- <date>: Row <N> approved, executed in commit <SHA>`.

    **Per-commit invariants (enforce all):**
    - Touches ≤ 10 files
    - Test count strictly ≥ baseline
    - Lint clean
    - Build succeeds
    - Bundle bytes ≤ baseline (for deletions this should trivially hold)

    **At the end of Task 2:** Run the full smoke suite: `npx playwright test tests/e2e/smoke/phase98-smoke.spec.js`. All 15 must pass. If any fail, revert the most recent commits in reverse order until smoke is green, mark the problem rows with "revert" in the execution log, and open an AUDIT.md addendum.
  </action>
  <verify>
    <automated>npm run test:run && npm run lint && npm run build && npx vitest run src/test/phase98-invariants.test.js && npx playwright test tests/e2e/smoke/phase98-smoke.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Every approved row in DELETIONS.md has a corresponding commit referenced in the execution log
    - `git log --grep="DELETIONS.md row" --since="<plan start date>"` returns one commit per approved row
    - `git log -n 20 --diff-filter=D --name-only` shows no deleted files without DELETIONS.md reference in commit body (HEALTH-03 invariant)
    - All 2535 existing tests still pass
    - Phase 98 feature invariants pass
    - All 15 Playwright smoke tests pass
    - Bundle size ≤ baseline
  </acceptance_criteria>
  <done>All approved deletions executed atomically. Execution log updated.</done>
</task>

<task type="auto">
  <name>Task 3: Run post-plan invariant sweep and append summary to 98-DELETIONS.md</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md</files>
  <read_first>
    - 98-DELETIONS.md (execution log section)
    - 98-01-BASELINE.json
  </read_first>
  <action>
    1. **Measure deltas:**
       - `find src server scripts -type f \( -name '*.js' -o -name '*.jsx' \) ! -name '*.test.*' | xargs wc -l | tail -1 | awk '{print $1}'` — current LOC
       - `du -sb dist | awk '{print $1}'` — current bundle bytes
       - `npm run test:run | tail -5 | grep -oE "[0-9]+ passed" | head -1` — current test count
    2. **Compute:**
       - LOC delta (negative = reduction)
       - Bundle bytes delta
       - Test count delta (must be ≥ 0)
    3. **Append to DELETIONS.md execution log:**
       ```markdown
       ### Plan 03 Post-Execution Summary

       | Metric | Baseline | Post-Plan 03 | Delta |
       |---|---|---|---|
       | LOC | <baseline.loc> | <current> | <delta> |
       | Bundle bytes | <baseline.bundleBytes> | <current> | <delta> |
       | Test count | <baseline.testCount> | <current> | <delta> |
       | Rows approved | <N> | executed | <K> |
       | Rows reverted | — | — | <R> |

       All HEALTH invariants preserved: ✅
       ```
    4. **HEALTH-03 git invariant check:** Run:
       ```bash
       git log --since="<plan start>" --pretty=format:"%H %s%n%b" | awk '/^[a-f0-9]{40}/{sha=$1; next} /delete mode/ && !/DELETIONS.md row/{print "VIOLATION: "sha; bad=1} END{exit bad}'
       ```
       Must report no violations.
  </action>
  <verify>
    <automated>grep -q "Plan 03 Post-Execution Summary" .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md && git log --since="24 hours ago" --pretty=format:"%s" | grep -E "DELETIONS.md" || echo "no deletions this plan"</automated>
  </verify>
  <acceptance_criteria>
    - 98-DELETIONS.md has Post-Execution Summary appended
    - LOC delta is ≤ 0 (never grows)
    - Bundle bytes delta is ≤ 0
    - Test count delta is ≥ 0
    - No `delete mode` commits without DELETIONS.md reference
  </acceptance_criteria>
  <done>Plan 03 complete. Audit trail persisted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DELETIONS.md tick → irreversible git rm | User tick authorises destructive action; single point of failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-03-01 | Tampering | Commit with `delete mode` but no DELETIONS.md reference | mitigate | Per-commit validation grep + Task 3 invariant sweep |
| T-98-03-02 | Denial of Service | Batch commit accidentally deletes unrelated code | mitigate | Atomic single-row commits; 10-file cap; test/lint/build gate |
| T-98-03-03 | Repudiation | User changes mind after commit | accept | Commit is atomic + reversible; `git revert` is 10 seconds |

</threat_model>

<verification>
- Every approved row has exactly one corresponding commit
- Zero silent deletions (HEALTH-03)
- `npm run test:run && npm run lint && npm run build` green after every commit
- Phase 98 invariants test green
- All 15 smoke tests green
- Execution log in DELETIONS.md updated
</verification>

<success_criteria>
1. Every approved DELETIONS.md row executed as atomic commit
2. Zero approved rows skipped without documented reason
3. Zero unapproved deletions
4. All baseline invariants hold (test count, bundle size, feature counts)
5. Smoke tests still green
6. Ralph pipeline remains paused (prd.json still renamed)
</success_criteria>

<output>
After completion, create `.planning/phases/98-codebase-audit-and-refactor/98-03-SUMMARY.md` listing:
- Rows approved vs executed vs reverted
- LOC / bundle / test count deltas
- Links to each commit SHA
- Invariant check results
</output>
