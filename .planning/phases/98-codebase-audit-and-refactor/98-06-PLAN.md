---
phase: 98-codebase-audit-and-refactor
plan: 06
type: execute
wave: 2
depends_on: ["98-01", "98-03"]
files_modified:
  - .planning/phases/86-*/
  - .planning/phases/87-*/
  - .planning/phases/88-*/
  - .planning/phases/89-*/
  - .planning/phases/90-*/
  - .planning/phases/91-*/
  - .planning/phases/92-*/
  - .planning/phases/93-*/
  - .planning/phases/94-*/
  - .planning/phases/95-*/
  - .planning/phases/96-*/
  - .planning/archive/phases-86-96/
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
autonomous: false
requirements:
  - HEALTH-09
  - HEALTH-11
tags:
  - planning-drift
  - archive
  - phase-stubs

must_haves:
  truths:
    - "Every phase 86-96 has been processed per its user-approved disposition (keep / archive / delete) from 98-DELETIONS.md Section 4"
    - "Archived phases are moved to .planning/archive/phases-86-96/<phase-slug>/ with a README.md explaining the archive reason"
    - "ROADMAP.md row for phase 86-96 reflects the disposition (kept + milestone-assigned, archived, or deleted)"
    - "No phase stub is deleted without a ticked `[x] delete` in DELETIONS.md Section 4"
    - "Planning-doc drift findings from AUDIT.md Section 9 are applied (STATE.md / ROADMAP.md claim corrections)"
    - "All 2535 tests still pass (this plan only touches .planning/ — no source code)"
  artifacts:
    - path: ".planning/archive/phases-86-96/"
      provides: "Archive directory for stubs user approved for archival; each subdirectory contains original stub plus README"
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap row for 86-96 reflecting disposition"
    - path: ".planning/STATE.md"
      provides: "Updated if drift findings applied"
  key_links:
    - from: ".planning/archive/phases-86-96/<phase>/README.md"
      to: "98-DELETIONS.md row for that phase"
      via: "README references DELETIONS.md row + approval date"
      pattern: "DELETIONS\\.md Section 4 row"
---

<objective>
**Planning-doc drift reconciliation.** Apply user dispositions for phase stubs 86-96 and apply planning-doc drift corrections from AUDIT.md Section 9.

Purpose: The `.planning/` directory must match reality at phase end. Stubs get kept-and-milestone-assigned, archived, or deleted per user tick. STATE.md and ROADMAP.md claims that don't match shipped reality get corrected.

**This plan touches ONLY `.planning/`** — no source code. No tests are affected.

**Hard constraint:** Phase stubs 86-96 can only be DELETED if the user ticked `[x] delete` in DELETIONS.md Section 4. Default disposition is `archive` (CONTEXT.md + orchestrator confirmation).
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md
@.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse phase stub dispositions from DELETIONS.md Section 4 and AUDIT.md Section 8</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-06-DISPOSITION-PLAN.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md (Section 4 — phase stub dispositions, ticked by user)
    - .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md (Section 8 — recommendations + evidence)
    - .planning/ROADMAP.md (current phase 86-96 rows)
  </read_first>
  <action>
    For each of phases 86, 87, 88, 89, 90, 91, 92, 93, 94, 95, 96:
    1. Find the Section 4 row in DELETIONS.md for that phase
    2. Identify which of `[x] keep`, `[x] archive`, `[x] delete` is ticked (exactly one must be)
    3. If none ticked, default to `archive` (CONTEXT.md default)
    4. If `keep` ticked: record milestone assignment from AUDIT.md Section 8 recommendation
    5. If `archive` ticked: record source path and archive destination
    6. If `delete` ticked: record source path for removal + git-log evidence from AUDIT.md

    Write `98-06-DISPOSITION-PLAN.md` with one row per phase:
    ```
    | Phase | Disposition | Source | Destination | Rationale | ROADMAP update |
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-06-DISPOSITION-PLAN.md && [ "$(grep -cE "^\| 8[6-9]\||^\| 9[0-6]\|" .planning/phases/98-codebase-audit-and-refactor/98-06-DISPOSITION-PLAN.md)" = "11" ]</automated>
  </verify>
  <acceptance_criteria>
    - 98-06-DISPOSITION-PLAN.md contains exactly 11 rows (one per phase 86-96)
    - Each row has an unambiguous disposition (keep/archive/delete)
    - Every `[x] delete` is backed by AUDIT.md evidence + user tick
  </acceptance_criteria>
  <done>Disposition roadmap clear.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 1.5: Final confirmation before destructive sweep on ticked `[x] delete` phases</name>
  <files>N/A (human-verify only)</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-06-DISPOSITION-PLAN.md (Task 1 output)
  </read_first>
  <action>
    **SKIP this task if no row in DISPOSITION-PLAN.md has disposition = `delete`.** (All-archive or all-keep sweeps do not require this gate.)

    **If one or more `delete` rows exist**, display a final confirmation block to the user:

    ```
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
     GSD ► CHECKPOINT: DESTRUCTIVE SWEEP CONFIRMATION
    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    Plan 06 is about to PERMANENTLY remove these phase-stub directories
    (approved in Plan 01 DELETIONS.md Section 4):

    [list phases with disposition = delete, one per line, with source path]

    Archives will proceed without this gate. Only the DELETE rows above require
    your final confirmation because approval was given 30-90 minutes earlier
    at the Plan 01 checkpoint — intent may have shifted.

    Type "confirm delete sweep" to proceed with git rm -r.
    Type "demote to archive" to convert all delete rows to archive instead
      (safe default — preserves directories under .planning/archive/phases-86-96/).
    Type "abort" to stop Plan 06 entirely; no changes are applied.
    ```

    Wait for user response before Task 2 executes the `git rm` branch of the disposition logic.

    If user demotes to archive: rewrite DISPOSITION-PLAN.md to change all `delete` rows to `archive` and proceed.
  </action>
  <verify>
    <automated>N/A — human-verify checkpoint</automated>
  </verify>
  <acceptance_criteria>
    - User has been shown the list of phases about to be destructively deleted
    - User has responded with one of: "confirm delete sweep" | "demote to archive" | "abort"
  </acceptance_criteria>
  <done>User has confirmed (or aborted / demoted) the destructive delete sweep.</done>
</task>

<task type="auto">
  <name>Task 2: Execute dispositions — archive or delete as approved, one commit per phase</name>
  <files>.planning/phases/86-* through 96-* (source), .planning/archive/phases-86-96/ (destination), .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-06-DISPOSITION-PLAN.md (Task 1)
    - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
    - .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md (Section 9 — planning-doc drift)
  </read_first>
  <action>
    For each phase in 98-06-DISPOSITION-PLAN.md, execute atomically (one commit per phase):

    **If `keep`:**
    1. Read AUDIT.md Section 8 milestone recommendation.
    2. Edit ROADMAP.md row for that phase to:
       - Add milestone assignment
       - Update status if applicable
    3. Commit: `docs(planning): assign phase <N> to milestone <Vxx> per user disposition (DELETIONS.md #<row>)`

    **If `archive`:**
    1. `mkdir -p .planning/archive/phases-86-96/<phase-dir>`
    2. `git mv .planning/phases/<phase-dir>/* .planning/archive/phases-86-96/<phase-dir>/`
    3. Write `.planning/archive/phases-86-96/<phase-dir>/ARCHIVE-README.md` explaining:
       - Reason for archive
       - Original location
       - DELETIONS.md row + user approval date
       - Cross-reference to AUDIT.md Section 8 row
    4. Update ROADMAP.md row for that phase to: `Phase <N>: <title> — ARCHIVED <date> (see .planning/archive/phases-86-96/<phase-dir>/)`
    5. Commit: `chore(planning): archive phase <N> stub per user disposition (DELETIONS.md #<row>)`

    **If `delete`:**
    1. `git rm -r .planning/phases/<phase-dir>/`
    2. Remove ROADMAP.md row for that phase (or mark as `— DELETED <date>`)
    3. Commit: `chore(planning): remove phase <N> stub per user approval (DELETIONS.md #<row>)` with DELETIONS.md reference in commit body

    **After all 11 processed, apply drift corrections from AUDIT.md Section 9:**
    - For each drift finding (STATE.md claim vs reality, etc.), update the .planning/ file
    - One commit: `docs(planning): reconcile .planning drift per AUDIT.md section 9`

    **Invariants (enforced after every commit):**
    - `npm run test:run` green (this plan doesn't modify source — tests MUST remain green)
    - `git diff --stat` shows only .planning/ changes (no src/, server/, scripts/ modifications)
  </action>
  <verify>
    <automated>npm run test:run && npx vitest run src/test/phase98-invariants.test.js && git diff --name-only HEAD~10..HEAD | grep -vE "^\.planning/" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Every phase 86-96 has been processed per disposition
    - Archived phases live under `.planning/archive/phases-86-96/<phase-dir>/` with ARCHIVE-README.md
    - ROADMAP.md no longer has stub rows pointing at archived/deleted phases (rows are updated or removed)
    - STATE.md drift corrections from AUDIT.md Section 9 applied
    - No source code modified
    - All tests still green
  </acceptance_criteria>
  <done>Planning-doc drift reconciled. .planning/ matches reality.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-06-01 | Tampering | Phase stub deleted without user approval | mitigate | Only `[x] delete` rows get removed; default is archive |
| T-98-06-02 | Repudiation | Archive destroys information silently | mitigate | ARCHIVE-README.md retains reason + cross-reference |
| T-98-06-03 | Tampering | ROADMAP.md update loses context | accept | Git history preserves prior version |

</threat_model>

<verification>
- Every phase 86-96 has been processed (kept/archived/deleted per user)
- `ls .planning/archive/phases-86-96/` shows exactly the phases user ticked as archive
- `git log --since=<plan start> -- .planning/phases/` shows one commit per phase
- `npm run test:run` still passes
</verification>

<success_criteria>
1. Every phase 86-96 processed per user tick
2. ARCHIVE-README.md present for every archived phase
3. ROADMAP.md + STATE.md reflect reality (no orphan phase rows)
4. Source code untouched
5. All tests still green
</success_criteria>

<output>
`.planning/phases/98-codebase-audit-and-refactor/98-06-SUMMARY.md` listing per-phase disposition + commit SHAs.
</output>
