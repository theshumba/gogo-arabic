---
phase: 98-codebase-audit-and-refactor
plan: 07
type: execute
wave: 3
depends_on: ["98-01", "98-02", "98-03", "98-04", "98-05", "98-06"]
files_modified:
  - .planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md
  - .planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - scripts/ralph/prd.json
  - .planning/milestones/v17.0-ROADMAP.md
  - vitest.config.js
autonomous: false
requirements:
  - HEALTH-04
  - HEALTH-05
  - HEALTH-06
  - HEALTH-07
  - HEALTH-12
  - HEALTH-13
  - HEALTH-14
tags:
  - phase-close
  - verification
  - ralph-resume

must_haves:
  truths:
    - "All 14 HEALTH-* requirements are checked complete in REQUIREMENTS.md"
    - "All 2535-or-more tests pass (npm run test:run exit 0)"
    - "All 15 Playwright smoke tests pass"
    - "Feature count invariants pass (src/test/phase98-invariants.test.js)"
    - "Bundle-size invariant holds: final dist byte count is less than or equal to 98-01-BASELINE.json bundleBytes"
    - "Test-runtime invariant holds: final vitest run wall-clock within 20% of baseline"
    - "LOC measurement taken; delta recorded (target at least 10% aspirational per user decision, not a gate)"
    - "Ralph pipeline is RESUMED: prd.json.paused-for-phase98 renamed back to prd.json"
    - "Coverage thresholds in vitest.config.js ratcheted upward if uplift landed (HEALTH-04)"
    - "STATE.md reflects v17.0 Code Health complete"
    - "ROADMAP.md marks Phase 98 as SHIPPED with date"
    - "v17.0-ROADMAP.md milestone archive created"
    - "DELETIONS.md and AUDIT.md remain in place as historical record"
  artifacts:
    - path: ".planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md"
      provides: "Phase 98 final report — all invariants, deltas, HEALTH status, lessons learned"
      min_lines: 100
    - path: ".planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json"
      provides: "Post-phase numeric metrics mirroring 98-01-BASELINE.json shape for before/after comparison"
    - path: "scripts/ralph/prd.json"
      provides: "Ralph pipeline restored to active state"
    - path: ".planning/milestones/v17.0-ROADMAP.md"
      provides: "v17.0 Code Health milestone archive"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "Plan 07 traceability update"
      via: "v17.0 Traceability rows updated to Complete"
      pattern: "HEALTH-.*Complete"
    - from: "scripts/ralph/prd.json"
      to: "Ralph resumed"
      via: "file exists where before it did not"
      pattern: "prd.json"
---

<objective>
**Phase close.** Run the full invariant sweep, produce the final report, ratchet coverage thresholds, resume Ralph, archive the milestone.

Purpose: Prove the phase guardrails held. Every number from 98-01-BASELINE.json is re-measured. Every HEALTH-XX requirement gets checked or marked complete. Ralph goes back online. The v17.0 milestone is archived.

Output:
- 98-07-FINAL-REPORT.md (narrative + metrics comparison)
- phase98-final-metrics.json (mirrors baseline shape for diff)
- REQUIREMENTS.md HEALTH-XX requirements marked checked
- STATE.md updated for v17.0 complete
- ROADMAP.md Phase 98 marked shipped
- v17.0-ROADMAP.md milestone archive
- scripts/ralph/prd.json restored
- vitest.config.js coverage thresholds ratcheted (if uplift landed)

Hard gate: If ANY HEALTH-XX invariant fails, Plan 07 does NOT close the phase. It reports failures and flags gap-closure planning via `/gsd-plan-phase 98 --gaps`.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-codebase-audit-and-refactor/98-CONTEXT.md
@.planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md
@.planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md
@.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
@.planning/phases/98-codebase-audit-and-refactor/98-01-COVERAGE-HEATMAP.md
@.planning/phases/98-codebase-audit-and-refactor/98-02-SUMMARY.md
@.planning/phases/98-codebase-audit-and-refactor/98-03-SUMMARY.md
@.planning/phases/98-codebase-audit-and-refactor/98-04-SUMMARY.md
@.planning/phases/98-codebase-audit-and-refactor/98-05-SUMMARY.md
@.planning/phases/98-codebase-audit-and-refactor/98-06-SUMMARY.md
@.planning/phases/98-codebase-audit-and-refactor/98-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full invariant sweep and capture phase98-final-metrics.json</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json, .planning/phases/98-codebase-audit-and-refactor/98-07-INVARIANT-FAILURES.md (only if failures)</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
    - vitest.config.js
    - package.json
  </read_first>
  <action>
    Re-run every measurement from 98-01-BASELINE.json capture. Produce `phase98-final-metrics.json` with the SAME SHAPE as baseline, covering: commit SHA, generatedAt ISO timestamp, loc, testCount, bundleBytes, testRuntimeSeconds (median of 3 runs), coverageByDirectory object.

    Then compute deltas and write them as additional fields `locDelta`, `bundleBytesDelta`, `testCountDelta`, `testRuntimeRatio`.

    Expected bounds:
    - locDelta is less than or equal to 0 (ideally negative — HEALTH-14 aspirational)
    - bundleBytesDelta is less than or equal to 0 (HARD gate per HEALTH-12)
    - testCountDelta is greater than or equal to 0 (HARD gate per HEALTH-07)
    - testRuntimeRatio is less than or equal to 1.2 (HARD gate per HEALTH-13)

    Run the full validation sweep, in order, stopping on first failure:
    1. `npm run test:run` must be green (HEALTH-05, HEALTH-07)
    2. `npm run lint` must be clean
    3. `npm run build` must succeed
    4. `npx vitest run src/test/phase98-invariants.test.js` must pass (feature counts hold)
    5. `npx playwright test tests/e2e/smoke/phase98-smoke.spec.js` must pass all 15 (HEALTH-06)
    6. `npm run test:coverage` per-directory coverage at or above Plan 02 levels (HEALTH-04)

    If any invariant fails:
    - Record details in `98-07-INVARIANT-FAILURES.md` listing which checks failed and their outputs
    - Do NOT proceed to Task 2 (phase close)
    - Recommend `/gsd-plan-phase 98 --gaps` to the user
    - Return task status failure
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json && node -e "const f=require('./.planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json'); const b=require('./.planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json'); if(f.testCount<b.testCount)process.exit(1); if(f.bundleBytes>b.bundleBytes)process.exit(2); if(f.testRuntimeSeconds>b.testRuntimeSeconds*1.2)process.exit(3);" && npm run test:run && npx playwright test tests/e2e/smoke/phase98-smoke.spec.js --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - phase98-final-metrics.json exists and is valid JSON
    - testCount is greater than or equal to baseline
    - bundleBytes is less than or equal to baseline
    - testRuntimeSeconds is less than or equal to baseline times 1.2
    - Full test suite green
    - Phase 98 invariants test green
    - All 15 Playwright smoke tests green
    - Coverage per directory at or above Plan 02 levels (HEALTH-04)
  </acceptance_criteria>
  <done>All invariants verified. Plan 07 can proceed to phase close.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet coverage thresholds, resume Ralph, update REQUIREMENTS.md + STATE.md + ROADMAP.md + milestone archive</name>
  <files>vitest.config.js, scripts/ralph/prd.json, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/milestones/v17.0-ROADMAP.md</files>
  <read_first>
    - vitest.config.js (current coverage thresholds)
    - .planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json (Task 1 output — coverageByDirectory)
    - .planning/REQUIREMENTS.md (v17.0 section and traceability table)
    - .planning/STATE.md (current milestone + progress counts)
    - .planning/ROADMAP.md (Phase 98 row and v17.0 entry)
    - .planning/milestones/ (existing archive format reference — e.g., v6.0-ROADMAP.md)
  </read_first>
  <action>
    Execute the phase-close sequence as a series of atomic commits. Order matters.

    1. Ratchet vitest coverage thresholds:
       - Read current thresholds from vitest.config.js.
       - For each metric (statements, branches, functions, lines), set new threshold = min(current global baseline from phase98-final-metrics.json global coverage, existing threshold + 5) — that is, bump each threshold by up to 5 percentage points but never above the actual achieved value.
       - Commit: `chore(tests): ratchet coverage thresholds after Phase 98 uplift`

    2. Resume Ralph:
       - `mv scripts/ralph/prd.json.paused-for-phase98 scripts/ralph/prd.json`
       - Verify rename: prd.json now exists, prd.json.paused-for-phase98 does not.
       - Commit: `chore(ralph): resume pipeline after Phase 98 close`

    3. Update REQUIREMENTS.md v17.0 section:
       - Change all 14 `- [ ] **HEALTH-XX**` lines to `- [x] **HEALTH-XX**`
       - Update the `## v17.0 Traceability` table: set Status column to `Complete` for each row
       - Add `**Coverage:**` block: `- v17.0 requirements: 14 total / Mapped to phases: 14 / Unmapped: 0`
       - Commit: `docs(requirements): mark HEALTH-01..14 complete after Phase 98 close`

    4. Update STATE.md:
       - milestone: v17.0
       - milestone_name: Code Health
       - Update progress.completed_phases (add 1) and completed_plans (add number of Plan 98 plans).
       - last_activity: "2026-04-XX — Phase 98 Code Health complete"
       - Status: ready for next milestone
       - Append to Shipped Milestones table: `v17.0 Code Health | 98 | 7 | 2026-04-XX`
       - Commit: `docs(state): record v17.0 Code Health shipped`

    5. Update ROADMAP.md:
       - Change `⏳ **v17.0 Code Health** — Phase 98 (planned)` to `✅ **v17.0 Code Health** — Phase 98 (shipped 2026-04-XX) → [archive](milestones/v17.0-ROADMAP.md)`
       - Update Progress table row for Phase 98: Status = Complete, Completed = 2026-04-XX, Plans Complete = 7/7
       - Commit: `docs(roadmap): Phase 98 shipped, v17.0 milestone complete`

    6. Create milestone archive `.planning/milestones/v17.0-ROADMAP.md`:
       - Use the same format as the existing `v6.0-ROADMAP.md` or `v11.0-ROADMAP.md` archive files.
       - Include: milestone summary, all 14 HEALTH requirements with checkmarks, phase list (just 98), links to AUDIT.md, DELETIONS.md, final metrics JSON, final report.
       - Commit: `docs(milestones): archive v17.0 Code Health`

    After every commit: `npm run test:run` must stay green. If any commit fails tests (shouldn't, since this plan touches only .planning/ + config/ + the Ralph rename), `git reset --hard HEAD` and diagnose.
  </action>
  <verify>
    <automated>test -f scripts/ralph/prd.json && test ! -f scripts/ralph/prd.json.paused-for-phase98 && [ "$(grep -c '^- \[x\] \*\*HEALTH-' .planning/REQUIREMENTS.md)" = "14" ] && test -f .planning/milestones/v17.0-ROADMAP.md && npm run test:run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/ralph/prd.json exists; prd.json.paused-for-phase98 does NOT
    - All 14 HEALTH-XX requirements have `[x]` ticked in REQUIREMENTS.md
    - v17.0 Traceability table shows all 14 rows as Complete
    - STATE.md milestone = v17.0, progress updated
    - ROADMAP.md v17.0 line shows shipped date and archive link
    - .planning/milestones/v17.0-ROADMAP.md exists and lists all 14 requirements
    - vitest.config.js thresholds bumped (not lowered)
    - npm run test:run still green after all commits
  </acceptance_criteria>
  <done>Phase 98 closed. Ralph resumed. v17.0 milestone archived. Ready for v18.0 planning.</done>
</task>

<task type="auto">
  <name>Task 3: Produce 98-07-FINAL-REPORT.md narrative summary</name>
  <files>.planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md</files>
  <read_first>
    - .planning/phases/98-codebase-audit-and-refactor/98-01-BASELINE.json
    - .planning/phases/98-codebase-audit-and-refactor/phase98-final-metrics.json
    - .planning/phases/98-codebase-audit-and-refactor/98-AUDIT.md (executive summary)
    - .planning/phases/98-codebase-audit-and-refactor/98-DELETIONS.md (execution log)
    - .planning/phases/98-codebase-audit-and-refactor/98-02-SUMMARY.md through 98-06-SUMMARY.md
  </read_first>
  <action>
    Write a narrative final report covering:

    1. **Phase 98 Overview** — what was audited, what was refactored, guiding principles honoured (audit-first, user sign-off, atomic commits, test-first).

    2. **Before/After Metrics Table** — columns Baseline / Final / Delta for: LOC, test count, bundle bytes, test runtime seconds, global coverage %, circular imports count, npm devDeps count.

    3. **HEALTH-XX Status** — one row per requirement, status Complete + evidence reference.

    4. **Artifacts produced** — pointers to AUDIT.md, DELETIONS.md, COVERAGE-HEATMAP.md, DEPENDENCY-GRAPH.svg, BASELINE.json, final-metrics.json, invariants.test.js, smoke.spec.js.

    5. **User sign-offs captured** — table of DELETIONS.md rows approved vs rejected, phase stubs kept/archived/deleted, open questions resolved.

    6. **Lessons learned** — what worked well (the three-layer filter? the smoke tests catching a regression?); what to improve next time (tooling, process).

    7. **Next steps** — v18.0 Advanced AI Systems (Phases 99-101) now unblocked; recommendations for Phase 99 planning.

    Minimum length: 100 lines. This is the historical record for the phase.
  </action>
  <verify>
    <automated>test -f .planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md && [ "$(wc -l < .planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md)" -ge "100" ]</automated>
  </verify>
  <acceptance_criteria>
    - 98-07-FINAL-REPORT.md exists and is at least 100 lines
    - Contains Before/After table, HEALTH status, artifacts list, sign-off table, lessons learned, next steps
    - References all other phase artifacts
  </acceptance_criteria>
  <done>Phase narrative preserved for posterity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User final sign-off — phase is closed</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md (user reviews, does not modify)</files>
  <action>CHECKPOINT — User reviews final report and confirms Phase 98 is shipped. No Claude action until user responds.</action>
  <verify>User types "Phase 98 shipped" OR describes outstanding gaps to trigger /gsd-plan-phase 98 --gaps.</verify>
  <done>User has confirmed phase closure OR gap-closure is triggered for remaining issues.</done>
  <what-built>
    Plan 07 has completed:
    - Full invariant sweep passed (testCount, bundleBytes, testRuntime, feature counts, smoke tests, coverage)
    - vitest coverage thresholds ratcheted
    - Ralph pipeline RESUMED (prd.json restored)
    - All 14 HEALTH-XX requirements marked complete in REQUIREMENTS.md
    - STATE.md updated for v17.0 complete
    - ROADMAP.md marks Phase 98 shipped
    - v17.0-ROADMAP.md milestone archive created
    - 98-07-FINAL-REPORT.md written
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/98-codebase-audit-and-refactor/98-07-FINAL-REPORT.md` — is the narrative accurate?
    2. Check `scripts/ralph/prd.json` exists — Ralph should be back online.
    3. Run `npm run test:run` — should be green with test count at or above 2535.
    4. Open `.planning/ROADMAP.md` — v17.0 line should show shipped date.
    5. Open `.planning/REQUIREMENTS.md` — v17.0 section should have all `[x]` ticked.
    6. Confirm open questions from AUDIT.md Section 10 are resolved (or documented as deferred to a later phase).
    7. If all good: respond "Phase 98 shipped."
    8. If something is off: respond with what needs gap-closure — Claude runs `/gsd-plan-phase 98 --gaps`.
  </how-to-verify>
  <resume-signal>Type "Phase 98 shipped" to close the phase, or describe issues to trigger gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07-01 | Denial of Service | Ralph not resumed leaves pipeline paused forever | mitigate | Task 2 step 2 explicitly renames back; Task 4 checkpoint verifies |
| T-98-07-02 | Tampering | Coverage threshold ratchet sets thresholds higher than achieved | mitigate | Task 2 step 1 uses min of baseline-coverage and existing+5 to never over-ratchet |
| T-98-07-03 | Repudiation | Missing final report makes regression debugging hard later | mitigate | Task 3 writes 100-line narrative report |

</threat_model>

<verification>
- Full test + lint + build + smoke + invariants suite green
- Ralph resumed (prd.json exists)
- All HEALTH-XX ticked in REQUIREMENTS.md
- STATE.md, ROADMAP.md, milestone archive updated
- Final report written
- User confirmed via checkpoint
</verification>

<success_criteria>
1. Every HEALTH-XX invariant verified Complete or failures flagged for gap-closure.
2. Ralph pipeline restored.
3. Coverage thresholds ratcheted.
4. REQUIREMENTS.md, STATE.md, ROADMAP.md reflect v17.0 complete.
5. Final report narrates before/after.
6. User approves via checkpoint.
</success_criteria>

<output>
After user approval, create `.planning/phases/98-codebase-audit-and-refactor/98-07-SUMMARY.md` listing all invariants final status, commit SHAs, and the "Phase 98 shipped" confirmation.
</output>
