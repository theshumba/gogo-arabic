---
phase: 43-zone-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - src/game/scenes/BootScene.js
  - src/game/systems/MapLoader.js
  - src/assets/maps/zone-export.json
  - src/game/systems/__tests__/InteractableManager.test.js
  - src/game/systems/__tests__/MapLoader.test.js
autonomous: true
requirements:
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "BootScene no longer loads old placeholder sprite files"
    - "MapLoader flat-tile fallback still works (tile-sand/tile-grass kept because flat-tile rendering path still uses them)"
    - "A Tiled-compatible JSON export exists at src/assets/maps/ documenting zone layouts"
    - "No console errors about missing textures when loading any zone"
    - "Test files updated to match new Kenmi key references"
  artifacts:
    - path: "src/game/scenes/BootScene.js"
      provides: "Cleaned BootScene without old placeholder object sprite loads"
    - path: "src/assets/maps/zone-export.json"
      provides: "Tiled-compatible JSON export of all 24 zone layouts"
      contains: "oasis_village"
    - path: "src/game/systems/__tests__/InteractableManager.test.js"
      provides: "Updated test assertions matching Kenmi keys"
      contains: "kenmi-"
  key_links:
    - from: "src/game/scenes/BootScene.js"
      to: "Phaser texture cache"
      via: "this.load.image/spritesheet calls"
      pattern: "kenmi-"
    - from: "src/assets/maps/zone-export.json"
      to: "Tiled Map Editor"
      via: "JSON format with layers/tilesets/objects"
      pattern: "tilesets"
---

<objective>
Remove old placeholder sprite loads from BootScene, create Tiled-compatible JSON export structure at src/assets/maps/, update test assertions, and verify no missing texture errors.

Purpose: PIPE-04 requires a Tiled-compatible JSON export for collaborator handoff. PIPE-05 requires removal of old placeholder sprites. After Plan 43-01 converts all references to Kenmi keys, the old sprite load calls in BootScene become dead code. Tests referencing old keys will fail and need updating.

Output: Clean BootScene, Tiled JSON export, updated tests.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/43-zone-cleanup/43-01-SUMMARY.md
@src/game/scenes/BootScene.js
@src/game/systems/MapLoader.js
@src/game/systems/__tests__/InteractableManager.test.js
@src/game/systems/__tests__/MapLoader.test.js
@src/data/zones.js (zone definitions — mapWidth, mapHeight, objects, npcs, interactables, exits)
@src/data/zones/fantasyZones.js
@src/data/zones/realWorldZones.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old placeholder sprite loads from BootScene and update tests</name>
  <files>
    src/game/scenes/BootScene.js
    src/game/systems/__tests__/InteractableManager.test.js
    src/game/systems/__tests__/MapLoader.test.js
  </files>
  <read_first>
    src/game/scenes/BootScene.js
    src/game/systems/__tests__/InteractableManager.test.js
    src/game/systems/__tests__/MapLoader.test.js
    src/game/systems/MapLoader.js (lines 445-467 — _renderFlatTiles still uses tile-sand/tile-grass)
    src/game/systems/InteractableManager.js (confirm gate-pillar still used for sign/painting)
  </read_first>
  <action>
    In BootScene.js, remove the LEGACY OBJECT SPRITES block (lines 87-103) that loads:
    - palm, palm-small, palm-alt
    - house-small, house-small-alt, house-large, house-large-alt
    - rock1, rock2
    - ruin-pillar, ruin-pillar-broke, ruin-gate
    - green-tree, green-tree-small, green-tree-bushy, ice-tree

    KEEP these lines because they are still actively used:
    - tile-sand (line 81) — used by MapLoader._renderFlatTiles as fallback
    - tile-grass (line 82) — used by MapLoader._renderFlatTiles as fallback
    - grass-ice (line 83) — used by MapLoader._renderFlatTiles for ICE_GRASS
    - gate-pillar (line 186) — used by InteractableManager for sign/painting sprites
    - gate-top (line 187) — potentially used
    - hospital (line 185) — potentially used
    - shadow (line 188) — used for character shadows

    Update the section comment from "LEGACY OBJECT SPRITES (fallback for any missing Kenmi textures)" to "GROUND TILES (flat fallback)" since the legacy objects are removed but ground tiles remain.

    In InteractableManager.test.js:
    - Line 86: Change 'ruin-pillar' to 'kenmi-desert-temple-desert-obelisk-small-1'
    - Line 87: Change 'rock1' to 'kenmi-desert-props-desert-rocks'
    - Update any other test assertions that reference old keys to match the new Kenmi keys from Plan 43-01

    In MapLoader.test.js:
    - Lines 128, 137, 156: tile-sand and tile-grass are KEPT — these tests for _renderFlatTiles are still valid. Leave unchanged.
    - If any test references old object keys (rock1, ruin-pillar, etc.), update to Kenmi keys.
  </action>
  <verify>
    Run: grep -n "'palm'\|'palm-small'\|'palm-alt'\|'house-small'\|'house-small-alt'\|'house-large'\|'house-large-alt'\|'rock1'\|'rock2'\|'ruin-pillar'\|'ruin-pillar-broke'\|'ruin-gate'\|'green-tree'\|'green-tree-small'\|'green-tree-bushy'\|'ice-tree'" src/game/scenes/BootScene.js
    Expected: 0 matches (all old loads removed)

    Run: npm run build
    Expected: builds without errors
  </verify>
  <acceptance_criteria>
    - grep -c "palm\|house-small\|house-large\|rock1\|rock2\|ruin-pillar\|ruin-gate\|green-tree\|ice-tree" src/game/scenes/BootScene.js returns 0 (only in comments is acceptable, not in load calls)
    - grep -n "tile-sand\|tile-grass\|grass-ice" src/game/scenes/BootScene.js still shows these ground tiles being loaded
    - grep -c "kenmi-" src/game/systems/__tests__/InteractableManager.test.js returns at least 1
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>17 old placeholder sprite load calls removed from BootScene, ground tile fallback preserved, test assertions updated to Kenmi keys</done>
</task>

<task type="auto">
  <name>Task 2: Create Tiled-compatible JSON export structure at src/assets/maps/</name>
  <files>
    src/assets/maps/zone-export.json
  </files>
  <read_first>
    src/data/zones.js (first 500 lines — zone structures with objects, npcs, interactables, exits)
    src/data/zones/fantasyZones.js
    src/data/zones/realWorldZones.js
    public/assets/maps/README.md (existing collaborator docs)
  </read_first>
  <action>
    Create src/assets/maps/ directory and a zone-export.json file containing a Tiled-compatible JSON structure for all 24 zones. This is a reference/handoff document — not loaded at runtime.

    The JSON structure should follow Tiled Map Editor's JSON format conventions:

    ```json
    {
      "version": "1.0",
      "generator": "gogo-arabic-v8.0-export",
      "tilewidth": 16,
      "tileheight": 16,
      "scale": 4,
      "zones": {
        "oasis_village": {
          "width": 40,
          "height": 30,
          "tilesetTheme": "desert",
          "spawnPoint": { "x": 14, "y": 20 },
          "layers": [
            {
              "name": "ground",
              "type": "tilelayer",
              "note": "Generated by buildOasisMap() — see src/data/zones.js"
            },
            {
              "name": "objects",
              "type": "objectgroup",
              "objects": [
                { "name": "kenmi-desert-props-palm-tree-1", "x": 15, "y": 11, "type": "decoration" },
                ...
              ]
            },
            {
              "name": "npcs",
              "type": "objectgroup",
              "objects": [
                { "name": "guide-amira", "x": 14, "y": 18, "type": "npc", "properties": { "key": "npc-guide-amira" } },
                ...
              ]
            },
            {
              "name": "interactables",
              "type": "objectgroup",
              "objects": [
                { "name": "sign-oasis", "x": 19, "y": 5, "type": "sign" },
                ...
              ]
            },
            {
              "name": "exits",
              "type": "objectgroup",
              "objects": [
                { "name": "oasis-to-library", "type": "exit", "properties": { "edge": "north", "tileRange": [17, 23], "targetZone": "ancient_library" } }
              ]
            }
          ],
          "tilesets": [
            { "name": "kenmi-desert-beach-tiles-1", "source": "../../../public/assets/kenmi/desert/tiles/desert-beach-tiles-1.png", "tilewidth": 16, "tileheight": 16 }
          ]
        }
      }
    }
    ```

    Include ALL 24 zones (8 core from zones.js, 8 real-world from realWorldZones.js, 8 fantasy from fantasyZones.js).

    For the 8 core zones: extract actual objects, npcs, interactables, exits from the zone definitions.
    For the 16 placeholder zones (real-world + fantasy): include dimensions, tilesetTheme, spawnPoint, and the default objects from getDefaultObjects().

    This is a STATIC export for collaborator reference — it does NOT need to be imported at runtime.
  </action>
  <verify>
    Run: node -e "const j = require('./src/assets/maps/zone-export.json'); console.log(Object.keys(j.zones).length)"
    Expected: 24

    Run: node -e "const j = require('./src/assets/maps/zone-export.json'); console.log(j.zones.oasis_village.layers.length >= 4)"
    Expected: true
  </verify>
  <acceptance_criteria>
    - File exists at src/assets/maps/zone-export.json
    - JSON is valid (node can parse it)
    - Contains exactly 24 zone entries
    - oasis_village entry has layers array with ground, objects, npcs, interactables, exits
    - Every zone has width, height, tilesetTheme, spawnPoint fields
    - Object keys in the export use kenmi- prefixed keys (not old placeholder keys)
  </acceptance_criteria>
  <done>Tiled-compatible JSON export at src/assets/maps/zone-export.json documents all 24 zones with object layers, NPC spawns, interactables, and exits in Tiled-compatible format</done>
</task>

</tasks>

<verification>
1. BootScene loads zero old placeholder object sprites (ground tile fallback preserved)
2. src/assets/maps/zone-export.json exists with 24 zones in Tiled-compatible format
3. npm run build passes
4. No old placeholder keys appear in test assertions (except tile-sand/tile-grass which are still active)
5. All old placeholder .png files in public/assets/sprites/objects/ can be safely deleted (not done in this plan — deferred to post-v8.0 since they are inert once not loaded)
</verification>

<success_criteria>
- PIPE-04 satisfied: Tiled-compatible JSON at src/assets/maps/zone-export.json with all 24 zones
- PIPE-05 satisfied: Old placeholder sprite load calls removed from BootScene (assets stay on disk but are no longer loaded — zero runtime cost)
- Tests pass with updated Kenmi key assertions
- npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/43-zone-cleanup/43-02-SUMMARY.md`
</output>
