---
phase: 45-quest-storylines
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/quests.json
  - src/data/npcStoryArcs.js
autonomous: true
requirements:
  - QUEST-01

must_haves:
  truths:
    - "Player can pick up Act 1 at Guide Amira in oasis_village and read a story-intro line about the time-traveling scholar and the lost manuscript"
    - "Completing Act N's primary objective unlocks Act N+1 in the next zone (prerequisites chain across all 8 acts)"
    - "Each act quest entry has a distinct storyAct number (1–8), a zone, an npcGiver, story-motivated objectives, and an Arabic title"
    - "Each act has an npcStoryArcs.js dialogue tree that starts the quest via quest_start effect and advances story flags"
  artifacts:
    - path: "src/data/quests.json"
      provides: "8 new main_story quest entries (act_1 through act_8)"
      contains: "\"storyAct\": 1"
    - path: "src/data/npcStoryArcs.js"
      provides: "8 arc dialogue trees — one per act NPC — with quest_start and story_flag effects"
      contains: "act_1_manuscript_call"
  key_links:
    - from: "src/data/npcStoryArcs.js"
      to: "src/data/quests.json"
      via: "quest_start effect referencing matching quest id"
      pattern: "quest_start.*act_[1-8]"
    - from: "src/data/quests.json"
      to: "src/store/slices/questSlice.js"
      via: "prerequisites array chains act_N → act_N+1"
      pattern: "\"prerequisites\".*\"act_"
---

<objective>
Add the 8-act main storyline to quests.json and wire each act's NPC dialogue in npcStoryArcs.js so the player can follow a continuous narrative arc as a time-traveling scholar collecting manuscript pages across all 8 zones.

Purpose: QUEST-01 — the core narrative spine that makes every zone visit feel story-motivated rather than arbitrary vocabulary drilling.
Output: 8 quests (type: main_story), 8 arc dialogue trees, prerequisite chain oasis_village → royal_palace.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-npc-dialogue-expansion/44-03-SUMMARY.md

# Key source files
@src/data/quests.json
@src/data/npcStoryArcs.js
@src/game/systems/DialogueEngine.js
@src/data/zones.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 8-act main storyline to quests.json</name>
  <files>src/data/quests.json</files>
  <read_first>
    - src/data/quests.json — understand existing 64 quests, type/zone/prerequisite patterns
    - src/data/zones.js — NPC ids present in each zone (npcGiver must match prefix of a real NPC id)
    - src/data/companions.js — companion zones for act placement reference
  </read_first>
  <action>
Append 8 new quest objects to quests.json. Existing 64 quests must not be modified.

Story: The player is a time-traveling scholar (الرحالة عبر الزمن) who discovers that 8 ancient manuscript pages have been scattered across the land by a cosmic sandstorm. Each act takes place in one zone; completing all 8 reunites the manuscript and reveals an ancient Arabic poem.

Quest ids and placement:
  act_1_manuscript_call        — zone: oasis_village        — npcGiver: guide
  act_2_library_cipher         — zone: ancient_library      — npcGiver: librarian
  act_3_market_bargain_scroll  — zone: desert_marketplace   — npcGiver: merchant
  act_4_farmland_harvest_clue  — zone: farmland             — npcGiver: farmer
  act_5_bedouin_night_vision   — zone: bedouin_camp         — npcGiver: wanderer
  act_6_mountain_healer_secret — zone: mountain_village     — npcGiver: healer
  act_7_port_navigator_compass — zone: coastal_port         — npcGiver: navigator
  act_8_palace_final_assembly  — zone: royal_palace         — npcGiver: scholar

For each quest object include these exact fields:
  - id: (act id above)
  - title: English title (e.g. "The Scholar's Calling")
  - titleArabic: Arabic title (e.g. "نِداء العالِم")
  - description: 1-sentence English story description
  - descriptionArabic: Arabic translation
  - zone: (zone id above)
  - type: "main_story"
  - storyAct: 1–8 (integer)
  - target: 1 (acts complete on a single key dialogue/action)
  - trackEvent: "story_act_complete"
  - autoStart: false (acts unlock via prerequisites)
  - prerequisites: act_1 has [], act_2 has ["act_1_manuscript_call"], ..., act_N has ["act_{N-1}_{id}"]
  - reward: { xp: 150 + (storyAct * 50), dirhams: 50 + (storyAct * 20) }
  - npcGiver: (role prefix above, e.g. "guide", "librarian")
  - storyFlag: "act_{N}_complete" (flag set when quest completes)
  - objectives: array of 2–3 objectives that describe narrative actions, not bare vocab tasks. Examples:
      - { id: "find_page", description: "Find the torn manuscript page in the ancient ruins", descriptionArabic: "اعثر على صفحة المخطوطة الممزقة في الأطلال القديمة" }
      - { id: "decipher_words", description: "Decipher 3 Arabic root words inscribed on the page", descriptionArabic: "افكّ شيفرة ثلاث كلمات عربية مكتوبة على الصفحة" }
  - lore: one-sentence lore note connecting this act's zone to real Islamic Golden Age history (field name: "lore", string)

act_1_manuscript_call must also set autoStart: false and its prerequisites: [] — it becomes available after the tutorial_welcome chain (prerequisites: ["master_of_letters"] so it opens once the player has done the zone-1 content).

Correction: act_1_manuscript_call prerequisites: ["words_of_oasis"] (the first main zone completion gate).
  </action>
  <verify>node -e "const q=require('./src/data/quests.json'); const acts=q.filter(x=>x.type==='main_story'); console.log('act count:', acts.length); acts.forEach(a=>console.log(a.storyAct, a.id, a.zone, 'prereqs:', a.prerequisites.join(',')))"</verify>
  <acceptance_criteria>
    - grep "main_story" src/data/quests.json returns exactly 8 matches
    - node -e returns act count: 8 with storyAct 1–8 each in a different zone
    - act_1 prerequisites contains "words_of_oasis", act_2 contains "act_1_manuscript_call", etc.
    - npm run build succeeds (JSON is valid)
  </acceptance_criteria>
  <done>quests.json contains 8 main_story quests (acts 1–8) chained by prerequisites across all 8 zones, each with English+Arabic title/description, story-motivated objectives, and lore field.</done>
</task>

<task type="auto">
  <name>Task 2: Add act dialogue trees to npcStoryArcs.js</name>
  <files>src/data/npcStoryArcs.js</files>
  <read_first>
    - src/data/npcStoryArcs.js — understand existing arc format (condition, lines, effects, choices structure)
    - src/data/quests.json — the 8 act quest ids just written in Task 1
    - src/game/systems/DialogueEngine.js — effect types: quest_start, quest_complete, story_flag, relationship_change
    - src/data/dialogueSchema.js — valid effect type enum list
  </read_first>
  <action>
Add 8 arc dialogue objects to npcStoryArcs.js — one for each act's npcGiver NPC. The existing arc objects in npcStoryArcs.js must not be modified.

For each act write an arc object with:
  - id: "arc_act_{N}" (e.g. "arc_act_1")
  - npcId: full NPC id from zones.js (e.g. "guide-amira" for act_1, "librarian-ibrahim" for act_2)
  - condition: { storyFlag: { key: "act_{N-1}_complete", value: true } } for acts 2–8; act_1 uses { quest: { id: "words_of_oasis", status: "completed" } }
  - lines: 3–5 dialogue lines that:
      a) Set the scene for this act with historical/cultural flavour tied to the zone
      b) Explain what manuscript page the scholar must find and why it matters
      c) Teach one Arabic word related to the zone's theme via teachWord field on the final line
  - effects on the final line: [{ type: "quest_start", questId: "{act_quest_id}" }, { type: "story_flag", flag: "arc_act_{N}_started", value: true }]
  - Also add a separate "completion" arc object (id: "arc_act_{N}_complete") for the same NPC with:
      - condition: { quest: { id: "{act_quest_id}", status: "active" } } AND storyFlag act_{N}_milestone_reached
      - lines: 2–3 lines of congratulation with cultural depth
      - effects: [{ type: "quest_complete", questId: "{act_quest_id}" }, { type: "story_flag", flag: "act_{N}_complete", value: true }, { type: "relationship_change", amount: 1 }]

NPC id mapping for npcStoryArcs entries:
  act_1 → guide-amira (oasis_village)
  act_2 → librarian-ibrahim (ancient_library)
  act_3 → merchant-fatima (desert_marketplace) — use existing merchant NPC, confirm id from zones.js
  act_4 → farmer-npc — check zones.js farmland npcs array for actual id
  act_5 → wanderer-ali (bedouin_camp) — confirm from zones.js
  act_6 → healer-npc — check zones.js mountain_village npcs array for actual id
  act_7 → navigator-npc — check zones.js coastal_port npcs array for actual id
  act_8 → scholar-yusuf (royal_palace section) — check zones.js royal_palace npcs; fall back to first NPC in that zone

IMPORTANT: Read zones.js npcs arrays for zones farmland, bedouin_camp, mountain_village, coastal_port, royal_palace before writing NPC ids. Use exact id strings from zones.js.

Each arc object's lines array item format (matching existing npcStoryArcs.js pattern):
  { arabic: "...", english: "...", transliteration: "...", teachWord: "word_id_if_applicable" }
  </action>
  <verify>node -e "const arcs=require('./src/data/npcStoryArcs.js'); const all=arcs.STORY_ARCS||arcs.default||arcs; const actArcs=Object.values(all).filter(a=>a.id&&a.id.startsWith('arc_act_')); console.log('act arcs:', actArcs.length); actArcs.forEach(a=>console.log(a.id, 'npc:', a.npcId, 'effects:', JSON.stringify(a.effects||a.lines?.slice(-1)[0]?.effects)))"</verify>
  <acceptance_criteria>
    - grep "arc_act_" src/data/npcStoryArcs.js returns 16 matches (8 start + 8 complete arcs)
    - grep "quest_start" src/data/npcStoryArcs.js returns 8 new matches (one per act)
    - grep "quest_complete" src/data/npcStoryArcs.js returns 8 new matches
    - npm run build succeeds
  </acceptance_criteria>
  <done>npcStoryArcs.js contains 16 new arc objects (start + complete per act) with culturally-grounded Arabic dialogue, quest_start/quest_complete effects, and story_flag progression for all 8 acts.</done>
</task>

</tasks>

<verification>
node -e "
const q = require('./src/data/quests.json');
const acts = q.filter(x => x.type === 'main_story');
console.log('Acts:', acts.length, '(expected 8)');
const zones = acts.map(a => a.zone);
const unique = [...new Set(zones)];
console.log('Unique zones covered:', unique.length, '(expected 8)');
console.log('All have lore?', acts.every(a => a.lore));
console.log('All have storyAct?', acts.every(a => typeof a.storyAct === 'number'));
const chain = acts.map(a => ({ act: a.storyAct, prereqs: a.prerequisites }));
chain.forEach(c => console.log('act', c.act, 'prereqs:', c.prereqs.join(',')));
"
npm run build 2>&1 | tail -5
</verification>

<success_criteria>
- quests.json has exactly 8 type === "main_story" quests, one per zone, storyActs 1–8
- Prerequisite chain is complete: each act N+1 requires act N's id in its prerequisites array
- npcStoryArcs.js has 16 new arc objects (arc_act_1 through arc_act_8 + arc_act_N_complete)
- All arc start effects include quest_start; all arc complete effects include quest_complete + story_flag act_N_complete
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/45-quest-storylines/45-01-SUMMARY.md`
</output>
