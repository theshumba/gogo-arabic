---
phase: 22-buildings-interiors
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/data/interiors.js
  - src/data/zones.js
autonomous: true

must_haves:
  truths:
    - "At least 15 interior map configs exist across all 8 zones"
    - "Each interior has a distinct purpose: shop, library, home, guild, mosque, or cultural"
    - "Interior maps are max 20x20 tiles with programmatic 2D array builders"
    - "Each interior has at least one exit door marked with isExit: true"
    - "Each interior has interactive objects relevant to its purpose (bookshelves, tables, workstations)"
    - "Door interactables in zones.js have interiorId linking them to interior configs"
    - "Some buildings are locked with lockMessage and unlock conditions referencing story flags"
  artifacts:
    - path: "src/data/interiors.js"
      provides: "All 15+ interior map configurations"
      exports: ["INTERIORS"]
    - path: "src/data/zones.js"
      provides: "Updated door interactables with interiorId fields"
      contains: "interiorId"
  key_links:
    - from: "src/data/zones.js"
      to: "src/data/interiors.js"
      via: "door interactable interiorId matches INTERIORS key"
      pattern: "interiorId: '.*_interior'"
    - from: "src/data/interiors.js"
      to: "src/game/systems/MapLoader.js"
      via: "buildMap function returns 2D array consumed by MapLoader.create()"
      pattern: "buildMap"
---

<objective>
Create all 15+ interior map configurations and wire every building door in zones.js to its interior.

Purpose: This plan makes buildings enterable — defining the layout, objects, NPCs, and purpose of each interior. Without this data, InteriorScene (from Plan 01) has nothing to load.

Output: interiors.js with 15+ interior configs using 5 base layout templates, updated zones.js with door interactables linking to interiors, building lock configs.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-buildings-interiors/22-RESEARCH.md
@.planning/phases/22-buildings-interiors/22-01-SUMMARY.md
@src/data/zones.js
@src/game/systems/MapLoader.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create interiors.js with 15+ interior map configurations</name>
  <files>src/data/interiors.js</files>
  <action>
Create `src/data/interiors.js` exporting an INTERIORS object keyed by interior ID.

**Import tile constants:** `import { SAND, GRASS, WATER } from './zones.js';`

**5 Base Layout Templates (builder functions):**

1. `buildSmallHouse(W=10, H=8)` — compact home layout. Stone floor (SAND), carpet area (GRASS) in center. For NPC homes.
2. `buildLargeHouse(W=14, H=10)` — larger home/guild. Stone floor with two rooms separated by a column gap.
3. `buildShop(W=12, H=10)` — shop layout. Counter area at top (GRASS strip at y=2), merchandise display areas on sides.
4. `buildLibraryRoom(W=16, H=12)` — study/library. Stone floor, reading area (GRASS) in center, bookshelf walls.
5. `buildMosque(W=18, H=14)` — large hall. Prayer area (GRASS carpet) in center, ablution area (small WATER) in corner, pillared sides.

Each function returns a 2D array of tile values. All layouts max 20x20.

**15 Interior Configs (distributed across zones):**

**Oasis Village (3 interiors):**
1. `scholar_house_interior` — Scholar Yusuf's Study (12x10, buildLargeHouse). Purpose: library/teaching.
   - objects: bookshelf (ruin-pillar x2), desk (rock1), decorative plant (green-tree-small)
   - npcs: `[{ id: 'scholar-yusuf-interior', key: 'npc-scholar-yusuf', name: 'Scholar Yusuf', nameArabic: 'الشَّيْخ يوسُف', x: 5, y: 3 }]`
   - interactables: exit-door (isExit: true) at bottom center, bookshelf-ancient (type: bookshelf, category: greetings)
   - spawnPoint: { x: 6, y: 8 }

2. `merchant_house_interior` — Merchant Fatima's Home (10x8, buildSmallHouse). Purpose: home/relationship.
   - objects: table (rock1), storage (ruin-pillar)
   - npcs: `[{ id: 'merchant-fatima-interior', key: 'npc-merchant-fatima', name: 'Merchant Fatima', nameArabic: 'التّاجِرَة فاطِمَة', x: 5, y: 3 }]`
   - interactables: exit-door, chest with trade items
   - spawnPoint: { x: 5, y: 6 }

3. `oasis_guild_interior` — Adventurer's Guild (14x10, buildLargeHouse). Purpose: guild/quests.
   - objects: notice board (ruin-gate), tables (rock1 x2), weapon rack (ruin-pillar)
   - npcs: none initially (Phase 23 will add guild NPC)
   - interactables: exit-door, bookshelf (category: phrases)
   - spawnPoint: { x: 7, y: 8 }

**Ancient Library (2 interiors):**
4. `library_archive_interior` — Ancient Archives (16x12, buildLibraryRoom). Purpose: library/lore.
   - objects: bookshelves (ruin-pillar x4), reading desk (rock1), ancient scroll display (ruin-pillar-broke)
   - npcs: `[{ id: 'librarian-interior', key: 'npc-elder', name: 'Elder Librarian', nameArabic: 'أَمين المَكتَبَة', x: 8, y: 4 }]`
   - interactables: exit-door, bookshelf-lore (category: numbers), bookshelf-history (category: colors)
   - spawnPoint: { x: 8, y: 10 }

5. `library_study_interior` — Private Study Room (10x8, buildSmallHouse). Purpose: teaching.
   - objects: desk (rock1), chair (rock2), bookshelf (ruin-pillar)
   - npcs: none (player studies alone)
   - interactables: exit-door, bookshelf-grammar (category: phrases)
   - spawnPoint: { x: 5, y: 6 }

**Desert Marketplace (3 interiors):**
6. `spice_shop_interior` — Spice Merchant's Shop (12x10, buildShop). Purpose: shop/trade.
   - objects: spice barrels (rock1 x3), counter (ruin-gate), shelves (ruin-pillar x2)
   - npcs: `[{ id: 'spice-merchant-interior', key: 'npc-merchant', name: 'Spice Merchant', nameArabic: 'تاجِر البُهارات', x: 6, y: 3 }]`
   - interactables: exit-door, chest-spices
   - spawnPoint: { x: 6, y: 8 }

7. `textile_shop_interior` — Textile Shop (12x10, buildShop). Purpose: shop/clothing.
   - objects: fabric rolls (rock2 x2), display table (rock1), mirror (ruin-pillar)
   - npcs: `[{ id: 'textile-merchant-interior', key: 'npc-merchant', name: 'Textile Seller', nameArabic: 'بائِع الأَقمِشَة', x: 6, y: 3 }]`
   - interactables: exit-door
   - spawnPoint: { x: 6, y: 8 }

8. `market_warehouse_interior` — Merchant Warehouse (14x10, buildLargeHouse). Purpose: storage/quest.
   - objects: crates (rock1 x4), barrels (rock2 x2), shelf (ruin-pillar)
   - npcs: none (empty warehouse for quest objectives)
   - interactables: exit-door, chest-warehouse
   - spawnPoint: { x: 7, y: 8 }

**Farmland (1 interior):**
9. `farmhouse_interior` — Farmer's House (10x8, buildSmallHouse). Purpose: home.
   - objects: bed area (green-tree-small as plant), table (rock1), tool storage (ruin-pillar)
   - npcs: `[{ id: 'farmer-interior', key: 'npc-farmer', name: 'Old Farmer', nameArabic: 'المُزارِع العَجوز', x: 5, y: 3 }]`
   - interactables: exit-door
   - spawnPoint: { x: 5, y: 6 }

**Bedouin Camp (1 interior):**
10. `bedouin_tent_interior` — Elder's Tent (12x10, buildSmallHouse but wider). Purpose: cultural lessons.
    - objects: cushions area (use GRASS tiles as carpet), tea set (rock2), storytelling fire pit area
    - npcs: `[{ id: 'bedouin-elder-interior', key: 'npc-elder', name: 'Bedouin Elder', nameArabic: 'شَيْخ البَدو', x: 6, y: 4 }]`
    - interactables: exit-door, bookshelf (category: time)
    - spawnPoint: { x: 6, y: 8 }

**Mountain Village (2 interiors):**
11. `mountain_home_interior` — Mountain Elder's Home (10x8, buildSmallHouse). Purpose: home/relationship.
    - objects: fireplace (rock1), rug (GRASS area), shelf (ruin-pillar)
    - npcs: `[{ id: 'mountain-elder-interior', key: 'npc-elder', name: 'Mountain Elder', nameArabic: 'شَيْخ الجَبَل', x: 5, y: 3 }]`
    - interactables: exit-door
    - spawnPoint: { x: 5, y: 6 }

12. `mountain_mosque_interior` — Village Mosque (18x14, buildMosque). Purpose: mosque/cultural.
    - objects: pillars (ruin-pillar x4), prayer rug area (GRASS), ablution area (WATER corner)
    - npcs: `[{ id: 'imam-interior', key: 'npc-elder', name: 'Imam Ibrahim', nameArabic: 'الإِمام إِبراهيم', x: 9, y: 5 }]`
    - interactables: exit-door, bookshelf (category: greetings)
    - spawnPoint: { x: 9, y: 12 }

**Coastal Port (2 interiors):**
13. `port_tavern_interior` — Sailor's Tavern (14x10, buildLargeHouse). Purpose: guild/social.
    - objects: bar counter (ruin-gate), tables (rock1 x3), barrel (rock2)
    - npcs: `[{ id: 'tavern-keeper-interior', key: 'npc-merchant', name: 'Tavern Keeper', nameArabic: 'صاحِب الحانَة', x: 7, y: 3 }]`
    - interactables: exit-door
    - spawnPoint: { x: 7, y: 8 }

14. `port_warehouse_interior` — Port Warehouse (14x10, buildLargeHouse). Purpose: storage/trade.
    - objects: crates (rock1 x5), nets (rock2), anchor decoration
    - npcs: none
    - interactables: exit-door, chest-port
    - spawnPoint: { x: 7, y: 8 }

**Royal Palace (1 interior):**
15. `palace_throne_interior` — Throne Room (18x14, buildMosque adapted). Purpose: quest/narrative.
    - objects: throne (ruin-gate), pillars (ruin-pillar x4), carpet (GRASS), decorative items
    - npcs: `[{ id: 'vizier-interior', key: 'npc-elder', name: 'Royal Vizier', nameArabic: 'الوَزير', x: 9, y: 4 }]`
    - interactables: exit-door, bookshelf (category: adjectives)
    - spawnPoint: { x: 9, y: 12 }

**Each interior config shape:**
```javascript
{
  id: 'string',
  name: 'Display Name',
  nameArabic: 'عربي',
  zone: 'parent_zone_id',
  mapWidth: number,
  mapHeight: number,
  buildMap: function,  // returns 2D array
  spawnPoint: { x, y },  // tile coords, near exit door
  objects: [{ key, x, y, collide, collideW?, collideH? }],
  npcs: [{ id, key, name, nameArabic, x, y }],
  interactables: [
    { id: 'exit-door', type: 'door', x, y, isExit: true, labelArabic: 'خروج', labelEnglish: 'Exit' },
    // Other interactables (bookshelves, chests, etc.)
  ],
}
```

Export: `export const INTERIORS = { ... }` (keyed by interior ID).
  </action>
  <verify>
- `grep -c "buildMap" src/data/interiors.js` returns at least 15 (one per interior)
- `grep -c "isExit: true" src/data/interiors.js` returns at least 15 (one exit per interior)
- `grep "export const INTERIORS" src/data/interiors.js` confirms export exists
- `npx vite build 2>&1 | tail -5` completes without errors
  </verify>
  <done>
15 interior map configs exist in interiors.js, each with unique layout, objects, NPCs, and exit door. 5 base layout templates (small house, large house, shop, library, mosque) reused across interiors with content variation. All maps are max 20x20 tiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update zones.js door interactables with interiorId links and building locks</name>
  <files>src/data/zones.js</files>
  <action>
Update door interactables across all 8 zones in `src/data/zones.js` to add `interiorId` field linking each door to its interior config.

**Oasis Village — update existing door + add 2 new doors:**
- `door-scholar-house` (existing, x:8, y:5): Change `locked: true` to `locked: false`, add `interiorId: 'scholar_house_interior'`
  - Actually, keep locked initially but add `unlockFlag: 'met_scholar_yusuf'` — unlocks after player has talked to Scholar Yusuf outside (which sets the story flag from Phase 21 tutorial)
- Add new door: `{ id: 'door-merchant-house', type: 'door', x: 10, y: 16, interiorId: 'merchant_house_interior', locked: false, labelArabic: 'بَيْت فاطِمَة', labelEnglish: "Fatima's House" }`
- Add new door: `{ id: 'door-oasis-guild', type: 'door', x: 34, y: 12, interiorId: 'oasis_guild_interior', locked: false, labelArabic: 'نادي المُغامِرين', labelEnglish: "Adventurer's Guild" }`

**Ancient Library — update existing door + add 1 new door:**
- `door-archive` (existing, x:20, y:8): Keep locked, add `interiorId: 'library_archive_interior'`, add `unlockFlag: 'library_access_granted'`
- Add new door: `{ id: 'door-library-study', type: 'door', x: 8, y: 12, interiorId: 'library_study_interior', locked: false, labelArabic: 'غُرفَة الدِّراسَة', labelEnglish: 'Study Room' }`

**Desert Marketplace — update existing door + add 2 new doors:**
- `door-warehouse` (existing, x:35, y:12): Keep locked, add `interiorId: 'market_warehouse_interior'`, add `unlockFlag: 'warehouse_key_obtained'`
- Add new door: `{ id: 'door-spice-shop', type: 'door', x: 10, y: 14, interiorId: 'spice_shop_interior', locked: false, labelArabic: 'دُكّان البُهارات', labelEnglish: 'Spice Shop' }`
- Add new door: `{ id: 'door-textile-shop', type: 'door', x: 30, y: 14, interiorId: 'textile_shop_interior', locked: false, labelArabic: 'دُكّان الأَقمِشَة', labelEnglish: 'Textile Shop' }`

**Farmland — update existing door:**
- `door-barn` (existing, x:15, y:8): Change locked to false (was flavor text, now enterable), add `interiorId: 'farmhouse_interior'`, update `lockMessage` removal, add `labelArabic: 'بَيْت المُزارِع', labelEnglish: "Farmer's House"`

**Bedouin Camp — add 1 new door:**
- `{ id: 'door-bedouin-tent', type: 'door', x: 7, y: 8, interiorId: 'bedouin_tent_interior', locked: false, labelArabic: 'خَيْمَة الشَّيْخ', labelEnglish: "Elder's Tent" }`

**Mountain Village — add 2 new doors:**
- `{ id: 'door-mountain-home', type: 'door', x: 15, y: 11, interiorId: 'mountain_home_interior', locked: false, labelArabic: 'بَيْت الشَّيْخ', labelEnglish: "Elder's Home" }`
- `{ id: 'door-mountain-mosque', type: 'door', x: 25, y: 11, interiorId: 'mountain_mosque_interior', locked: false, labelArabic: 'المَسْجِد', labelEnglish: 'Mosque' }`

**Coastal Port — add 2 new doors:**
- `{ id: 'door-port-tavern', type: 'door', x: 15, y: 12, interiorId: 'port_tavern_interior', locked: false, labelArabic: 'حانَة البَحّارَة', labelEnglish: "Sailor's Tavern" }`
- `{ id: 'door-port-warehouse', type: 'door', x: 22, y: 12, interiorId: 'port_warehouse_interior', locked: false, labelArabic: 'مَخْزَن المِيناء', labelEnglish: 'Port Warehouse' }`

**Royal Palace — add 1 new door:**
- `{ id: 'door-palace-throne', type: 'door', x: 25, y: 10, interiorId: 'palace_throne_interior', locked: true, lockMessage: 'The throne room requires an audience with the Vizier.', unlockFlag: 'palace_audience_granted', labelArabic: 'قاعَة العَرش', labelEnglish: 'Throne Room' }`

**Building lock integration:**
- For doors with `unlockFlag`, the InteractableManager (from Plan 01) already checks `obj.locked`. The executor should update InteractableManager.handleInteractable() to also check `obj.unlockFlag` against narrativeSlice story flags:
  - Import store and selectStoryFlag
  - Before emitting DOOR_LOCKED, check: `if (obj.unlockFlag) { const flag = selectStoryFlag(obj.unlockFlag)(store.getState()); if (flag) { /* treat as unlocked */ } }`
  - This enables dynamic unlocking based on quest/story progress (BLDG-11)

**Total door count:** 15 doors across 8 zones (4 updated + 11 new).
  </action>
  <verify>
- `grep -c "interiorId:" src/data/zones.js` returns at least 15
- `grep -c "type: 'door'" src/data/zones.js` returns at least 15
- `grep "unlockFlag" src/data/zones.js` shows at least 3 locked buildings
- `npx vite build 2>&1 | tail -5` completes without errors
  </verify>
  <done>
All 8 zones have door interactables with interiorId fields linking to interior configs. 15 doors total. 4 buildings are locked with unlockFlag conditions that check narrativeSlice story flags. Each door has Arabic and English labels.
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without errors
2. interiors.js exports INTERIORS with at least 15 keys
3. Each interior has buildMap, spawnPoint, objects, interactables (with exit door), and max 20x20 dimensions
4. zones.js has 15 door interactables with interiorId fields
5. Door interiorId values match keys in INTERIORS object
6. At least 3 buildings have unlockFlag for quest-based locking
7. 5 distinct interior purposes represented: shop, library, home, guild/social, mosque/cultural
</verification>

<success_criteria>
- 15+ interior map configs with unique layouts, objects, and purposes
- All zones have enterable building doors with interiorId
- 5 base layout templates reused across interiors
- Building lock/unlock via story flags works for 3+ buildings
- Interior maps all max 20x20 tiles with clear exit indicators
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-buildings-interiors/22-02-SUMMARY.md`
</output>
