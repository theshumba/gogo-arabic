---
phase: 32-advanced-combat
plan: 11
type: execute
wave: 7
depends_on: ["32-02", "32-05", "32-06", "32-07", "32-08", "32-09", "32-10"]
files_modified:
  - src/components/Battle/BattleOverlay.jsx
  - src/components/Battle/BattleMenu.jsx
  - src/components/Battle/BattleResult.jsx
  - src/store/store.js
autonomous: true

must_haves:
  truths:
    - "BattleOverlay renders all new Phase 32 UI components (StatusEffectBar, ComboMeter, GrammarComboInput, TargetSelector, ArenaHUD, PostBattleReview, BossRushInterlude)"
    - "BattleOverlay handles flee challenge via BattleArabicInput in flee mode"
    - "BattleMenu includes combo and item action options"
    - "BattleResult offers post-battle review option leading to PostBattleReview"
    - "arenaSlice registered in store with localStorage persistence"
  artifacts:
    - path: "src/components/Battle/BattleOverlay.jsx"
      provides: "Integrated battle UI with all Phase 32 components"
      contains: "StatusEffectBar|ComboMeter|GrammarComboInput|TargetSelector|ArenaHUD"
    - path: "src/components/Battle/BattleMenu.jsx"
      provides: "Battle menu with combo action"
      contains: "combo"
    - path: "src/components/Battle/BattleResult.jsx"
      provides: "Battle result with Arabic review button"
      contains: "onReview"
    - path: "src/store/store.js"
      provides: "arenaSlice registered in store"
      contains: "arenaReducer"
  key_links:
    - from: "src/components/Battle/BattleOverlay.jsx"
      to: "src/components/Battle/StatusEffectBar.jsx"
      via: "renders status effects"
      pattern: "StatusEffectBar"
    - from: "src/components/Battle/BattleOverlay.jsx"
      to: "src/components/Battle/PostBattleReview.jsx"
      via: "renders post-battle review"
      pattern: "PostBattleReview"
    - from: "src/components/Battle/BattleOverlay.jsx"
      to: "src/components/Battle/BattleArabicInput.jsx"
      via: "renders flee challenge via flee mode"
      pattern: "mode.*flee|fleeChallengeData"
    - from: "src/store/store.js"
      to: "src/store/slices/arenaSlice.js"
      via: "reducer registration"
      pattern: "arena.*Reducer"
---

<objective>
Wire all Phase 32 React UI components into BattleOverlay, add combo/item actions to BattleMenu, add review button to BattleResult, and register arenaSlice in store.

Purpose: This is the UI integration plan that connects all Phase 32 React components into the battle overlay and registers the arena store slice. Separated from FSM mechanics (Plan 32-10) for manageable scope.
Output: Fully integrated battle UI with all Phase 32 components visible and interactive
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/32-advanced-combat/32-05-SUMMARY.md
@.planning/phases/32-advanced-combat/32-06-SUMMARY.md
@.planning/phases/32-advanced-combat/32-07-SUMMARY.md
@.planning/phases/32-advanced-combat/32-08-SUMMARY.md
@.planning/phases/32-advanced-combat/32-09-SUMMARY.md
@.planning/phases/32-advanced-combat/32-10-SUMMARY.md
@src/components/Battle/BattleOverlay.jsx
@src/components/Battle/BattleMenu.jsx
@src/components/Battle/BattleResult.jsx
@src/store/store.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update BattleOverlay with all Phase 32 components</name>
  <files>src/components/Battle/BattleOverlay.jsx</files>
  <action>
  Add all new Phase 32 components to BattleOverlay. PRESERVE all existing rendering and event handling.

  **Import and render:**
  - StatusEffectBar (for player effects, positioned bottom-left)
  - StatusEffectBar (for enemy effects, positioned top-left)
  - ComboMeter (positioned left side)
  - GrammarComboInput (visible during GRAMMAR_COMBO phase)
  - BattleItemMenu (visible during ITEM_USE phase)
  - TargetSelector (visible during TARGET_SELECT phase)
  - ArenaHUD (visible when arena is active)
  - PostBattleReview (shown when user clicks "Review" on BattleResult)
  - BossRushInterlude (visible during BOSS_RUSH_INTERLUDE phase)

  **Add EventBus listeners (in useEffect cleanup pattern):**
  - BATTLE_GRAMMAR_COMBO -> show GrammarComboInput with combo data
  - BATTLE_FLEE_CHALLENGE -> set fleeChallengeData, render BattleArabicInput with `mode="flee"`, word from event data, timeLimit=10000. On result, emit flee result back via EventBus for BattleStateMachine to process.
  - BATTLE_TARGET_SELECT -> show TargetSelector
  - BATTLE_ITEM_MENU_OPEN -> show BattleItemMenu
  - BATTLE_POST_REVIEW -> set postReviewData from event (arabicUsedThisBattle array), enable review button
  - ARENA_WAVE_START -> show ArenaHUD
  - ARENA_COMPLETE -> show arena result overlay

  **Add state variables:**
  - showGrammarCombo, grammarComboData
  - showItemMenu
  - showTargetSelector, targetSelectorData
  - showPostReview, postReviewData
  - showArenaHUD
  - showBossInterlude, interludeData
  - fleeChallengeData (when set, renders BattleArabicInput with mode="flee")

  **Flee challenge rendering (key wiring):**
  When fleeChallengeData is set:
  ```jsx
  {fleeChallengeData && (
    <BattleArabicInput
      mode="flee"
      word={fleeChallengeData.word}
      timeLimit={10000}
      onResult={(result) => {
        EventBus.emit(EVENTS.BATTLE_FLEE_RESULT, result);
        setFleeChallengeData(null);
      }}
    />
  )}
  ```
  </action>
  <verify>
  - `npx vite build` succeeds
  - `grep 'StatusEffectBar' src/components/Battle/BattleOverlay.jsx` — component imported
  - `grep 'fleeChallengeData' src/components/Battle/BattleOverlay.jsx` — flee UI wired
  - `grep 'PostBattleReview' src/components/Battle/BattleOverlay.jsx` — review wired
  </verify>
  <done>BattleOverlay renders all Phase 32 components including flee challenge via BattleArabicInput in flee mode, grammar combo input, target selector, arena HUD, and post-battle review</done>
</task>

<task type="auto">
  <name>Task 2: Update BattleMenu, BattleResult, and register arenaSlice in store</name>
  <files>
    src/components/Battle/BattleMenu.jsx
    src/components/Battle/BattleResult.jsx
    src/store/store.js
  </files>
  <action>
  **BattleMenu.jsx**: Add 'combo' and 'item' actions.
  Update MENU_ACTIONS array to include:
  `{ id: 'combo', label: 'Combo', arabic: 'تركيبة', key: '6' }`
  Position after existing 'item' entry in the list.
  The 'combo' action should only be visible when available (check grammarComboState availability from props or Redux). If no grammar combos available (no lessons completed), hide the option.

  **BattleResult.jsx**: Add "Review Arabic" button.
  After the existing "Continue" button, add:
  ```jsx
  <button onClick={() => onReview?.()}>مراجعة العربية — Review Arabic</button>
  ```
  Add `onReview` prop to the component. When clicked, parent (BattleOverlay) shows PostBattleReview with the battle's arabicUsedThisBattle data.
  Style the review button as secondary (gold outline, not filled) to distinguish from primary Continue button.

  **store.js**:
  - Import arenaReducer from './slices/arenaSlice.js'
  - Add `arena: arenaReducer` to rootReducer combineReducers call
  - Add 'arena' to root persistConfig whitelist array (lightweight data, localStorage is fine)
  - Verify statusEffectVocabMiddleware is already registered from Plan 32-01 (if not, add it)
  </action>
  <verify>
  - `npx vite build` succeeds — CRITICAL: must pass as this is the final integration
  - `npx vitest run --reporter=verbose 2>&1 | tail -10` — all tests pass
  - `grep 'arena' src/store/store.js` — arenaReducer registered
  - `grep 'combo' src/components/Battle/BattleMenu.jsx` — combo action added
  - `grep 'onReview' src/components/Battle/BattleResult.jsx` — review button added
  </verify>
  <done>BattleMenu has combo action (key 6), BattleResult has Arabic review button, arenaSlice registered in store with localStorage persistence</done>
</task>

</tasks>

<verification>
- BattleOverlay renders StatusEffectBar, ComboMeter, GrammarComboInput, BattleItemMenu, TargetSelector, ArenaHUD, PostBattleReview, BossRushInterlude
- BattleOverlay handles flee challenge by rendering BattleArabicInput in flee mode
- BattleMenu shows 'combo' action (key 6) when grammar combos available
- BattleResult has "Review Arabic" button leading to PostBattleReview
- arenaSlice registered in store with localStorage persistence
- statusEffectVocabMiddleware in middleware chain
- Build succeeds, all tests pass
- NO regressions on existing battle functionality
</verification>

<success_criteria>
- Complete end-to-end battle UI flow: all Phase 32 components visible and interactive in battle overlay
- Arena mode UI: ArenaHUD visible during arena, BossRushInterlude between boss fights
- Flee challenge renders BattleArabicInput in flee mode with 10s timer
- Post-battle review accessible via BattleResult "Review Arabic" button
- arenaSlice persists leaderboard data across sessions
- Zero regressions on v6.0-v6.1 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/32-advanced-combat/32-11-SUMMARY.md`
</output>
