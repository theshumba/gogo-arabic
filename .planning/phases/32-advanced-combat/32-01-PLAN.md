---
phase: 32-advanced-combat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/statusEffects.js
  - src/store/middleware/statusEffectVocabMiddleware.js
  - src/store/store.js
  - src/utils/eventBusTypes.js
autonomous: true

must_haves:
  truths:
    - "20+ status effects exist with Arabic names, transliterations, and English translations"
    - "Status effects are categorized as buff/debuff with mechanical properties"
    - "When a status effect is applied in battle, its Arabic vocabulary auto-queues to FSRS"
    - "New EventBus constants exist for status effect and arena events"
  artifacts:
    - path: "src/data/statusEffects.js"
      provides: "20+ status effects with Arabic names and compound effect definitions"
      contains: "COMPOUND_EFFECTS"
    - path: "src/store/middleware/statusEffectVocabMiddleware.js"
      provides: "Auto-sync status effect vocabulary to FSRS on applyStatusEffect"
      exports: ["statusEffectVocabMiddleware"]
    - path: "src/utils/eventBusTypes.js"
      provides: "New Phase 32 EventBus constants"
      contains: "ARENA_WAVE_START"
  key_links:
    - from: "src/store/middleware/statusEffectVocabMiddleware.js"
      to: "src/data/statusEffects.js"
      via: "getStatusEffect lookup"
      pattern: "getStatusEffect"
    - from: "src/store/middleware/statusEffectVocabMiddleware.js"
      to: "src/store/slices/vocabularySlice.js"
      via: "addFsrsCard dispatch"
      pattern: "addFsrsCard"
---

<objective>
Expand status effects to 20+ with Arabic vocabulary, add compound effect definitions, create FSRS auto-sync middleware, and register Phase 32 EventBus constants.

Purpose: Foundation data and middleware for all Phase 32 battle mechanics. Status effects teach Arabic vocabulary through combat — every buff/debuff is a learning opportunity.
Output: Extended statusEffects.js with compound effects, statusEffectVocabMiddleware.js, updated eventBusTypes.js and store.js
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/data/statusEffects.js
@src/store/middleware/craftingVocabMiddleware.js
@src/store/store.js
@src/utils/eventBusTypes.js
@src/store/slices/vocabularySlice.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand status effects to 20+ and add compound effect definitions</name>
  <files>src/data/statusEffects.js</files>
  <action>
  Expand STATUS_EFFECTS from 14 to 22 effects. Keep all 14 existing effects exactly as-is. Add these 8 new effects:

  NEW BUFFS:
  - `focus`: arabic: 'تَركيز', transliteration: 'tarkīz', english: 'Focus', turns: 3, accuracyBoost: 1.2, type: 'buff', description: 'Increases accuracy by 20%.'
  - `swiftness`: arabic: 'خِفَّة', transliteration: 'khiffa', english: 'Swiftness', turns: 2, extraTurn: true, type: 'buff', description: 'Grants an extra action this turn.'
  - `inspiration`: arabic: 'إِلهام', transliteration: 'ilhām', english: 'Inspiration', turns: 3, comboBoost: 1.5, type: 'buff', description: 'Combo meter charges 50% faster.'

  NEW DEBUFFS:
  - `freeze`: arabic: 'تَجمُّد', transliteration: 'tajammud', english: 'Freeze', turns: 1, skipTurn: true, type: 'debuff', description: 'Target skips their next turn.'
  - `curse`: arabic: 'لَعنة', transliteration: 'laʿna', english: 'Curse', turns: 3, healReduce: 0.5, type: 'debuff', description: 'Reduces healing received by 50%.'
  - `fatigue`: arabic: 'إِرهاق', transliteration: 'irhāq', english: 'Fatigue', turns: 2, mpDrain: 5, type: 'debuff', description: 'Drains MP each turn.'
  - `expose`: arabic: 'كَشف', transliteration: 'kashf', english: 'Expose', turns: 2, dmgTakenBoost: 1.3, type: 'debuff', description: 'Target takes 30% more damage.'
  - `confusion`: arabic: 'اِرتباك', transliteration: 'irtibāk', english: 'Bewilderment', turns: 2, comboBreak: true, type: 'debuff', description: 'Breaks enemy combo chain.'

  Then add a new export `COMPOUND_EFFECTS` (flat object keyed by compound ID) with 6 compounds:
  1. `resilience`: components: ['shield', 'strength'], arabic: 'صُمود', english: 'Resilience', effect: { dmgBoost: 1.2, dmgReduce: 0.6 }, turns: 2
  2. `corrosion`: components: ['poison', 'burn'], arabic: 'تَآكُل', english: 'Corrosion', effect: { dot: 15 }, turns: 2
  3. `petrify`: components: ['freeze', 'slow'], arabic: 'تَحَجُّر', english: 'Petrification', effect: { skipTurn: true, dmgTakenBoost: 1.5 }, turns: 1
  4. `clarity`: components: ['wisdom', 'focus'], arabic: 'صَفاء', english: 'Clarity', effect: { xpBoost: 1.5, accuracyBoost: 1.3 }, turns: 3
  5. `berserk`: components: ['strength', 'courage'], arabic: 'هِياج', english: 'Berserk', effect: { dmgBoost: 1.8, dmgReduce: 0.8 }, turns: 2
  6. `doom`: components: ['curse', 'weakness'], arabic: 'هَلاك', english: 'Doom', effect: { dot: 10, healReduce: 0.0 }, turns: 3

  Add helper function `detectCompoundEffect(activeEffects)` that checks if active effect IDs contain all components of any compound. Returns the compound object with `shouldReplace` array of component IDs, or null.

  Add helper `getStatusEffectsByLevel(playerLevel)` that gates effects: level 1-5 returns first 10, level 6-10 returns first 16, level 11+ returns all 22. This prevents FSRS queue flooding.
  </action>
  <verify>
  - `node -e "import('./src/data/statusEffects.js').then(m => { console.log('Effects:', Object.keys(m.STATUS_EFFECTS).length); console.log('Compounds:', Object.keys(m.COMPOUND_EFFECTS).length); const c = m.detectCompoundEffect([{id:'poison'},{id:'burn'}]); console.log('Compound detect:', c?.id); console.log('Level gate:', m.getStatusEffectsByLevel(3).length, m.getStatusEffectsByLevel(7).length, m.getStatusEffectsByLevel(15).length); })"`
  - Should output: Effects: 22, Compounds: 6, Compound detect: corrosion, Level gate: 10 16 22
  </verify>
  <done>22 status effects with Arabic names exist, 6 compound effects defined, detectCompoundEffect returns correct compound, getStatusEffectsByLevel gates properly</done>
</task>

<task type="auto">
  <name>Task 2: Create statusEffectVocabMiddleware and register Phase 32 EventBus constants</name>
  <files>
    src/store/middleware/statusEffectVocabMiddleware.js
    src/store/store.js
    src/utils/eventBusTypes.js
  </files>
  <action>
  **statusEffectVocabMiddleware.js**: Create following the exact pattern of craftingVocabMiddleware.js.
  - Listen for action type `'battle/applyStatusEffect'`
  - On match, extract `effect.id` from `action.payload`
  - Look up status effect data via `getStatusEffect(effect.id)` from statusEffects.js
  - If effect has `arabic` field, create wordId as `status_${effect.id}`
  - Check if wordId already exists in `store.getState().vocabulary.fsrsCards`
  - If not, dispatch `addFsrsCard({ wordId, card: createDefaultCard(), source: 'battle_status_effect' })`
  - Use the same `createDefaultCard()` helper pattern from craftingVocabMiddleware

  Also listen for compound effect application. When `'battle/applyStatusEffect'` payload includes `compound: true`, look up COMPOUND_EFFECTS for the effect id and queue that compound's Arabic vocabulary too (wordId: `compound_${effect.id}`).

  **store.js**: Import and add `statusEffectVocabMiddleware` to the middleware chain after `craftingVocabMiddleware`.

  **eventBusTypes.js**: Add these Phase 32 constants to the EVENTS object (after existing battle events):
  ```
  // Phase 32 — Status Effects & Advanced Combat
  BATTLE_STATUS_APPLIED: 'phaser:battle:status-applied',
  BATTLE_COMPOUND_TRIGGERED: 'phaser:battle:compound-triggered',
  BATTLE_COMBO_CHAIN: 'phaser:battle:combo-chain',
  BATTLE_GRAMMAR_COMBO: 'phaser:battle:grammar-combo',
  BATTLE_FLEE_CHALLENGE: 'phaser:battle:flee-challenge',
  BATTLE_FLEE_FAILED: 'phaser:battle:flee-failed',
  BATTLE_TARGET_SELECT: 'phaser:battle:target-select',
  BATTLE_ITEM_MENU_OPEN: 'phaser:battle:item-menu-open',
  BATTLE_POST_REVIEW: 'phaser:battle:post-review',
  ARENA_WAVE_START: 'phaser:arena:wave-start',
  ARENA_WAVE_COMPLETE: 'phaser:arena:wave-complete',
  ARENA_COMPLETE: 'phaser:arena:complete',
  ARENA_SCORE_SUBMITTED: 'redux:arena:score-submitted',
  ```
  </action>
  <verify>
  - `npx vite build` succeeds
  - `npx vitest run --reporter=verbose 2>&1 | tail -20` — all tests pass
  - `grep -c 'ARENA_WAVE_START' src/utils/eventBusTypes.js` returns 1
  </verify>
  <done>statusEffectVocabMiddleware registered in store, auto-queues status effect Arabic to FSRS on applyStatusEffect, 13 new EventBus constants added</done>
</task>

</tasks>

<verification>
- STATUS_EFFECTS has 22 entries (14 existing + 8 new)
- COMPOUND_EFFECTS has 6 entries
- detectCompoundEffect correctly identifies compounds from active effect arrays
- getStatusEffectsByLevel gates effects by player level (10/16/22)
- statusEffectVocabMiddleware dispatches addFsrsCard on applyStatusEffect
- 13 new EVENTS constants registered in eventBusTypes.js
- Build succeeds, all existing tests pass
</verification>

<success_criteria>
- 22 status effects with Arabic names, transliterations, types, and mechanical properties
- 6 compound effects with component pairs and Arabic names
- Middleware auto-syncs status effect vocabulary to FSRS review queue
- All Phase 32 EventBus constants registered
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/32-advanced-combat/32-01-SUMMARY.md`
</output>
