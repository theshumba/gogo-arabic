---
phase: 32-advanced-combat
plan: 03
type: tdd
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - src/game/systems/battle/GrammarComboDetector.js
  - src/game/systems/battle/__tests__/GrammarComboDetector.test.js
  - src/game/systems/battle/CompoundEffectResolver.js
  - src/game/systems/battle/__tests__/CompoundEffectResolver.test.js
autonomous: true

must_haves:
  truths:
    - "GrammarComboDetector validates noun+adjective combos against grammar lesson completion"
    - "GrammarComboDetector validates verb conjugation chains with escalating damage multipliers"
    - "GrammarComboDetector validates complete sentence construction for ultimate attacks"
    - "CompoundEffectResolver detects and resolves compound effects from active status effect arrays"
    - "Both classes reject invalid inputs and respect lesson gating"
  artifacts:
    - path: "src/game/systems/battle/GrammarComboDetector.js"
      provides: "Grammar combo validation with lesson gating"
      exports: ["GrammarComboDetector"]
    - path: "src/game/systems/battle/__tests__/GrammarComboDetector.test.js"
      provides: "Tests for all 3 combo types + edge cases"
      contains: "describe.*GrammarComboDetector"
    - path: "src/game/systems/battle/CompoundEffectResolver.js"
      provides: "Compound status effect detection and resolution"
      exports: ["CompoundEffectResolver"]
    - path: "src/game/systems/battle/__tests__/CompoundEffectResolver.test.js"
      provides: "Tests for compound detection, priority, and edge cases"
      contains: "describe.*CompoundEffectResolver"
  key_links:
    - from: "src/game/systems/battle/GrammarComboDetector.js"
      to: "src/data/grammarCombos.js"
      via: "import combo templates"
      pattern: "NOUN_ADJ_COMBOS|VERB_CHAIN_PATTERNS|SENTENCE_TEMPLATES"
    - from: "src/game/systems/battle/CompoundEffectResolver.js"
      to: "src/data/statusEffects.js"
      via: "import COMPOUND_EFFECTS"
      pattern: "COMPOUND_EFFECTS|detectCompoundEffect"
---

<objective>
TDD: Build GrammarComboDetector (validates Arabic grammar patterns for combat combos) and CompoundEffectResolver (detects/applies compound status effects).

Purpose: Core logic classes for grammar-based damage multipliers and status effect combinations. TDD ensures correct Arabic grammar validation and effect composition without regressions.
Output: Two tested classes with comprehensive test suites
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/32-advanced-combat/32-01-SUMMARY.md
@.planning/phases/32-advanced-combat/32-02-SUMMARY.md
@src/data/grammarCombos.js
@src/data/statusEffects.js
@src/game/systems/__tests__/mocks/sceneMock.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD GrammarComboDetector</name>
  <files>
    src/game/systems/battle/GrammarComboDetector.js
    src/game/systems/battle/__tests__/GrammarComboDetector.test.js
  </files>
  <action>
  **RED phase — Write tests first:**

  Create test file with these test cases:

  ```
  describe('GrammarComboDetector', () => {
    describe('detectNounAdjectiveCombo', () => {
      it('returns valid combo when input matches a noun+adj template')
      it('returns damageMultiplier from matched template')
      it('returns invalid when lesson not completed')
      it('returns invalid when input has wrong word count')
      it('returns invalid when words do not match any template')
      it('records arabicUsed in result')
    })

    describe('detectVerbConjugationChain', () => {
      it('returns valid chain with correct root across forms')
      it('escalates damage multiplier per chain link')
      it('returns invalid when lesson not completed')
      it('returns invalid when root mismatch')
      it('tracks chainLength in result')
      it('caps chain at maxChainLength')
    })

    describe('detectSentenceCombo', () => {
      it('returns valid ultimate when all 3 slots filled correctly')
      it('returns 3.0x damage multiplier for perfect sentence')
      it('returns partial credit for 2 of 3 slots correct')
      it('returns invalid when sentence lesson not completed')
      it('returns invalid when no slots match')
    })

    describe('getAvailableComboTypes', () => {
      it('returns empty when no lessons completed')
      it('returns noun_adjective when noun-adj-agreement completed')
      it('returns all types when all lessons completed and level 7+')
    })
  })
  ```

  Constructor: `new GrammarComboDetector(completedLessons)` — takes array of lesson IDs from grammarSlice.

  **GREEN phase — Implement:**

  `GrammarComboDetector` class:
  - `constructor(completedLessons)` — stores lessons, imports combo data from grammarCombos.js
  - `detectNounAdjectiveCombo(arabicInput)` — splits input into 2 tokens, matches against NOUN_ADJ_COMBOS, checks lesson gate. Returns `{ valid, damageMultiplier, comboType, arabicUsed, matchedCombo }` or `{ valid: false, reason }`
  - `detectVerbConjugationChain(currentVerb, previousVerbs)` — finds verb in VERB_CHAIN_PATTERNS, validates root consistency, calculates cumulative multiplier. Returns `{ valid, damageMultiplier, comboType, chainLength, root }` or `{ valid: false, reason }`
  - `detectSentenceCombo(sentenceParts)` — sentenceParts is { subject, verb, object }. Matches against SENTENCE_TEMPLATES. Returns `{ valid, damageMultiplier, comboType, accuracy, arabicSentence }` or `{ valid: false }`
  - `getAvailableComboTypes(playerLevel)` — returns array of available combo types based on lessons and level

  Use exact string matching for noun/adjective/verb lookups (strip diacritics for comparison). Do NOT use regex for Arabic — use normalized comparison.
  </action>
  <verify>
  - `npx vitest run src/game/systems/battle/__tests__/GrammarComboDetector.test.js --reporter=verbose` — all tests pass
  </verify>
  <done>GrammarComboDetector validates all 3 combo types with lesson gating, 15+ tests passing</done>
</task>

<task type="auto">
  <name>Task 2: TDD CompoundEffectResolver</name>
  <files>
    src/game/systems/battle/CompoundEffectResolver.js
    src/game/systems/battle/__tests__/CompoundEffectResolver.test.js
  </files>
  <action>
  **RED phase — Write tests first:**

  ```
  describe('CompoundEffectResolver', () => {
    describe('checkForCompounds', () => {
      it('returns null when no compound possible')
      it('detects resilience from shield + strength')
      it('detects corrosion from poison + burn')
      it('detects petrify from freeze + slow')
      it('returns compound with shouldReplace listing component IDs')
      it('returns null when only 1 of 2 components present')
    })

    describe('resolveCompounds', () => {
      it('replaces component effects with compound effect')
      it('preserves non-component effects in the array')
      it('handles multiple possible compounds — picks first match')
      it('returns unchanged array when no compounds possible')
    })

    describe('getCompoundArabic', () => {
      it('returns arabic name for known compound')
      it('returns null for unknown compound')
    })
  })
  ```

  **GREEN phase — Implement:**

  `CompoundEffectResolver` class (pure functions, no dependencies on store):
  - `static checkForCompounds(activeEffects)` — takes array of `{ id, remainingTurns, ... }`, uses `detectCompoundEffect` from statusEffects.js. Returns compound object or null.
  - `static resolveCompounds(activeEffects)` — checks for compounds, replaces component effects with compound effect (preserving remainingTurns from compound definition). Returns new array.
  - `static getCompoundArabic(compoundId)` — returns Arabic name from COMPOUND_EFFECTS.

  This is a pure utility class with static methods — no scene dependency, easily testable.
  </action>
  <verify>
  - `npx vitest run src/game/systems/battle/__tests__/CompoundEffectResolver.test.js --reporter=verbose` — all tests pass
  - `npx vitest run --reporter=verbose 2>&1 | tail -5` — all tests still passing
  </verify>
  <done>CompoundEffectResolver detects and resolves all 6 compound effects, 12+ tests passing, zero regressions</done>
</task>

</tasks>

<verification>
- GrammarComboDetector test suite: 15+ tests all green
- CompoundEffectResolver test suite: 12+ tests all green
- Full test suite: zero regressions
- Build succeeds
</verification>

<success_criteria>
- GrammarComboDetector validates noun+adj, verb chains, and sentence combos
- CompoundEffectResolver detects and resolves compound effects
- Lesson gating prevents combos without prerequisite completion
- All tests pass with RED->GREEN->REFACTOR cycle documented in commits
</success_criteria>

<output>
After completion, create `.planning/phases/32-advanced-combat/32-03-SUMMARY.md`
</output>
