---
phase: 52-vocabulary-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/vocabularyExpanded.js
  - src/data/vocabularyAll.js
autonomous: true
requirements:
  - CONT-02
  - CONT-07

must_haves:
  truths:
    - "vocabularyExpanded.js contains 5,000 words with zero numbered placeholder entries"
    - "Every word in vocabularyAll.js has a cefrLevel field (A1, A2, B1, or B2)"
    - "CATEGORY_AFFINITY covers 60+ categories so domain affinity applies to >70% of expanded words"
    - "Arabic-text deduplication removes duplicate Arabic entries from the merged output"
    - "50-100 words have ambiguous: true flag on their entry in vocabularyExpanded.js"
  artifacts:
    - path: "src/data/vocabularyExpanded.js"
      provides: "596 real B2 words replacing placeholders + ambiguous flags on ~75 words"
      contains: "ambiguous: true"
    - path: "src/data/vocabularyAll.js"
      provides: "Extended CATEGORY_AFFINITY (60+ categories) + Arabic-text dedup + CEFR inference for legacy words"
      contains: "CATEGORY_AFFINITY"
  key_links:
    - from: "src/data/vocabularyAll.js"
      to: "src/data/vocabularyExpanded.js"
      via: "import expandedWords"
      pattern: "import expandedWords from"
---

<objective>
Replace 596 placeholder B2 words in vocabularyExpanded.js with real Arabic vocabulary, extend CATEGORY_AFFINITY to cover 60+ categories for learning-path domain affinity, add Arabic-text deduplication to vocabularyAll.js, infer CEFR levels for 1,220 legacy words, and tag ~75 ambiguous words with `ambiguous: true`.

Purpose: Phase 52's data foundation — all downstream plans (validation script, tashkeel lock, UI verification) depend on the vocabulary data being complete and correctly tagged.
Output: vocabularyExpanded.js with 5,000 real words + ambiguous flags; vocabularyAll.js with full domain affinity coverage, Arabic-text dedup, and CEFR inference.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-vocabulary-expansion/52-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 596 placeholder B2 words and add ambiguous flags in vocabularyExpanded.js</name>
  <files>src/data/vocabularyExpanded.js</files>
  <read_first>
    src/data/vocabularyExpanded.js
    .planning/phases/52-vocabulary-expansion/52-RESEARCH.md
  </read_first>
  <action>
Replace the 596 numbered placeholder entries in vocabularyExpanded.js (IDs `exp_b2_0905` through `exp_b2_1500`) that have Arabic text matching the pattern `مُصْطَلَح [number]` and English text `term [number]`. These are all in the `B2_WORDS` array and have `category: "supplementary"`.

Replace each placeholder 1-for-1 with a real B2-level Arabic word. The 596 replacement words MUST:
- Keep the same IDs (`exp_b2_0905` through `exp_b2_1500`)
- Have `cefrLevel: "B2"` and `difficulty: 4`
- Have `frequency` values in range 1-499 (B2 frequency band)
- Have real Arabic text with tashkeel, real English glosses, real transliterations, and real trilateral roots (or null for particles/loanwords)
- Use diverse categories that BALANCE Traveler and Historian domains (the existing non-placeholder B2 words are heavily Scholar-domain). Target distribution for the 596 replacements:
  - ~200 words in Traveler-domain categories: travel, food, health, house_home, work_business, transport, professions, emotions, media_communication, daily_life, clothing, weather
  - ~200 words in Historian-domain categories: history_civilization, literary_arabic, law_society, religion_philosophy, arts_culture, arts_literature, rhetoric_eloquence, quranic_classical, islamic_sciences_advanced
  - ~196 words in Scholar-domain categories: science_nature, academic_discourse, philosophy_advanced, psychology_sociology, advanced_verbs, advanced_nouns_abstract

Each replacement entry follows this exact schema:
```js
{ id: "exp_b2_0905", arabic: "تَنَافُسِيَّة", english: "competitiveness", transliteration: "tanaafusiyya", root: "نفس", category: "economy_finance", cefrLevel: "B2", frequency: 245, difficulty: 4 },
```

Additionally, add `ambiguous: true` to approximately 75 words across ALL CEFR levels in vocabularyExpanded.js where the undiacritised Arabic form is homographic (same consonant skeleton, different reading). These are words where stripping tashkeel would cause learner confusion. Key categories of ambiguous words:

- Form I verbs vs verbal nouns sharing the same root letters (e.g., كَتَبَ kataba vs كُتُب kutub — both written as كتب)
- Form I verbs vs Form II/III verbs (e.g., عَلِمَ alima vs عَلَّمَ allama — both written as علم)
- Active vs passive participles sharing consonant skeleton
- Nouns with identical consonants but different vowel patterns (e.g., مَلِك malik king vs مِلْك milk possession)

Specific words that MUST get `ambiguous: true` (find these by their ID or Arabic text in the existing data):
- كَتَبَ (kataba) — also كُتُب kutub, كِتَاب kitaab
- عَلِمَ (alima) — also عِلْم ilm, عَلَم alam
- عَمِلَ (amila) — also عَمَل amal
- دَرَسَ (darasa) — also دَرْس dars
- حَكَمَ (hakama) — also حُكْم hukm
- نَظَرَ (nathara) — also نَظَر nathar
- صَبَرَ (sabara) — also صَبْر sabr
- مَلِك (malik) — also مِلْك milk, مَلَك malak
- حَسَنَ (hasana) — also حُسْن husn
- قَدَرَ (qadara) — also قَدَر qadar, قُدْرَة qudra
- جَمَعَ (jamaa) — also جَمْع jam, جُمْعَة juma
- فَتَحَ (fataha) — also فَتْح fath
- خَرَجَ (kharaja) — also خُرُوج khuruuj
- وَصَلَ (wasala) — also وَصْل wasl
- رَجَعَ (rajaa) — also رُجُوع rujuu
- سَمِعَ (samia) — also سَمْع sam
- شَرِبَ (shariba) — also شُرْب shurb
- ضَرَبَ (daraba) — also ضَرْب darb
- أَمَرَ (amara) — also أَمْر amr
- قَتَلَ (qatala) — also قَتْل qatl
- And ~55 more verbs/nouns where the root pattern creates genuine ambiguity

Add the `ambiguous: true` field ONLY to the specific word objects (appended after the `difficulty` field). Do NOT add `ambiguous: false` to other words — absence means not ambiguous.

IMPORTANT: Do not modify any non-placeholder entries except to add `ambiguous: true` where applicable. The A1_WORDS, A2_WORDS, and B1_WORDS arrays must remain exactly as-is except for ambiguous flag additions. The B2_WORDS array entries with IDs before `exp_b2_0905` must remain exactly as-is except for ambiguous flag additions.
  </action>
  <verify>
Run these verification commands:
```bash
# Verify zero numbered placeholders remain
node --input-type=module -e "
import e from './src/data/vocabularyExpanded.js';
const bad = e.filter(w => /مُصْطَلَح \d+/.test(w.arabic));
console.log('numbered placeholders:', bad.length);
if (bad.length > 0) process.exit(1);
console.log('PASS: zero numbered placeholders');
"

# Verify total word count still 5,000
node --input-type=module -e "
import e from './src/data/vocabularyExpanded.js';
console.log('total:', e.length);
if (e.length !== 5000) process.exit(1);
console.log('PASS: 5000 words');
"

# Verify B2 count still 1,500
node --input-type=module -e "
import e from './src/data/vocabularyExpanded.js';
const b2 = e.filter(w => w.cefrLevel === 'B2').length;
console.log('B2 count:', b2);
if (b2 !== 1500) process.exit(1);
console.log('PASS: 1500 B2 words');
"

# Verify ambiguous flags exist (target ~75)
node --input-type=module -e "
import e from './src/data/vocabularyExpanded.js';
const amb = e.filter(w => w.ambiguous === true);
console.log('ambiguous words:', amb.length);
if (amb.length < 50 || amb.length > 100) process.exit(1);
console.log('PASS: ambiguous count in range 50-100');
"

# Verify no supplementary category remains
node --input-type=module -e "
import e from './src/data/vocabularyExpanded.js';
const supp = e.filter(w => w.category === 'supplementary');
console.log('supplementary category:', supp.length);
if (supp.length > 0) process.exit(1);
console.log('PASS: zero supplementary');
"
```
  </verify>
  <acceptance_criteria>
- `node --input-type=module -e "import e from './src/data/vocabularyExpanded.js'; console.log(e.filter(w => /مُصْطَلَح \d+/.test(w.arabic)).length)"` outputs `0`
- `node --input-type=module -e "import e from './src/data/vocabularyExpanded.js'; console.log(e.length)"` outputs `5000`
- `node --input-type=module -e "import e from './src/data/vocabularyExpanded.js'; console.log(e.filter(w => w.cefrLevel === 'B2').length)"` outputs `1500`
- `node --input-type=module -e "import e from './src/data/vocabularyExpanded.js'; console.log(e.filter(w => w.ambiguous === true).length)"` outputs a number between 50 and 100
- `node --input-type=module -e "import e from './src/data/vocabularyExpanded.js'; console.log(e.filter(w => w.category === 'supplementary').length)"` outputs `0`
- grep for `ambiguous: true` in vocabularyExpanded.js returns matches
  </acceptance_criteria>
  <done>596 placeholder B2 words replaced with real Arabic vocabulary distributed across Traveler/Historian/Scholar domains. ~75 words across all CEFR levels tagged with `ambiguous: true`. Total count remains exactly 5,000. No supplementary category entries remain.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CATEGORY_AFFINITY, add Arabic-text dedup, and add CEFR inference in vocabularyAll.js</name>
  <files>src/data/vocabularyAll.js</files>
  <read_first>
    src/data/vocabularyAll.js
    src/data/vocabularyExpanded.js
    .planning/phases/52-vocabulary-expansion/52-RESEARCH.md
  </read_first>
  <action>
Make three additions to `src/data/vocabularyAll.js`:

**Addition 1: Extend CATEGORY_AFFINITY (lines 39-77)**

Add these category mappings after the existing entries in CATEGORY_AFFINITY (before the closing `}`). These cover the 73 currently unmapped categories from vocabularyExpanded.js:

```js
  // Extended Scholar domain
  grammar_patterns: ['scholar'],
  grammar_particles: ['scholar'],
  academic_discourse: ['scholar'],
  verbs_form_II_III: ['scholar'],
  advanced_grammar_forms: ['scholar'],
  advanced_verbs: ['scholar'],
  abstract_general: ['scholar'],
  advanced_nouns_abstract: ['scholar'],
  science_nature: ['scholar'],
  philosophy_advanced: ['scholar'],
  psychology_sociology: ['scholar'],
  verbs_intermediate: ['scholar', 'traveler', 'historian'],
  adjectives_intermediate: ['scholar', 'traveler', 'historian'],

  // Extended Traveler domain
  professions: ['traveler'],
  house_home: ['traveler'],
  travel: ['traveler'],
  health: ['traveler'],
  work_business: ['traveler'],
  transport: ['traveler'],
  emotions: ['traveler', 'scholar'],
  media_communication: ['traveler'],
  shopping: ['traveler'],
  sports_leisure: ['traveler'],
  technology: ['traveler', 'scholar'],
  social_life: ['traveler'],
  city_urban: ['traveler'],
  hospitality: ['traveler'],
  tools_objects: ['traveler'],
  kitchen_cooking: ['traveler'],

  // Extended Historian domain
  history_civilization: ['historian'],
  literary_arabic: ['historian'],
  law_society: ['historian'],
  rhetoric_eloquence: ['historian'],
  arts_culture: ['historian'],
  arts_literature: ['historian'],
  religion_philosophy: ['historian'],
  quranic_classical: ['historian', 'scholar'],
  islamic_sciences_advanced: ['historian'],
  philosophy_thought: ['scholar', 'historian'],

  // Extended Shared domains
  economy_finance: ['traveler', 'historian'],
  politics_governance: ['historian', 'scholar'],
  environment: ['traveler', 'historian'],
  medicine_health: ['scholar', 'traveler'],
  education_teaching: ['scholar'],
  language_linguistics: ['scholar'],
  music_arts: ['historian', 'traveler'],
  agriculture: ['traveler', 'historian'],
  maritime_naval: ['traveler', 'historian'],
  advanced_natural_science: ['scholar'],
  islamic_jurisprudence: ['historian'],
  calligraphy_art: ['historian', 'scholar'],
  poetry_literature: ['historian'],
  astronomy_advanced: ['scholar'],
  philosophy_ethics: ['scholar', 'historian'],
  diplomacy: ['historian', 'traveler'],
  trade_commerce: ['traveler'],
  crafts_manufacturing: ['traveler', 'historian'],
  urban_planning: ['historian'],
  water_irrigation: ['traveler', 'historian'],
  navigation: ['traveler'],
  textiles: ['traveler'],
  ceramics_pottery: ['historian'],
  metallurgy: ['scholar', 'historian'],
  optics_physics: ['scholar'],
  alchemy_chemistry: ['scholar'],
  cartography: ['historian', 'scholar'],
  music_theory: ['historian'],
  medicine_pharmacology: ['scholar'],
  library_sciences: ['scholar'],
```

NOTE: This list covers all 91 categories found in vocabularyExpanded.js. Some may not exist yet (the replacement words in Task 1 may introduce new ones). Include all mappings — unmapped categories simply have no words and cause no harm.

**Addition 2: Arabic-text deduplication (after line 117)**

After the merge line `const vocabulary = [...curatedWords, ...additionalWords, ...newExpandedWords];`, add Arabic-text dedup as a secondary filter:

```js
// Secondary dedup: remove entries with duplicate Arabic text (keep first occurrence = highest priority source)
const seenArabic = new Set();
const deduped = [];
for (const word of vocabulary) {
  const stripped = word.arabic;
  if (seenArabic.has(stripped)) continue;
  seenArabic.add(stripped);
  deduped.push(word);
}
```

Then change the domainAffinity loop and dev summary to iterate over `deduped` instead of `vocabulary`. Change the final export to `export default deduped;` — BUT rename the variable to keep it clean: use `vocabularyDeduped` internally, then `export default vocabularyDeduped;`. Or simpler: just reassign `const vocabulary` to `deduped` by changing the flow. The cleanest approach:

Replace `const vocabulary = [...curatedWords, ...additionalWords, ...newExpandedWords];` with:
```js
// Merge: curated first (richest metadata), then additional, then expanded
const merged = [...curatedWords, ...additionalWords, ...newExpandedWords];

// Secondary dedup: remove entries with duplicate Arabic text (keep first occurrence = highest priority source)
const seenArabic = new Set();
const vocabulary = [];
for (const word of merged) {
  if (seenArabic.has(word.arabic)) continue;
  seenArabic.add(word.arabic);
  vocabulary.push(word);
}
```

Then the rest of the file (domainAffinity loop, dev summary, export) stays exactly the same since it already references `vocabulary`.

**Addition 3: CEFR inference for legacy words (after the domainAffinity loop)**

After the `for (const word of vocabulary) { word.domainAffinity = ... }` loop (around line 123), add:

```js
// Infer CEFR level for legacy words (vocabulary.json / vocabulary-final.json) that lack cefrLevel.
// Uses frequency bands matching vocabularyExpanded.js conventions, with difficulty fallback.
for (const word of vocabulary) {
  if (!word.cefrLevel) {
    if (word.frequency >= 4000) word.cefrLevel = 'A1';
    else if (word.frequency >= 2000) word.cefrLevel = 'A2';
    else if (word.frequency >= 500) word.cefrLevel = 'B1';
    else if (word.frequency != null) word.cefrLevel = 'B2';
    else if (word.difficulty === 1) word.cefrLevel = 'A1';
    else if (word.difficulty === 2) word.cefrLevel = 'A2';
    else if (word.difficulty === 3) word.cefrLevel = 'B1';
    else if (word.difficulty === 4) word.cefrLevel = 'B2';
    else word.cefrLevel = 'A2'; // safe fallback for words with no frequency or difficulty
  }
}
```

Place this AFTER the domainAffinity loop but BEFORE the dev summary block.
  </action>
  <verify>
Run these verification commands:
```bash
# Verify domain affinity coverage improved to >70%
node --input-type=module -e "
import v from './src/data/vocabularyAll.js';
const withAffinity = v.filter(w => w.domainAffinity && w.domainAffinity.length > 0);
const pct = ((withAffinity.length / v.length) * 100).toFixed(1);
console.log('domain affinity coverage:', pct + '%', '(' + withAffinity.length + '/' + v.length + ')');
if (parseFloat(pct) < 50) { console.error('FAIL: coverage under 50%'); process.exit(1); }
console.log('PASS: domain affinity coverage above 50%');
"

# Verify all words have cefrLevel
node --input-type=module -e "
import v from './src/data/vocabularyAll.js';
const missing = v.filter(w => !w.cefrLevel);
console.log('missing cefrLevel:', missing.length);
if (missing.length > 0) { console.log('example:', missing[0]?.id); process.exit(1); }
console.log('PASS: all words have cefrLevel');
"

# Verify Arabic-text dedup removed duplicates
node --input-type=module -e "
import v from './src/data/vocabularyAll.js';
const arabicTexts = v.map(w => w.arabic);
const uniqueCount = new Set(arabicTexts).size;
console.log('total:', v.length, 'unique Arabic:', uniqueCount);
if (uniqueCount !== v.length) { console.error('FAIL: still has Arabic dupes'); process.exit(1); }
console.log('PASS: no Arabic text duplicates');
"

# Verify total is still above 5,000
node --input-type=module -e "
import v from './src/data/vocabularyAll.js';
console.log('total after dedup:', v.length);
if (v.length < 5000) { console.error('FAIL: under 5000'); process.exit(1); }
console.log('PASS: 5000+ words');
"

# Verify path balance (each path has 200+ words)
node --input-type=module -e "
import v from './src/data/vocabularyAll.js';
const paths = { scholar: 0, traveler: 0, historian: 0 };
for (const w of v) {
  for (const p of (w.domainAffinity || [])) {
    paths[p] = (paths[p] || 0) + 1;
  }
}
console.log('path distribution:', paths);
const min = Math.min(...Object.values(paths));
if (min < 200) { console.error('FAIL: a path has under 200 words'); process.exit(1); }
console.log('PASS: all paths have 200+ words');
"
```
  </verify>
  <acceptance_criteria>
- grep for `grammar_patterns:` in vocabularyAll.js returns a match (extended CATEGORY_AFFINITY)
- grep for `seenArabic` in vocabularyAll.js returns a match (Arabic-text dedup code present)
- grep for `word.cefrLevel` in vocabularyAll.js returns a match in the inference block
- `node --input-type=module -e "import v from './src/data/vocabularyAll.js'; console.log(v.filter(w => !w.cefrLevel).length)"` outputs `0`
- `node --input-type=module -e "import v from './src/data/vocabularyAll.js'; const a = v.map(w=>w.arabic); console.log(a.length === new Set(a).size)"` outputs `true`
- `node --input-type=module -e "import v from './src/data/vocabularyAll.js'; console.log(v.length >= 5000)"` outputs `true`
  </acceptance_criteria>
  <done>CATEGORY_AFFINITY extended to 60+ categories covering >70% of words with domain affinity. Arabic-text deduplication applied as secondary filter in vocabularyAll.js. CEFR level inferred for all 1,220 legacy words. All paths have 200+ domain-boosted words.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` completes without errors (vocabulary data is valid JS)
2. Zero numbered placeholder words remain in vocabularyExpanded.js
3. vocabularyAll.js exports 5,000+ unique-Arabic words
4. Every word has a cefrLevel field
5. Domain affinity covers all three learning paths with 200+ words each
6. ~75 words have ambiguous: true flag
</verification>

<success_criteria>
- vocabularyExpanded.js contains 5,000 words with zero placeholder entries and ~75 ambiguous flags
- vocabularyAll.js has extended CATEGORY_AFFINITY (60+ categories), Arabic-text dedup, and CEFR inference
- `npm run build` passes
- All verification commands in both tasks exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/52-vocabulary-expansion/52-01-SUMMARY.md`
</output>
