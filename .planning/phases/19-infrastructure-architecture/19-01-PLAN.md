---
phase: 19-infrastructure-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/eventBusTypes.js
  - src/utils/eventBus.js
  - src/hooks/useEventBusListeners.js
  - src/game/scenes/WorldScene.js
  - src/game/scenes/BootScene.js
  - src/game/systems/InteractableManager.js
  - src/game/systems/NPCManager.js
  - src/game/systems/ZoneTransition.js
  - src/game/sprites/Player.js
  - src/components/Router/GameLayout.jsx
  - src/components/NPC/DialogueOverlay.jsx
  - src/components/Quiz/QuizOverlay.jsx
  - src/components/UI/LevelUpModal.jsx
  - src/components/HUD/HUD.jsx
  - src/components/HUD/MiniMap.jsx
  - src/components/HUD/QuestTracker.jsx
  - src/components/Onboarding/ContextualOnboarding.jsx
  - src/components/Wardrobe/Wardrobe.jsx
  - src/components/World/WorldMap.jsx
  - src/hooks/useOverlayClose.js
  - src/hooks/useDialogue.js
  - src/hooks/useBattle.js
  - src/hooks/useQuiz.js
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All EventBus calls use namespaced constants from eventBusTypes.js — zero raw strings remain"
    - "Event names follow source:category:action convention"
    - "No event name collisions exist in the registry"
    - "Existing game functionality works identically after rename"
  artifacts:
    - path: "src/utils/eventBusTypes.js"
      provides: "Centralized event name registry with namespaced constants"
      exports: ["EVENTS"]
      min_lines: 40
  key_links:
    - from: "src/game/scenes/WorldScene.js"
      to: "src/utils/eventBusTypes.js"
      via: "import { EVENTS }"
      pattern: "EVENTS\\."
    - from: "src/hooks/useEventBusListeners.js"
      to: "src/utils/eventBusTypes.js"
      via: "import { EVENTS }"
      pattern: "EVENTS\\."
    - from: "src/game/systems/InteractableManager.js"
      to: "src/utils/eventBusTypes.js"
      via: "import { EVENTS }"
      pattern: "EVENTS\\."
---

<objective>
Create the centralized EventBus event name registry (eventBusTypes.js) and atomically rename all raw event strings across the entire codebase to use namespaced constants.

Purpose: INFR-02 requires namespaced naming convention (`source:category:action`) with centralized registry preventing event conflicts. This must happen first because all subsequent Phase 19 work (sub-hooks, SceneStackManager) imports from this registry.

Output: `src/utils/eventBusTypes.js` with all event constants; 21 files updated to use `EVENTS.X` instead of raw strings.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-infrastructure-architecture/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create eventBusTypes.js centralized event registry</name>
  <files>src/utils/eventBusTypes.js</files>
  <action>
Create `src/utils/eventBusTypes.js` exporting a frozen `EVENTS` object with ALL event names used in the codebase, mapped to namespaced strings using `source:category:action` convention.

**Complete event inventory (verified from codebase grep — 28 unique events):**

```
Old name                  -> New constant                      -> New string value
freeze-player             -> PLAYER_FREEZE                     -> 'phaser:player:freeze'
unfreeze-player           -> PLAYER_UNFREEZE                   -> 'phaser:player:unfreeze'
player-position-update    -> PLAYER_POSITION_UPDATE            -> 'phaser:player:position-update'
player-stamina-update     -> PLAYER_STAMINA_UPDATE             -> 'phaser:player:stamina-update'
npc-interact              -> NPC_INTERACT                      -> 'phaser:npc:interact'
zone-change               -> ZONE_CHANGE                       -> 'phaser:zone:change'
zone-transition           -> ZONE_TRANSITION                   -> 'phaser:zone:transition'
check-zone-unlock         -> ZONE_CHECK_UNLOCK                 -> 'phaser:zone:check-unlock'
fast-travel               -> FAST_TRAVEL                       -> 'react:zone:fast-travel'
show-sign                 -> SIGN_SHOW                         -> 'phaser:object:sign-show'
bookshelf-interact        -> BOOKSHELF_INTERACT                -> 'phaser:object:bookshelf-interact'
chest-opened              -> CHEST_OPENED                      -> 'phaser:object:chest-opened'
chest-empty               -> CHEST_EMPTY                       -> 'phaser:object:chest-empty'
door-locked               -> DOOR_LOCKED                       -> 'phaser:object:door-locked'
door-opened               -> DOOR_OPENED                       -> 'phaser:object:door-opened'
open-quiz                 -> QUIZ_OPEN                         -> 'phaser:quiz:open'
quiz-closed               -> QUIZ_CLOSED                       -> 'react:quiz:closed'
open-review-session       -> REVIEW_SESSION_OPEN               -> 'react:nav:open-review'
open-alphabet             -> ALPHABET_OPEN                     -> 'react:nav:open-alphabet'
open-world-map            -> WORLD_MAP_OPEN                    -> 'react:nav:open-world-map'
open-shop                 -> SHOP_OPEN                         -> 'react:nav:open-shop'
sfx-correct               -> SFX_CORRECT                       -> 'react:sfx:correct'
sfx-wrong                 -> SFX_WRONG                         -> 'react:sfx:wrong'
sfx-wordlearned           -> SFX_WORDLEARNED                   -> 'react:sfx:wordlearned'
sfx-levelup               -> SFX_LEVELUP                       -> 'react:sfx:levelup'
sfx-quest                 -> SFX_QUEST                         -> 'react:sfx:quest'
sfx-click                 -> SFX_CLICK                         -> 'react:sfx:click'
vfx-shake                 -> VFX_SHAKE                         -> 'phaser:vfx:shake'
vfx-particles-burst       -> VFX_PARTICLES_BURST               -> 'phaser:vfx:particles-burst'
vfx-particles-continuous  -> VFX_PARTICLES_CONTINUOUS           -> 'phaser:vfx:particles-continuous'
scene-ready               -> SCENE_READY                       -> 'phaser:scene:ready'
outfit-changed            -> OUTFIT_CHANGED                    -> 'react:player:outfit-changed'
boss-dialogue             -> BOSS_DIALOGUE                     -> 'react:battle:boss-dialogue'
```

Group constants by domain with section comments: PLAYER, NPC, ZONE, OBJECTS, QUIZ/LEARNING, SFX, VFX, SCENE, BATTLE. Add a header comment explaining the naming convention.

Freeze the object with `Object.freeze()` to prevent accidental mutation.

Include a comment block at the top showing the old-to-new mapping for any renamed event that is not obvious, so future developers can trace the history.
  </action>
  <verify>
`node -e "const { EVENTS } = require('./src/utils/eventBusTypes.js'); console.log(Object.keys(EVENTS).length);"` — if ESM, verify via: `grep -c 'EVENTS\.' src/utils/eventBusTypes.js` returns 30+ property definitions. Verify no duplicate values: `grep "'" src/utils/eventBusTypes.js | sort | uniq -d` returns empty.
  </verify>
  <done>eventBusTypes.js exists with 30+ named constants, all using `source:category:action` format, no duplicate values, object is frozen.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic rename of all EventBus raw strings to EVENTS constants</name>
  <files>
    src/hooks/useEventBusListeners.js
    src/game/scenes/WorldScene.js
    src/game/scenes/BootScene.js
    src/game/systems/InteractableManager.js
    src/game/systems/NPCManager.js
    src/game/systems/ZoneTransition.js
    src/game/sprites/Player.js
    src/components/Router/GameLayout.jsx
    src/components/NPC/DialogueOverlay.jsx
    src/components/Quiz/QuizOverlay.jsx
    src/components/UI/LevelUpModal.jsx
    src/components/HUD/HUD.jsx
    src/components/HUD/MiniMap.jsx
    src/components/HUD/QuestTracker.jsx
    src/components/Onboarding/ContextualOnboarding.jsx
    src/components/Wardrobe/Wardrobe.jsx
    src/components/World/WorldMap.jsx
    src/hooks/useOverlayClose.js
    src/hooks/useDialogue.js
    src/hooks/useBattle.js
    src/hooks/useQuiz.js
  </files>
  <action>
For every file that contains `EventBus.emit('`, `EventBus.on('`, or `EventBus.off('` with a raw string:

1. Add `import { EVENTS } from '../utils/eventBusTypes.js';` (adjust relative path per file location — `../../utils/eventBusTypes.js` for deeper files)
2. Replace every raw string event name with the corresponding `EVENTS.CONSTANT_NAME`

**File-by-file mapping (sorted by number of replacements):**

**useEventBusListeners.js** (42 replacements — 21 on + 21 off + emits):
- All 21 `EventBus.on('event-name',` -> `EventBus.on(EVENTS.CONSTANT,`
- All 21 `EventBus.off('event-name',` -> `EventBus.off(EVENTS.CONSTANT,`
- Internal emits: `'unfreeze-player'` -> `EVENTS.PLAYER_UNFREEZE`, `'zone-transition'` -> `EVENTS.ZONE_TRANSITION`, `'vfx-shake'` -> `EVENTS.VFX_SHAKE`, `'vfx-particles-burst'` -> `EVENTS.VFX_PARTICLES_BURST`, `'vfx-particles-continuous'` -> `EVENTS.VFX_PARTICLES_CONTINUOUS`

**WorldScene.js** (13 replacements):
- 5 `EventBus.on(...)` in create(), 5 `EventBus.off(...)` in shutdown()
- 3 `EventBus.emit(...)`: `'scene-ready'`, `'player-position-update'`, `'check-zone-unlock'`

**InteractableManager.js** (10 replacements):
- All `EventBus.emit(...)` calls: `'show-sign'`, `'freeze-player'` (x3), `'bookshelf-interact'` (x2), `'chest-opened'`, `'chest-empty'`, `'door-locked'`, `'door-opened'`

**HUD.jsx** (10 replacements):
- `EventBus.on('player-stamina-update')`, `EventBus.off('player-stamina-update')`
- Emits: `'freeze-player'` (x3), `'unfreeze-player'` (x2), `'open-world-map'`, `'open-alphabet'`, `'open-review-session'`

**ZoneTransition.js** (3): `'freeze-player'`, `'zone-change'`, `'unfreeze-player'`
**NPCManager.js** (2): `'npc-interact'`, `'freeze-player'`
**Player.js** (1): `'player-stamina-update'`
**BootScene.js** (1): `'scene-ready'`
**GameLayout.jsx** (1): `'unfreeze-player'`
**DialogueOverlay.jsx** (1): `'unfreeze-player'`
**QuizOverlay.jsx** (1): `'quiz-closed'`
**LevelUpModal.jsx** (1): `'sfx-levelup'`
**MiniMap.jsx** (1): `'open-world-map'`
**QuestTracker.jsx** (2): `'player-position-update'` on/off
**ContextualOnboarding.jsx** (2): `'player-position-update'` on/off
**Wardrobe.jsx** (1): `'outfit-changed'`
**WorldMap.jsx** (1): `'fast-travel'`
**useOverlayClose.js** (2): `'unfreeze-player'` (x2)
**useDialogue.js** (4): `'unfreeze-player'`, `'sfx-wordlearned'`, `'sfx-click'`, `'open-shop'`, `'open-alphabet'`
**useBattle.js** (5): `'boss-dialogue'` (x4), `'sfx-quest'` (x2), `'sfx-wrong'`
**useQuiz.js** (1): `'unfreeze-player'`

**CRITICAL:** This is an atomic operation. Every single raw string must be replaced in the same commit. Missing even one creates a silent event mismatch where emitter uses new name but listener uses old name (or vice versa), causing the game to break.

After all replacements, verify NO raw event strings remain: `grep -rn "EventBus\.\(emit\|on\|off\)('" src/` should return ZERO results.
  </action>
  <verify>
1. `grep -rn "EventBus\.\(emit\|on\|off\)('" src/` returns zero matches (no raw strings left)
2. `grep -rn "EVENTS\." src/ | wc -l` returns 90+ matches (all converted)
3. `npx vite build` succeeds (no import errors, no missing constants)
4. `npx vitest run` — 551+ tests pass (same baseline: 554 total, 3 pre-existing failures in DailyDashboard/HUD)
  </verify>
  <done>Zero raw EventBus string literals remain in the codebase. All 21 files import EVENTS from eventBusTypes.js. Build succeeds. Test suite shows no new regressions.</done>
</task>

</tasks>

<verification>
1. `grep -rn "EventBus\.\(emit\|on\|off\)('" src/` returns 0 results
2. `npx vite build` succeeds with bundle under 500KB
3. `npx vitest run` shows 551+ passing (no new failures beyond the 3 pre-existing)
4. `src/utils/eventBusTypes.js` exists and exports frozen EVENTS object with 30+ constants
</verification>

<success_criteria>
- eventBusTypes.js is the single source of truth for all event names
- Every EventBus call in 21 files uses EVENTS.CONSTANT_NAME
- Zero raw event string literals remain
- Build passes, tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/19-infrastructure-architecture/19-01-SUMMARY.md`
</output>
