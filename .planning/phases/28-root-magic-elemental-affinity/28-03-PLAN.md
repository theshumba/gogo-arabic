---
phase: 28-root-magic-elemental-affinity
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - src/components/Magic/MagicOverlay.jsx
  - src/components/Magic/SpellMenu.jsx
  - src/components/Magic/RootDiscoveryToast.jsx
  - src/game/systems/battle/BattleEffectManager.js
  - src/game/scenes/BattleScene.js
autonomous: true

must_haves:
  truths:
    - "Player sees a 6-slot spell hotbar during battle that shows equipped spells with root letters, element color, and MP cost"
    - "Player can open a spell list UI with element filter, root letters, MP cost, and mastery level"
    - "Player sees Arabic calligraphy particle VFX when casting spells with 10 distinct element visuals"
    - "Player sees a toast notification when discovering a new root with element icon and Arabic root letters"
    - "Player can equip spells from the spell menu into hotbar slots"
  artifacts:
    - path: "src/components/Magic/MagicOverlay.jsx"
      provides: "Battle spell hotbar with 6 slots, MP bar, affinity indicator"
      exports: ["default"]
    - path: "src/components/Magic/SpellMenu.jsx"
      provides: "Full spell list with element filtering, equip/unequip, mastery indicators"
      exports: ["default"]
    - path: "src/components/Magic/RootDiscoveryToast.jsx"
      provides: "Animated toast for root discovery notifications"
      exports: ["default"]
    - path: "src/game/systems/battle/BattleEffectManager.js"
      provides: "playSpellEffect method for Arabic calligraphy particle VFX"
      contains: "playSpellEffect"
    - path: "src/game/scenes/BattleScene.js"
      provides: "RootMagicManager integration in create() and destroy()"
      contains: "RootMagicManager"
  key_links:
    - from: "src/components/Magic/MagicOverlay.jsx"
      to: "src/store/slices/magicSlice.js"
      via: "useSelector(selectEquippedSpells) for hotbar data"
      pattern: "selectEquippedSpells"
    - from: "src/components/Magic/MagicOverlay.jsx"
      to: "src/utils/eventBusTypes.js"
      via: "EventBus.emit(EVENTS.MAGIC_CAST_REQUESTED) on spell click"
      pattern: "MAGIC_CAST_REQUESTED"
    - from: "src/components/Magic/SpellMenu.jsx"
      to: "src/store/slices/magicSlice.js"
      via: "useDispatch to dispatch equipSpell/unequipSpell"
      pattern: "equipSpell|unequipSpell"
    - from: "src/game/systems/battle/BattleEffectManager.js"
      to: "src/data/rootMagic.js"
      via: "ELEMENT_INFO colors for spell VFX tinting"
      pattern: "ELEMENT_INFO"
---

<objective>
Build the React UI components for the magic system (spell hotbar, spell menu, root discovery toast) and extend BattleEffectManager with Arabic calligraphy spell VFX for all 10 elements.

Purpose: These are the visual layer that makes root magic visible and interactive to the player. The hotbar enables spell casting during battle, the spell menu enables spell management, the toast provides discovery feedback, and the VFX make spell casting feel impactful.

Output: MagicOverlay.jsx, SpellMenu.jsx, RootDiscoveryToast.jsx, modified BattleEffectManager.js, modified BattleScene.js
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-root-magic-elemental-affinity/28-RESEARCH.md
@.planning/phases/28-root-magic-elemental-affinity/28-01-SUMMARY.md

@src/store/slices/magicSlice.js (selectEquippedSpells, selectDiscoveredRoots, selectAllRootMastery, selectAffinity, selectAffinityBonuses, equipSpell, unequipSpell)
@src/store/slices/battleSlice.js (selectBattleStats for playerMP/playerMaxMP)
@src/data/rootMagic.js (ELEMENT_INFO for element colors/labels/Arabic names, ROOT_ELEMENTS, SPELL_TIERS)
@src/data/spellData.js (SPELLS, getSpellByRoot, getSpellsByElement)
@src/utils/eventBusTypes.js (MAGIC_* events)
@src/utils/eventBus.js (EventBus.emit, EventBus.on)
@src/components/Battle/BattleOverlay.jsx (pattern reference for battle React overlay)
@src/components/Battle/BattleMenu.jsx (pattern reference for battle UI component)
@src/game/systems/battle/BattleEffectManager.js (existing ELEMENT_CONFIGS, playElementalEffect)
@src/game/scenes/BattleScene.js (subsystem initialization pattern in create())
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MagicOverlay, SpellMenu, and RootDiscoveryToast React components</name>
  <files>src/components/Magic/MagicOverlay.jsx, src/components/Magic/SpellMenu.jsx, src/components/Magic/RootDiscoveryToast.jsx</files>
  <action>
**File 1: src/components/Magic/MagicOverlay.jsx**

The spell hotbar overlay shown during battle. Follow the BattleOverlay.jsx pattern for structure and styling.

```jsx
// Structure:
// - Wraps in AnimatePresence + motion.div for enter/exit
// - Shows only during player turn (useSelector state.battle.currentTurn === 'player')
// - Has 6 SpellSlot buttons in a horizontal bar
// - Shows MP bar below hotbar (current MP / max MP from battleSlice)
// - Shows affinity indicator if affinity is locked (primary element icon)
```

**SpellSlot sub-component** (inline in same file):
- If slot is empty: render gray placeholder with "Empty" text
- If slot has spell: render element-colored button with:
  - Root letters in Arabic (e.g., "ك-ت-ب") — large font
  - Element name in small text below
  - MP cost badge in corner
  - Disabled state if playerMP < spell.mpCost (opacity 0.5, cursor not-allowed)
- On click: emit `EventBus.emit(EVENTS.MAGIC_CAST_REQUESTED, { slot: index })`
- On right-click (or long press): emit `EventBus.emit(EVENTS.MAGIC_SPELL_MENU_OPEN)` to open full spell menu

**MP Bar sub-component** (inline):
- Framer Motion animated width based on playerMP / playerMaxMP
- Color gradient: green (>50%), yellow (20-50%), red (<20%)
- Text: "{playerMP}/{playerMaxMP} MP"

**Affinity Indicator** (inline):
- Show only if affinity.primary is not null
- Small icon with element color and Arabic element name
- Tooltip: "Primary: {element} (2x power)"

Use useSelector for: selectEquippedSpells, selectAffinity, state.battle.playerMP, state.battle.playerMaxMP, state.battle.currentTurn.
Use useEffect with EventBus cleanup for: MAGIC_CAST_COMPLETE (flash the cast slot), MAGIC_MP_DEPLETED (shake MP bar).

Styling: Use inline styles or CSS module (check existing component pattern — BattleOverlay uses inline styles with Tailwind-like values). Position at bottom-center of battle viewport.

**File 2: src/components/Magic/SpellMenu.jsx**

Full-screen spell management overlay (opened from hotbar right-click or pause menu).

```jsx
// Structure:
// - Full-screen overlay with dark backdrop (same as existing overlays)
// - Header: "Spells" with close button (X)
// - Element filter tabs: 10 element buttons + "All" — each colored per ELEMENT_INFO
// - Spell grid: cards for each discovered spell
// - Hotbar assignment area: 6 slots at bottom, drag-drop or click-to-assign
```

**SpellCard sub-component** (inline):
- Background tinted with element color (subtle, 10% opacity)
- Root letters in Arabic (large, centered)
- Spell name (English) and nameArabic below
- Mastery level indicator: "Lvl {level}" with XP progress bar (xp % 100 / 100)
- Form tier badge: "Form {form}" with tier name
- MP cost: "{mpCost} MP"
- Equip button: click to assign to next empty hotbar slot, or show slot picker if all full
- Locked state: if form not unlocked, show grayed with "Requires Form {form}" text

**Element filter:**
- Use ELEMENT_INFO for colors and Arabic names
- Filter discovered spells by element
- Show count badge: "Fire (3)" for 3 discovered fire spells

**Hotbar assignment:**
- Show 6 slots at bottom
- Click a slot then click a spell to assign
- Click occupied slot to unequip
- Dispatch equipSpell({ slot, rootId, form }) or unequipSpell(slot) via useDispatch

Use useSelector for: selectDiscoveredRoots, selectAllRootMastery, selectEquippedSpells.
Import: getSpellsByElement, SPELLS from spellData.js, ELEMENT_INFO from rootMagic.js.

Emit EVENTS.MAGIC_SPELL_MENU_CLOSE on close (so Phaser can unfreeze).
Emit EVENTS.PLAYER_FREEZE on open, EVENTS.PLAYER_UNFREEZE on close.

**File 3: src/components/Magic/RootDiscoveryToast.jsx**

A toast notification component that listens for MAGIC_ROOT_DISCOVERED events.

```jsx
// Structure:
// - Listens to EventBus MAGIC_ROOT_DISCOVERED
// - Queues discoveries (in case multiple fire in quick succession)
// - Shows one at a time with Framer Motion slideDown + fadeOut animation
// - Each toast: element-colored left border, Arabic root letters, element name, "Root Discovered!" text
// - Auto-dismiss after 3 seconds with exit animation
```

Use useState for toast queue and current toast.
Use useEffect with EventBus.on(EVENTS.MAGIC_ROOT_DISCOVERED, ...) and cleanup.
Use Framer Motion AnimatePresence for enter/exit.

Position: top-center of viewport, z-index above game but below modals.

Listen also for EVENTS.MAGIC_ROOT_LEVEL_UP and EVENTS.MAGIC_FORM_UNLOCKED to show "Level Up!" and "Form Unlocked!" toasts respectively.

All three components should be rendered inside the main App.jsx or GameLayout — check existing overlay rendering pattern (where BattleOverlay is rendered) and add MagicOverlay and RootDiscoveryToast at the same level. SpellMenu renders conditionally based on a show state toggled by MAGIC_SPELL_MENU_OPEN/CLOSE events.
  </action>
  <verify>
Run `npx vite build` to confirm no JSX compilation errors.
Verify all three components export default: grep for "export default" in each file.
Verify EventBus imports are correct: grep for "EVENTS.MAGIC_" in all three files.
  </verify>
  <done>MagicOverlay shows 6-slot hotbar with MP bar during battle. SpellMenu provides full spell list with element filters and hotbar assignment. RootDiscoveryToast shows animated notifications for root discoveries and level-ups. All components use existing Redux selectors and EventBus patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BattleEffectManager with spell VFX and wire BattleScene</name>
  <files>src/game/systems/battle/BattleEffectManager.js, src/game/scenes/BattleScene.js</files>
  <action>
**File 1: src/game/systems/battle/BattleEffectManager.js**

Add a `playSpellEffect(element, rootId, targetIndex)` method to the existing BattleEffectManager class.

This method creates Arabic calligraphy particle VFX for spell casting:

1. Look up element color from ELEMENT_INFO (import from rootMagic.js)
2. Get target sprite position using the existing sprite reference pattern (same as playElementalEffect)
3. Create particle burst at target position using existing ELEMENT_CONFIGS for the element's particle behavior (tint, speed, lifespan, scale, gravityY, count)
4. Create Arabic calligraphy text overlay:
   - Use `this.scene.add.text(x, y - 50, rootId, { fontSize: '32px', fontFamily: 'Amiri, serif', color: hexColor, stroke: '#000000', strokeThickness: 2 })`
   - Set origin to center: `.setOrigin(0.5)`
   - Animate with tween: float up 50px over 800ms, fade to 0 alpha, Cubic.easeOut, destroy on complete
5. If the element has screenFlash in ELEMENT_CONFIGS, trigger screen flash using existing pattern
6. Play SFX via EventBus.emit(EVENTS.SFX_CORRECT) for successful cast (reuse existing SFX)

Also add `playComboEffect(combo, targetX, targetY)` method:
1. Create larger particle burst with combo.vfxType color
2. Show combo name text in Arabic (combo.nameArabic) floating up
3. Screen flash with combined element colors
4. The combo VFX should be noticeably more dramatic than single spell VFX (2x particle count, 1.5x duration)

Import ELEMENT_INFO from `../../../data/rootMagic.js`.

Do NOT modify existing methods (playElementalEffect, playHitEffect, etc.). Only ADD new methods.

**File 2: src/game/scenes/BattleScene.js**

Wire the magic components into BattleScene:

1. Add EventBus listener for EVENTS.MAGIC_VFX_START in create():
```
EventBus.on(EVENTS.MAGIC_VFX_START, ({ element, rootId, targetIndex }) => {
  this.effects.playSpellEffect(element, rootId, targetIndex);
});
```

2. Add EventBus listener for EVENTS.MAGIC_COMBO_TRIGGERED:
```
EventBus.on(EVENTS.MAGIC_COMBO_TRIGGERED, (combo) => {
  const targetSprite = this.sprites.getEnemy(0); // Default to first enemy
  this.effects.playComboEffect(combo, targetSprite?.x || 400, targetSprite?.y || 300);
});
```

3. In the destroy() or shutdown() method, clean up the magic event listeners (use the same pattern as existing BATTLE_* listener cleanup).

4. Ensure the BattleScene does NOT directly import RootMagicManager — that's handled by BattleStateMachine (Plan 02). BattleScene only needs to listen for VFX events.

Do NOT modify the existing preload, init, or create flow beyond adding the new listeners. Preserve all existing subsystem initialization.
  </action>
  <verify>
Run `npx vite build` to confirm no errors.
Grep BattleEffectManager.js for 'playSpellEffect' — should exist as a method.
Grep BattleEffectManager.js for 'playComboEffect' — should exist as a method.
Grep BattleScene.js for 'MAGIC_VFX_START' — should appear in EventBus.on listener.
  </verify>
  <done>BattleEffectManager has playSpellEffect (Arabic calligraphy particles + element VFX for 10 elements) and playComboEffect (enhanced VFX for combos). BattleScene wired to respond to MAGIC_VFX_START and MAGIC_COMBO_TRIGGERED events. No regressions to existing VFX.</done>
</task>

</tasks>

<verification>
- `npx vite build` succeeds
- MagicOverlay.jsx renders 6-slot hotbar with MP bar
- SpellMenu.jsx has element filter tabs for all 10 elements + "All"
- RootDiscoveryToast.jsx listens for 3 magic events (discovered, level-up, form-unlocked)
- BattleEffectManager.js has playSpellEffect and playComboEffect methods
- BattleScene.js has MAGIC_VFX_START and MAGIC_COMBO_TRIGGERED listeners
- All new components follow existing codebase patterns (inline styles, Framer Motion, EventBus cleanup)
</verification>

<success_criteria>
Visual layer complete: player sees spell hotbar in battle, can browse spells in menu, sees calligraphy VFX on spell cast, and gets toast notifications on root discovery. All 10 element types have distinct visual treatments.
</success_criteria>

<output>
After completion, create `.planning/phases/28-root-magic-elemental-affinity/28-03-SUMMARY.md`
</output>
