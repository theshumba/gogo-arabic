---
phase: 28-root-magic-elemental-affinity
plan: 04
type: tdd
wave: 3
depends_on: ["28-01", "28-02", "28-03"]
files_modified:
  - src/store/slices/__tests__/magicSlice.test.js
  - src/store/middleware/__tests__/rootFsrsSyncMiddleware.test.js
  - src/game/systems/magic/__tests__/RootMagicManager.test.js
  - src/data/__tests__/spellData.test.js
  - src/data/__tests__/elementCombos.test.js
autonomous: true

must_haves:
  truths:
    - "All magic system tests pass (target: 60+ new tests)"
    - "magicSlice reducers correctly handle root discovery, mastery tracking, affinity locking, spell equipping"
    - "rootFsrsSyncMiddleware correctly syncs FSRS reviews to root XP and root level-ups to FSRS card suggestions"
    - "RootMagicManager correctly calculates spell damage with mastery, affinity, and grammar multipliers"
    - "spellData has exactly 50 spells and elementCombos has exactly 20 combos with valid structure"
    - "All 647 existing tests still pass (no regressions)"
    - "Build succeeds and bundle remains under 500KB"
  artifacts:
    - path: "src/store/slices/__tests__/magicSlice.test.js"
      provides: "Unit tests for all magicSlice reducers and selectors"
      min_lines: 200
    - path: "src/store/middleware/__tests__/rootFsrsSyncMiddleware.test.js"
      provides: "Tests for bidirectional FSRS-root sync"
      min_lines: 100
    - path: "src/game/systems/magic/__tests__/RootMagicManager.test.js"
      provides: "Tests for spell casting and damage calculation"
      min_lines: 100
    - path: "src/data/__tests__/spellData.test.js"
      provides: "Data integrity tests for spell definitions"
      min_lines: 40
    - path: "src/data/__tests__/elementCombos.test.js"
      provides: "Data integrity tests for combo definitions"
      min_lines: 40
  key_links:
    - from: "src/store/slices/__tests__/magicSlice.test.js"
      to: "src/store/slices/magicSlice.js"
      via: "imports all reducers and selectors for testing"
      pattern: "import.*magicSlice"
    - from: "src/store/middleware/__tests__/rootFsrsSyncMiddleware.test.js"
      to: "src/store/middleware/rootFsrsSyncMiddleware.js"
      via: "tests middleware dispatch chain"
      pattern: "rootFsrsSyncMiddleware"
---

<objective>
Write comprehensive tests for the magic system — magicSlice reducers/selectors, rootFsrsSyncMiddleware bidirectional sync, RootMagicManager damage calculation, and data integrity for spells and combos. Then verify full regression (647+ existing tests pass, build succeeds, bundle under 500KB).

Purpose: Phase 28 adds ~15 new files and ~3K LOC. Tests prove correctness and prevent regressions before shipping. The TDD approach ensures each component works as specified.

Output: 5 test files with 60+ tests total, all passing alongside existing 647 tests.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-root-magic-elemental-affinity/28-01-SUMMARY.md
@.planning/phases/28-root-magic-elemental-affinity/28-02-SUMMARY.md
@.planning/phases/28-root-magic-elemental-affinity/28-03-SUMMARY.md

@src/store/slices/magicSlice.js
@src/store/middleware/rootFsrsSyncMiddleware.js
@src/game/systems/magic/RootMagicManager.js
@src/game/systems/magic/AffinityTracker.js
@src/data/spellData.js
@src/data/elementCombos.js
@src/data/rootMagic.js (ROOT_ELEMENTS, SPELL_TIERS, ELEMENT_CHART, ELEMENT_INFO)
@src/store/slices/battleSlice.js (for mock integration)
@src/store/slices/vocabularySlice.js (for mock FSRS sync)
@src/test/testUtils.jsx (existing test utility patterns)
@src/game/systems/__tests__/mocks/sceneMock.js (Phaser scene mock for RootMagicManager)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write magicSlice and data integrity tests (RED then GREEN)</name>
  <files>src/store/slices/__tests__/magicSlice.test.js, src/data/__tests__/spellData.test.js, src/data/__tests__/elementCombos.test.js</files>
  <action>
**File 1: src/store/slices/__tests__/magicSlice.test.js**

Follow the existing test patterns in the __tests__ directory. Use vitest (import { describe, it, expect, beforeEach } from 'vitest').

Create a reducer function by importing magicSlice's reducer default export and action creators.

**Test groups (30+ tests):**

`describe('magicSlice')`:

`describe('discoverRoot')`:
- it('adds root to discoveredRoots array')
- it('initializes rootMastery entry with level 1, xp 0, formsUnlocked: ["I"]')
- it('sets correct element from payload')
- it('skips duplicate root discovery (idempotent)')
- it('handles multiple roots for same element')

`describe('recordRootUse')`:
- it('increments timesUsed')
- it('adds 15 XP for perfect accuracy (>=0.95)')
- it('adds 10 XP for good accuracy (>=0.7)')
- it('adds 5 XP for partial accuracy (<0.7)')
- it('levels up from 1 to 2 at 100 XP')
- it('handles level 3 at 200 XP')
- it('does nothing for undiscovered root')

`describe('recordAffinityChoice')`:
- it('pushes choice to discoveryChoices array')
- it('increments choiceCount')
- it('does NOT lock affinity before 50 choices')
- it('locks primary and secondary affinity at exactly 50 choices')
- it('selects highest weighted element as primary')
- it('selects second highest as secondary')
- it('does not re-lock after already locked (additional choices are recorded but affinity stays)')

`describe('equipSpell')`:
- it('equips spell to specified slot (0-5)')
- it('includes rootId, form, element, and mpCost in slot data')
- it('does nothing for undiscovered root')

`describe('unequipSpell')`:
- it('sets slot to null')

`describe('clearBattleState')`:
- it('resets activeCombos and lastCastTimestamp')

`describe('selectors')`:
- it('selectDiscoveredRoots returns discoveredRoots array')
- it('selectRootMastery returns mastery for specific root')
- it('selectAllRootMastery returns full mastery object')
- it('selectAffinity returns affinity object')
- it('selectEquippedSpells returns 6-element array')
- it('selectAffinityBonuses returns multipliers when affinity locked')
- it('selectAffinityBonuses returns nulls when affinity not locked')

**Test pattern:** Use reducer(initialState, action) pattern directly, not the store. This avoids IndexedDB persistence issues in tests.

```javascript
import reducer, { discoverRoot, recordRootUse, ... } from '../magicSlice.js';

const initialState = reducer(undefined, { type: '@@INIT' });

it('adds root to discoveredRoots array', () => {
  const state = reducer(initialState, discoverRoot({ rootId: 'ك-ت-ب', element: 'knowledge' }));
  expect(state.discoveredRoots).toContain('ك-ت-ب');
});
```

**File 2: src/data/__tests__/spellData.test.js**

Data integrity tests (10 tests):

- it('exports exactly 50 spells')
- it('every spell has required fields: id, rootId, element, form, name, nameArabic, baseDamage, mpCost')
- it('every spell rootId exists in ROOT_ELEMENTS')
- it('every spell element matches ROOT_ELEMENTS[rootId]')
- it('has exactly 5 spells per element (10 elements)')
- it('all spell IDs are unique')
- it('getSpellByRoot returns correct spell for known root')
- it('getSpellByRoot returns null for unknown root')
- it('getSpellsByElement returns 5 spells for each element')
- it('mpCost matches SPELL_TIERS for the spell form')

```javascript
import { SPELLS, getSpellByRoot, getSpellsByElement } from '../spellData.js';
import { ROOT_ELEMENTS, SPELL_TIERS } from '../rootMagic.js';
```

**File 3: src/data/__tests__/elementCombos.test.js**

Data integrity tests (8 tests):

- it('exports exactly 20 combos')
- it('every combo has required fields: id, elements, name, nameArabic, damageMultiplier, rootRequirement')
- it('every combo elements array has exactly 2 elements')
- it('every combo element exists in ELEMENT_INFO')
- it('all combo IDs are unique')
- it('checkCombo returns combo for valid element pair with sufficient levels')
- it('checkCombo returns null for insufficient root levels')
- it('checkCombo returns null for element pair with no combo')

**RED phase:** Write all tests first. They should fail because they test real behavior.
**GREEN phase:** If any test fails due to actual bugs in the implementation (not test errors), fix the source file.
**REFACTOR:** Clean up any test duplication.
  </action>
  <verify>
Run `npx vitest run src/store/slices/__tests__/magicSlice.test.js src/data/__tests__/spellData.test.js src/data/__tests__/elementCombos.test.js` — all tests pass.
Count: at least 48 tests across the 3 files.
  </verify>
  <done>magicSlice has 30+ passing tests covering all reducers and selectors. spellData has 10 passing data integrity tests. elementCombos has 8 passing data integrity tests.</done>
</task>

<task type="auto">
  <name>Task 2: Write middleware and manager tests, then full regression</name>
  <files>src/store/middleware/__tests__/rootFsrsSyncMiddleware.test.js, src/game/systems/magic/__tests__/RootMagicManager.test.js</files>
  <action>
**File 1: src/store/middleware/__tests__/rootFsrsSyncMiddleware.test.js**

Test the bidirectional sync middleware. Use vi.mock() for store and slice imports.

**Test pattern:** Create a mock store with getState/dispatch, pass action through middleware, verify dispatched actions.

```javascript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { rootFsrsSyncMiddleware } from '../rootFsrsSyncMiddleware.js';
```

Mock `getWordRoot` from rootsData.js and `getRootWords` from rootsData.js.

**Tests (12+ tests):**

`describe('rootFsrsSyncMiddleware')`:

`describe('FSRS -> Root sync')`:
- it('increments root XP when FSRS card reviewed for word with known root')
- it('uses 0.8 accuracy for FSRS rating >= 3 (Good/Easy)')
- it('uses 0.5 accuracy for FSRS rating 2 (Hard)')
- it('ignores FSRS reviews for words with no matching root')
- it('ignores FSRS reviews for undiscovered roots')
- it('passes through non-vocabulary actions unchanged')

`describe('Root -> FSRS sync')`:
- it('suggests derived words when root levels up')
- it('limits to 3 new FSRS cards per level-up')
- it('does not add FSRS cards for words already in vocabulary')
- it('emits MAGIC_ROOT_LEVEL_UP event on level-up')

`describe('Form unlock')`:
- it('unlocks Form II at root level 3')
- it('unlocks Form III at root level 5')
- it('does not unlock forms if already unlocked')

**Middleware mock pattern:**
```javascript
const createMockStore = (state) => ({
  getState: () => state,
  dispatch: vi.fn(),
});

const next = vi.fn((action) => action);

it('increments root XP when FSRS card reviewed', () => {
  const store = createMockStore({
    magic: { discoveredRoots: ['كتب'], rootMastery: { 'كتب': { level: 1, xp: 50 } } },
    vocabulary: { fsrsCards: {} },
  });
  const middleware = rootFsrsSyncMiddleware(store)(next);

  middleware({ type: 'vocabulary/updateFsrsCard', payload: { wordId: 'kitaab', card: {}, log: { rating: 3 } } });

  expect(store.dispatch).toHaveBeenCalledWith(expect.objectContaining({
    type: 'magic/recordRootUse',
  }));
});
```

Note: Mock getWordRoot to return `{ root: 'كتب' }` for known test words.

**File 2: src/game/systems/magic/__tests__/RootMagicManager.test.js**

Test RootMagicManager's damage calculation and spell casting logic. Use the sceneMock from `src/game/systems/__tests__/mocks/sceneMock.js`.

Mock the Redux store with vi.mock():

```javascript
vi.mock('../../../../store/store.js', () => ({
  store: {
    getState: vi.fn(),
    dispatch: vi.fn(),
  },
}));
```

**Tests (12+ tests):**

`describe('RootMagicManager')`:

`describe('_calculateSpellDamage')`:
- it('returns base damage * form multiplier for level 1 root, no affinity, perfect accuracy')
- it('applies mastery multiplier (level 5 = 1.5x)')
- it('applies primary affinity multiplier (2.0x)')
- it('applies secondary affinity multiplier (1.5x)')
- it('applies grammar accuracy multiplier: perfect (>=0.95) = 1.2x')
- it('applies grammar accuracy multiplier: good (>=0.7) = 1.0x')
- it('applies grammar accuracy multiplier: partial (<0.7) = 0.5x')
- it('returns minimum 1 damage')
- it('stacks mastery + affinity + grammar multipliers correctly')

`describe('castSpell')`:
- it('returns false and emits MP_DEPLETED if not enough MP')
- it('dispatches spendMP with correct cost')
- it('dispatches recordRootUse after spell cast')

Note: Since castSpell uses scene.time.delayedCall, mock the scene timer to execute callbacks immediately in tests.

**After both test files pass, run full regression:**

```bash
npx vitest run
npx vite build
```

- ALL tests (647 existing + 60+ new) must pass
- Build must succeed
- Check bundle size with `npx vite build 2>&1 | grep 'gzip'` — must be under 500KB

If any existing tests break, fix the source code (not the tests) to restore backward compatibility.
  </action>
  <verify>
Run `npx vitest run` — all tests pass (707+ total: 647 existing + 60+ new).
Run `npx vite build` — succeeds, bundle under 500KB.
Count new test files: 5 files in the magic system.
  </verify>
  <done>60+ new tests pass covering magicSlice, rootFsrsSyncMiddleware, RootMagicManager, spellData, and elementCombos. All 647 existing tests still pass (zero regressions). Build succeeds with bundle under 500KB. Total test count: 707+.</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes with 707+ tests (647 baseline + 60+ new)
- `npx vite build` succeeds with bundle under 500KB
- magicSlice: 30+ tests covering all 9 reducers and 8 selectors
- rootFsrsSyncMiddleware: 12+ tests covering both sync directions and form unlocking
- RootMagicManager: 12+ tests covering damage calculation and casting
- spellData: 10 tests covering data integrity
- elementCombos: 8 tests covering data integrity and combo lookup
- No regressions in any existing test file
</verification>

<success_criteria>
All magic system behavior is verified by automated tests. The test suite proves: roots can be discovered, spells cast with correct damage, FSRS syncs bidirectionally, affinity locks at threshold, combos detect correctly, and data files have correct structure. Zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/28-root-magic-elemental-affinity/28-04-SUMMARY.md`
</output>
