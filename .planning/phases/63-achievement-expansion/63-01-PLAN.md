---
phase: 63-achievement-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/achievements.js
  - src/data/__tests__/achievements.test.js
autonomous: true
requirements: [ACH-01, ACH-02]

must_haves:
  truths:
    - "Every achievement entry has a tier field with value Bronze, Silver, Gold, or Legendary"
    - "ACHIEVEMENTS array contains 250+ entries total"
    - "SKILL_TREE, QUIZ, and CEFR categories exist in ACHIEVEMENT_CATEGORIES"
    - "New v12.0 achievements cover skill_tree_nodes, skill_tree_complete, quiz_type_streak, cefr_level_reached, and placement_complete requirement types"
  artifacts:
    - path: "src/data/achievements.js"
      provides: "TIER/TIER_COLORS/RARITY_TO_TIER constants + tier field on all entries + ~10 new v12.0 entries + 3 new ACHIEVEMENT_CATEGORIES"
      exports: ["TIER", "TIER_COLORS", "RARITY_TO_TIER", "ACHIEVEMENT_CATEGORIES", "ACHIEVEMENTS"]
    - path: "src/data/__tests__/achievements.test.js"
      provides: "Unit tests for tier field, count >= 250, valid tier values, new categories"
  key_links:
    - from: "src/data/achievements.js"
      to: "src/store/middleware/achievementMiddleware.js"
      via: "ACHIEVEMENTS import"
      pattern: "import.*ACHIEVEMENTS.*from.*achievements"
---

<objective>
Add TIER constants, RARITY_TO_TIER mapping, tier field to all 252 existing achievement entries, 3 new ACHIEVEMENT_CATEGORIES (SKILL_TREE, QUIZ, CEFR), and ~10 new v12.0 achievement entries covering skill tree, quiz streak, CEFR level, and placement completion requirement types.

Purpose: Satisfies ACH-01 (250+ achievements across 15+ categories) and ACH-02 (4-tier system with Bronze/Silver/Gold/Legendary) at the data layer. All existing achievements gain a tier field and new v12.0 event sources get dedicated achievements.

Output: Updated achievements.js with tier infrastructure + new entries; new achievements.test.js with data integrity tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-achievement-expansion/63-RESEARCH.md

<interfaces>
<!-- From src/data/achievements.js (lines 8-45) — current exports -->
export const ACHIEVEMENT_CATEGORIES = {
  VOCABULARY: 'vocabulary', ALPHABET: 'alphabet', QUESTS: 'quests',
  STREAKS: 'streaks', XP: 'xp', EXPLORATION: 'exploration',
  REVIEW: 'review', ECONOMY: 'economy', SPECIAL: 'special',
  GRAMMAR: 'grammar', COMBAT: 'combat', CRAFTING: 'crafting',
  SOCIAL: 'social', COLLECTING: 'collecting', DAILY: 'daily',
  LEARNING_PATH: 'learning_path', ROOT_MAGIC: 'root_magic',
  CULTURE: 'culture', HIDDEN: 'hidden', MILESTONE: 'milestone',
};

export const RARITY = {
  COMMON: 'common', UNCOMMON: 'uncommon', RARE: 'rare',
  EPIC: 'epic', LEGENDARY: 'legendary',
};

export const RARITY_COLORS = {
  common: '#f4fefa', uncommon: '#2ecc71', rare: '#66d7ee',
  epic: '#c084fc', legendary: '#FFD700',
};

<!-- Each achievement entry shape: -->
{ id, name, description, category, icon, requirement: { type, threshold?, ... }, xpReward, rarity }

<!-- Helper exports: -->
export function getAchievementById(id) { ... }
export function getAchievementsByCategory(category) { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TIER constants, RARITY_TO_TIER mapping, 3 new categories, tier field on all 252 entries, and ~10 new v12.0 achievements</name>
  <files>src/data/achievements.js, src/data/__tests__/achievements.test.js</files>
  <read_first>
    - src/data/achievements.js (current 252 entries, ACHIEVEMENT_CATEGORIES, RARITY constants, RARITY_COLORS)
    - src/store/middleware/achievementMiddleware.js (isAchievementMet switch cases — confirms which requirement types exist)
    - src/data/skillTrees.js (SKILL_TREES export — 6 tree IDs: reading, writing, listening, speaking, grammar, culture)
    - src/store/slices/placementSlice.js (hasCompleted field shape)
    - src/store/slices/cefrProgressSlice.js (currentLevel field shape)
  </read_first>
  <behavior>
    - Test 1: ACHIEVEMENTS.length >= 260 (252 existing + ~10 new)
    - Test 2: Every entry in ACHIEVEMENTS has a `tier` field
    - Test 3: Every `tier` value is one of ['Bronze', 'Silver', 'Gold', 'Legendary']
    - Test 4: TIER constant has exactly 4 keys: BRONZE, SILVER, GOLD, LEGENDARY
    - Test 5: TIER_COLORS has entries for Bronze, Silver, Gold, Legendary
    - Test 6: RARITY_TO_TIER maps all 5 rarity values to valid tiers (common->Bronze, uncommon->Bronze, rare->Silver, epic->Gold, legendary->Legendary)
    - Test 7: ACHIEVEMENT_CATEGORIES contains SKILL_TREE, QUIZ, and CEFR keys
    - Test 8: At least 1 achievement has requirement type 'skill_tree_nodes'
    - Test 9: At least 1 achievement has requirement type 'skill_tree_complete'
    - Test 10: At least 1 achievement has requirement type 'quiz_type_streak'
    - Test 11: At least 1 achievement has requirement type 'cefr_level_reached'
    - Test 12: At least 1 achievement has requirement type 'placement_complete'
    - Test 13: All achievement IDs are unique (no duplicates)
    - Test 14: Every achievement has required fields: id, name, description, category, icon, requirement, xpReward, rarity, tier
  </behavior>
  <action>
    **Step 1 — Write tests first** in `src/data/__tests__/achievements.test.js`:
    Import ACHIEVEMENTS, ACHIEVEMENT_CATEGORIES, RARITY, TIER, TIER_COLORS, RARITY_TO_TIER from '../../data/achievements.js'.
    Write all 14 tests described in behavior. Run tests — they MUST fail (TIER/TIER_COLORS/RARITY_TO_TIER don't exist, tier field missing, new categories missing).

    **Step 2 — Add TIER, TIER_COLORS, RARITY_TO_TIER constants** to achievements.js after RARITY_COLORS:
    ```javascript
    export const TIER = {
      BRONZE: 'Bronze',
      SILVER: 'Silver',
      GOLD: 'Gold',
      LEGENDARY: 'Legendary',
    };

    export const TIER_COLORS = {
      Bronze: '#cd7f32',
      Silver: '#c0c0c0',
      Gold: '#FFD700',
      Legendary: '#a855f7',
    };

    export const RARITY_TO_TIER = {
      common: TIER.BRONZE,
      uncommon: TIER.BRONZE,
      rare: TIER.SILVER,
      epic: TIER.GOLD,
      legendary: TIER.LEGENDARY,
    };
    ```

    **Step 3 — Add 3 new ACHIEVEMENT_CATEGORIES** entries at end of existing object:
    ```javascript
    SKILL_TREE: 'skill_tree',
    QUIZ: 'quiz',
    CEFR: 'cefr',
    ```

    **Step 4 — Add `tier` field to ALL 252 existing entries.** For each existing entry, add `tier: RARITY_TO_TIER[rarity]` as the last field. This is mechanical: every `rarity: RARITY.COMMON` gets `tier: TIER.BRONZE`, every `rarity: RARITY.UNCOMMON` gets `tier: TIER.BRONZE`, every `rarity: RARITY.RARE` gets `tier: TIER.SILVER`, every `rarity: RARITY.EPIC` gets `tier: TIER.GOLD`, every `rarity: RARITY.LEGENDARY` gets `tier: TIER.LEGENDARY`.

    **Step 5 — Add ~12 new v12.0 achievements** at end of ACHIEVEMENTS array (before the closing `];`), organized by category:

    SKILL_TREE category (4 entries):
    - id:'skill_tree_first_node', name:'First Skill Unlocked', desc:'Unlock your first skill tree node', requirement:{type:'skill_tree_nodes', threshold:1}, xpReward:50, rarity:RARITY.COMMON, tier:TIER.BRONZE
    - id:'skill_tree_10_nodes', name:'Branching Out', desc:'Unlock 10 skill tree nodes', requirement:{type:'skill_tree_nodes', threshold:10}, xpReward:200, rarity:RARITY.UNCOMMON, tier:TIER.BRONZE
    - id:'skill_tree_50_nodes', name:'Deep Roots', desc:'Unlock 50 skill tree nodes', requirement:{type:'skill_tree_nodes', threshold:50}, xpReward:500, rarity:RARITY.RARE, tier:TIER.SILVER
    - id:'skill_tree_reading_complete', name:'Reading Master', desc:'Complete the Reading skill tree', requirement:{type:'skill_tree_complete', treeId:'reading'}, xpReward:1000, rarity:RARITY.EPIC, tier:TIER.GOLD

    QUIZ category (3 entries):
    - id:'quiz_streak_3', name:'Hat Trick', desc:'Get 3 perfect quizzes in a row with one quiz type', requirement:{type:'quiz_type_streak', quizType:'ar-to-en', threshold:3}, xpReward:100, rarity:RARITY.UNCOMMON, tier:TIER.BRONZE
    - id:'quiz_streak_5', name:'Quiz Ace', desc:'Get 5 perfect quizzes in a row with one quiz type', requirement:{type:'quiz_type_streak', quizType:'ar-to-en', threshold:5}, xpReward:300, rarity:RARITY.RARE, tier:TIER.SILVER
    - id:'quiz_streak_10', name:'Unstoppable', desc:'Get 10 perfect quizzes in a row with one quiz type', requirement:{type:'quiz_type_streak', quizType:'ar-to-en', threshold:10}, xpReward:750, rarity:RARITY.EPIC, tier:TIER.GOLD

    CEFR category (4 entries):
    - id:'cefr_placement_done', name:'Placed!', desc:'Complete the CEFR placement test', requirement:{type:'placement_complete'}, xpReward:100, rarity:RARITY.COMMON, tier:TIER.BRONZE
    - id:'cefr_reached_a2', name:'A2 Level', desc:'Reach CEFR A2 level', requirement:{type:'cefr_level_reached', level:'A2'}, xpReward:300, rarity:RARITY.UNCOMMON, tier:TIER.BRONZE
    - id:'cefr_reached_b1', name:'B1 Breakthrough', desc:'Reach CEFR B1 level', requirement:{type:'cefr_level_reached', level:'B1'}, xpReward:750, rarity:RARITY.RARE, tier:TIER.SILVER
    - id:'cefr_reached_b2', name:'B2 Vantage', desc:'Reach CEFR B2 level', requirement:{type:'cefr_level_reached', level:'B2'}, xpReward:1500, rarity:RARITY.EPIC, tier:TIER.GOLD

    Each entry follows the exact same schema as existing entries: `{ id, name, description, category, icon, requirement, xpReward, rarity, tier }`. Use trophy/tree/quiz/target emojis for icons.

    **Step 6 — Run tests** — all 14 MUST pass.
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/achievements.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/data/achievements.js contains `export const TIER = {`
    - src/data/achievements.js contains `export const TIER_COLORS = {`
    - src/data/achievements.js contains `export const RARITY_TO_TIER = {`
    - src/data/achievements.js contains `SKILL_TREE: 'skill_tree'`
    - src/data/achievements.js contains `QUIZ: 'quiz'`
    - src/data/achievements.js contains `CEFR: 'cefr'`
    - src/data/achievements.js contains `id: 'skill_tree_first_node'`
    - src/data/achievements.js contains `id: 'cefr_placement_done'`
    - src/data/achievements.js contains `id: 'quiz_streak_3'`
    - Every achievement entry contains `tier:` field (grep count of `tier:` in ACHIEVEMENTS array matches entry count)
    - src/data/__tests__/achievements.test.js exits 0
    - src/data/__tests__/achievements.test.js contains `ACHIEVEMENTS.length` assertion
    - src/data/__tests__/achievements.test.js contains `tier` assertion
  </acceptance_criteria>
  <done>
    ACHIEVEMENTS array has 260+ entries (252 existing + ~12 new v12.0). Every entry has a `tier` field with value Bronze/Silver/Gold/Legendary. TIER, TIER_COLORS, RARITY_TO_TIER constants exported. SKILL_TREE, QUIZ, CEFR categories added. All 14 data integrity tests pass.
  </done>
</task>

</tasks>

<verification>
npx vitest run src/data/__tests__/achievements.test.js
</verification>

<success_criteria>
- achievements.js exports TIER, TIER_COLORS, RARITY_TO_TIER constants
- All 252 existing + ~12 new entries have tier field
- 3 new ACHIEVEMENT_CATEGORIES (SKILL_TREE, QUIZ, CEFR) added
- New requirement types (skill_tree_nodes, skill_tree_complete, quiz_type_streak, cefr_level_reached, placement_complete) present in at least one achievement each
- 14 data integrity tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/63-achievement-expansion/63-01-SUMMARY.md`
</output>
