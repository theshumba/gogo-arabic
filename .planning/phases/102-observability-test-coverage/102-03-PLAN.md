---
phase: 102-observability-test-coverage
plan: 03
type: execute
wave: 2
depends_on: [102-02]
files_modified:
  - src/store/middleware/telemetryMiddleware.js
  - src/utils/eventBusTypes.js
  - src/store/middleware/__tests__/telemetryMiddleware.test.js
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "All 8 canonical learning-loop events emit with stable names (quest.started, quest.completed, fsrs.reviewed, zone.entered, lesson.completed, teaching.started, teaching.completed, dashboard.viewed)"
    - "Event property extractors return ONLY enum/ID fields — no learner-facing Arabic text reaches PostHog"
    - "Phaser-side zone.entered events relay through EventBus into telemetry without touching Phaser scenes directly"
    - "Telemetry never crashes the app — extractor errors are caught and (in DEV) console.warn'd"
  artifacts:
    - path: "src/store/middleware/telemetryMiddleware.js"
      provides: "Full ACTION_EVENT_MAP + PROP_EXTRACTORS + EventBus relay"
      contains: "8 canonical event names + PII-safe property extractors"
    - path: "src/utils/eventBusTypes.js"
      provides: "ZONE_ENTERED event registration (if not already present)"
  key_links:
    - from: "Redux actions (quest/completeQuest, etc.)"
      to: "posthog.capture('quest.completed', enumProps)"
      via: "telemetryMiddleware ACTION_EVENT_MAP + PROP_EXTRACTORS"
      pattern: "ACTION_EVENT_MAP|PROP_EXTRACTORS"
    - from: "Phaser WorldScene zone-entry"
      to: "posthog.capture('zone.entered', { zone_id })"
      via: "EventBus -> initTelemetryEventBusRelay"
      pattern: "EVENTS.ZONE_ENTERED|zone.entered"
---

<objective>
Fill the telemetryMiddleware skeleton from Plan 02 with the canonical 8-event vocabulary. Every event name follows `domain.action`. Every property extractor returns ONLY enum/ID fields (per RESEARCH PII discipline — free text is FORBIDDEN). Wire the Phaser EventBus relay for `zone.entered` since zone transitions originate in Phaser, not Redux.

Purpose: This is the OBS-02 deliverable — the single source of truth for what gets captured and what gets blocked at the PII boundary.

Output: Plan 01's telemetryMiddleware.test.js turns fully GREEN; PostHog Live Events shows 8 distinct event types when a learner plays through the golden path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-observability-test-coverage/102-02-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-RESEARCH.md

<interfaces>
<!-- The RED test from Plan 01 — implementation must satisfy these assertions -->
From src/store/middleware/__tests__/telemetryMiddleware.test.js (Plan 01):
- All 8 events: quest.started, quest.completed, fsrs.reviewed, zone.entered, lesson.completed, teaching.started, teaching.completed, dashboard.viewed
- Property extractors return ONLY enums/IDs; spies assert no free-text keys (questTitle, arabicText, npcName, lessonTitle) appear in any capture payload
- Opt-out gate: when settings.telemetryOptOut === true, capture spy is called 0 times
- Error handling: throwing extractor does NOT propagate (telemetry never crashes the app)

<!-- Existing Redux action types to map (verify exact strings during implementation) -->
Likely action types based on existing slices (verify in code):
- 'quest/startQuest' or 'questSlice/startQuest' -> 'quest.started'
- 'quest/completeQuest' -> 'quest.completed'
- 'vocabulary/reviewCard' or 'fsrsSlice/reviewCard' -> 'fsrs.reviewed'
- 'grammar/completeLesson' -> 'lesson.completed'
- 'teaching/startSession' -> 'teaching.started'
- 'teaching/endSession' -> 'teaching.completed'
- 'dashboard/viewDashboard' (may need new action OR derive from route change) -> 'dashboard.viewed'
- (zone.entered originates in Phaser via EventBus, NOT a Redux action)

<!-- Existing eventBusTypes -->
From src/utils/eventBusTypes.js:
```javascript
// Centralized EventBus event name registry. ADD: ZONE_ENTERED if not already present.
export const EVENTS = { /* ...existing entries... */ };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit existing Redux action types and EventBus event names for canonical event mapping</name>
  <files>src/store/middleware/__tests__/telemetryMiddleware.test.js</files>
  <read_first>
    - src/store/slices/questSlice.js (find exact action type strings for startQuest, completeQuest)
    - src/store/slices/vocabularySlice.js OR src/store/slices/fsrsSlice.js (find fsrs review action type)
    - src/store/slices/grammarSlice.js (find completeLesson action type)
    - src/store/slices/onboardingSlice.js / src/store/slices/dialogueSlice.js (find teaching session start/end actions)
    - src/store/slices/dashboardSlice.js OR src/components/Dashboard/ (find dashboard-view trigger)
    - src/utils/eventBusTypes.js (confirm ZONE_ENTERED exists OR identify the existing zone-entry event name)
    - src/game/scenes/WorldScene.js OR src/game/scenes/*.js (find where EventBus.emit fires on zone change — provides the payload shape)
  </read_first>
  <behavior>
    - Update the Plan 01 RED test file to reference the EXACT action type strings found in the codebase (e.g., `quest/startQuest` becomes whatever Redux Toolkit's `createSlice` autogenerated — likely `quest/startQuest` if slice name is `quest`).
    - Add a section in the test file at the top with a comment block listing the 8 canonical events and their source (Redux action name OR EventBus event name) — single source of truth for Task 2.
  </behavior>
  <action>
Read each slice listed in `<read_first>` and grep for `createSlice({ name: ... })` and reducer names. Build a definitive mapping table (in a comment block at the top of `src/store/middleware/__tests__/telemetryMiddleware.test.js`):

```
// Canonical event -> source mapping (verified against actual slice action types)
// quest.started        <- '<exact-string>'    e.g. 'quest/startQuest'
// quest.completed      <- '<exact-string>'
// fsrs.reviewed        <- '<exact-string>'
// lesson.completed     <- '<exact-string>'
// teaching.started     <- '<exact-string>'
// teaching.completed   <- '<exact-string>'
// dashboard.viewed     <- '<exact-string>'  (or "synthesized — see notes")
// zone.entered         <- EventBus event '<exact-name>' (NOT a Redux action)
```

Update the existing test assertions in `telemetryMiddleware.test.js` to use these exact action strings. If `dashboard.viewed` has no natural Redux action, add a TODO in the test referencing Plan 03 Task 2's resolution path (recommend: derive from React Router route change in a useEffect hook that dispatches `dashboard/viewed`).

If any slice action name is ambiguous (e.g., the codebase has both `quest/startQuest` and `questSlice/startQuest` patterns), prefer the one already used in existing tests and document the choice in the comment block.
  </action>
  <verify>
    <automated>grep -c "Canonical event -> source mapping" src/store/middleware/__tests__/telemetryMiddleware.test.js</automated>
  </verify>
  <done>Comment block at top of test file lists exact action strings for all 7 Redux-sourced events + 1 EventBus-sourced event.</done>
  <acceptance_criteria>
    - `grep -c "quest.started" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "quest.completed" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "fsrs.reviewed" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "lesson.completed" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "teaching.started" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "teaching.completed" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "dashboard.viewed" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - `grep -c "zone.entered" src/store/middleware/__tests__/telemetryMiddleware.test.js >= 1`.
    - Every event-name line is paired with an exact Redux action string OR explicit `EventBus event` annotation.
    - File still fails (RED) because Task 2 hasn't implemented the middleware yet — `npx vitest run src/store/middleware/__tests__/telemetryMiddleware.test.js` exits non-zero.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ACTION_EVENT_MAP, PROP_EXTRACTORS, and EventBus relay</name>
  <files>src/store/middleware/telemetryMiddleware.js, src/utils/eventBusTypes.js</files>
  <read_first>
    - src/store/middleware/__tests__/telemetryMiddleware.test.js (Task 1's verified action strings — single source of truth)
    - src/store/middleware/telemetryMiddleware.js (current skeleton from Plan 02 — extend, do not replace the opt-out short-circuit)
    - src/utils/eventBusTypes.js (confirm ZONE_ENTERED entry; add if missing)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Pattern 2: Telemetry Middleware (Redux action listener)" for PROP_EXTRACTORS shape
  </read_first>
  <behavior>
    - `ACTION_EVENT_MAP` is a frozen const object mapping the 7 Redux action strings to event names. (zone.entered is NOT in this map — it uses EventBus.)
    - `PROP_EXTRACTORS` has an entry per event name. Each returns an object containing ONLY enum/ID fields. Examples:
      - `quest.completed`: `{ quest_id, zone_id, objectives_count }` (NOT questTitle, NOT objectives array)
      - `quest.started`: `{ quest_id, zone_id }`
      - `fsrs.reviewed`: `{ word_id, rating, cefr_level }` (NOT arabicText, NOT translation)
      - `lesson.completed`: `{ lesson_id, lesson_category, cefr_level }` (NOT lessonTitle)
      - `teaching.started`: `{ session_id, topic_id }`
      - `teaching.completed`: `{ session_id, topic_id, score_bucket }` (score bucketed into 'low'|'mid'|'high', NEVER raw score with PII risk)
      - `dashboard.viewed`: `{}` (no payload needed)
      - `zone.entered`: `{ zone_id }`
    - When an extractor throws, the middleware catches and (in DEV) `console.warn`s. The Redux action still propagates normally.
    - `initTelemetryEventBusRelay(store)` subscribes to `EventBus.on(EVENTS.ZONE_ENTERED, payload => { if opted-out return; posthog.capture('zone.entered', { zone_id: payload.zoneId }); })`.
  </behavior>
  <action>
1. In `src/utils/eventBusTypes.js`: if `ZONE_ENTERED` is not in `EVENTS`, add it with the exact string used by existing zone-entry emitters (verified during Task 1 read_first). If the existing event name differs from `ZONE_ENTERED` (e.g. `zoneChanged`), reuse the existing name to avoid breaking other listeners.

2. In `src/store/middleware/telemetryMiddleware.js`:
   - At top of file: `import { getPostHog } from '../../services/posthogClient.js';` and `import { EventBus } from '../../utils/eventBus.js';` and `import { EVENTS } from '../../utils/eventBusTypes.js';`.
   - Define `const ACTION_EVENT_MAP = Object.freeze({ [exact-action-string-1]: 'quest.started', ... });` using Task 1's mapping.
   - Define `const PROP_EXTRACTORS = Object.freeze({ 'quest.completed': (action) => ({ quest_id: action.payload.questId, zone_id: action.payload.zoneId, objectives_count: action.payload.objectives?.length ?? 0 }), ... });` per behavior list. NEVER include `action.payload.title`, `action.payload.text`, `action.payload.arabicText`, `action.payload.translation`, or `action.payload.name` — PII gate.
   - Replace the Plan-02 TODO inside `telemetryMiddleware` with: `const eventName = ACTION_EVENT_MAP[action.type]; if (!eventName) return result;` then `try { const props = PROP_EXTRACTORS[eventName]?.(action) ?? {}; getPostHog().capture(eventName, props); } catch (err) { if (import.meta.env.DEV) console.warn('[telemetry] capture failed', err); }`.
   - Replace the Plan-02 TODO inside `initTelemetryEventBusRelay` with: `EventBus.on(EVENTS.ZONE_ENTERED, (payload) => { if (store.getState().settings?.telemetryOptOut === true) return; try { getPostHog().capture('zone.entered', { zone_id: payload?.zoneId }); } catch (err) { if (import.meta.env.DEV) console.warn('[telemetry] zone.entered failed', err); } });`.

3. If the `dashboard.viewed` event has no natural Redux action (per Task 1 finding), add a NEW thin action `dashboardSlice.viewDashboard()` (or a similar slice that owns dashboard) that the existing `<DailyDashboard>` component dispatches inside a `useEffect(() => { dispatch(viewDashboard()); }, [])`. This is the minimum surface area to capture the event without restructuring routing.
  </action>
  <verify>
    <automated>npx vitest run src/store/middleware/__tests__/telemetryMiddleware.test.js --reporter=verbose 2>&1 | tail -10 | grep -E "passed|✓"</automated>
  </verify>
  <done>telemetryMiddleware.test.js is fully GREEN. All 8 canonical events are captured via the middleware OR EventBus relay. No free-text fields in any extractor.</done>
  <acceptance_criteria>
    - `npx vitest run src/store/middleware/__tests__/telemetryMiddleware.test.js` exits 0.
    - `grep -c "ACTION_EVENT_MAP" src/store/middleware/telemetryMiddleware.js >= 2` (declaration + use).
    - `grep -c "PROP_EXTRACTORS" src/store/middleware/telemetryMiddleware.js >= 2`.
    - `grep -c "EventBus.on" src/store/middleware/telemetryMiddleware.js >= 1` (zone.entered relay wired).
    - `grep -E "questTitle|arabicText|translation|lessonTitle|npcName" src/store/middleware/telemetryMiddleware.js | grep -v '^//' | wc -l` returns 0 (no free-text fields in extractors).
    - All 8 event names appear: `for e in 'quest.started' 'quest.completed' 'fsrs.reviewed' 'zone.entered' 'lesson.completed' 'teaching.started' 'teaching.completed' 'dashboard.viewed'; do grep -c "$e" src/store/middleware/telemetryMiddleware.js; done` — each returns >=1.
    - Pre-existing 5623 passing tests still green AND settingsSlice test still green: `npx vitest run --reporter=json 2>&1 | grep -c '"status":"passed"' >= 5624`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redux action payload -> PostHog event property | Last point where free-text PII could leak |
| Phaser EventBus payload -> PostHog event property | Same boundary, different source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-07 | Information Disclosure | PROP_EXTRACTORS leaking free text | mitigate | Each extractor explicitly lists allowed keys; no spread `...action.payload`; unit-tested with assertion that capture spy receives ONLY enum/ID keys |
| T-102-08 | Tampering | EventBus consumers crashing telemetry init | mitigate | try/catch inside relay handler; DEV-only console.warn; production silent |
| T-102-09 | Repudiation | Missing event audit trail | accept | PostHog is the audit log; no app-side duplicate logging this phase |
</threat_model>

<verification>
- `npx vitest run` — full suite ≥5624 passing.
- Manual: `npm run dev`, open settings, toggle opt-in. Trigger one quest start (any quest), check PostHog Live Events tab — `quest.started` appears within 30 seconds with only `{ quest_id, zone_id }` payload. Repeat for one zone change (`zone.entered`).
- Grep audit: `grep -rE "questTitle|arabicText|translation|lessonTitle" src/store/middleware/telemetryMiddleware.js` returns nothing.
</verification>

<success_criteria>
- All 8 canonical events emit with stable names from one centralized file.
- PROP_EXTRACTORS contain no free-text keys (PII discipline verified by automated grep).
- Plan 01 telemetryMiddleware.test.js is fully GREEN.
- Pre-existing test suite green AND new tests green (>=5624).
</success_criteria>

<output>
Create `.planning/phases/102-observability-test-coverage/102-03-SUMMARY.md`: action-name mapping table, PROP_EXTRACTORS shape, EventBus relay event name used, any new actions added (e.g., dashboardSlice.viewDashboard), test pass count.
</output>
