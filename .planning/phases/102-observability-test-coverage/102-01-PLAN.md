---
phase: 102-observability-test-coverage
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/102-observability-test-coverage/102-01-BASELINE.md
  - src/services/__tests__/posthogClient.test.js
  - src/store/middleware/__tests__/telemetryMiddleware.test.js
  - src/services/__tests__/devicePerformance.test.js
  - src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx
  - src/game/ui/__tests__/PerfOverlay.test.js
  - src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx
  - src/store/middleware/__tests__/devicePerformancePersist.test.js
autonomous: false
requirements: [OBS-01, OBS-02, OBS-03, OBS-04, OBS-05, OBS-06, OBS-07, OBS-09]
user_setup:
  - service: posthog
    why: "Phase 102 needs a Gogo Arabic PostHog project to receive events"
    env_vars:
      - name: VITE_POSTHOG_KEY
        source: "PostHog -> FrameCoach org -> NEW Gogo Arabic project -> Project Settings -> Project API Key"
      - name: VITE_POSTHOG_HOST
        source: "PostHog project URL (https://eu.i.posthog.com for EU region)"
    dashboard_config:
      - task: "Create new project named 'Gogo Arabic' in FrameCoach org (do NOT use Default project 148422)"
        location: "PostHog UI -> Organization FrameCoach (id 019d2bf5-3889-0000-1412-5918dc7408b0) -> Projects -> New"
      - task: "Select EU region for data residency (UK user)"
        location: "Project creation dialog"
      - task: "Allow-list app origin in Project Settings"
        location: "Project Settings -> Authorized URLs"
must_haves:
  truths:
    - "Bundle-size baseline measured before any posthog-js install (delta tracking ground truth)"
    - "Vitest baseline measured at ≥5623 passing tests (OBS-09 reference)"
    - "All Wave 0 RED test files exist and fail with deterministic 'not implemented' errors"
    - "Human-approved checkpoint clears posthog-js + @posthog/react legitimacy (slopcheck unavailable)"
    - "PostHog Gogo Arabic project exists in FrameCoach org"
  artifacts:
    - path: ".planning/phases/102-observability-test-coverage/102-01-BASELINE.md"
      provides: "Pre-install bundle size + test counts + perf overlay budget targets"
      contains: "bundle size, test count, posthog project id"
    - path: "src/services/__tests__/posthogClient.test.js"
      provides: "Failing test scaffold for OBS-01, OBS-03, OBS-04 init config assertions"
    - path: "src/store/middleware/__tests__/telemetryMiddleware.test.js"
      provides: "Failing test scaffold for OBS-02 event vocabulary + PII gate"
    - path: "src/services/__tests__/devicePerformance.test.js"
      provides: "Failing test scaffold for OBS-06 warmup sampler"
    - path: "src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx"
      provides: "Failing test scaffold for OBS-04 boundary → posthog"
    - path: "src/game/ui/__tests__/PerfOverlay.test.js"
      provides: "Failing test scaffold for OBS-05 1Hz update + zero-cost-when-off"
    - path: "src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx"
      provides: "Failing test scaffold for OBS-07 opt-in/out toggle"
    - path: "src/store/middleware/__tests__/devicePerformancePersist.test.js"
      provides: "Failing test scaffold for OBS-06 IndexedDB persistence via migration v13"
  key_links:
    - from: "102-01-BASELINE.md"
      to: "102-09 regression verification"
      via: "Baseline numbers (bundle size, test count) feed phase-end delta check"
      pattern: "bundle_baseline_kb|vitest_baseline"
    - from: "checkpoint:human-verify (slopcheck substitute)"
      to: "Plan 02 npm install"
      via: "Blocking human gate before any package install"
      pattern: "posthog-js|@posthog/react"
---

<objective>
Wave 0 foundation for the Observability phase. Three things ship from this plan:

1. A baseline document that captures bundle size, vitest counts, and PostHog project ID — used by Plan 09 to detect regressions.
2. Seven failing test files (RED) that downstream plans (02..08) must turn GREEN. Every Wave 0 file uses a deterministic `throw new Error('not implemented')` shim so the failure is signal, not noise.
3. A blocking `checkpoint:human-verify` task that gates posthog-js + @posthog/react legitimacy because slopcheck is unavailable in sandbox (per RESEARCH.md Package Legitimacy Audit).

Purpose: Enforce TDD discipline across the entire phase, lock in the regression baseline before any code change, and surface package legitimacy to the developer as a blocking gate rather than a silent install.

Output: 102-01-BASELINE.md committed, 7 RED test files committed, one signed human approval recorded in the chat log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-observability-test-coverage/102-RESEARCH.md
@.planning/phases/102-observability-test-coverage/102-CONTEXT.md

<interfaces>
<!-- Existing IndexedDB migration entry point -->
From src/services/storage/migrations.js:
```javascript
export const CURRENT_VERSION = 12; // bumped to 13 in Plan 07
const migrations = { /* version -> migrate fn map */ };
export const migrate = createMigrate(migrations, { debug: false });
export { migrations };
```

<!-- Existing settings slice surface (no telemetryOptOut yet) -->
From src/store/slices/settingsSlice.js:
```javascript
const initialState = { /* ...22 existing keys, NO telemetryOptOut */ };
// existing reducers: toggleTransliteration, toggleDiacritics, toggleMute, ...
```

<!-- React shell mount point used by Plan 02 to wire PostHog init BEFORE createRoot -->
From src/main.jsx:
```javascript
import { store, persistor } from './store/store.js';
// existing module-eval dispatches at lines ~22-25:
store.dispatch(initializeQuests(questsData));
store.dispatch(checkPrerequisites());
store.dispatch(updateStreak());
store.dispatch(checkDailyReset());
// AppRoot defined below — posthog.init MUST run before these dispatches
```

<!-- Phaser EventBus used by telemetry middleware (Plan 03) for zone.entered relay -->
From src/utils/eventBus.js:
```javascript
export const EventBus = new Phaser.Events.EventEmitter();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-install baseline</name>
  <files>.planning/phases/102-observability-test-coverage/102-01-BASELINE.md</files>
  <read_first>
    - .planning/STATE.md (test counts: 5650 total / 5623 passing / 19 known-failing across 5 files)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md (perf budgets, EU host, deviation notes)
    - package.json (current bundle constraint per PROJECT.md is <500KB; current main is 402KB)
    - vite.config.js (manualChunks structure for future posthog vendor chunk)
  </read_first>
  <action>
Create the BASELINE document. Run `npm run build` (or `npm run build:analyze` if defined) and record main bundle size in KB gzipped. Run `npm run test:run` and record (a) total test count, (b) passing count, (c) names of the 5 known-failing test files (WorldSnapshot, zoneReviewMiddleware, datasetValidation, settingsSlice, battle.integration). Record PostHog target project metadata: organization `FrameCoach` (id `019d2bf5-3889-0000-1412-5918dc7408b0`), project name `Gogo Arabic` (id TBD — captured during Task 3), region `EU` (host `https://eu.i.posthog.com`), key env var `VITE_POSTHOG_KEY`. Record perf overlay budget (≤1ms/frame when on, 0ms when off), Playwright budget (<3 minutes in CI). Record OBS-09 regression rule: phase MUST exit with vitest passing count ≥5623 and the 19 pre-existing failures unchanged in identity (not new failures).
  </action>
  <verify>
    <automated>test -f .planning/phases/102-observability-test-coverage/102-01-BASELINE.md && grep -q "vitest_baseline: 5623" .planning/phases/102-observability-test-coverage/102-01-BASELINE.md && grep -q "VITE_POSTHOG_KEY" .planning/phases/102-observability-test-coverage/102-01-BASELINE.md && grep -q "eu.i.posthog.com" .planning/phases/102-observability-test-coverage/102-01-BASELINE.md</automated>
  </verify>
  <done>BASELINE.md exists with bundle KB, vitest pass count, known-failing list, PostHog project metadata, perf budgets, and OBS-09 regression rule.</done>
  <acceptance_criteria>
    - File `.planning/phases/102-observability-test-coverage/102-01-BASELINE.md` exists.
    - Contains the literal string `vitest_baseline: 5623` (no other number — uses STATE.md value, not CONTEXT.md's stale 2535).
    - Lists all 5 known-failing test files by exact filename.
    - Documents `VITE_POSTHOG_KEY` and `VITE_POSTHOG_HOST` env var names.
    - Defaults host to `https://eu.i.posthog.com` (per correction in planning_context).
    - States perf overlay budget: `≤1ms/frame when active, 0ms when disabled` per OBS-05.
    - States Playwright budget: `<3 minutes CI runtime` per OBS-08.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author Wave 0 failing test scaffolds (RED)</name>
  <files>src/services/__tests__/posthogClient.test.js, src/store/middleware/__tests__/telemetryMiddleware.test.js, src/services/__tests__/devicePerformance.test.js, src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx, src/game/ui/__tests__/PerfOverlay.test.js, src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx, src/store/middleware/__tests__/devicePerformancePersist.test.js</files>
  <read_first>
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md sections "Phase Requirements -> Test Map" and "Wave 0 Gaps"
    - src/store/middleware/__tests__/ (existing middleware test patterns — mirror imports and vitest setup)
    - src/services/storage/__tests__/ (existing storage test patterns — fake-indexeddb usage)
    - vite.config.js (vitest config block — environment, setupFiles)
  </read_first>
  <action>
Author 7 failing test files using vitest 3.0.0 + @testing-library/react 16 + jsdom 25 + fake-indexeddb 6 (all pre-installed). Each file describes the GREEN expectations from RESEARCH.md and includes `it.todo` or `it(..., () => { throw new Error('not implemented — Plan NN'); })` shims so they fail deterministically.

File 1 — `src/services/__tests__/posthogClient.test.js` (covers OBS-01, OBS-03, OBS-04, Plan 02):
- `init() calls posthog.init with autocapture: false`
- `init() passes capture_exceptions: { capture_unhandled_errors: true, capture_unhandled_rejections: true, capture_console_errors: false }` (per RESEARCH correction — NOT the old enable_exception_autocapture API)
- `init() passes session_recording.maskAllInputs: true`
- `init() does NOT pass session_recording.captureCanvas: true` (canvas recording must stay OFF — Phaser 120→25fps risk)
- `init() defaults api_host to https://eu.i.posthog.com when VITE_POSTHOG_HOST is unset` (UK user per correction 6)
- `init() is a no-op when VITE_POSTHOG_KEY is missing` (graceful degradation per RESEARCH Pattern 1)
- `init() sets opt_out_capturing_by_default: true` (per correction 1 — no age-gate, opt-OUT for ALL users)

File 2 — `src/store/middleware/__tests__/telemetryMiddleware.test.js` (covers OBS-02, Plan 03):
- `'quest/completeQuest' action emits posthog.capture('quest.completed', { quest_id, zone_id, objectives_count })`
- All 8 canonical events emit with correct names: `quest.started`, `quest.completed`, `fsrs.reviewed`, `zone.entered`, `lesson.completed`, `teaching.started`, `teaching.completed`, `dashboard.viewed`
- Free-text fields (questTitle, arabicText) are NEVER passed to posthog.capture (PII discipline assertion — spy on capture, assert payload keys are an enum allowlist)
- When `state.settings.telemetryOptOut === true`, middleware short-circuits (posthog.capture spy called 0 times)
- Middleware catches thrown errors inside extractors (telemetry never crashes the app)

File 3 — `src/services/__tests__/devicePerformance.test.js` (covers OBS-06, Plan 07):
- Use `vi.useFakeTimers()`. After 10s of warmup samples averaging 30fps, dispatched action contains `{ isLowEnd: true, avgFps: <45, sampleCount: 40 }`.
- After 10s of warmup samples averaging 60fps with `navigator.deviceMemory: 8`, dispatched action contains `{ isLowEnd: false }`.
- When `navigator.deviceMemory` is undefined (Firefox/Safari), treats as not-low-end (Infinity fallback per RESEARCH Pitfall 4).

File 4 — `src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx` (covers OBS-04, Plan 05):
- Component renders fallback UI when child throws.
- `componentDidCatch` calls `posthog.captureException(error)` exactly once with the caught error.

File 5 — `src/game/ui/__tests__/PerfOverlay.test.js` (covers OBS-05, Plan 06):
- `new PerfOverlay(scene)` creates a `scene.time.addEvent` with `delay: 1000` (1Hz, not per-frame).
- `update()` reads `scene.game.loop.actualFps`, `scene.game.loop.delta`, `scene.game.renderer.drawCount`.
- `update()` is typeof-guarded for `performance.memory` (works on Firefox/Safari without throw).
- `destroy()` calls `timer.remove()` and `text.destroy()`.

File 6 — `src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx` (covers OBS-07, Plan 02):
- Default state: `telemetryOptOut: true` (opt-OUT for all per correction 1).
- Toggling opt-in dispatches the slice reducer that sets `telemetryOptOut: false` AND calls `posthog.opt_in_capturing()`.
- Toggling opt-out dispatches reducer that sets `telemetryOptOut: true` AND calls `posthog.opt_out_capturing()`.

File 7 — `src/store/middleware/__tests__/devicePerformancePersist.test.js` (covers OBS-06 persistence, Plan 07):
- After dispatch of `setLowEndFlag({ isLowEnd: true, avgFps: 30, deviceMemory: 2 })`, IndexedDB store `redux-state` contains the devicePerformance slice (use fake-indexeddb).
- Migration v12 -> v13 exists in `src/services/storage/migrations.js` and adds an empty `devicePerformance` slice to state when applied to a v12 snapshot.

Every test file's failing assertion path MUST reference the absolute import path it expects (e.g., `import { initPostHog } from '../posthogClient.js';`). When the module does not yet exist, the import throws `MODULE_NOT_FOUND` — this IS the RED signal; do not stub the modules to bypass it.
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/posthogClient.test.js src/store/middleware/__tests__/telemetryMiddleware.test.js src/services/__tests__/devicePerformance.test.js src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx src/game/ui/__tests__/PerfOverlay.test.js src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx src/store/middleware/__tests__/devicePerformancePersist.test.js --reporter=verbose 2>&1 | grep -E "FAIL|Test Files" | head -20</automated>
  </verify>
  <done>All 7 files exist, each runs under vitest, each reports at least one failing assertion or MODULE_NOT_FOUND import error (RED state).</done>
  <acceptance_criteria>
    - All 7 test files exist at the paths listed in `<files>`.
    - `npx vitest run src/services/__tests__/posthogClient.test.js` exits non-zero (RED).
    - `npx vitest run src/store/middleware/__tests__/telemetryMiddleware.test.js` exits non-zero.
    - `npx vitest run src/services/__tests__/devicePerformance.test.js` exits non-zero.
    - `npx vitest run src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx` exits non-zero.
    - `npx vitest run src/game/ui/__tests__/PerfOverlay.test.js` exits non-zero.
    - `npx vitest run src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx` exits non-zero.
    - `npx vitest run src/store/middleware/__tests__/devicePerformancePersist.test.js` exits non-zero.
    - `grep -l "opt_out_capturing_by_default: true" src/services/__tests__/posthogClient.test.js` returns a match.
    - `grep -l "captureCanvas" src/services/__tests__/posthogClient.test.js` returns a match (asserting NOT-set or false).
    - `grep -l "https://eu.i.posthog.com" src/services/__tests__/posthogClient.test.js` returns a match.
    - `grep -c "quest.completed\|quest.started\|fsrs.reviewed\|zone.entered\|lesson.completed\|teaching.started\|teaching.completed\|dashboard.viewed" src/store/middleware/__tests__/telemetryMiddleware.test.js | grep -v '^#' >= 8` (all 8 canonical event names referenced).
    - `npx vitest run --reporter=json 2>&1 | grep -c '"status":"passed"' >= 5623` (pre-existing passing tests still pass — no regressions added by scaffolds).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human gate — posthog-js + @posthog/react legitimacy verification AND PostHog project creation</name>
  <what-built>Task 1 captured BASELINE.md. Task 2 authored 7 RED test scaffolds. This gate stops execution before any npm install or any production code so the developer can (a) manually verify posthog-js and @posthog/react legitimacy because slopcheck is unavailable in sandbox, AND (b) create the PostHog Gogo Arabic project + capture API key.</what-built>
  <how-to-verify>
    1. **Package legitimacy (substitutes for unavailable slopcheck):**
       - Visit https://www.npmjs.com/package/posthog-js — confirm publisher is `posthog` org, last publish within 30 days, no postinstall script, weekly download count >100k. Expected: HIGH legitimacy.
       - Visit https://www.npmjs.com/package/@posthog/react — confirm publisher is `posthog` org, peerDependency `posthog-js >=1.257.2`, no postinstall script. Expected: HIGH legitimacy (sibling package).
       - Optional: run `pip install slopcheck && slopcheck install posthog-js @posthog/react --json` locally if Python is available.
    2. **PostHog project creation:**
       - Sign in to PostHog -> FrameCoach organization (id `019d2bf5-3889-0000-1412-5918dc7408b0`).
       - Click `+ New project` -> name `Gogo Arabic` -> region `EU` (UK user per correction 6) -> do NOT select Default project 148422.
       - Copy the Project API Key (starts with `phc_`).
       - In Project Settings -> Authorized URLs, add `http://localhost:3000` and the production URL when known.
    3. **Local env wiring (do NOT commit secrets):**
       - In repo root, create `.env.local` (already gitignored). Add:
         ```
         VITE_POSTHOG_KEY=phc_<key from step 2>
         VITE_POSTHOG_HOST=https://eu.i.posthog.com
         ```
       - GitHub Actions: add `VITE_POSTHOG_KEY` as a repository secret (Settings -> Secrets and variables -> Actions -> New repository secret). Leave `VITE_POSTHOG_HOST` defaulted in code.
    4. Update `.planning/phases/102-observability-test-coverage/102-01-BASELINE.md` Task 1 created with the project ID captured in step 2 (replace the `TBD` placeholder).
  </how-to-verify>
  <resume-signal>Type `approved` (with project ID recorded in BASELINE.md and `.env.local` set) OR describe issues to address.</resume-signal>
  <acceptance_criteria>
    - Human approval recorded in chat log.
    - `.planning/phases/102-observability-test-coverage/102-01-BASELINE.md` contains the actual PostHog project ID (not `TBD`).
    - Developer confirms `.env.local` contains `VITE_POSTHOG_KEY=phc_...` (do NOT echo the secret in logs).
    - Developer confirms GitHub Actions secret `VITE_POSTHOG_KEY` is set OR has explicitly deferred CI wiring to Plan 09.
    - Auto-approval (workflow.auto_advance) is IGNORED for this gate — legitimacy checkpoints are never auto-approvable per slopcheck protocol.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> npm registry | npm install of posthog-js + @posthog/react crosses an untrusted supply chain |
| developer -> PostHog cloud | API key creation requires PostHog org access (FrameCoach org) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-SC | Tampering | npm packages posthog-js + @posthog/react | mitigate | Task 3 blocking-human checkpoint — verify publisher, postinstall, peer-deps on npmjs.com (slopcheck unavailable in sandbox) |
| T-102-01 | Information Disclosure | VITE_POSTHOG_KEY in .env.local | mitigate | `.env.local` is gitignored; key is a public ingest key per PostHog design; further hardened by Project Settings -> Authorized URLs allow-list |
| T-102-02 | Tampering | Wave 0 RED tests committed to repo | accept | Tests are scaffolds containing no production logic; failing-by-design is the contract |
</threat_model>

<verification>
- Vitest runs (with new tests in RED state): pre-existing 5623 passing tests remain green; the 7 new test files are the only new failures.
- `.env.local` is NOT committed (confirmed by `git status --porcelain | grep -v '^??'` showing no .env.local).
- BASELINE.md exists with concrete numbers (no `TBD` placeholders after Task 3 resolves the project ID).
- Human gate signed off in chat log before any subsequent plan executes.
</verification>

<success_criteria>
- BASELINE.md committed with bundle size, vitest pass count ≥5623, 5 known-failing file names, PostHog project ID, perf/Playwright budgets, OBS-09 regression rule.
- 7 RED test files exist and fail deterministically (MODULE_NOT_FOUND or assertion failure) — no `it.skip` masking the gap.
- Pre-existing 5623 passing tests remain green after this plan (`npx vitest run --reporter=json 2>&1 | grep -c '"status":"passed"' >= 5623`).
- Human-approved legitimacy + PostHog project creation gate recorded.
</success_criteria>

<output>
Create `.planning/phases/102-observability-test-coverage/102-01-SUMMARY.md` when done. Include: BASELINE numbers (bundle KB, vitest pass count), 7 RED file paths, PostHog project ID, human approval timestamp, any deviations from the plan with reason.
</output>
