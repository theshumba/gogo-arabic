---
phase: 102-observability-test-coverage
plan: 04
type: execute
wave: 2
depends_on: [102-02]
files_modified:
  - src/services/posthogClient.js
  - src/services/__tests__/posthogClient.test.js
autonomous: true
requirements: [OBS-03]
must_haves:
  truths:
    - "Session replay records DOM (React shell) with all input fields masked"
    - "Canvas recording is explicitly disabled — Phaser canvas is NEVER captured (documented 120fps→25fps perf risk)"
    - "Any free-text DOM nodes (conversation builder, writing canvas, search inputs) are marked with ph-no-capture class for defense-in-depth"
  artifacts:
    - path: "src/services/posthogClient.js"
      provides: "Confirmed session_recording config with maskAllInputs:true, captureCanvas:false"
    - path: "src/services/__tests__/posthogClient.test.js"
      provides: "Strengthened assertions on captureCanvas:false (explicit, not just absent)"
  key_links:
    - from: "posthog.init session_recording config"
      to: "PostHog session replay engine"
      via: "{ maskAllInputs: true, captureCanvas: false } literal in init args"
      pattern: "maskAllInputs.*true|captureCanvas.*false"
---

<objective>
Harden session replay configuration. Plan 02 already passed `session_recording: { maskAllInputs: true }` to satisfy OBS-03's RED test. This plan EXPLICITLY sets `captureCanvas: false` (rather than relying on the default-off behavior), strengthens the unit test to assert the explicit value, and adds `ph-no-capture` className to free-text DOM nodes (writing canvas, conversation builder input, search fields) as defense-in-depth.

Per planning_context correction 5: PostHog canvas recording MUST stay OFF — Phaser/WebGL drops from 120fps → 25fps when canvas recording is on. DOM-only session replay is what we want anyway (no Arabic learner text in canvas).

Purpose: Replays show user clicks/navigation in the DOM chrome around the canvas, but NEVER the canvas itself and NEVER the text users type.

Output: Updated posthogClient with explicit `captureCanvas: false`; strengthened tests; ph-no-capture classes on identified free-text DOM nodes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-observability-test-coverage/102-02-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-RESEARCH.md

<interfaces>
<!-- Current posthogClient session_recording config from Plan 02 -->
From src/services/posthogClient.js (Plan 02 ship):
```javascript
session_recording: {
  maskAllInputs: true,
  // captureCanvas: OMITTED — default off — this plan makes it EXPLICIT false
}
```

<!-- Existing free-text DOM nodes that should also wear ph-no-capture as defense-in-depth -->
Identified during research (verify in implementation):
- src/components/Reading/ writing canvas — user-drawn Arabic letters
- src/components/Conversation/ sentence-builder input — user-arranged Arabic word tokens
- src/components/Writing/ Arabic calligraphy canvas
- Any <input type="text"> bound to player-name or save-slot-name
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make captureCanvas:false explicit in posthog.init config and strengthen the assertion</name>
  <files>src/services/posthogClient.js, src/services/__tests__/posthogClient.test.js</files>
  <read_first>
    - src/services/posthogClient.js (current Plan 02 ship — session_recording block)
    - src/services/__tests__/posthogClient.test.js (current assertion may only check `maskAllInputs:true` — strengthen to also assert `captureCanvas:false` explicitly)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Pitfall 1: Canvas session-recording destroys Phaser FPS" + planning_context correction 5
  </read_first>
  <behavior>
    - `posthog.init` config has `session_recording: { maskAllInputs: true, captureCanvas: false }` — explicit, not relying on default.
    - Test assertion checks `config.session_recording.captureCanvas === false` (strict equality, not just truthy/absent).
    - Test asserts that no key in `config.session_recording` is set to anything truthy that could re-enable canvas (defensive — `recordCanvas` is the legacy name; assert that too is not truthy).
  </behavior>
  <action>
1. `src/services/__tests__/posthogClient.test.js`: Add (or strengthen existing) assertion:
   ```
   expect(initArgs[1].session_recording.captureCanvas).toBe(false);
   expect(initArgs[1].session_recording.maskAllInputs).toBe(true);
   expect(initArgs[1].session_recording.recordCanvas).toBeFalsy(); // legacy alias, defensive
   ```

2. `src/services/posthogClient.js`: Update the `session_recording` object to:
   ```
   session_recording: {
     maskAllInputs: true,
     captureCanvas: false,
   }
   ```
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/posthogClient.test.js --reporter=verbose 2>&1 | tail -5 | grep -E "passed|✓"</automated>
  </verify>
  <done>captureCanvas: false is explicit in init config; tests assert the value strictly.</done>
  <acceptance_criteria>
    - `npx vitest run src/services/__tests__/posthogClient.test.js` exits 0.
    - `grep -v '^//\|^ *\*' src/services/posthogClient.js | grep -c "captureCanvas: false" >= 1`.
    - `grep -v '^//\|^ *\*' src/services/__tests__/posthogClient.test.js | grep -c "captureCanvas" >= 1`.
    - Pre-existing tests still green (>=5624 passing).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add ph-no-capture className to identified free-text DOM nodes</name>
  <files>(determined by Task 2 read_first audit — likely src/components/Writing/*.jsx, src/components/Reading/*.jsx, src/components/Conversation/*.jsx, src/components/Onboarding/PlayerNameInput.jsx)</files>
  <read_first>
    - src/components/Writing/ (find canvas elements and text inputs)
    - src/components/Reading/ (find any inline reader inputs)
    - src/components/Conversation/ (sentence-builder Arabic word selection)
    - src/components/Onboarding/ (player-name input fields)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Security Domain -> Known Threat Patterns" — ph-no-capture pattern
  </read_first>
  <action>
Grep for `<input`, `<textarea`, and `<canvas>` in `src/components/Writing/`, `src/components/Reading/`, `src/components/Conversation/`, and `src/components/Onboarding/`. For each that captures learner-typed/drawn Arabic content OR player-typed names, add `className="ph-no-capture"` to the element (or append to existing className via `clsx` / template literal if className already in use).

This is defense-in-depth. PostHog's `maskAllInputs: true` masks all `<input>` and `<textarea>` content by default; adding `ph-no-capture` makes the element itself invisible to the recorder (stronger than masking the text inside). For canvases (writing practice), `ph-no-capture` removes them from the DOM scrape entirely.

Update `<files>` frontmatter (in the SUMMARY at plan completion) with the actual list of files modified.
  </action>
  <verify>
    <automated>grep -rln "ph-no-capture" src/components/ | wc -l | awk '{ if ($1 >= 3) print "OK"; else print "FAIL: expected ph-no-capture in >=3 component files" }'</automated>
  </verify>
  <done>At least 3 free-text DOM nodes (covering writing, reading, conversation, and onboarding-name surfaces) carry ph-no-capture className.</done>
  <acceptance_criteria>
    - `grep -rln "ph-no-capture" src/components/ | wc -l >= 3` (at least 3 files have the class — writing canvas + conversation input + player-name input at minimum).
    - The relevant Arabic-text inputs in Writing/Reading/Conversation are covered (a sampling spot-check during implementation).
    - Pre-existing tests still green (>=5624 passing).
    - No production code path is broken (a smoke vitest run of related component tests stays green).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM render -> session replay rrweb scraper | Recorder reads every text node unless `ph-no-capture` or `maskAllInputs` applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-10 | Information Disclosure | Phaser canvas containing learner text being recorded | mitigate | Explicit `captureCanvas: false` in posthog.init session_recording config (per correction 5 — Phaser perf risk + no learner Arabic text in canvas anyway) |
| T-102-11 | Information Disclosure | Free-text Arabic input fields leaking into replay | mitigate | Belt+braces: `maskAllInputs: true` at SDK level + `ph-no-capture` className on writing canvas, conversation builder, and player-name input |
| T-102-12 | Information Disclosure | Future developer enables captureCanvas to debug something | mitigate | Unit test explicitly asserts `captureCanvas: false`; PR review surfaces any change |
</threat_model>

<verification>
- Unit test asserts explicit `captureCanvas: false` (not just absent).
- Manual: opt-in, navigate writing/reading/conversation; PostHog session replay (after Plan 09 dashboard setup, post-merge) — replay should not show any Arabic the user wrote, and should not show canvas frames.
- Vitest suite: >=5624 still green.
</verification>

<success_criteria>
- `session_recording.captureCanvas` explicitly `false` in posthog.init config.
- Unit test asserts explicit value.
- At least 3 free-text Arabic DOM nodes carry `ph-no-capture` className.
- Pre-existing tests green.
</success_criteria>

<output>
Create `.planning/phases/102-observability-test-coverage/102-04-SUMMARY.md`: explicit captureCanvas config change, list of files that received ph-no-capture, any free-text surfaces deferred to future phases.
</output>
