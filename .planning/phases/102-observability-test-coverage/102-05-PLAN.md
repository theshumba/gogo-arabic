---
phase: 102-observability-test-coverage
plan: 05
type: execute
wave: 2
depends_on: [102-02]
files_modified:
  - src/components/ErrorBoundary/RouteErrorBoundary.jsx
  - src/components/ErrorBoundary/ErrorBoundary.jsx
  - src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx
  - src/main.jsx
autonomous: true
requirements: [OBS-04]
must_haves:
  truths:
    - "Uncaught errors flow to PostHog via capture_exceptions config (already wired in Plan 02; this plan adds the React boundary integration)"
    - "RouteErrorBoundary.componentDidCatch calls posthog.captureException(error) once per caught error"
    - "PostHogErrorBoundary from @posthog/react wraps the app inside ErrorBoundaryClass to catch React render errors"
    - "Telemetry opt-out STILL gates captureException — opted-out users do not send error reports either"
  artifacts:
    - path: "src/components/ErrorBoundary/RouteErrorBoundary.jsx"
      provides: "captureException call in componentDidCatch, gated by opt-out"
    - path: "src/components/ErrorBoundary/ErrorBoundary.jsx"
      provides: "Same captureException + opt-out gate"
    - path: "src/main.jsx"
      provides: "PostHogErrorBoundary wrapping AppRoot, fallback to existing ErrorFallback"
  key_links:
    - from: "Render error inside React tree"
      to: "posthog.captureException(error)"
      via: "RouteErrorBoundary.componentDidCatch OR ErrorBoundary.componentDidCatch OR PostHogErrorBoundary"
      pattern: "captureException"
    - from: "window.onerror / window.onunhandledrejection"
      to: "posthog.captureException"
      via: "capture_exceptions config (wired in Plan 02; verified here)"
      pattern: "capture_exceptions"
---

<objective>
Complete the OBS-04 wiring. Plan 02 already configured `capture_exceptions: { capture_unhandled_errors: true, capture_unhandled_rejections: true }` in posthog.init, which catches `window.onerror` and `window.onunhandledrejection`. React render errors do NOT hit those handlers — they need a React error boundary.

This plan: (1) adds `posthog.captureException(error)` inside `RouteErrorBoundary.componentDidCatch` AND inside `ErrorBoundary.componentDidCatch`, both gated by the telemetry opt-out check; (2) wraps the React tree with `PostHogErrorBoundary` from `@posthog/react` as a final safety net (any error not caught by the existing boundaries still reaches PostHog).

Purpose: Triple-layer defense — window-level handlers (Plan 02), per-component boundaries (this plan), and PostHogErrorBoundary (this plan). All gated by opt-out.

Output: Plan 01's RouteErrorBoundary.test.jsx turns GREEN; uncaught error in a React component routes to PostHog without crashing the app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/102-observability-test-coverage/102-02-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-RESEARCH.md

<interfaces>
<!-- The RED test from Plan 01 — implementation must satisfy these assertions -->
From src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx (Plan 01):
- Component renders fallback UI when child throws.
- `componentDidCatch` calls `posthog.captureException(error)` exactly once with the caught error.

<!-- Existing ErrorBoundary class -->
From src/components/ErrorBoundary/ErrorBoundary.jsx:
```javascript
class ErrorBoundary extends Component {
  componentDidCatch(error, errorInfo) {
    console.error('[ErrorBoundary] Caught error:', error);
    console.error('[ErrorBoundary] Component stack:', errorInfo?.componentStack);
    // ADD: posthog.captureException(error, { extra: { componentStack: errorInfo?.componentStack } })
    //      gated by store.getState().settings?.telemetryOptOut === true
  }
}
```

<!-- Existing RouteErrorBoundary functional component -->
From src/components/ErrorBoundary/RouteErrorBoundary.jsx:
```javascript
// Functional component using useRouteError() — this is a router error boundary, not a class.
// Plan adds a useEffect that calls posthog.captureException when the error first arrives.
```

<!-- main.jsx current wrapping (after Plan 02) -->
From src/main.jsx (post-Plan-02):
```javascript
<PostHogProvider client={posthog}>
  <Provider store={store}>
    <PersistGate ...>
      <AccessibilityBridge />
      <ErrorBoundaryClass>  {/* ADD: wrap with <PostHogErrorBoundary> INSIDE the provider but OUTSIDE ErrorBoundaryClass */}
        ...
      </ErrorBoundaryClass>
    </PersistGate>
  </Provider>
</PostHogProvider>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add captureException call to RouteErrorBoundary and ErrorBoundary with opt-out gate</name>
  <files>src/components/ErrorBoundary/RouteErrorBoundary.jsx, src/components/ErrorBoundary/ErrorBoundary.jsx</files>
  <read_first>
    - src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx (Plan 01 RED — turn GREEN)
    - src/components/ErrorBoundary/RouteErrorBoundary.jsx (current implementation — note this is functional, uses useRouteError)
    - src/components/ErrorBoundary/ErrorBoundary.jsx (current class component — extend componentDidCatch)
    - src/services/posthogClient.js (Plan 02 — confirm getPostHog() exported)
    - src/store/store.js (Plan 02 — confirm store exported for direct getState in boundaries)
  </read_first>
  <behavior>
    - `RouteErrorBoundary.jsx`: When `useRouteError()` returns a non-null error, a `useEffect` runs ONCE (deps: [error]) that:
      - Reads `store.getState().settings?.telemetryOptOut`. If true, returns early (no capture).
      - Calls `getPostHog().captureException(error, { extra: { source: 'RouteErrorBoundary' } })`.
      - Logs to console in DEV (preserve existing console.error behavior).
    - `ErrorBoundary.jsx` (class): `componentDidCatch(error, errorInfo)` adds (after existing console.error calls):
      - `if (store.getState().settings?.telemetryOptOut === true) return;`
      - `getPostHog().captureException(error, { extra: { componentStack: errorInfo?.componentStack, source: 'ErrorBoundary' } });`
      - try/catch around the capture call (telemetry never re-throws inside an error handler).
  </behavior>
  <action>
1. `src/components/ErrorBoundary/RouteErrorBoundary.jsx`:
   - Add imports: `import { useEffect } from 'react';`, `import { getPostHog } from '../../services/posthogClient.js';`, `import { store } from '../../store/store.js';`.
   - In the component body, after the `useRouteError()` call: add `useEffect(() => { if (!error) return; if (store.getState().settings?.telemetryOptOut === true) return; try { getPostHog().captureException(error, { extra: { source: 'RouteErrorBoundary' } }); } catch (e) { if (import.meta.env.DEV) console.warn('[RouteErrorBoundary] captureException failed', e); } }, [error]);`.

2. `src/components/ErrorBoundary/ErrorBoundary.jsx`:
   - Add same imports as above.
   - Extend `componentDidCatch(error, errorInfo)` — after existing two `console.error` calls, add: `if (store.getState().settings?.telemetryOptOut === true) return; try { getPostHog().captureException(error, { extra: { componentStack: errorInfo?.componentStack, source: 'ErrorBoundary' } }); } catch (e) { if (import.meta.env.DEV) console.warn('[ErrorBoundary] captureException failed', e); }`.
  </action>
  <verify>
    <automated>npx vitest run src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx --reporter=verbose 2>&1 | tail -5 | grep -E "passed|✓"</automated>
  </verify>
  <done>RouteErrorBoundary.test.jsx is GREEN. Both boundaries call captureException with opt-out gate and try/catch.</done>
  <acceptance_criteria>
    - `npx vitest run src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx` exits 0.
    - `grep -v '^//\|^ *\*' src/components/ErrorBoundary/RouteErrorBoundary.jsx | grep -c "captureException" >= 1`.
    - `grep -v '^//\|^ *\*' src/components/ErrorBoundary/ErrorBoundary.jsx | grep -c "captureException" >= 1`.
    - `grep -v '^//\|^ *\*' src/components/ErrorBoundary/RouteErrorBoundary.jsx | grep -c "telemetryOptOut" >= 1` (opt-out gate present).
    - `grep -v '^//\|^ *\*' src/components/ErrorBoundary/ErrorBoundary.jsx | grep -c "telemetryOptOut" >= 1`.
    - Pre-existing tests still green (>=5624 passing).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrap React tree with PostHogErrorBoundary in main.jsx as final safety net</name>
  <files>src/main.jsx, src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx</files>
  <read_first>
    - src/main.jsx (current Plan 02 ship — `<PostHogProvider>` is already wrapping, `<ErrorBoundaryClass>` already inside)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Pattern 1: PostHog Init in main.jsx" — the `<PostHogErrorBoundary>` wrapping example
  </read_first>
  <behavior>
    - `<PostHogErrorBoundary>` from `@posthog/react` wraps the React tree INSIDE `<PostHogProvider>` but OUTSIDE `<ErrorBoundaryClass>` (so PostHog's boundary is the outermost catcher AFTER the existing boundary fails).
    - When an error escapes `ErrorBoundaryClass` (which shouldn't happen, but belt+braces), `PostHogErrorBoundary` catches it and renders an `ErrorFallback` component AND sends to PostHog.
    - The wrapping must NOT change existing fallback UI for normal errors (ErrorBoundaryClass handles them and shows its own fallback).
  </behavior>
  <action>
1. `src/main.jsx`:
   - Add import: `import { PostHogErrorBoundary } from '@posthog/react';`.
   - In the JSX tree, place `<PostHogErrorBoundary fallback={<ErrorFallback />}>` immediately inside `<PersistGate>` and immediately wrapping `<ErrorBoundaryClass>`. The `fallback` prop should reuse the existing ErrorFallback component if defined; if not, use a minimal `<div>Something went wrong.</div>` placeholder.
   - If `<ErrorFallback />` does not exist in the codebase, define a tiny inline functional component at the top of main.jsx: `const ErrorFallback = () => <div role="alert">Something went wrong. Please reload.</div>;`.

2. `src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx`: extend the test file (NOT the same assertion as Task 1; add a new test case) — assert that wrapping a throwing component with PostHogErrorBoundary still results in a `posthog.captureException` call (verifies the `@posthog/react` boundary forwards to the SDK).
  </action>
  <verify>
    <automated>npx vitest run src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx --reporter=verbose 2>&1 | tail -10 | grep -E "passed|✓"</automated>
  </verify>
  <done>main.jsx wraps with PostHogErrorBoundary; tests confirm React render errors reach posthog.captureException via PostHogErrorBoundary.</done>
  <acceptance_criteria>
    - `grep -v '^//\|^ *\*' src/main.jsx | grep -c "PostHogErrorBoundary" >= 2` (import + JSX use).
    - `npx vitest run src/components/ErrorBoundary/__tests__/RouteErrorBoundary.test.jsx` exits 0 with the new test case included.
    - Pre-existing tests still green (>=5624 passing).
    - Smoke run: `npm run dev` boots without React errors (manual check during execution).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React render error -> error boundary | First catch point inside React tree |
| Error boundary -> PostHog cloud | Stack trace + componentStack leave the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-13 | Information Disclosure | Error message contains learner Arabic text | mitigate | Audit thrown errors during execution; ensure no `throw new Error(arabicText)` patterns; error messages are template strings (per RESEARCH Security Domain) |
| T-102-14 | Privacy | Opted-out user still sends exception | mitigate | telemetryOptOut check inside componentDidCatch BEFORE captureException call |
| T-102-15 | Tampering | Telemetry crashes the error path itself | mitigate | try/catch around every captureException call; DEV-only console.warn on internal failure |
| T-102-16 | Repudiation | Duplicate exception captures (boundary + window.onerror) | accept | PostHog deduplicates by fingerprint per docs; one error appearing twice is acceptable as Phase-103 hardening (mark for follow-up if Insights show duplicates >20%) |
</threat_model>

<verification>
- Unit tests for both boundaries assert captureException + opt-out gate.
- Pre-existing 5624 passing tests still green.
- Manual smoke: temporarily `throw new Error('test')` inside a low-traffic component, opt in, reload, confirm PostHog Live Events shows one `$exception` event with `source: 'ErrorBoundary'` extra. Then opt out and repeat — no event sent.
</verification>

<success_criteria>
- All three layers (window-level via capture_exceptions, class boundaries, PostHogErrorBoundary) wired.
- Opt-out gate present in both class boundary catch handlers.
- Plan 01's RouteErrorBoundary.test.jsx fully GREEN.
- Pre-existing tests green.
</success_criteria>

<output>
Create `.planning/phases/102-observability-test-coverage/102-05-SUMMARY.md`: boundaries modified, PostHogErrorBoundary wrap location, opt-out gate confirmation.
</output>
