---
phase: 102-observability-test-coverage
plan: 02
type: execute
wave: 1
depends_on: [102-01]
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/services/posthogClient.js
  - src/main.jsx
  - src/store/slices/settingsSlice.js
  - src/components/Menu/SettingsMenu.jsx
  - src/components/Menu/SettingsMenu.module.css
  - src/store/middleware/telemetryMiddleware.js
  - src/store/store.js
autonomous: true
requirements: [OBS-01, OBS-07]
must_haves:
  truths:
    - "posthog-js + @posthog/react installed at the versions verified in Plan 01"
    - "PostHog SDK initialised in main.jsx BEFORE the first store.dispatch (boot events not lost)"
    - "Telemetry is opt-OUT by default for all users (no age-gate exists — per planning_context correction 1)"
    - "Settings menu exposes a working opt-in toggle that calls posthog.opt_in_capturing / opt_out_capturing"
    - "Telemetry middleware skeleton is registered in the store but emits no events yet (Plan 03 fills in the vocabulary)"
  artifacts:
    - path: "src/services/posthogClient.js"
      provides: "initPostHog(), getPostHog() exports"
      exports: ["initPostHog", "getPostHog"]
    - path: "src/store/middleware/telemetryMiddleware.js"
      provides: "Redux middleware skeleton (no action -> event map yet); EventBus relay init function"
      exports: ["telemetryMiddleware", "initTelemetryEventBusRelay"]
    - path: ".env.example"
      provides: "VITE_POSTHOG_KEY + VITE_POSTHOG_HOST documentation"
    - path: "src/store/slices/settingsSlice.js"
      provides: "telemetryOptOut: true default + setTelemetryOptOut reducer"
  key_links:
    - from: "src/main.jsx"
      to: "src/services/posthogClient.js"
      via: "initPostHog() called BEFORE ReactDOM.createRoot and BEFORE module-eval dispatches"
      pattern: "import.*posthogClient.*initPostHog"
    - from: "src/components/Menu/SettingsMenu.jsx"
      to: "posthog.opt_in_capturing / posthog.opt_out_capturing"
      via: "Toggle handler dispatches setTelemetryOptOut AND calls SDK opt API"
      pattern: "opt_in_capturing|opt_out_capturing"
    - from: "src/store/store.js"
      to: "src/store/middleware/telemetryMiddleware.js"
      via: "configureStore middleware array includes telemetryMiddleware"
      pattern: "telemetryMiddleware"
---

<objective>
Install posthog-js + @posthog/react, wire the SDK into the React shell with the canonical config from RESEARCH.md, default ALL users to opt-OUT (no age-gate exists — see planning_context correction 1), and ship the settings opt-in toggle. The telemetry middleware ships as a skeleton (registered in the store, no action map yet) so Plan 03 can fill in the canonical event vocabulary without touching store configuration.

Purpose: Get the SDK into the bundle, prove the opt-out gate works, and lock the foundation that Plans 03-05 build on.

Output: 5623+ vitest still green, Plan 01's RED tests for OBS-01 and OBS-07 turn GREEN, settings menu has a working "Share usage data" toggle that hits the PostHog API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/102-observability-test-coverage/102-01-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-RESEARCH.md

<interfaces>
<!-- The RED tests from Plan 01 — implementation must satisfy these exact expectations -->
From src/services/__tests__/posthogClient.test.js (Plan 01):
- initPostHog() reads import.meta.env.VITE_POSTHOG_KEY and VITE_POSTHOG_HOST
- when VITE_POSTHOG_KEY missing -> no-op (graceful degradation per RESEARCH Pattern 1)
- when present -> posthog.init(key, { autocapture: false, capture_pageview: true, capture_pageleave: true,
    capture_exceptions: { capture_unhandled_errors: true, capture_unhandled_rejections: true, capture_console_errors: false },
    session_recording: { maskAllInputs: true }, // NOTE: captureCanvas omitted/false — see correction 5
    api_host: VITE_POSTHOG_HOST ?? 'https://eu.i.posthog.com',
    opt_out_capturing_by_default: true,         // per correction 1 — no age-gate
    defaults: '2026-01-30'
  })

From src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx (Plan 01):
- default state: settings.telemetryOptOut === true
- toggle-in dispatches setTelemetryOptOut(false) AND calls posthog.opt_in_capturing()
- toggle-out dispatches setTelemetryOptOut(true) AND calls posthog.opt_out_capturing()

<!-- Existing settings slice surface — must extend, not replace -->
From src/store/slices/settingsSlice.js:
```javascript
const initialState = { /* 22 existing keys; ADD telemetryOptOut: true at the end */ };
// reducers: toggleTransliteration, toggleDiacritics, toggleMute, ...
// ADD: setTelemetryOptOut(state, action) { state.telemetryOptOut = action.payload; }
```

<!-- Existing main.jsx structure — module-eval dispatches at lines ~22-25 -->
From src/main.jsx:
```javascript
import { store, persistor } from './store/store.js';
store.dispatch(initializeQuests(questsData)); // line ~22 — MUST be AFTER initPostHog()
```

<!-- Existing store middleware chain — telemetryMiddleware appended -->
From src/store/store.js:
```javascript
export const store = configureStore({
  reducer: /* ...existing... */,
  middleware: (getDefaultMiddleware) => getDefaultMiddleware({ /* ...existing... */ }).concat(/* existing middlewares; ADD telemetryMiddleware */),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install posthog-js + @posthog/react and document env vars</name>
  <files>package.json, package-lock.json, .env.example</files>
  <read_first>
    - .planning/phases/102-observability-test-coverage/102-01-SUMMARY.md (Plan 01 human-approval record — verify the gate was signed before npm install)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Standard Stack" (pinned versions: posthog-js@^1.376.2, @posthog/react@^1.9.1; no postinstall scripts)
    - package.json (current `dependencies` block to confirm versions aren't already present and react-19 is pinned)
    - vite.config.js (manualChunks for vendor-splitting)
  </read_first>
  <behavior>
    - After install, `npm ls posthog-js` reports `posthog-js@1.376.2 or higher (^1.376.2)`.
    - After install, `npm ls @posthog/react` reports `@posthog/react@1.9.1 or higher (^1.9.1)`.
    - `.env.example` (file in repo root) contains `VITE_POSTHOG_KEY=` and `VITE_POSTHOG_HOST=https://eu.i.posthog.com` with comments explaining each.
    - `vite.config.js` `manualChunks` puts `posthog-js` and `@posthog/react` into their own `posthog-vendor` chunk per RESEARCH Pitfall 2.
  </behavior>
  <action>
Run `npm install posthog-js@^1.376.2 @posthog/react@^1.9.1` (Plan 01 human gate has already cleared legitimacy; do NOT re-prompt). Verify no postinstall side effects with `npm ls posthog-js @posthog/react`. Update `vite.config.js`: add a `posthog-vendor` entry under `manualChunks` containing `posthog-js` and `@posthog/react` so the SDK doesn't invalidate the main app chunk. Create `.env.example` at repo root (or extend if exists) with `VITE_POSTHOG_KEY=` (blank, with comment "Set in .env.local — never commit") and `VITE_POSTHOG_HOST=https://eu.i.posthog.com` (with comment "EU host for UK user — override only if region changes"). Do NOT add `.env.local` to git.
  </action>
  <verify>
    <automated>npm ls posthog-js 2>&1 | grep -q 'posthog-js@1\.' && npm ls @posthog/react 2>&1 | grep -q '@posthog/react@1\.' && grep -q 'VITE_POSTHOG_KEY' .env.example && grep -q 'VITE_POSTHOG_HOST' .env.example && grep -v '^#' vite.config.js | grep -q 'posthog-vendor'</automated>
  </verify>
  <done>Both packages installed at pinned ranges, vite manualChunks has posthog-vendor entry, .env.example documents both env vars.</done>
  <acceptance_criteria>
    - `npm ls posthog-js` exits 0 and prints a version matching `1.*`.
    - `npm ls @posthog/react` exits 0 and prints a version matching `1.*`.
    - `grep -v '^#' .env.example | grep -c 'VITE_POSTHOG_KEY'` returns 1.
    - `grep -v '^#' .env.example | grep -c 'VITE_POSTHOG_HOST'` returns 1.
    - `grep -v '^#' vite.config.js | grep -c 'posthog-vendor'` returns >=1 (posthog packages chunked separately).
    - `git status --porcelain .env.local` shows nothing (file is not tracked).
    - No `postinstall` script ran (verifiable from npm output — no scripts.postinstall in either package per RESEARCH).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author posthogClient.js + wire into main.jsx BEFORE createRoot</name>
  <files>src/services/posthogClient.js, src/main.jsx</files>
  <read_first>
    - src/services/__tests__/posthogClient.test.js (the RED test from Plan 01 — this task turns it GREEN; implement exactly the config the test asserts)
    - src/main.jsx (current structure — module-eval dispatches at lines ~22-25 MUST run AFTER initPostHog())
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Pattern 1: PostHog Init in main.jsx (BEFORE createRoot)" and "Pitfall 5: PostHog init AFTER first store dispatch loses early events"
  </read_first>
  <behavior>
    - `initPostHog()` is a no-op when `import.meta.env.VITE_POSTHOG_KEY` is undefined/empty (graceful degradation; init test asserts spy.called === false).
    - When key is set, `posthog.init` is called exactly once with the exact config in the RED test (autocapture: false; capture_exceptions object; session_recording.maskAllInputs: true; captureCanvas NOT set; api_host defaults to 'https://eu.i.posthog.com'; opt_out_capturing_by_default: true; defaults: '2026-01-30').
    - Calling `initPostHog()` twice is idempotent (second call is no-op — guard with module-scoped boolean).
    - `getPostHog()` returns the posthog default export so other modules can call `posthog.capture` / `posthog.captureException` without re-importing the npm name.
    - In `src/main.jsx`, `initPostHog()` is called at module-eval BEFORE the four existing dispatches (initializeQuests, checkPrerequisites, updateStreak, checkDailyReset).
  </behavior>
  <action>
Create `src/services/posthogClient.js`:
- `import posthog from 'posthog-js'`.
- Module-scoped `let _initialized = false;`.
- Export `function initPostHog()` — early-return if `_initialized` is true. Early-return if `!import.meta.env.VITE_POSTHOG_KEY`. Otherwise call `posthog.init(key, configObject)` where configObject is the literal from the RED test (do not improvise — match the assertions). Set `_initialized = true`.
- Export `function getPostHog()` returning the imported `posthog` singleton.
- Export `function optInTelemetry()` calling `posthog.opt_in_capturing()`.
- Export `function optOutTelemetry()` calling `posthog.opt_out_capturing()`.

Modify `src/main.jsx`:
- Add `import { initPostHog } from './services/posthogClient.js';` at the top of the import block.
- Insert `initPostHog();` AFTER the imports and BEFORE the first `store.dispatch(...)` line (currently `store.dispatch(initializeQuests(questsData))` around line 22). This sequencing is non-negotiable — see RESEARCH Pitfall 5.
- Wrap `<AccessibilityBridge>` and its children with `<PostHogProvider client={posthog}>` (import `PostHogProvider` from `@posthog/react` and `posthog` from `./services/posthogClient.js` via `getPostHog()` — pass the singleton to the provider). Place `<PostHogProvider>` immediately INSIDE `<Provider store={store}>` so React renders inherit it.
- Do NOT yet wrap with `<PostHogErrorBoundary>` — that's Plan 05's job alongside RouteErrorBoundary changes (avoids merge collisions).
  </action>
  <verify>
    <automated>npx vitest run src/services/__tests__/posthogClient.test.js --reporter=verbose 2>&1 | tail -5 | grep -E "passed|✓"</automated>
  </verify>
  <done>Plan 01's posthogClient.test.js is fully GREEN. main.jsx imports initPostHog and calls it before the first dispatch.</done>
  <acceptance_criteria>
    - `npx vitest run src/services/__tests__/posthogClient.test.js` exits 0 with all assertions passing.
    - `grep -n "initPostHog" src/main.jsx` shows the call appears at a line number LESS than the line of `store.dispatch(initializeQuests` (init runs first).
    - `grep -c "PostHogProvider" src/main.jsx` returns >=2 (import + JSX use).
    - `grep -v '^//\|^ *\*' src/services/posthogClient.js | grep -c "opt_out_capturing_by_default: true" >= 1` (default opt-out wired per correction 1).
    - `grep -v '^//\|^ *\*' src/services/posthogClient.js | grep -c "https://eu.i.posthog.com" >= 1` (EU default per correction 6).
    - `grep -v '^//\|^ *\*' src/services/posthogClient.js | grep -c "captureCanvas" >= 0` — and any match must NOT set the value to `true` (per correction 5). Acceptable: omitted OR explicit `false`.
    - `grep -v '^//\|^ *\*' src/services/posthogClient.js | grep -c "enable_exception_autocapture" == 0` (old API per correction 4).
    - `grep -v '^//\|^ *\*' src/services/posthogClient.js | grep -c "capture_exceptions" >= 1` (new API per correction 4).
    - Pre-existing vitest suite still green: `npx vitest run --reporter=json 2>&1 | grep -c '"status":"passed"' >= 5623`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add telemetryOptOut to settingsSlice, ship SettingsMenu toggle, register middleware skeleton</name>
  <files>src/store/slices/settingsSlice.js, src/components/Menu/SettingsMenu.jsx, src/components/Menu/SettingsMenu.module.css, src/store/middleware/telemetryMiddleware.js, src/store/store.js</files>
  <read_first>
    - src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx (Plan 01 RED test — implement to satisfy)
    - src/store/slices/settingsSlice.js (existing initialState + reducers — extend, do not replace)
    - src/components/Menu/SettingsMenu.jsx (existing toggle patterns — mirror them for the new "Share usage data" row)
    - src/store/store.js (existing middleware chain — append telemetryMiddleware)
    - .planning/phases/102-observability-test-coverage/102-RESEARCH.md section "Pattern 2: Telemetry Middleware (Redux action listener)" (skeleton structure; vocabulary deferred to Plan 03)
  </read_first>
  <behavior>
    - `settingsSlice.initialState.telemetryOptOut === true` (default for ALL users per correction 1; STATE.md flagged settingsSlice key-count drift as a known-failing test — adding telemetryOptOut bumps initial-state key count by 1, the test must be updated in this task to match new expected count).
    - `setTelemetryOptOut(boolean)` action reducer sets the flag.
    - `SettingsMenu` renders a new row "Share anonymous usage data" with a toggle bound to `settings.telemetryOptOut`. The toggle handler dispatches `setTelemetryOptOut(!current)` AND calls `optInTelemetry()` / `optOutTelemetry()` from posthogClient accordingly.
    - `telemetryMiddleware` is a skeleton: it reads `store.getState().settings.telemetryOptOut`, short-circuits when true, and currently has an EMPTY ACTION_EVENT_MAP (Plan 03 fills it in). The middleware MUST be wired into store.js's `configureStore({ middleware: ... })` chain so Plan 03 doesn't need to touch the store.
    - `initTelemetryEventBusRelay(store)` is exported but is a no-op for now (Plan 03 fills in `EventBus.on(EVENTS.ZONE_ENTERED, ...)`); call it once from main.jsx after `initPostHog()`.
  </behavior>
  <action>
1. `src/store/slices/settingsSlice.js`:
   - Add `telemetryOptOut: true` to initialState (per correction 1 — opt-OUT default for ALL users).
   - Add reducer `setTelemetryOptOut(state, action) { state.telemetryOptOut = action.payload; }`.
   - Export `setTelemetryOptOut` alongside existing toggles.
   - Update the existing settingsSlice key-count test (currently failing per STATE.md: "settingsSlice.test.js (1) — initial-state key-count drift (20 expected, 22 actual)"). Bump expected count to 23 to reflect the new key. This is INTENTIONAL alignment with a pre-existing known-failing test — confirm STATE.md still records 19 known-failing files after this change.

2. `src/store/middleware/telemetryMiddleware.js`:
   - Export `telemetryMiddleware = (store) => (next) => (action) => { const result = next(action); const optedOut = store.getState().settings?.telemetryOptOut === true; if (optedOut) return result; /* TODO Plan 03: lookup ACTION_EVENT_MAP[action.type] and call posthog.capture */ return result; };`.
   - Export `initTelemetryEventBusRelay = (store) => { /* TODO Plan 03: EventBus.on(EVENTS.ZONE_ENTERED, ...) */ };`.

3. `src/store/store.js`:
   - Import `telemetryMiddleware` and append it to the `getDefaultMiddleware().concat(...)` chain. Place AFTER existing middlewares so it sees actions after other middleware processing.

4. `src/main.jsx`:
   - After `initPostHog()` add `initTelemetryEventBusRelay(store)` (skeleton no-op until Plan 03).

5. `src/components/Menu/SettingsMenu.jsx`:
   - Add a new section "Privacy" (or extend an existing one if it exists) with a toggle row labeled "Share anonymous usage data" bound to `useSelector(s => s.settings.telemetryOptOut)`. On change: `dispatch(setTelemetryOptOut(!current))` AND call `optInTelemetry()` (when next state is opt-IN, i.e., new telemetryOptOut === false) or `optOutTelemetry()` (when next state is opt-OUT).
   - Style with `SettingsMenu.module.css` — mirror the existing toggle row class.
   - Include helper text below the toggle: "Off by default. Helps us improve Gogo Arabic. No Arabic text you write is ever sent."
  </action>
  <verify>
    <automated>npx vitest run src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx src/store/slices/__tests__/settingsSlice.test.js --reporter=verbose 2>&1 | tail -10 | grep -E "passed|✓"</automated>
  </verify>
  <done>SettingsMenuTelemetry.test.jsx is GREEN; settingsSlice key-count test passes (no longer in the known-failing list); store includes telemetryMiddleware in the middleware chain; main.jsx calls initTelemetryEventBusRelay after initPostHog.</done>
  <acceptance_criteria>
    - `npx vitest run src/components/Menu/__tests__/SettingsMenuTelemetry.test.jsx` exits 0.
    - `grep -v '^//\|^ *\*' src/store/slices/settingsSlice.js | grep -c "telemetryOptOut" >= 2` (initialState + reducer).
    - `grep -v '^//\|^ *\*' src/store/slices/settingsSlice.js | grep -c "telemetryOptOut: true" >= 1` (default opt-OUT per correction 1).
    - `grep -v '^//\|^ *\*' src/store/store.js | grep -c "telemetryMiddleware" >= 1` (registered in middleware chain).
    - `grep -v '^//\|^ *\*' src/main.jsx | grep -c "initTelemetryEventBusRelay" >= 1` (called after initPostHog).
    - `grep -v '^//\|^ *\*' src/components/Menu/SettingsMenu.jsx | grep -c "telemetryOptOut" >= 1` (UI bound to slice).
    - `grep -v '^//\|^ *\*' src/store/middleware/telemetryMiddleware.js | grep -c "settings.telemetryOptOut\|settings\?.telemetryOptOut" >= 1` (opt-out gate).
    - Pre-existing 5623 passing tests still pass: `npx vitest run --reporter=json 2>&1 | grep -c '"status":"passed"' >= 5623`.
    - The previously-known-failing `settingsSlice.test.js (1)` now PASSES (key-count expectation updated to 23). STATE.md baseline of 19 known-failing files now drops to 18 — this is allowed (it's a fix, not a regression).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> PostHog cloud | First network egress to PostHog occurs the moment a user opts in |
| settings UI -> SDK opt-out state | Toggle must reach both the Redux slice (for UI) and the SDK (for wire-level) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03 | Information Disclosure | Telemetry default behaviour | mitigate | Default opt-OUT for ALL users (`opt_out_capturing_by_default: true` in posthog.init AND `telemetryOptOut: true` in settings initial state). Belt+braces per RESEARCH Pitfall 7. |
| T-102-04 | Privacy/Compliance | Opt-out bypass | mitigate | Two-layer gate: telemetryMiddleware short-circuits on `state.settings.telemetryOptOut === true`; SDK-level `posthog.opt_out_capturing()` also stops events at the wire. Unit-tested in Plan 01 RED tests. |
| T-102-05 | Information Disclosure | VITE_POSTHOG_KEY in client bundle | accept | Public ingest key per PostHog design; protected by Project Settings -> Authorized URLs allow-list configured during Plan 01 Task 3. |
| T-102-06 | Tampering | posthog.init called twice | mitigate | Module-scoped `_initialized` boolean in posthogClient.js; idempotent. |
</threat_model>

<verification>
- `npx vitest run` — full suite passes ≥5623 (BASELINE) + 1 newly-passing test (settingsSlice key-count) + 2 newly-passing tests (posthogClient + SettingsMenuTelemetry) = ≥5626 passing.
- `npm run build` — `dist/` contains a `posthog-vendor-*.js` chunk separate from the main bundle.
- `git status --porcelain | grep -E '\.env\.local$'` returns nothing (no leaked secrets).
- Manual sanity: `npm run dev` and inspect browser devtools network tab — with `VITE_POSTHOG_KEY` set in `.env.local` and the settings toggle OFF (default), no requests to `*.posthog.com` should appear. Toggle ON, navigate, watch one request appear.
</verification>

<success_criteria>
- posthog-js@^1.376.2 and @posthog/react@^1.9.1 installed; manualChunks splits them into `posthog-vendor`.
- `initPostHog()` runs before any `store.dispatch` in main.jsx.
- Settings opt-in toggle works end-to-end (dispatches reducer + calls SDK opt API).
- `telemetryMiddleware` and `initTelemetryEventBusRelay` registered in store and main.jsx but empty (Plan 03 fills the vocabulary).
- Pre-existing tests still green; Plan 01 RED tests for OBS-01 and OBS-07 now GREEN; the known-failing settingsSlice key-count test is now GREEN.
</success_criteria>

<output>
Create `.planning/phases/102-observability-test-coverage/102-02-SUMMARY.md`: package versions, manualChunks change, files modified, RED tests turned GREEN, any deviations.
</output>
