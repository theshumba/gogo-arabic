---
phase: 102-observability-test-coverage
plan: 09
type: execute
wave: 4
depends_on: [102-02, 102-03, 102-04, 102-05, 102-06, 102-07, 102-08]
files_modified:
  - .planning/phases/102-observability-test-coverage/102-09-REGRESSION.md
autonomous: false
requirements: [OBS-09]
must_haves:
  truths:
    - "Full vitest suite reports ≥5623 passing tests (BASELINE from Plan 01) — zero regressions per OBS-09"
    - "The 5 known-failing files from STATE.md are either still failing for the SAME reasons (allowed) or have moved to passing (improvement — also allowed)"
    - "Playwright golden-path spec passes locally in <180s"
    - "Bundle size delta documented; posthog-vendor chunk verified as separate from main"
    - "Human confirmation that PostHog Live Events shows all 8 canonical events when telemetry is opted in"
  artifacts:
    - path: ".planning/phases/102-observability-test-coverage/102-09-REGRESSION.md"
      provides: "Final regression report — vitest delta, e2e timing, bundle delta, PostHog event verification"
  key_links:
    - from: "BASELINE.md (Plan 01)"
      to: "REGRESSION.md (Plan 09)"
      via: "Numerical comparison: bundle KB before/after, vitest pass count before/after"
      pattern: "vitest_baseline|bundle_baseline"
---

<objective>
Phase-end regression verification. This plan does NOT add features — it confirms that Plans 02-08 left the codebase in a state that satisfies the entire OBS-* requirement set AND introduced zero regressions against the Plan 01 BASELINE.

Three artifacts must be re-verified end-to-end:
1. **Vitest**: full suite passes ≥5623 (BASELINE). The pre-existing 19 known-failing tests should now be 18 (Plan 02 fixed settingsSlice key-count) OR unchanged — both acceptable. NEW failures are NOT acceptable.
2. **Playwright golden-path**: passes locally in <180 seconds.
3. **Bundle delta**: main bundle is still under 500KB per PROJECT.md constraint; posthog-vendor is a separate chunk per Plan 02 manualChunks config.

Plus a manual human-verify step: opt in to telemetry in `npm run dev`, play through the golden path manually, confirm PostHog Live Events shows all 8 canonical events with PII-safe payloads.

Purpose: One final, holistic gate before phase close. Catches any regression that slipped through individual-plan tests.

Output: `102-09-REGRESSION.md` documents the final numbers; human-verified PostHog event flow; STATE.md updated with phase completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/102-observability-test-coverage/102-01-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-01-BASELINE.md
@.planning/phases/102-observability-test-coverage/102-02-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-03-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-04-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-05-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-06-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-07-SUMMARY.md
@.planning/phases/102-observability-test-coverage/102-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full vitest + Playwright + bundle analysis and author REGRESSION.md</name>
  <files>.planning/phases/102-observability-test-coverage/102-09-REGRESSION.md</files>
  <read_first>
    - .planning/phases/102-observability-test-coverage/102-01-BASELINE.md (numerical comparison source-of-truth)
    - .planning/STATE.md (5650 total / 5623 passing / 19 known-failing — verify these numbers haven't shifted in unexpected ways)
    - All 8 prior plan SUMMARY files (catch any deferred items or known issues to flag in the regression report)
  </read_first>
  <action>
1. **Full vitest run:**
   - `npm run test:run 2>&1 | tee /tmp/vitest-phase-end.log`.
   - Extract from log: total tests, passing, failing, known-failing file names.
   - Compare against BASELINE.md: passing count MUST be ≥5623; any new failure is a regression that blocks phase close.
   - Identify which of the 5 known-failing files are now passing (Plan 02 was expected to fix `settingsSlice.test.js (1)`). Document the delta.

2. **Playwright golden-path run:**
   - `npx playwright test e2e/golden-path.spec.js --reporter=line 2>&1 | tee /tmp/playwright-phase-end.log`.
   - Extract: pass/fail status, total duration in seconds.
   - Confirm duration <180 seconds. If >180s, document as a regression and flag for human action.

3. **Bundle delta:**
   - `npm run build 2>&1 | tee /tmp/build-phase-end.log`.
   - Parse output for main chunk size in KB (gzipped if reported, raw otherwise — match BASELINE.md's units).
   - Compare against BASELINE.md `bundle_baseline_kb`. Expected delta: +50KB (posthog-js core) routed into a separate `posthog-vendor` chunk per Plan 02 manualChunks. Main app chunk should be ≈ baseline (the SDK is in its own chunk, not main).
   - Verify `posthog-vendor` chunk exists: `find dist -name 'posthog-vendor-*.js' | head -3`. If missing, the manualChunks config didn't take effect — flag as regression.

4. **Author `.planning/phases/102-observability-test-coverage/102-09-REGRESSION.md`** with sections:
   - **Summary**: PASS / FAIL / PARTIAL.
   - **Vitest**: baseline 5623 -> current N. Delta breakdown (new passes from Plan 02 settingsSlice fix; any new failures listed by file:line).
   - **Playwright**: baseline 6 specs -> current 7 (golden-path added). Duration.
   - **Bundle**: baseline KB -> current KB main chunk. posthog-vendor chunk size.
   - **Known-failing audit**: the 19 from STATE.md, reconciled (likely 18 after settingsSlice fix; OR all 19 if Plan 02 chose to revert that change).
   - **OBS coverage matrix**: For each of OBS-01..OBS-09, link to the plan that delivered it and the verifying test command.

5. **Update `.planning/STATE.md`**:
   - Add a row to "Shipped Milestones" or the Phase 102 section noting completion.
   - Update test counts: baseline 5623 passing -> new passing count.
   - Note the move of `settingsSlice.test.js` out of the known-failing list (if Plan 02 fixed it).
   - Do NOT mark v17.5 complete (Phase 103 and 104 remain).

The regression report MUST be machine-readable enough that a future planner can grep `phase: 102.*status: PASS|FAIL` and act accordingly.
  </action>
  <verify>
    <automated>test -f .planning/phases/102-observability-test-coverage/102-09-REGRESSION.md && grep -E "vitest.*5[6-9][0-9][0-9]" .planning/phases/102-observability-test-coverage/102-09-REGRESSION.md && grep -E "OBS-0[1-9]" .planning/phases/102-observability-test-coverage/102-09-REGRESSION.md | wc -l | awk '{ if ($1 >= 9) print "OK"; else print "FAIL: not all 9 OBS reqs documented" }'</automated>
  </verify>
  <done>REGRESSION.md exists with vitest/Playwright/bundle numbers and all 9 OBS req IDs accounted for. STATE.md updated.</done>
  <acceptance_criteria>
    - `.planning/phases/102-observability-test-coverage/102-09-REGRESSION.md` exists.
    - Vitest passing count documented and is >=5623 (no regressions per OBS-09).
    - Playwright golden-path documented as passing with duration <180s.
    - Bundle delta documented; posthog-vendor chunk verified.
    - All 9 OBS requirement IDs (OBS-01..OBS-09) appear in REGRESSION.md OBS coverage matrix.
    - `.planning/STATE.md` updated with phase-102 completion entry.
    - `git status --porcelain` shows REGRESSION.md and STATE.md as the only newly modified files in this task.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify PostHog Live Events flow + bundle inspection</name>
  <what-built>Task 1 wrote REGRESSION.md with vitest/Playwright/bundle numbers. This gate has the developer manually confirm PostHog is actually receiving events (no amount of unit testing proves the live cloud connection).</what-built>
  <how-to-verify>
    1. **PostHog Live Events sanity check (5 min):**
       - Start dev server: `npm run dev`.
       - Open browser to `http://localhost:3000`.
       - In settings, toggle "Share anonymous usage data" to ON.
       - Open PostHog (FrameCoach org -> Gogo Arabic project) -> Activity -> Live Events.
       - In the game, perform these actions and watch Live Events:
         - Start a new game (or load existing save) -> expect `quest.started` within 30s (initial quest auto-starts).
         - Walk into a different zone -> expect `zone.entered` within 30s.
         - Open dashboard -> expect `dashboard.viewed`.
         - Trigger any FSRS review -> expect `fsrs.reviewed`.
         - Complete a quest -> expect `quest.completed`.
         - Start + complete a teaching session if available -> expect `teaching.started` + `teaching.completed`.
         - Complete a grammar lesson -> expect `lesson.completed`.
       - For each event, click into Live Events to inspect properties:
         - Confirm only enum/ID keys appear (e.g., `quest_id`, `zone_id`, `cefr_level`, `rating`).
         - Confirm NO free-text fields appear (`questTitle`, `arabicText`, `npcName`, `translation`) — if any do, flag as PII regression.
       - Toggle "Share anonymous usage data" to OFF in settings.
       - Continue playing for 1 minute. Confirm NO new events appear in PostHog Live Events (opt-out gate works).

    2. **Bundle inspection (2 min):**
       - `npm run build` -> open `dist/` -> verify `posthog-vendor-*.js` exists as a separate file from the main app chunk.
       - Main app chunk size should be roughly the same as BASELINE (within ±5%).

    3. **Perf overlay sanity (1 min):**
       - Visit `http://localhost:3000/?perf=1`.
       - Confirm the green text overlay appears in top-left of canvas with FPS, Δms, draws, heap MB.
       - Confirm overlay updates roughly once per second (not flickering per-frame).
       - Confirm normal FPS (no degradation from overlay being on).
       - Revisit without `?perf=1` -> overlay absent.

    4. **Final sign-off:**
       - Update REGRESSION.md status section to `PASS` if all three checks succeed.
       - If any PII leak, opt-out failure, or unexpected event behavior is found, document the issue in REGRESSION.md and DO NOT mark phase complete — file a follow-up plan or call /gsd:plan-phase 102 --gaps.
  </how-to-verify>
  <resume-signal>Type `approved` (with REGRESSION.md status updated to PASS) OR describe issues to address.</resume-signal>
  <acceptance_criteria>
    - Human approval recorded in chat log.
    - REGRESSION.md status set to PASS (or PARTIAL with explicit follow-up plan reference if any blocking issue found).
    - All 8 canonical events confirmed visible in PostHog Live Events with PII-safe payloads.
    - Opt-out gate confirmed working (no events flow when telemetryOptOut is true).
    - posthog-vendor chunk confirmed in dist/.
    - Perf overlay confirmed visible at ?perf=1 and absent otherwise.
    - This is a `gate="blocking"` checkpoint — auto-approval (workflow.auto_advance) ignored only if `security_enforcement` requires it. For this end-of-phase verification, blocking is appropriate.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase artifacts -> PostHog cloud (first real traffic) | First time live events flow; PII gate must hold in production |
| Phase artifacts -> STATE.md (mutates project memory) | Regression report becomes the canonical source for future planners |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-26 | Information Disclosure | Live verification surfaces PII leak not caught by unit tests | mitigate | Human inspects every property of every event in Live Events; blocking gate fails phase if any free-text key found |
| T-102-27 | Tampering | New vitest regression masked by "known-failing" miscategorization | mitigate | Task 1 explicitly reconciles the 19 known-failing files; new failures stand out clearly in delta |
| T-102-28 | Repudiation | Phase claimed complete without OBS-09 verification | mitigate | Task 1 acceptance criteria require >=5623 vitest passing AND all 9 OBS IDs documented |
</threat_model>

<verification>
- REGRESSION.md exists with numerical proof for OBS-09 baseline.
- Human gate signs off PostHog Live Events flow.
- STATE.md updated.
</verification>

<success_criteria>
- Full vitest passes >=5623.
- Playwright golden-path passes <180s.
- Bundle main chunk within ±5% of BASELINE; posthog-vendor exists as separate chunk.
- Human-verified PostHog event flow with no PII leaks.
- All 9 OBS requirements satisfied and documented in REGRESSION.md.
- Phase 102 ready to close.
</success_criteria>

<output>
Create `.planning/phases/102-observability-test-coverage/102-09-SUMMARY.md`: phase-end status, key numbers, any issues surfaced, links to REGRESSION.md and updated STATE.md sections. This SUMMARY closes the phase.
</output>
