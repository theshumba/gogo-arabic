---
phase: 51-dialogue-foundation-learning-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.js
  - src/game/systems/InkDialogueEngine.js
  - src/data/ink/guide-amira.ink.json
  - src/data/ink/scholar-yusuf.ink.json
  - src/data/ink/merchant-fatima.ink.json
  - src/data/ink/student-khalid.ink.json
  - src/data/ink/librarian-ibrahim.ink.json
autonomous: true
requirements:
  - INFRA-07
  - INFRA-08
  - INFRA-09

must_haves:
  truths:
    - "Talking to guide-amira runs dialogue from compiled .ink.json file"
    - "Talking to scholar-yusuf runs dialogue from compiled .ink.json file"
    - "Talking to a non-migrated NPC like elder-tariq runs legacy JSON dialogue without errors"
    - "inkjs loads as a separate Vite chunk, not embedded in the main bundle"
    - "Ink variables are synced from Redux worldState before dialogue and flushed back after dialogue ends"
  artifacts:
    - path: "src/game/systems/InkDialogueEngine.js"
      provides: "Adapter wrapping ink Story and legacy DialogueEngine"
      exports: ["InkDialogueEngine"]
    - path: "src/data/ink/guide-amira.ink.json"
      provides: "Compiled ink story for Guide Amira"
    - path: "src/data/ink/scholar-yusuf.ink.json"
      provides: "Compiled ink story for Scholar Yusuf"
    - path: "src/data/ink/merchant-fatima.ink.json"
      provides: "Compiled ink story for Merchant Fatima"
    - path: "src/data/ink/student-khalid.ink.json"
      provides: "Compiled ink story for Student Khalid"
    - path: "src/data/ink/librarian-ibrahim.ink.json"
      provides: "Compiled ink story for Librarian Ibrahim"
  key_links:
    - from: "src/game/systems/InkDialogueEngine.js"
      to: "inkjs"
      via: "dynamic import of Story class"
      pattern: "import\\('inkjs'\\)"
    - from: "src/game/systems/InkDialogueEngine.js"
      to: "src/data/ink/*.ink.json"
      via: "import.meta.glob"
      pattern: "import\\.meta\\.glob"
    - from: "src/game/systems/InkDialogueEngine.js"
      to: "src/store/slices/worldStateSlice.js"
      via: "syncStateIn/syncStateOut reading and writing flags"
      pattern: "variablesState"
---

<objective>
Install inkjs, create the InkDialogueEngine adapter with legacy JSON fallback, author 5 pilot NPC ink dialogue files, and configure Vite chunking for ink assets.

Purpose: Establishes the foundation for ink-based dialogue scripting (INFRA-07), ensures .ink.json files get their own Vite chunks (INFRA-08), and bridges ink variablesState with Redux worldState (INFRA-09). All subsequent ink work (Amira path-choice tree in 51-03, gossip in Phase 54, inscriptions in Phase 54) depends on this adapter.

Output: Working InkDialogueEngine that can run any of 5 pilot NPCs from .ink.json and falls back to legacy DialogueEngine for all other NPCs.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-dialogue-foundation-learning-paths/51-RESEARCH.md
@src/game/systems/DialogueEngine.js
@src/store/slices/worldStateSlice.js
@src/data/worldStateKeys.js
@vite.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install inkjs + Create InkDialogueEngine adapter + Vite chunking</name>
  <files>
    package.json
    package-lock.json
    vite.config.js
    src/game/systems/InkDialogueEngine.js
  </files>
  <read_first>
    src/game/systems/DialogueEngine.js
    src/store/slices/worldStateSlice.js
    src/data/worldStateKeys.js
    src/store/store.js
    vite.config.js
    package.json
  </read_first>
  <action>
    1. Install inkjs:
       ```
       npm install inkjs
       ```

    2. Add inkjs vendor chunk to vite.config.js `manualChunks` function. Insert BEFORE the existing `if (id.includes('node_modules'))` block:
       ```javascript
       if (id.includes('node_modules/inkjs')) {
         return 'ink-vendor';
       }
       ```
       Also add a chunk for compiled ink data files — insert AFTER the lore-data chunk and BEFORE the skill-data chunk:
       ```javascript
       // Compiled ink dialogue files
       if (id.includes('src/data/ink/') && id.endsWith('.ink.json')) {
         return 'ink-dialogue';
       }
       ```

    3. Create `src/game/systems/InkDialogueEngine.js` with the following implementation:

       - Import `store` from `../../store/store.js`
       - Import `{ setFlag }` from `../../store/slices/worldStateSlice.js`
       - Import `{ WORLD_STATE_KEYS }` from `../../data/worldStateKeys.js`
       - Import `{ setLearningPath, setTutorialPhase }` from `../../store/slices/playerSlice.js`
       - Import `{ updateQuestProgress }` from `../../store/slices/questSlice.js`
       - Import `{ incrementNpcRelationship }` from `../../store/slices/narrativeSlice.js`

       - Use `import.meta.glob('../data/ink/*.ink.json')` to enumerate all ink files at build time (Vite-safe dynamic import pattern from RESEARCH.md Pitfall 2). Store result in module-level `INK_FILES` constant.

       - Export class `InkDialogueEngine` with:
         - Constructor: `constructor(scene, legacyDialogueEngine)` — stores scene ref, legacy engine ref, initializes `_story = null`, `_inkLoaded = false`, `_currentNpcId = null`

         - `async loadForNpc(npcId)`:
           - Build key `../data/ink/${npcId}.ink.json`
           - Look up key in `INK_FILES` — if no loader found, set `_inkLoaded = false` and return
           - If loader found: `const mod = await loader()`, then `const { Story } = await import('inkjs')`, then `this._story = new Story(mod.default)`, call `this._bindExternalFunctions()`, set `this._inkLoaded = true`, set `this._currentNpcId = npcId`
           - Wrap in try/catch — on error, set `_inkLoaded = false` (fallback to legacy)

         - `get isInkLoaded()` — returns `this._inkLoaded`

         - `syncStateIn()`:
           - If not ink loaded, return
           - Get `state.worldState.flags` from store
           - For each `[key, value]` in flags entries: try `this._story.variablesState[key] = value` catch (skip — ink variable not declared)
           - Also sync `player_learning_path` from `state.player.learningPath` (string or empty string)
           - Also sync `player_onboarding_complete` from `state.player.onboardingComplete` (boolean)

         - `syncStateOut()`:
           - If not ink loaded, return
           - Build `knownValues` Set from `Object.values(WORLD_STATE_KEYS)`
           - Iterate `this._story.variablesState._globalVariables` keys (use optional chaining `?. keys() ?? []`)
           - For each varName in knownValues: dispatch `setFlag({ key: varName, value: this._story.variablesState[varName] })`

         - `_bindExternalFunctions()`:
           - `this._story.BindExternalFunction('setLearningPath', (path) => { store.dispatch(setLearningPath(path)); store.dispatch(setFlag({ key: WORLD_STATE_KEYS.ONBOARDING_PATH_CHOSEN, value: true })); })`
           - `this._story.BindExternalFunction('startQuest', (questId) => { store.dispatch(updateQuestProgress({ questId, amount: 0 })); })`
           - `this._story.BindExternalFunction('changeRelationship', (npcId, amount) => { store.dispatch(incrementNpcRelationship({ npcId, amount })); })`
           - `this._story.BindExternalFunction('getFlag', (key) => { return store.getState().worldState.flags[key] ?? false; })`
           - `this._story.BindExternalFunction('getLearningPath', () => { return store.getState().player.learningPath || ''; })`

         - `canContinue()` — returns `this._inkLoaded ? this._story.canContinue : false`

         - `continue()` — returns `this._inkLoaded ? this._story.Continue() : null`

         - `currentChoices()` — returns `this._inkLoaded ? this._story.currentChoices : []`

         - `chooseChoiceIndex(index)` — calls `this._story.ChooseChoiceIndex(index)` if ink loaded

         - `reset()` — if ink loaded, calls `this._story.ResetState()`, resets `_inkLoaded = false`, `_story = null`

         - `getDialogueLines()` — convenience method: calls `syncStateIn()`, then collects all lines by looping `while (canContinue()) { lines.push(continue()) }` and returns `{ lines, choices: currentChoices() }`

  </action>
  <verify>
    - `npm ls inkjs` shows inkjs 2.x installed
    - `grep -n 'ink-vendor' vite.config.js` shows the manualChunks entry
    - `grep -n 'ink-dialogue' vite.config.js` shows the data chunk entry
    - `grep -n 'import.meta.glob' src/game/systems/InkDialogueEngine.js` shows Vite-safe glob pattern
    - `grep -n 'BindExternalFunction' src/game/systems/InkDialogueEngine.js` shows at least 5 bound functions
    - `grep -n 'syncStateIn\|syncStateOut' src/game/systems/InkDialogueEngine.js` shows both sync methods
    - `grep -n 'class InkDialogueEngine' src/game/systems/InkDialogueEngine.js` confirms the class export
  </verify>
  <acceptance_criteria>
    - package.json contains "inkjs" in dependencies
    - vite.config.js contains `id.includes('node_modules/inkjs')` returning `'ink-vendor'`
    - vite.config.js contains `id.includes('src/data/ink/')` returning `'ink-dialogue'`
    - src/game/systems/InkDialogueEngine.js contains `export class InkDialogueEngine`
    - src/game/systems/InkDialogueEngine.js contains `import.meta.glob('../data/ink/*.ink.json')`
    - src/game/systems/InkDialogueEngine.js contains `BindExternalFunction('setLearningPath'`
    - src/game/systems/InkDialogueEngine.js contains `BindExternalFunction('getFlag'`
    - src/game/systems/InkDialogueEngine.js contains `syncStateIn()`
    - src/game/systems/InkDialogueEngine.js contains `syncStateOut()`
    - src/game/systems/InkDialogueEngine.js contains `import('inkjs')` for lazy Story class load
    - src/game/systems/InkDialogueEngine.js does NOT modify DialogueEngine.js (legacy engine untouched)
  </acceptance_criteria>
  <done>InkDialogueEngine.js exists with adapter pattern: Vite-safe glob for ink files, lazy inkjs import, syncStateIn/syncStateOut for Redux bridging, and 5 BindExternalFunction calls. inkjs installed as dependency. Vite chunks configured for ink-vendor and ink-dialogue.</done>
</task>

<task type="auto">
  <name>Task 2: Author 5 pilot NPC compiled .ink.json dialogue files</name>
  <files>
    src/data/ink/guide-amira.ink.json
    src/data/ink/scholar-yusuf.ink.json
    src/data/ink/merchant-fatima.ink.json
    src/data/ink/student-khalid.ink.json
    src/data/ink/librarian-ibrahim.ink.json
  </files>
  <read_first>
    src/data/npcs.json (lines 1-50 for scholar-yusuf structure, lines 744-800 for merchant-fatima, lines 1348-1400 for student-khalid, lines 1830-1900 for librarian-ibrahim, lines 15652-15700 for guide-amira)
    src/game/systems/InkDialogueEngine.js (just created — confirm BindExternalFunction names)
    src/data/worldStateKeys.js
  </read_first>
  <action>
    Create `src/data/ink/` directory and 5 compiled `.ink.json` files.

    The inkjs npm package at version 2.4.0 ships the compiler module at `inkjs/compiler/Compiler`. Confirm this exists before proceeding:
    ```
    ls node_modules/inkjs/compiler/
    ```
    The Compiler module MUST be available — it ships with inkjs 2.x. Use it to compile .ink source to .ink.json. Do NOT hand-roll compiled ink JSON — the format is complex and brittle (RESEARCH.md explicitly lists this as an anti-pattern in the "Don't Hand-Roll" table: "Compiled ink story format / Custom JSON format parser / Use Inklecate / Inky compiler").

    If for any reason `inkjs/compiler/Compiler` does not resolve (verify with a quick `node -e "import('inkjs/compiler/Compiler').then(m => console.log('OK', Object.keys(m))).catch(e => console.log('MISSING', e.message))"`), use the Inky desktop app to compile — do NOT attempt to write raw .ink.json by hand.

    Create a Node.js build script `scripts/compile-ink.mjs` that:
    1. Reads `.ink` source files from `src/data/ink-source/` directory
    2. Uses inkjs Compiler to compile them to `.ink.json`
    3. Writes output to `src/data/ink/` directory

    Then create 5 ink SOURCE files in `src/data/ink-source/`:

    **guide-amira.ink** — Amira's general dialogue (NOT the path-choice tree — that comes in Plan 51-03):
    ```ink
    VAR onboarding_complete = false
    VAR onboarding_first_word_learned = false
    VAR onboarding_mentor_met = false

    -> main

    === main ===
    {onboarding_complete:
      -> returning_player
    - else:
      -> first_meeting
    }

    === first_meeting ===
    السلام عليكم يا صديقي! أنا أميرة، سأكون مرشدتك.
    ~ changeRelationship("guide-amira", 5)
    أهلاً وسهلاً في عالم اللغة العربية!
    * [ما هذا المكان؟]
      هذه واحة المعرفة — مكان آمن لتعلم العربية.
      -> main_hub
    * [من أنتِ؟]
      أنا أميرة، عالمة ومرشدة. سأساعدك في رحلتك.
      -> main_hub

    === returning_player ===
    عودة ميمونة! ماذا تريد أن نفعل اليوم؟
    -> main_hub

    === main_hub ===
    * [أريد أن أتعلم]
      تعال، لنبدأ الدرس!
      -> END
    * [أخبريني عن هذا المكان]
      هذا المكان يحتوي على كنوز المعرفة العربية.
      -> END
    * [وداعاً]
      مع السلامة! سأكون هنا عندما تحتاجني.
      -> END
    ```

    **scholar-yusuf.ink** — Scholar Yusuf's dialogue with cultural/historical content:
    ```ink
    VAR quest_act_1_complete = false

    -> main

    === main ===
    {quest_act_1_complete:
      -> advanced_topics
    - else:
      -> introduction
    }

    === introduction ===
    بسم الله الرحمن الرحيم. أنا يوسف، عالم المخطوطات.
    ~ changeRelationship("scholar-yusuf", 3)
    هل تعلم أن بيت الحكمة في بغداد كان أعظم مكتبة في العالم؟
    * [أخبرني المزيد عن بيت الحكمة]
      أسسه الخليفة هارون الرشيد في القرن الثامن الميلادي.
      ترجموا فيه كتب الإغريق والفرس والهنود إلى العربية.
      -> knowledge_hub
    * [ما هذه المخطوطات؟]
      هذه مخطوطات من العصر الذهبي الإسلامي.
      فيها علوم الرياضيات والفلك والطب.
      -> knowledge_hub

    === advanced_topics ===
    أهلاً بالعالم الشاب! أرى أنك تقدمت في رحلتك.
    -> knowledge_hub

    === knowledge_hub ===
    * [علمني كلمة جديدة]
      كلمة اليوم: مخطوط — manuscript.
      من جذر خ-ط-ط الذي يعني الكتابة.
      -> END
    * [عن الخوارزمي]
      الخوارزمي أبو الجبر — كلمة algorithm مشتقة من اسمه!
      -> END
    * [وداعاً]
      في أمان الله. العلم نور.
      -> END
    ```

    **merchant-fatima.ink** — Merchant Fatima with trade/commerce dialogue:
    ```ink
    VAR global_first_purchase_made = false

    -> main

    === main ===
    {global_first_purchase_made:
      -> returning_customer
    - else:
      -> first_visit
    }

    === first_visit ===
    أهلاً وسهلاً في دكاني! أنا فاطمة التاجرة.
    ~ changeRelationship("merchant-fatima", 3)
    عندي أجود البضائع من طريق الحرير.
    * [ماذا تبيعين؟]
      عندي توابل من الهند، حرير من الصين، وعطور من الحجاز.
      التجارة فن قديم — المساومة جزء منها!
      -> shop_hub
    * [أخبريني عن طريق الحرير]
      طريق الحرير ربط الشرق بالغرب لألف سنة.
      التجار العرب كانوا وسطاء بين الحضارات.
      -> shop_hub

    === returning_customer ===
    أهلاً بزبوني المفضل! عندي بضاعة جديدة اليوم.
    -> shop_hub

    === shop_hub ===
    * [أريد أن أشتري]
      تفضل، انظر إلى بضاعتي!
      -> END
    * [علميني عن التجارة]
      كلمة سوق — market — من أقدم الكلمات العربية.
      وكلمة تاجر — merchant — من الجذر ت-ج-ر.
      -> END
    * [وداعاً]
      مع السلامة! ارجع قريباً.
      -> END
    ```

    **student-khalid.ink** — Student Khalid, tutorial-oriented dialogue:
    ```ink
    VAR onboarding_complete = false

    -> main

    === main ===
    {onboarding_complete:
      -> study_buddy
    - else:
      -> fellow_student
    }

    === fellow_student ===
    مرحباً! أنا خالد، طالب مثلك.
    ~ changeRelationship("student-khalid", 3)
    أنا أيضاً أتعلم العربية. نتعلم معاً!
    * [كيف أتعلم بسرعة؟]
      التكرار مهم جداً. راجع كلماتك كل يوم.
      ونظام FSRS يساعدك — يعرض الكلمات الصعبة أكثر.
      -> study_hub
    * [ما هي النصائح؟]
      اقرأ اللافتات في القرية — كل واحدة تعلمك كلمة.
      وتحدث مع كل شخص تقابله.
      -> study_hub

    === study_buddy ===
    يا صديقي! كم كلمة تعلمت اليوم؟
    -> study_hub

    === study_hub ===
    * [لنتدرب معاً]
      هيا بنا! التدريب يجعلنا أفضل.
      -> END
    * [ما معنى هذه الكلمة؟]
      أي كلمة؟ أنا أحب المفردات الجديدة!
      -> END
    * [إلى اللقاء]
      مع السلامة يا صديقي! لا تنسَ المراجعة.
      -> END
    ```

    **librarian-ibrahim.ink** — Librarian Ibrahim, scholarly/manuscript focus:
    ```ink
    VAR quest_act_1_complete = false

    -> main

    === main ===
    {quest_act_1_complete:
      -> deep_collection
    - else:
      -> welcome
    }

    === welcome ===
    أهلاً! أنا إبراهيم، أمين المكتبة.
    ~ changeRelationship("librarian-ibrahim", 3)
    هذه المكتبة تحتوي على آلاف المخطوطات.
    * [ما أهم كتاب هنا؟]
      كتاب الحيل لبني موسى — عن الآلات الميكانيكية.
      كُتب في القرن التاسع وما زلنا نتعلم منه!
      -> library_hub
    * [كيف أجد كتاباً؟]
      المخطوطات مرتبة حسب الموضوع.
      الطب هنا، الفلك هناك، والرياضيات في الطابق العلوي.
      -> library_hub

    === deep_collection ===
    عودة طيبة! عندي مخطوطات جديدة لأريك.
    -> library_hub

    === library_hub ===
    * [أريد أن أقرأ]
      تفضل. "اقرأ" — أول كلمة نزلت في القرآن.
      -> END
    * [عن المكتبة]
      هذه المكتبة نموذج لبيت الحكمة الأصلي.
      -> END
    * [شكراً]
      العفو. المعرفة حق للجميع.
      -> END
    ```

    Create the compile script `scripts/compile-ink.mjs`:
    ```javascript
    #!/usr/bin/env node
    import { readFileSync, writeFileSync, mkdirSync, readdirSync } from 'fs';
    import { join, basename } from 'path';

    // Use inkjs compiler
    const srcDir = join(import.meta.dirname, '..', 'src', 'data', 'ink-source');
    const outDir = join(import.meta.dirname, '..', 'src', 'data', 'ink');

    mkdirSync(outDir, { recursive: true });

    async function compile() {
      const { Compiler } = await import('inkjs/compiler/Compiler');
      const files = readdirSync(srcDir).filter(f => f.endsWith('.ink'));

      for (const file of files) {
        const source = readFileSync(join(srcDir, file), 'utf-8');
        const compiler = new Compiler(source);
        const story = compiler.Compile();
        const jsonStr = story.ToJson();
        const outName = basename(file, '.ink') + '.ink.json';
        writeFileSync(join(outDir, outName), jsonStr);
        console.log(`Compiled: ${file} -> ${outName}`);
      }
    }

    compile().catch(err => {
      console.error('Ink compilation failed:', err);
      process.exit(1);
    });
    ```

    Add npm script to package.json scripts: `"ink:compile": "node scripts/compile-ink.mjs"`

    Run `npm run ink:compile` to generate the 5 `.ink.json` files.

    Verify all 5 .ink.json files load correctly by checking they are valid JSON with an `inkVersion` field.
  </action>
  <verify>
    - `ls node_modules/inkjs/compiler/` confirms the Compiler module ships with inkjs 2.x
    - `ls src/data/ink/*.ink.json | wc -l` shows 5
    - `ls src/data/ink-source/*.ink | wc -l` shows 5
    - `node -e "const fs = require('fs'); const files = fs.readdirSync('src/data/ink').filter(f => f.endsWith('.ink.json')); files.forEach(f => { const d = JSON.parse(fs.readFileSync('src/data/ink/' + f)); console.log(f, 'inkVersion:', d.inkVersion); });"` shows all 5 files with valid inkVersion
    - `npm run build` completes without errors (ink files chunked correctly)
    - `grep 'ink:compile' package.json` shows the npm script
  </verify>
  <acceptance_criteria>
    - src/data/ink/guide-amira.ink.json exists and contains valid JSON with `inkVersion` field
    - src/data/ink/scholar-yusuf.ink.json exists and contains valid JSON with `inkVersion` field
    - src/data/ink/merchant-fatima.ink.json exists and contains valid JSON with `inkVersion` field
    - src/data/ink/student-khalid.ink.json exists and contains valid JSON with `inkVersion` field
    - src/data/ink/librarian-ibrahim.ink.json exists and contains valid JSON with `inkVersion` field
    - src/data/ink-source/guide-amira.ink exists with ink source containing `-> main` and EXTERNAL function declarations
    - scripts/compile-ink.mjs exists and uses `inkjs/compiler/Compiler` (NOT hand-rolled JSON)
    - package.json contains `"ink:compile"` script
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>5 pilot NPC ink source files authored with Arabic dialogue, cultural content, and EXTERNAL function calls. Compile script generates valid .ink.json files via inkjs Compiler. Build succeeds with ink assets in their own Vite chunk.</done>
</task>

</tasks>

<verification>
1. `npm ls inkjs` confirms inkjs installed
2. `npm run build` succeeds — check `dist/assets/` for `ink-vendor-*.js` chunk
3. InkDialogueEngine.js has import.meta.glob, syncStateIn, syncStateOut, 5+ BindExternalFunction calls
4. All 5 .ink.json files parse as valid JSON with inkVersion field
5. DialogueEngine.js is UNCHANGED (adapter pattern, no modification of legacy engine)
</verification>

<success_criteria>
- INFRA-07: inkjs installed and InkDialogueEngine wraps both ink Story and legacy JSON via adapter pattern
- INFRA-08: .ink.json files load via import.meta.glob (Vite-safe dynamic import) and get their own Vite chunk
- INFRA-09: InkDialogueEngine.syncStateIn() reads Redux worldState flags into ink variablesState; syncStateOut() flushes ink mutations back to Redux
- 5 pilot NPCs (guide-amira, scholar-yusuf, merchant-fatima, student-khalid, librarian-ibrahim) have compiled .ink.json dialogue files
</success_criteria>

<output>
After completion, create `.planning/phases/51-dialogue-foundation-learning-paths/51-01-SUMMARY.md`
</output>
</output>
