---
phase: 51-dialogue-foundation-learning-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/companionDialogue.js
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "All 12 companions have complete dialogue with no placeholder Array.from lines"
    - "No companion responds with a repeated identical phrase across all entries in a category"
    - "Each companion's dialogue reflects their distinct personality and specialty"
  artifacts:
    - path: "src/data/companionDialogue.js"
      provides: "2,400+ unique contextual dialogue lines across 12 companions"
      contains: "COMPANION_DIALOGUE"
  key_links:
    - from: "src/data/companionDialogue.js"
      to: "src/store/slices/companionSlice.js"
      via: "COMPANION_DIALOGUE import for companion chat system"
      pattern: "COMPANION_DIALOGUE"
---

<objective>
Fill all 573 missing companion dialogue lines across the 11 companions that currently use Array.from placeholders, plus add ~29 lines to Amira to reach her full target.

Purpose: CONT-01 requires that all 12 companions have complete, unique dialogue. Currently 11 of 12 companions (everyone except Amira) have Array.from placeholder lines where every entry in each category is identical text. This plan replaces all placeholders with unique, contextual Arabic dialogue that matches each companion's personality and specialty.

Output: companionDialogue.js with 573+ new unique lines replacing all Array.from placeholders.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/companionDialogue.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill companion dialogue for Khalid, Zahra, Omar, Layla, Hassan, and Fatima</name>
  <files>src/data/companionDialogue.js</files>
  <read_first>
    src/data/companionDialogue.js
  </read_first>
  <action>
    Replace all `Array.from({length: N}, ...)` placeholder lines for companions 2-7 with unique, contextual Arabic dialogue. Each line must have unique arabic, english, and transliteration text (no two lines within any single companion should have the same arabic text).

    **Companion personalities and specialties to guide dialogue writing:**

    - **companion_khalid** (Student Khalid) — young, enthusiastic learner; specialty: vocabulary practice and study tips; zones: oasis_village, sacred_library, desert_market
    - **companion_zahra** (Zahra the Healer) — wise herbalist; specialty: nature vocabulary, healing plants; zones: farmland, mountain_pass, oasis_village
    - **companion_omar** (Omar the Cook) — jovial chef; specialty: food vocabulary, cooking; zones: desert_market, oasis_village, coastal_town (NOTE: Omar already has real greetings and some crafting lines — preserve those and only replace remaining Array.from blocks)
    - **companion_layla** (Layla the Scribe) — meticulous scholar; specialty: writing, grammar, manuscripts; zones: sacred_library, ancient_ruins, mountain_pass
    - **companion_hassan** (Hassan the Guard) — honorable warrior; specialty: military/honor vocabulary, protection; zones: mountain_pass, oasis_village, ancient_ruins
    - **companion_fatima** (Fatima the Explorer) — adventurous archaeologist; specialty: history, artifacts, exploration; zones: ancient_ruins, sacred_library, desert_market

    **Per-companion line counts (matching the existing Array.from structure):**
    Each companion with full placeholder has: 11 greetings + 45 zone_comments (15 per zone x 3 zones) + 20 object_comments + 15 battle_comments + 30 teaching + 15 relationship (5 low + 5 medium + 5 high) + 15 idle = ~151 lines to fill.

    **Important rules:**
    1. Keep the exact same data structure: `{ id, arabic, english, transliteration, context/trigger/objectTypes/topic/cefrMin/minRelationship }` matching each category's schema from Amira's real data
    2. Keep the exact same ID patterns: e.g., `khalid_greet_01`, `layla_lib_01`, `hassan_battle_01`
    3. Zone keys must match exactly what's in the existing Array.from blocks (e.g., `sacred_library`, `ancient_ruins`, `mountain_pass` for Layla)
    4. Teaching lines: use appropriate `topic` and `cefrMin` fields matching the companion's specialty. Khalid=vocabulary, Zahra=nature, Omar=food (his teaching key is `cooking`), Layla=grammar, Hassan=culture, Fatima=culture
    5. Relationship lines must have `minRelationship` values: low=0, medium=30, high=70
    6. All Arabic must be grammatically correct MSA (Modern Standard Arabic / Fusha)
    7. For Omar: read his existing real lines first (greetings lines 1-11 and crafting lines already exist) — only replace the remaining Array.from blocks

    Write all 6 companions' complete dialogue in this task. That is approximately 6 x ~151 = ~906 lines, but CONT-01's scope is filling the 573 missing lines. Some companions (Omar) have partial real content — only fill their remaining gaps.
  </action>
  <verify>
    - `grep -c 'Array.from' src/data/companionDialogue.js` shows the count decreasing (should approach 0 for these 6 companions)
    - `grep 'companion_khalid' src/data/companionDialogue.js | head -5` shows unique Arabic text (not identical)
    - `node -e "const {COMPANION_DIALOGUE} = require('./src/data/companionDialogue.js'); console.log(Object.keys(COMPANION_DIALOGUE));"` — may not work with ESM; alternatively check with grep
  </verify>
  <acceptance_criteria>
    - src/data/companionDialogue.js contains no `Array.from` calls for companion_khalid
    - src/data/companionDialogue.js contains no `Array.from` calls for companion_zahra
    - src/data/companionDialogue.js contains no `Array.from` calls for companion_layla
    - src/data/companionDialogue.js contains no `Array.from` calls for companion_hassan
    - src/data/companionDialogue.js contains no `Array.from` calls for companion_fatima
    - companion_khalid has 11 greetings with unique `arabic` values (no two identical)
    - companion_layla has 15 zone_comments entries for sacred_library with unique `arabic` values
    - companion_hassan has 15 battle_comments with unique `arabic` values
    - All teaching lines for these companions have `topic` and `cefrMin` fields
    - All relationship lines have `minRelationship` field (0, 30, or 70)
  </acceptance_criteria>
  <done>6 companions (Khalid, Zahra, Omar remainder, Layla, Hassan, Fatima) have all Array.from placeholders replaced with unique contextual Arabic dialogue lines matching their personality and specialty.</done>
</task>

<task type="auto">
  <name>Task 2: Fill companion dialogue for Ali, Maryam, Samir, Nadia, Tariq + Amira additions</name>
  <files>src/data/companionDialogue.js</files>
  <read_first>
    src/data/companionDialogue.js
  </read_first>
  <action>
    Replace all `Array.from({length: N}, ...)` placeholder lines for the remaining 5 companions and add ~29 lines to Amira's existing real dialogue.

    **Companion personalities and specialties:**

    - **companion_ali** (Ali the Healer) — gentle physician; specialty: medical vocabulary, healing, pronunciation; zones: oasis_village, coastal_town, sacred_library
    - **companion_maryam** (Maryam the Weaver) — creative artisan; specialty: textile/art vocabulary, patterns, colors; zones: desert_market, oasis_village, mountain_pass
    - **companion_samir** (Samir the Traveler) — worldly merchant; specialty: geography, travel vocabulary, directions; zones: coastal_town, desert_market, mountain_pass
    - **companion_nadia** (Nadia the Musician) — expressive artist; specialty: music, poetry, emotional vocabulary; zones: oasis_village, sacred_library, coastal_town
    - **companion_tariq** (Tariq the Elder) — wise storyteller; specialty: proverbs, wisdom, historical vocabulary; zones: oasis_village, ancient_ruins, sacred_library

    **Per-companion line counts:** Same as Task 1 (~151 lines each for the 5 full-placeholder companions).

    **Amira additions (~29 lines):**
    Amira already has ~171 real lines. Add lines to reach ~200:
    - Add 5 more zone_comments for a 4th zone (ancient_ruins — Amira doesn't have this zone yet)
    - Add 5 more teaching lines (etymology topic, cefrMin A1/A2)
    - Add 4 more battle_comments (varied triggers)
    - Add 5 more idle comments (varied Arabic phrases)
    - Add 5 more greetings (context: quest, battle, evening)
    - Add 5 more object_comments (objectTypes: fountain, garden, statue)

    **Same rules as Task 1:**
    1. Same data structure per category matching Amira's schema
    2. Same ID patterns: `ali_greet_01`, `maryam_market_01`, etc.
    3. Zone keys must match the existing Array.from blocks
    4. Teaching topics: Ali=pronunciation, Maryam=vocabulary (textiles), Samir=geography, Nadia=poetry, Tariq=proverbs
    5. Relationship: low=0, medium=30, high=70
    6. Grammatically correct MSA Arabic
    7. For Amira additions: continue her ID sequence (e.g., amira_greet_12, amira_teach_19, etc.)

    After all replacements, verify the total new unique lines is >= 573 (5 companions x ~151 = ~755 + 29 Amira = ~784, exceeding the 573 requirement).
  </action>
  <verify>
    - `grep -c 'Array.from' src/data/companionDialogue.js` returns 0 (no remaining Array.from calls)
    - `grep 'companion_ali\|companion_maryam\|companion_samir\|companion_nadia\|companion_tariq' src/data/companionDialogue.js | head -10` shows unique Arabic text
    - `grep -c 'amira_greet_12\|amira_greet_13\|amira_greet_14\|amira_greet_15\|amira_greet_16' src/data/companionDialogue.js` shows 5 new Amira greetings
    - `npm run build` completes without errors
  </verify>
  <acceptance_criteria>
    - src/data/companionDialogue.js contains exactly 0 occurrences of `Array.from`
    - companion_ali has 11 greetings with unique `arabic` values
    - companion_maryam has 11 greetings with unique `arabic` values
    - companion_samir has 11 greetings with unique `arabic` values
    - companion_nadia has 11 greetings with unique `arabic` values
    - companion_tariq has 11 greetings with unique `arabic` values
    - companion_amira zone_comments contains an `ancient_ruins` key with entries
    - companion_amira greetings array has 16+ entries (was 11, added 5+)
    - companion_amira teaching.grammar has 18+ entries (was ~18, added new etymology entries)
    - All teaching lines for these companions have `topic` and `cefrMin` fields
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 12 companions have complete, unique dialogue — zero Array.from placeholders remain. 573+ new unique lines added across the full companion roster. CONT-01 fully satisfied.</done>
</task>

</tasks>

<verification>
1. `grep -c 'Array.from' src/data/companionDialogue.js` returns exactly 0
2. `wc -l src/data/companionDialogue.js` shows significant increase from baseline 533 lines
3. `npm run build` succeeds without errors
4. Spot-check: 3 random companions have unique Arabic text in their greetings (no identical entries)
</verification>

<success_criteria>
- CONT-01: All 573+ missing companion dialogue lines are filled across 12 companions
- Zero Array.from placeholder calls remain in companionDialogue.js
- Each companion's dialogue is thematically consistent with their personality
- All Arabic text is grammatically correct MSA
- Build succeeds with the expanded dialogue data
</success_criteria>

<output>
After completion, create `.planning/phases/51-dialogue-foundation-learning-paths/51-02-SUMMARY.md`
</output>
