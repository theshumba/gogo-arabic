---
plan: 62-01
phase: 62
name: B1-B2 Grammar Lesson Expansion
objective: Expand all 23 existing B1/B2 stub lessons to 12+ exercises and 4+ quiz questions each, add 7 new B2 lessons to reach 50 total, and update grammarChecker tests
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/data/grammar.js
  - src/data/__tests__/grammarChecker.test.js
requirements: [GRAM-01]
must_haves:
  truths:
    - "grammar.js contains exactly 50 lessons total"
    - "Every B1 lesson has 12+ exercises and 4+ quiz questions"
    - "Every B2 lesson has 12+ exercises and 4+ quiz questions"
    - "Every B1/B2 lesson uses at least 4 distinct exercise types"
    - "grammarChecker tests validate all 50 lessons pass data integrity"
  artifacts:
    - src/data/grammar.js
    - src/data/__tests__/grammarChecker.test.js
  key_links:
    - from: grammarChecker.test.js
      to: grammar.js
      via: "import grammarLessons"
---

# Plan 62-01: B1-B2 Grammar Lesson Expansion

## Objective

Expand all 23 existing B1/B2 stub lessons (1 exercise, 0 quiz questions each) to full standard (12+ exercises, 4+ quiz questions, 4+ distinct exercise types), and add 7 new B2 lessons to reach the 50-lesson target. Update grammarChecker tests to validate all CEFR levels, not just A1/A2.

## Tasks

<task id="62-01-01" type="auto">
<name>Expand B1/B2 stubs + add 7 new lessons + update tests</name>

<read_first>
- src/data/grammar.js (current 43 lessons, B1/B2 stubs at 1 exercise each)
- src/data/__tests__/grammarChecker.test.js (current tests validate A1/A2 only)
</read_first>

<action>
**Part A: Expand 13 B1 stub lessons**

Each of the 13 B1 lessons (verb-forms-2-5 through indirect-object, orders 25-37) currently has 1 exercise and 0 quiz questions. Expand each to:
- 12 exercises using at least 4 distinct types from: fill-blank, translate, match, conjugation-drill, sentence-transformation, word-order, error-identification, true-false, multiple-select, cloze, classify, build-sentence
- 4 quiz questions with 4 options each, one correct
- All Arabic text must be real Arabic (not transliteration)

Follow the exact schema pattern from the A1/A2 lessons (e.g., al-definite). Each exercise needs: `type`, `prompt`, `answer`, and type-specific fields (`options` for choice types, `pairs` for match, `sentence`/`error`/`correction` for error-identification, etc.)

**Part B: Expand 10 B2 stub lessons**

Same as Part A for the 10 B2 lessons (complex-conditionals through formal-letter, orders 38-47).

**Part C: Add 7 new B2 lessons to reach 50 total**

Create 7 new B2 lessons with orders 48-54. Suggested topics (all genuine B2 Arabic grammar):
1. `idafa-complex` (order 48) — Complex إضافة chains
2. `jussive-mood` (order 49) — المجزوم (jussive verb mood)
3. `subjunctive-mood` (order 50) — المنصوب (subjunctive verb mood)
4. `vocative-case` (order 51) — أسلوب النداء (vocative expressions)
5. `specification-tamyiz-2` (order 52) — تمييز العدد (number specification)
6. `circumstantial-hal-2` (order 53) — الحال الجملة (sentential circumstantial)
7. `style-variety` (order 54) — الأساليب البلاغية (rhetorical styles)

Each new lesson needs: `id`, `title`, `titleArabic`, `category` (use existing categories: verbs, nouns, prepositions, etc.), `cefrLevel: 'B2'`, `difficulty` (4 or 5), `order`, 12+ exercises, 4+ quiz questions. Add to the `grammarLessons` array in order.

**Part D: Update grammarChecker tests**

In `src/data/__tests__/grammarChecker.test.js`, update tests to validate ALL CEFR levels (not just A1/A2):

1. Change the A1/A2-only filter to include B1/B2:
```javascript
// Was: const a1a2 = grammarLessons.filter(l => ['A1','A2'].includes(l.cefrLevel));
// Now: all lessons should pass the same quality bar
const allLessons = grammarLessons;
```

2. Add a test: `it('contains exactly 50 lessons', () => expect(grammarLessons).toHaveLength(50));`

3. Add a test for B1/B2 distribution:
```javascript
it('has at least 13 B1 and at least 17 B2 lessons', () => {
  const b1 = grammarLessons.filter(l => l.cefrLevel === 'B1');
  const b2 = grammarLessons.filter(l => l.cefrLevel === 'B2');
  expect(b1.length).toBeGreaterThanOrEqual(13);
  expect(b2.length).toBeGreaterThanOrEqual(17);
});
```
</action>

<verify>
```bash
npx vitest run src/data/__tests__/grammarChecker.test.js
```
```bash
node -e "const g = require('./src/data/grammar.js'); console.log('Total:', g.grammarLessons.length); const b1b2 = g.grammarLessons.filter(l => l.cefrLevel === 'B1' || l.cefrLevel === 'B2'); const stubs = b1b2.filter(l => l.exercises.length < 12); console.log('Stubs remaining:', stubs.length); console.log('B1:', g.grammarLessons.filter(l=>l.cefrLevel==='B1').length, 'B2:', g.grammarLessons.filter(l=>l.cefrLevel==='B2').length);"
```
```bash
npx vitest run
```
</verify>

<acceptance_criteria>
- grammar.js contains exactly 50 lessons (grep `id:` returns 50+ matches including categories)
- `node -e "..."` above shows Total: 50, Stubs remaining: 0
- Every B1 lesson has exercises.length >= 12 and quizQuestions.length >= 4
- Every B2 lesson has exercises.length >= 12 and quizQuestions.length >= 4
- grammarChecker.test.js passes with all lessons validated
- Full test suite passes with no regressions
- Orders are contiguous from 1 to 54 with no gaps within B1/B2 range
</acceptance_criteria>
</task>
