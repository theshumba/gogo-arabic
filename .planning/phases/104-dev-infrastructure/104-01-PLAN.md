---
phase: 104-dev-infrastructure
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/spike-atlas-naming.mjs
  - scripts/verify-atlas-determinism.mjs
  - package.json
  - package-lock.json
autonomous: false
requirements: [PIPE-01]
tags: [phaser, atlas, free-tex-packer-core, determinism, spike, ci]
user_setup: []

must_haves:
  truths:
    - "free-tex-packer-core@0.3.5 is installed as a devDependency and importable from a Node ESM script"
    - "Given a `{path: 'kenmi-desert-foo-bar', contents: buf}` input, the resulting atlas JSON has a `frames['kenmi-desert-foo-bar']` entry — frame names preserve the full catalog-style key (no codemod required across zones.js)"
    - "Running scripts/verify-atlas-determinism.mjs twice in clean temp directories produces byte-identical PNGs and byte-identical JSON files (sha256 match)"
    - "If determinism check fails, the script exits non-zero and prints which file diverged"
  artifacts:
    - path: scripts/spike-atlas-naming.mjs
      provides: "30-min spike: packs 3 fake images with various path values, prints resulting JSON frames keys to stdout, exits 0"
      contains: "packAsync"
    - path: scripts/verify-atlas-determinism.mjs
      provides: "Packs a fixed mini-fixture twice into two temp dirs, sha256-compares every output file, exits 1 on mismatch"
      contains: "createHash('sha256')"
    - path: package.json
      provides: "free-tex-packer-core in devDependencies; npm script `pipe:verify-atlas` wired"
      contains: "free-tex-packer-core"
  key_links:
    - from: scripts/verify-atlas-determinism.mjs
      to: free-tex-packer-core
      via: "import { packAsync } from 'free-tex-packer-core'"
      pattern: "from 'free-tex-packer-core'"
    - from: package.json
      to: scripts/verify-atlas-determinism.mjs
      via: "scripts.pipe:verify-atlas npm script"
      pattern: "pipe:verify-atlas"
---

<objective>
Wave-0 spike that de-risks the entire phase before committing to free-tex-packer-core across 969 sprites. Two open questions must be answered with running code, not assumptions:

1. **Frame-naming question (RESEARCH Open Question #1):** Does passing `{path: 'kenmi-desert-foo'}` to packAsync produce a frame literally named `kenmi-desert-foo`? If yes, we avoid a 2091-line codemod across `src/data/zones.js`. If no, the planner must escalate.
2. **Determinism question (RESEARCH Pitfall 1 + Assumption A1):** With the three locked levers (sorted input + `allowRotation:false, allowTrim:false, detectIdentical:true` + per-image SHA256 `hash`), does the packer produce byte-identical output across two runs? This is the linchpin for PIPE-01 and the CI cache.

Purpose: Eliminate the highest-uncertainty assumptions before we build Plans 02-08 on top of them. The verifier script also becomes the CI gate that Plan 09 wires into `.github/workflows/`.

Output: Two reusable Node scripts and a devDependency install that downstream plans can rely on. The verifier is the determinism contract for the entire phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/104-dev-infrastructure/104-CONTEXT.md
@.planning/phases/104-dev-infrastructure/104-RESEARCH.md
@.planning/phases/38-asset-pipeline/38-01-SUMMARY.md
@.planning/phases/38-asset-pipeline/38-02-SUMMARY.md

<interfaces>
<!-- Existing exports the spike does NOT touch but is sized against -->

From src/data/kenmiCatalog.js (969 entries):
```js
export const KENMI_CATALOG = [
  { key: 'kenmi-desert-npc-desert-person-1', path: '/assets/kenmi/desert/npc/desert-person-1.png', type: 'spritesheet', frameWidth: 16, frameHeight: 16 },
  // ... 968 more
];
```

From free-tex-packer-core@0.3.5 README (target API):
```js
import { packAsync } from 'free-tex-packer-core';
// images: Array<{ path: string, contents: Buffer, hash?: string }>
// options: { textureName, width, height, padding, exporter: 'Phaser3', allowRotation, allowTrim, detectIdentical, ... }
const result = await packAsync(images, options);
// result: Array<{ name: string, buffer: Buffer }> — PNGs + JSON files
```
</interfaces>

<package_legitimacy>
<!-- RESEARCH.md Package Legitimacy Audit — all packages [ASSUMED]; checkpoint:human-verify required per protocol -->

| Package | Why needed | npmjs.com check |
|---------|-----------|-----------------|
| free-tex-packer-core@0.3.5 | Atlas packer with Phaser3 exporter | https://www.npmjs.com/package/free-tex-packer-core — verify "odrick" is publisher, last publish 2025-08-29, weekly downloads ~5K |
| Transitive: jimp@1.6.1, maxrects-packer@2.7.3, mustache@2.3.0 | Pulled in by free-tex-packer-core | All confirmed no postinstall in RESEARCH; legitimacy via the parent install gate |
</package_legitimacy>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate for free-tex-packer-core</name>
  <what-built>Pre-install verification — slopcheck CLI is unavailable in this dev environment, so every new dependency in Phase 104 is gated by a blocking human check before `npm install`. No code has been written yet.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/free-tex-packer-core in a browser.
    2. Confirm publisher is `odrick` and the linked GitHub repo is https://github.com/odrick/free-tex-packer-core (MIT license).
    3. Confirm latest version is 0.3.5 and last published date is 2025-08-29 (or later if a patch shipped).
    4. Weekly downloads should be ≥1K (~5K expected per RESEARCH.md).
    5. Spot-check that `package.json` in the npm tarball has no `scripts.postinstall` field. (Optional: `npm view free-tex-packer-core@0.3.5 scripts.postinstall` should print nothing.)
    6. Approve the install of `free-tex-packer-core@0.3.5` as a devDependency (which transitively pulls jimp@1.6.x, maxrects-packer@2.7.x, mustache@2.3.x — all manually cross-checked in RESEARCH.md).
  </how-to-verify>
  <action>Human-only verification step. No code executes in this task. The human opens npmjs.com to confirm package legitimacy (publisher identity, publish date, download count, postinstall absence) before the next task is allowed to run `npm install`. On approval, Task 2 proceeds; on rejection, this phase replans without free-tex-packer-core.</action>
  <resume-signal>Type "approved" to allow `npm install --save-dev free-tex-packer-core@0.3.5` in the next task, or describe any concerns.</resume-signal>
  <read_first>
    - .planning/phases/104-dev-infrastructure/104-RESEARCH.md (Package Legitimacy Audit section)
  </read_first>
  <acceptance_criteria>
    - Human types "approved" or equivalent.
    - If rejected, the orchestrator must replan without free-tex-packer-core.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Install free-tex-packer-core and write the frame-naming spike</name>
  <files>scripts/spike-atlas-naming.mjs, package.json, package-lock.json</files>
  <read_first>
    - package.json (verify `"type": "module"` and existing devDependencies block)
    - scripts/generate-kenmi-catalog.js (existing ESM script style to mirror)
    - .planning/phases/104-dev-infrastructure/104-RESEARCH.md (Open Question #1, Pattern 1)
  </read_first>
  <action>
    Run `npm install --save-dev free-tex-packer-core@0.3.5` exactly (no `^`, no `latest`) — pin the version explicitly per RESEARCH.md determinism guidance.

    Create `scripts/spike-atlas-naming.mjs` as an ESM script that answers RESEARCH Open Question #1 in ~30 minutes of work:

    - Imports `packAsync` from `free-tex-packer-core`.
    - Synthesizes three 16x16 transparent PNG `Buffer`s in memory using a hand-encoded minimal PNG (or by reading one real PNG from `public/assets/kenmi/base/tiles/` if synthesis is overkill — use the simpler path).
    - Calls `packAsync(images, options)` three times with three different `path` styles for each variant:
      a) `path: 'kenmi-desert-foo'` (catalog-key style, no extension, no slashes)
      b) `path: 'kenmi-desert/foo.png'` (path-like with extension)
      c) `path: 'foo.png'` (filename only)
    - Use options: `{ textureName: 'spike', width: 64, height: 64, padding: 0, exporter: 'Phaser3', allowRotation: false, allowTrim: false, detectIdentical: true, removeFileExtension: false }`.
    - For each variant, parse the resulting JSON file from `result` and `console.log` the array of frame names found inside `frames` (or `textures[].frames[].filename` depending on Phaser3 multiatlas shape).
    - At the end, `console.log` a recommendation: "USE STYLE (a)" if variant (a) produces a frame named `kenmi-desert-foo`; otherwise print which style preserves the catalog key intact.
    - Exit 0 on success, 1 on any thrown error.

    Add an npm script `"pipe:spike-atlas": "node scripts/spike-atlas-naming.mjs"` to package.json.

    DO NOT inline implementation code in this action — keep the script self-contained; the executor writes the actual JS.
  </action>
  <verify>
    <automated>node scripts/spike-atlas-naming.mjs 2>&1 | tee /tmp/spike-out.txt; grep -q "USE STYLE" /tmp/spike-out.txt</automated>
  </verify>
  <done>
    - `free-tex-packer-core@0.3.5` appears in `devDependencies` (exact version, no caret).
    - `scripts/spike-atlas-naming.mjs` exists and exits 0.
    - Script output contains the literal string `USE STYLE` followed by the recommended `path` style and one of the three frame-name listings shows the catalog-style key (e.g., `kenmi-desert-foo`) preserved verbatim.
    - `npm run pipe:spike-atlas` works from a clean checkout.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write scripts/verify-atlas-determinism.mjs and wire CI npm script</name>
  <files>scripts/verify-atlas-determinism.mjs, package.json</files>
  <read_first>
    - scripts/spike-atlas-naming.mjs (from Task 2 — reuse the packAsync calling pattern)
    - .planning/phases/104-dev-infrastructure/104-RESEARCH.md (Pitfall 1 — three determinism levers; Pattern 1 example code)
  </read_first>
  <behavior>
    - Test 1: Running the script twice in sequence produces sha256-equal outputs for every file.
    - Test 2: If a fixture file is mutated between runs, the script exits non-zero and prints the diverging filename.
    - Test 3: The script uses sorted filenames (alphabetical), per-input SHA256 `hash` property, and the three pinned options `allowRotation:false, allowTrim:false, detectIdentical:true`.
  </behavior>
  <action>
    Create `scripts/verify-atlas-determinism.mjs` per RESEARCH.md Pattern 1, in ESM Node syntax (the project uses `"type": "module"`):

    - Read a small fixed input set: take the first 10 PNGs from `public/assets/kenmi/base/tiles/grass/` (sorted alphabetically). If that directory is empty, fall back to globbing `public/assets/kenmi/base/**/*.png` and taking the first 10 sorted. Hard-fail with a clear error if neither yields 10 files.
    - Compute SHA256 of each file and build the `{path, contents, hash}` input array per RESEARCH Pattern 1. Use the frame-naming style discovered by the Task 2 spike (default: style (a) `kenmi-base-tiles-grass-foo` style keys); this is configurable via a constant near the top of the file.
    - Call `packAsync(images, options)` with options frozen to: `{ textureName: 'verify', width: 1024, height: 1024, padding: 2, extrude: 0, allowRotation: false, allowTrim: false, detectIdentical: true, powerOfTwo: false, exporter: 'Phaser3', removeFileExtension: false, packer: 'MaxRectsPacker', packerMethod: 'Smart' }`. Sort the result array by `name` before writing.
    - Write outputs to a fresh tmp dir (e.g., `await mkdtemp(join(tmpdir(), 'atlas-verify-a-'))`). Run packAsync a second time and write to a second fresh tmp dir.
    - For each output file in dir A, sha256 the bytes and compare to sha256 of the same-named file in dir B. Print a row per file. If any row mismatches OR file sets differ, print `DIVERGENCE: <filename>` and exit 1.
    - On full match, print `DETERMINISTIC: N files verified` and exit 0.
    - Use `node:crypto`, `node:fs/promises`, `node:os`, `node:path` (built-ins, no new deps).

    Add npm script `"pipe:verify-atlas": "node scripts/verify-atlas-determinism.mjs"` to package.json.

    Note: The script is the determinism contract. It is invoked unchanged by Plan 09's CI workflow — do not couple it to any specific pack subset; the 10-file fixture is intentional small surface that runs in <5s.
  </action>
  <verify>
    <automated>node scripts/verify-atlas-determinism.mjs && node scripts/verify-atlas-determinism.mjs && node scripts/verify-atlas-determinism.mjs</automated>
  </verify>
  <done>
    - `scripts/verify-atlas-determinism.mjs` exists and exits 0 on three consecutive invocations.
    - Output contains the literal string `DETERMINISTIC: 10 files verified` (or however many fixtures resolved — at least 6).
    - `npm run pipe:verify-atlas` succeeds.
    - File contains the constants `allowRotation: false`, `allowTrim: false`, `detectIdentical: true` (grep-verifiable).
    - File contains `createHash('sha256')` (per-input hash and per-output hash both used).
    - If the determinism check were ever to fail in CI (Plan 09 wires this in), the non-zero exit is the gate that fails PIPE-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | New devDependency installs; supply-chain risk |
| Local filesystem → build artifacts | Atlas outputs written to temp dirs; no production runtime exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | npm install free-tex-packer-core (transitive jimp, maxrects-packer, mustache) | mitigate | `checkpoint:human-verify` Task 1 (blocking-human gate) per RESEARCH Package Legitimacy Audit; postinstall scripts verified absent |
| T-104-02 | Tampering | Atlas determinism (non-byte-identical output) | mitigate | scripts/verify-atlas-determinism.mjs — three determinism levers + double-pack diff; non-zero exit on mismatch |
| T-104-SC | Tampering | npm install gate | mitigate | slopcheck unavailable in dev env → blocking-human checkpoint per protocol (`[ASSUMED]` packages) |
</threat_model>

<verification>
- `npm run pipe:spike-atlas` exits 0 and prints `USE STYLE` recommendation.
- `npm run pipe:verify-atlas` exits 0 on first run AND second run (idempotent).
- `git diff package.json` shows exactly one new devDependency: `free-tex-packer-core` at version `0.3.5` (no caret, no tilde).
- Existing 5650-test vitest suite continues to pass: `npm run test:run --reporter=dot` shows no new failures vs STATE.md baseline (5623 passing).
</verification>

<success_criteria>
1. `free-tex-packer-core@0.3.5` installed with human approval.
2. Frame-naming question answered with running code (RESEARCH Open Question #1 closed).
3. Determinism verifier exists, exits 0 deterministically, and is wired as `npm run pipe:verify-atlas`.
4. No regression in vitest suite count (baseline 5623 passing).
5. Plan 02 can safely call `packAsync` knowing both questions are answered.
</success_criteria>

<output>
Create `.planning/phases/104-dev-infrastructure/104-01-SUMMARY.md` with:
- Which frame-naming style the spike recommended (a, b, or c) and the exact frame-name strings emitted by each variant.
- The verifier's output (N files verified).
- Any pinned transitive versions (`jimp`, `maxrects-packer`, `mustache`) from the resulting package-lock.json.
- A go/no-go signal for Plan 02 (i.e., is the no-codemod strategy viable, or must Plan 02 add a zones.js codemod?).
</output>
