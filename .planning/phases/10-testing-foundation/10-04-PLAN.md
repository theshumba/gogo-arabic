---
phase: 10-testing-foundation
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/store/middleware/__tests__/achievementMiddleware.test.js
  - src/store/middleware/__tests__/dailyGoalsMiddleware.test.js
  - src/components/HUD/__tests__/HUD.test.jsx
  - src/components/NPC/__tests__/DialogueOverlay.test.jsx
  - src/components/Quiz/__tests__/QuizOverlay.test.jsx
  - src/components/Game/__tests__/PauseMenu.test.jsx
  - src/components/Dashboard/__tests__/DailyDashboard.test.jsx
  - src/components/World/__tests__/WorldMap.test.jsx
  - src/components/Profile/__tests__/PlayerProfile.test.jsx
autonomous: true

must_haves:
  truths:
    - "Component tests cover HUD, DialogueOverlay, QuizOverlay, PauseMenu, DailyDashboard, WorldMap, and PlayerProfile with user interaction scenarios"
    - "Achievement middleware and daily goals middleware have integration tests verifying action sequences and side effects"
    - "Component tests use renderWithProviders with real Redux store (not mocked selectors)"
    - "User interactions are simulated with @testing-library/user-event (not fireEvent)"
  artifacts:
    - path: "src/store/middleware/__tests__/achievementMiddleware.test.js"
      provides: "Achievement middleware integration tests"
    - path: "src/store/middleware/__tests__/dailyGoalsMiddleware.test.js"
      provides: "Daily goals middleware integration tests"
    - path: "src/components/HUD/__tests__/HUD.test.jsx"
      provides: "HUD component tests (may already exist — extend if so)"
    - path: "src/components/NPC/__tests__/DialogueOverlay.test.jsx"
      provides: "DialogueOverlay component tests"
    - path: "src/components/Quiz/__tests__/QuizOverlay.test.jsx"
      provides: "QuizOverlay component tests"
    - path: "src/components/Game/__tests__/PauseMenu.test.jsx"
      provides: "PauseMenu component tests"
    - path: "src/components/Dashboard/__tests__/DailyDashboard.test.jsx"
      provides: "DailyDashboard component tests"
    - path: "src/components/World/__tests__/WorldMap.test.jsx"
      provides: "WorldMap component tests"
    - path: "src/components/Profile/__tests__/PlayerProfile.test.jsx"
      provides: "PlayerProfile component tests"
  key_links:
    - from: "src/store/middleware/__tests__/achievementMiddleware.test.js"
      to: "src/store/slices/achievementSlice.js"
      via: "configureStore with real middleware"
      pattern: "achievementMiddleware"
    - from: "src/components/**/__tests__/*.test.jsx"
      to: "src/test/testUtils.jsx"
      via: "renderWithProviders for Redux + Router context"
      pattern: "renderWithProviders"
---

<objective>
Write integration tests for both Redux middleware (achievementMiddleware, dailyGoalsMiddleware) and component tests for 7 key UI components (HUD, DialogueOverlay, QuizOverlay, PauseMenu, DailyDashboard, WorldMap, PlayerProfile).

Purpose: Middleware tests verify that action sequences trigger correct side effects (achievement unlocks, daily goal progress). Component tests verify user interactions work correctly with real Redux state. Both are critical safety nets before Phase 11 architecture refactoring.

Output: 2 middleware test files, 7 component test files (or 6 new + 1 extended if HUD.test.jsx exists).
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-testing-foundation/10-RESEARCH.md

# Plan 01 SUMMARY (test infra, factories, EventBus cleanup)
@.planning/phases/10-testing-foundation/10-01-SUMMARY.md

# Test utilities
@src/test/testUtils.jsx
@src/test/setup.js
@src/test/factories/playerFactory.js
@src/test/factories/questFactory.js
@src/test/factories/vocabularyFactory.js

# Middleware source files
@src/store/middleware/achievementMiddleware.js
@src/store/middleware/dailyGoalsMiddleware.js

# Component source files — read ALL before writing tests
@src/components/HUD/HUD.jsx
@src/components/NPC/DialogueOverlay.jsx
@src/components/Quiz/QuizOverlay.jsx
@src/components/Game/PauseMenu.jsx
@src/components/Dashboard/DailyDashboard.jsx
@src/components/World/WorldMap.jsx
@src/components/Profile/PlayerProfile.jsx

# Existing HUD test (extend, don't replace)
@src/components/HUD/__tests__/HUD.test.jsx

# Slice files (for preloaded state in component tests)
@src/store/slices/uiSlice.js
@src/store/slices/playerSlice.js
@src/store/slices/questSlice.js
@src/store/slices/achievementSlice.js
@src/store/slices/dailyGoalsSlice.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write integration tests for achievement and daily goals middleware</name>
  <files>
    src/store/middleware/__tests__/achievementMiddleware.test.js
    src/store/middleware/__tests__/dailyGoalsMiddleware.test.js
  </files>
  <action>
    **Middleware testing pattern:** Create a real Redux store with the middleware under test + relevant reducers. Dispatch action sequences and verify the middleware's side effects on the store state.

    **1. achievementMiddleware.test.js:**
    Read `achievementMiddleware.js` (164 lines) to understand:
    - Which actions it listens for (e.g., incrementWordsLearned, completeQuest, etc.)
    - What thresholds trigger achievement unlocks
    - What side effects it dispatches (unlockAchievement, addXP)

    Create test store with: `achievementMiddleware`, `achievementReducer`, `playerReducer`, `vocabularyReducer`, `questReducer`

    Test cases:
    - Dispatching the action that triggers a word-count achievement → verify achievement unlocked in state
    - Dispatching below threshold → verify no achievement unlocked
    - Dispatching same trigger twice → verify achievement not duplicated (idempotent)
    - Achievement unlock awards XP → verify player.xp increased
    - Multiple achievements can unlock in sequence

    **2. dailyGoalsMiddleware.test.js:**
    Read `dailyGoalsMiddleware.js` (86 lines) to understand:
    - Which actions it listens for
    - How it tracks daily goal progress
    - What happens when goals are completed

    Create test store with: `dailyGoalsMiddleware`, `dailyGoalsReducer`, `playerReducer`

    Test cases:
    - Relevant action dispatched → daily goal progress incremented
    - Goal progress reaches target → goal marked complete
    - Goal completion awards rewards
    - Actions outside goal scope → no change to goals

    **Important:** Use `configureStore` directly (NOT createTestStore from testUtils, which omits middleware). Include only the slices each middleware needs.
  </action>
  <verify>
    Run `npx vitest run --reporter=verbose src/store/middleware/__tests__/` — both test files pass.
    Each middleware test has at least 3 test cases covering threshold, idempotency, and side effects.
  </verify>
  <done>
    Both middleware have integration tests proving: correct action triggers unlock/progress, thresholds work, side effects (XP awards, goal completion) fire correctly, and idempotency is maintained. TEST-03 complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write component tests for 7 key UI components</name>
  <files>
    src/components/HUD/__tests__/HUD.test.jsx
    src/components/NPC/__tests__/DialogueOverlay.test.jsx
    src/components/Quiz/__tests__/QuizOverlay.test.jsx
    src/components/Game/__tests__/PauseMenu.test.jsx
    src/components/Dashboard/__tests__/DailyDashboard.test.jsx
    src/components/World/__tests__/WorldMap.test.jsx
    src/components/Profile/__tests__/PlayerProfile.test.jsx
  </files>
  <action>
    For each component, read the source file to understand:
    - Props it accepts
    - Redux state it reads (useSelector calls)
    - Actions it dispatches
    - User interactions (clicks, keyboard shortcuts)
    - Conditional rendering logic

    **Pattern for all component tests:**
    ```jsx
    import { describe, it, expect, vi } from 'vitest';
    import { screen, waitFor } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { renderWithProviders } from '../../../test/testUtils.jsx';
    import ComponentName from '../ComponentName.jsx';
    ```

    Use `renderWithProviders` with `preloadedState` to set up Redux state for the component's needs. Use `userEvent.setup()` for all interactions (not fireEvent).

    **Per-component specifics:**

    1. **HUD.test.jsx** — EXTEND existing test file (don't replace). Add:
       - Renders player level, XP bar, dirhams from Redux state
       - Compass visibility toggles based on active quest
       - Quest objective text displays from active quest state

    2. **DialogueOverlay.test.jsx** — NEW:
       - Renders NPC name and dialogue text when ui.dialogueOpen is true
       - Does not render when ui.dialogueOpen is false
       - Advance button moves to next dialogue line
       - Close button/Escape key closes overlay and dispatches closeDialogue
       - Renders quiz prompt when dialogue has quiz config
       - Mock Framer Motion: `vi.mock('framer-motion', () => ({ motion: { div: (props) => <div {...props} /> }, AnimatePresence: ({ children }) => children }))`

    3. **QuizOverlay.test.jsx** — NEW:
       - Renders quiz question and answer choices
       - Clicking a choice highlights it (selected state)
       - Submit button dispatches answer and shows feedback
       - Correct answer shows success feedback
       - Incorrect answer shows error feedback with correct answer
       - Next button advances to next question
       - Mock Framer Motion same as above

    4. **PauseMenu.test.jsx** — NEW:
       - Renders menu items (Resume, Settings, World Map, Wardrobe, etc.)
       - Clicking Resume calls onResume callback
       - Clicking Settings navigates to /settings
       - Clicking World Map navigates to /game/map
       - Escape key triggers close

    5. **DailyDashboard.test.jsx** — NEW:
       - Renders streak count from player state
       - Renders daily goals with progress bars
       - Renders review count (due reviews)
       - Start Review button navigates to /review
       - Activity suggestions render based on player progress

    6. **WorldMap.test.jsx** — NEW:
       - Renders zone nodes on the map
       - Completed zones show completion percentage
       - Locked zones show lock indicator
       - Clicking unlocked zone dispatches fast travel action
       - Clicking locked zone shows teaser (doesn't navigate)

    7. **PlayerProfile.test.jsx** — NEW:
       - Renders player name, level, XP from Redux state
       - Renders streak calendar
       - Renders achievement badges (unlocked vs locked)
       - Stats section shows words learned, quests completed, etc.

    **Important:**
    - Use `waitFor` for async state updates after user interactions
    - Mock Framer Motion in every component that uses it (prevents animation-related test failures)
    - If a component uses Phaser-related imports, mock them at the module level
    - Use factories from Plan 01 for complex preloadedState objects
    - Don't test CSS styling — test rendered content and user interactions
  </action>
  <verify>
    Run `npx vitest run --reporter=verbose src/components/` — all 7 component test files pass.
    Each component test has at least: 1 render test, 1 interaction test, 1 conditional rendering test.
  </verify>
  <done>
    All 7 specified components have passing tests covering rendering, user interactions, and conditional UI states. Tests use real Redux store via renderWithProviders and userEvent for realistic interactions. TEST-02 complete.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/store/middleware/__tests__/ --reporter=verbose` — middleware tests pass
2. `npx vitest run src/components/ --reporter=verbose` — all component tests pass
3. `npx vitest run --reporter=verbose` — full test suite passes (no regressions from Plans 01/03)
4. `grep -r "userEvent.setup" src/components/` — confirms user-event usage (not fireEvent)
5. `grep -r "renderWithProviders" src/components/` — confirms real Redux store usage
</verification>

<success_criteria>
- 2 middleware integration test files created and passing
- 7 component test files created/extended and passing
- Middleware tests verify action sequences, thresholds, side effects, idempotency
- Component tests verify rendering, interactions, conditional UI
- All tests use real Redux store (not mocked selectors)
- All interactions use userEvent (not fireEvent)
- TEST-02 complete: Component tests with user interaction scenarios
- TEST-03 complete: Middleware integration tests for action sequences and side effects
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-foundation/10-04-SUMMARY.md`
</output>
