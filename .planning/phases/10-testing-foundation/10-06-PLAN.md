---
phase: 10-testing-foundation
plan: 06
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - vitest.config.js
  - package.json
autonomous: true

must_haves:
  truths:
    - "Coverage thresholds are configured and enforced at current baseline (target 80%+) in vitest.config.js"
    - "Coverage thresholds fail the test run if not met"
    - "npm test:coverage script runs coverage report with threshold enforcement"
  artifacts:
    - path: "vitest.config.js"
      provides: "Coverage thresholds configuration"
      contains: "thresholds"
    - path: "package.json"
      provides: "Updated test:coverage script"
  key_links:
    - from: "vitest.config.js"
      to: "src/**/*.{js,jsx}"
      via: "coverage.include matching all source files"
      pattern: "coverage.*include.*src"
---

<objective>
Configure coverage thresholds in vitest.config.js to enforce 80%+ overall coverage, with per-directory overrides for Phaser systems (lower) and components (standard). Run the full test suite to verify coverage meets thresholds.

Purpose: Coverage thresholds prevent regression — any future change that drops coverage below 80% will fail CI. This ensures the testing investment from Plans 01-04 is maintained throughout Phase 11-13 refactoring.

Output: Updated vitest.config.js with coverage thresholds, verified passing coverage run.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-testing-foundation/10-RESEARCH.md

# Prior plan summaries (need to know what was built)
@.planning/phases/10-testing-foundation/10-01-SUMMARY.md
@.planning/phases/10-testing-foundation/10-03-SUMMARY.md
@.planning/phases/10-testing-foundation/10-04-SUMMARY.md

# Current config
@vitest.config.js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run coverage report, analyze gaps, configure thresholds</name>
  <files>
    vitest.config.js
    package.json
  </files>
  <action>
    1. **Run current coverage report:**
       ```bash
       npx vitest run --coverage --reporter=verbose
       ```
       Analyze the output to understand current coverage percentages for:
       - `src/store/slices/` (should be high — 12 test files)
       - `src/store/middleware/` (should be high — 2 test files)
       - `src/components/` (should be moderate — 7 test files, but many untested components exist)
       - `src/game/systems/` (should be moderate — 6 test files, but business logic focus)
       - `src/utils/` (should have some — 3 existing test files)
       - `src/services/` (should have some — 1 existing test)
       - `src/hooks/` (may be low — no dedicated tests)

    2. **Set coverage thresholds in `vitest.config.js`:**
       Based on the actual coverage numbers, set thresholds that are:
       - At or slightly below current coverage (so they pass now)
       - At least 80% for well-tested areas
       - Lower for harder-to-test areas (game systems, hooks)

       Update the `coverage` section:
       ```javascript
       coverage: {
         provider: 'v8',
         reporter: ['text', 'html', 'json-summary'],
         include: ['src/**/*.{js,jsx}'],
         exclude: ['src/test/**', 'src/data/**'],
         thresholds: {
           // Global thresholds — overall coverage must meet these
           lines: 80,
           functions: 75,
           branches: 70,
           statements: 80,
         },
       }
       ```

       **Adjust thresholds based on actual numbers:**
       - If overall is 82% → set threshold to 80%
       - If overall is 75% → set threshold to 73% and add a comment `// TODO: raise to 80% after adding hook tests`
       - If game systems are at 50% → that's expected (Phaser is hard to test), the global threshold accounts for this
       - The goal is thresholds that PASS now but catch regressions

    3. **Add `json-summary` reporter** (useful for CI badge generation).

    4. **Verify `package.json` test scripts** are correct:
       - `"test": "vitest"` (watch mode)
       - `"test:run": "vitest run"` (single run)
       - `"test:coverage": "vitest run --coverage"` (with thresholds)

    5. **Run coverage with thresholds to confirm they pass:**
       ```bash
       npx vitest run --coverage
       ```
       If thresholds fail, lower them to match current coverage (rounding down to nearest 5%). The point is establishing a floor, not aspirational targets.

    6. **If overall coverage is below 80%:** Document which areas are low in the SUMMARY.md. This is acceptable — the tests from Plans 01-04 cover the critical paths. Coverage can be raised incrementally. Set thresholds to current values (not 80%) and add a comment noting the gap.
  </action>
  <verify>
    Run `npx vitest run --coverage` — passes with 0 threshold violations.
    Confirm `vitest.config.js` has `thresholds` in coverage config.
    Run `npx vitest run --coverage 2>&1 | grep "Coverage"` — shows coverage percentages.
  </verify>
  <done>
    Coverage thresholds configured and enforced in vitest.config.js. All thresholds pass on current test suite. Any future code change that drops coverage below thresholds will fail the test run. TEST-07 complete (or documented with plan to reach 80% if currently below).
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run --coverage` — passes with all thresholds met
2. `grep "thresholds" vitest.config.js` — confirms thresholds configured
3. Coverage report shows per-directory breakdown
4. No manual overrides needed to pass (thresholds match actual coverage)
</verification>

<success_criteria>
- vitest.config.js has coverage thresholds configured
- `npx vitest run --coverage` passes with all thresholds met
- Coverage reporter includes text, html, and json-summary
- If below 80%: thresholds set to current level with TODO comment to improve
- If at/above 80%: thresholds set to 80%
- TEST-07 complete: Coverage thresholds enforced
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-foundation/10-06-SUMMARY.md`
</output>
