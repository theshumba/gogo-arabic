---
phase: 10-testing-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/__tests__/NPCManager.test.js
  - src/game/systems/__tests__/PlayerController.test.js
  - src/game/systems/__tests__/MapLoader.test.js
  - src/game/systems/__tests__/DOMOverlay.test.js
  - src/game/systems/__tests__/InteractableManager.test.js
  - src/game/systems/__tests__/ZoneTransition.test.js
  - src/game/systems/__tests__/mocks/sceneMock.js
autonomous: true

must_haves:
  truths:
    - "Phaser game systems (MapLoader, NPCManager, PlayerController, DOMOverlay, InteractableManager, ZoneTransition) have unit tests with proper scene mocks"
    - "Business logic in Phaser systems is tested (proximity detection, quest markers, zone transitions) without requiring a running Phaser instance"
    - "Scene mocks are reusable across all game system tests"
  artifacts:
    - path: "src/game/systems/__tests__/mocks/sceneMock.js"
      provides: "Reusable Phaser scene mock factory"
      exports: ["createMockScene"]
    - path: "src/game/systems/__tests__/NPCManager.test.js"
      provides: "NPCManager unit tests"
    - path: "src/game/systems/__tests__/PlayerController.test.js"
      provides: "PlayerController unit tests"
    - path: "src/game/systems/__tests__/MapLoader.test.js"
      provides: "MapLoader unit tests"
    - path: "src/game/systems/__tests__/DOMOverlay.test.js"
      provides: "DOMOverlay unit tests"
    - path: "src/game/systems/__tests__/InteractableManager.test.js"
      provides: "InteractableManager unit tests"
    - path: "src/game/systems/__tests__/ZoneTransition.test.js"
      provides: "ZoneTransition unit tests"
  key_links:
    - from: "src/game/systems/__tests__/mocks/sceneMock.js"
      to: "src/game/systems/*.js"
      via: "Mock scene injected into system constructors"
      pattern: "createMockScene"
    - from: "src/game/systems/__tests__/*.test.js"
      to: "src/store/store.js"
      via: "Redux store reads in Phaser systems (store.getState())"
      pattern: "store\\.getState"
---

<objective>
Create reusable Phaser scene mocks and write unit tests for all 6 game systems (NPCManager, PlayerController, MapLoader, DOMOverlay, InteractableManager, ZoneTransition).

Purpose: Phaser systems contain business logic (proximity detection, quest marker display, zone transitions, interactable management) that must be tested before Phase 11 refactoring. These systems read Redux store directly (can't use React hooks) and require minimal scene mocks that provide only the Phaser APIs each system actually uses.

Output: 1 shared scene mock factory, 6 game system test files.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-testing-foundation/10-RESEARCH.md

# Source files — read ALL of these before writing tests
@src/game/systems/NPCManager.js
@src/game/systems/PlayerController.js
@src/game/systems/MapLoader.js
@src/game/systems/DOMOverlay.js
@src/game/systems/InteractableManager.js
@src/game/systems/ZoneTransition.js
@src/utils/eventBus.js
@src/store/store.js

# Test setup (for global mocks already configured)
@src/test/setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable Phaser scene mock factory</name>
  <files>
    src/game/systems/__tests__/mocks/sceneMock.js
  </files>
  <action>
    Read ALL 6 game system files to understand which Phaser.Scene APIs they use. Build a minimal mock that covers only what's needed.

    **Create `src/game/systems/__tests__/mocks/sceneMock.js`:**

    Export `createMockScene(overrides = {})` that returns a mock scene object with:

    - `physics.add.collider(a, b, callback)` → vi.fn()
    - `physics.add.sprite(x, y, key)` → returns mock sprite with `{ x, y, setOrigin: vi.fn(), setDepth: vi.fn(), setScale: vi.fn(), setSize: vi.fn(), setOffset: vi.fn(), body: { setSize: vi.fn(), setOffset: vi.fn() }, play: vi.fn(), anims: { play: vi.fn() } }`
    - `physics.add.staticGroup()` → returns mock group with `{ create: vi.fn(), getChildren: vi.fn(() => []) }`
    - `add.image(x, y, key)` → vi.fn() returning mock image
    - `add.sprite(x, y, key)` → vi.fn() returning mock sprite
    - `add.tilemap(config)` → vi.fn() returning mock tilemap
    - `make.tilemap(config)` → vi.fn() returning mock tilemap with `{ createLayer: vi.fn(), addTilesetImage: vi.fn(), getObjectLayer: vi.fn() }`
    - `cameras.main` → `{ scrollX: 0, scrollY: 0, centerOn: vi.fn(), startFollow: vi.fn(), setBounds: vi.fn(), setZoom: vi.fn() }`
    - `input.keyboard.addKey(key)` → vi.fn() returning mock key
    - `input.keyboard.createCursorKeys()` → vi.fn() returning `{ up: {}, down: {}, left: {}, right: {} }`
    - `time.delayedCall(delay, callback)` → vi.fn()
    - `load.tilemapTiledJSON(key, url)` → vi.fn()
    - `load.image(key, url)` → vi.fn()
    - `scene.start(key)` → vi.fn()
    - `scene.key` → 'TestScene'
    - `sys.game.canvas` → `{ width: 1024, height: 768 }`

    Also mock Phaser module at top of file:
    ```javascript
    vi.mock('phaser', () => ({
      default: {
        Math: { Distance: { Between: vi.fn((x1,y1,x2,y2) => Math.sqrt((x2-x1)**2 + (y2-y1)**2)) } },
        Input: { Keyboard: { JustDown: vi.fn(() => false), KeyCodes: { E: 69, SPACE: 32, M: 77, SHIFT: 16 } } },
        Physics: { Arcade: { Sprite: class {} } },
        Events: { EventEmitter: class { on() {} off() {} emit() {} removeAllListeners() {} } }
      }
    }));
    ```

    Allow overrides: `createMockScene({ cameras: { main: customCamera } })` merges with defaults.
  </action>
  <verify>
    Confirm `src/game/systems/__tests__/mocks/sceneMock.js` exists and exports `createMockScene`.
  </verify>
  <done>
    Reusable scene mock factory created with all Phaser APIs used by the 6 game systems, allowing unit testing without a running Phaser instance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for all 6 Phaser game systems</name>
  <files>
    src/game/systems/__tests__/NPCManager.test.js
    src/game/systems/__tests__/PlayerController.test.js
    src/game/systems/__tests__/MapLoader.test.js
    src/game/systems/__tests__/DOMOverlay.test.js
    src/game/systems/__tests__/InteractableManager.test.js
    src/game/systems/__tests__/ZoneTransition.test.js
  </files>
  <action>
    For each game system, read the source file carefully, then write tests focusing on **business logic** (not rendering or physics simulation).

    **Pattern for each test file:**
    ```javascript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { createMockScene } from './mocks/sceneMock.js';
    // Import the system class
    ```

    **1. NPCManager.test.js** (128 lines source):
    - Test `create()`: Verify NPC objects created from config, DOM labels created
    - Test `update()`: Verify proximity detection (player near NPC → interaction prompt visible, player far → hidden)
    - Test interaction range constant (INTERACT_RANGE)
    - Mock Redux store for NPC quest state

    **2. PlayerController.test.js** (71 lines source):
    - Test `create()`: Verify player sprite created at correct position
    - Test movement logic: cursor keys → velocity changes
    - Test sprint: shift key → increased speed
    - Test animation switching based on direction

    **3. MapLoader.test.js** (294 lines source):
    - Test `loadMap()`: Verify tilemap creation, layer setup, collision configuration
    - Test `getSpawnPoint()`: Verify spawn point extraction from object layer
    - Test NPC spawn point extraction
    - Test interactable object extraction
    - Mock tilemap.getObjectLayer to return test data

    **4. DOMOverlay.test.js** (182 lines source):
    - Test `createNpcLabel()`: Verify DOM element creation with correct attributes
    - Test `createInteractionPrompt()`: Verify prompt element creation
    - Test `setVisible()`: Verify visibility toggling
    - Test `updatePosition()`: Verify position calculation from world coords to screen coords
    - Test `destroy()`: Verify cleanup of all DOM elements
    - Test HTML escaping of NPC names (XSS prevention)

    **5. InteractableManager.test.js** (169 lines source):
    - Test `create()`: Verify interactable objects created from config
    - Test proximity detection logic
    - Test interaction triggering (dispatch events via EventBus)
    - Test different interactable types (chest, sign, etc.)

    **6. ZoneTransition.test.js** (46 lines source):
    - Test zone boundary detection
    - Test transition event emission
    - Test zone ID mapping

    **Important:**
    - Mock `phaser` module using vi.mock() at top of each test file or import from sceneMock
    - Mock Redux store: `vi.mock('../../../store/store.js', () => ({ store: { getState: vi.fn(() => mockState), dispatch: vi.fn() } }))`
    - Mock EventBus: `vi.mock('../../../utils/eventBus.js', () => ({ EventBus: { emit: vi.fn(), on: vi.fn(), off: vi.fn() } }))`
    - Focus on testable business logic. Skip testing Phaser physics engine behavior (collisions, velocity).
    - If a system constructor requires scene methods that are hard to mock, test the public methods that DON'T depend on the constructor. Alternatively, test the constructor doesn't throw.
  </action>
  <verify>
    Run `npx vitest run --reporter=verbose src/game/systems/__tests__/` — all 6 test files should pass.
    Each test should have at least 2 test cases per public method.
  </verify>
  <done>
    All 6 Phaser game systems have passing unit tests covering business logic (proximity detection, spawn point extraction, DOM element management, zone transitions). Scene mocks prevent Phaser runtime dependency. TEST-06 complete.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run src/game/systems/__tests__/ --reporter=verbose` — all game system tests pass
2. `ls src/game/systems/__tests__/mocks/` — sceneMock.js exists
3. `ls src/game/systems/__tests__/*.test.js | wc -l` — shows 6 test files
4. No test imports real Phaser (all mocked)
</verification>

<success_criteria>
- Reusable scene mock factory covering all required Phaser APIs
- 6 game system test files created and passing
- Tests cover business logic (proximity, spawning, DOM management, transitions)
- No Phaser runtime required for tests to pass
- TEST-06 complete: Phaser game systems have unit tests with proper scene mocks
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-foundation/10-03-SUMMARY.md`
</output>
