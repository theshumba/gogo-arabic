---
phase: 55-mini-games-content-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/systems/battle/__tests__/BattleStateMachine.test.js
  - src/game/systems/battle/__tests__/GrammarComboDetector.test.js
  - src/components/Battle/__tests__/StatusEffectBar.test.jsx
autonomous: true
requirements:
  - CALL-01
  - POET-01

must_haves:
  truths:
    - "Running npm test produces passing test output for BattleStateMachine"
    - "Running npm test produces passing test output for GrammarComboDetector"
    - "Running npm test produces passing test output for StatusEffectBar"
  artifacts:
    - path: "src/game/systems/battle/__tests__/BattleStateMachine.test.js"
      provides: "BattleStateMachine unit test coverage"
      min_lines: 80
    - path: "src/game/systems/battle/__tests__/GrammarComboDetector.test.js"
      provides: "GrammarComboDetector unit test coverage"
      min_lines: 60
    - path: "src/components/Battle/__tests__/StatusEffectBar.test.jsx"
      provides: "StatusEffectBar render tests"
      min_lines: 40
  key_links:
    - from: "src/game/systems/battle/__tests__/BattleStateMachine.test.js"
      to: "src/game/systems/battle/BattleStateMachine.js"
      via: "import { BattleStateMachine, BATTLE_STATES }"
      pattern: "import.*BattleStateMachine.*BATTLE_STATES"
    - from: "src/game/systems/battle/__tests__/GrammarComboDetector.test.js"
      to: "src/game/systems/battle/GrammarComboDetector.js"
      via: "import { GrammarComboDetector }"
      pattern: "import.*GrammarComboDetector"
    - from: "src/components/Battle/__tests__/StatusEffectBar.test.jsx"
      to: "src/components/Battle/StatusEffectBar.jsx"
      via: "import StatusEffectBar"
      pattern: "import StatusEffectBar"
---

<objective>
Add unit test coverage for the three battle system modules that poetry battles will indirectly rely on — BattleStateMachine (FSM transitions and Redux integration), GrammarComboDetector (pattern matching), and StatusEffectBar (React component rendering). These are pure additive test files with zero production code changes.

Purpose: Safety net before Phase 55 poetry battles build on the combat infrastructure; validates existing contracts.
Output: 3 new test files, all tests green, no production code modifications.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-mini-games-content-polish/55-RESEARCH.md
@src/game/systems/battle/BattleStateMachine.js
@src/game/systems/battle/GrammarComboDetector.js
@src/components/Battle/StatusEffectBar.jsx
@src/test/setup.js
@vitest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: BattleStateMachine + GrammarComboDetector unit tests</name>
  <files>
    src/game/systems/battle/__tests__/BattleStateMachine.test.js
    src/game/systems/battle/__tests__/GrammarComboDetector.test.js
  </files>
  <read_first>
    src/game/systems/battle/BattleStateMachine.js
    src/game/systems/battle/GrammarComboDetector.js
    src/store/store.js
    src/test/setup.js
    vitest.config.js
    src/data/grammarCombos.js
    src/data/enemies.js
    src/store/slices/battleSlice.js
  </read_first>
  <action>
Create `src/game/systems/battle/__tests__/BattleStateMachine.test.js`:

1. Mock the store module. BattleStateMachine imports `store` directly from `../../../store/store.js`. Use vi.mock to provide a configureStore with real battleReducer, vocabularyReducer, grammarReducer (import from their source files). This pattern avoids dealing with the full persisted store setup.

2. Mock Phaser scene object with:
   - `scene.time.delayedCall = vi.fn()`
   - `scene.time.addEvent = vi.fn()`
   - `scene.tweens.add = vi.fn()`
   - `scene.scene.stop = vi.fn()`
   - `scene.exitBattle = vi.fn()`
   - `scene.equipmentStats = { getTotalBonuses: () => ({ hp: 0, mp: 0 }) }`

3. Write tests:
   - `it('starts in IDLE state')` — new BSM has `state === 'IDLE'`
   - `it('exports BATTLE_STATES as frozen object')` — verify BATTLE_STATES contains IDLE, INTRO, TURN_START, PLAYER_TURN, ENEMY_TURN, VICTORY, DEFEAT, GRAMMAR_COMBO, TARGET_SELECT, ITEM_USE, FLEE_CHALLENGE, COMPOUND_CHECK, ARENA_WAVE_TRANSITION
   - `it('start() transitions to INTRO state')` — call bsm.start(), check state === 'INTRO'
   - `it('constructor initializes grammarComboDetector')` — bsm.grammarComboDetector is instanceof GrammarComboDetector

4. Also mock `../../../data/enemies.js` with `getEnemy: vi.fn(() => ({ baseHP: 100, name: 'Test Enemy' }))` so tests don't need real enemy data.

5. Also mock `../magic/RootMagicManager.js`, `../companions/CompanionBattleAI.js`, `./EnemyAI.js`, `./CompoundEffectResolver.js`, `./MultiTargetManager.js`, `./BattleDamageCalculator.js` with vi.fn class stubs so the constructor doesn't fail on missing Phaser scene methods.

Create `src/game/systems/battle/__tests__/GrammarComboDetector.test.js`:

1. Import GrammarComboDetector from `../GrammarComboDetector.js`.
2. Mock `../../../data/grammarCombos` with NOUN_ADJ_COMBOS, VERB_CHAIN_PATTERNS, SENTENCE_TEMPLATES stubs.
3. Tests:
   - `it('is a class that can be instantiated')` — new GrammarComboDetector([]) works
   - `it('constructor accepts completedLessons array')` — no throw
   - `it('exports GrammarComboDetector as named export')` — typeof GrammarComboDetector === 'function'

Keep tests focused on public API. Do not test internal methods.
  </action>
  <verify>Run `npx vitest run src/game/systems/battle/__tests__/ --reporter=verbose` — all tests pass, no failures.</verify>
  <acceptance_criteria>
    - File `src/game/systems/battle/__tests__/BattleStateMachine.test.js` exists and contains `describe('BattleStateMachine'`
    - File `src/game/systems/battle/__tests__/GrammarComboDetector.test.js` exists and contains `describe('GrammarComboDetector'`
    - `npx vitest run src/game/systems/battle/__tests__/` exits with code 0
    - BattleStateMachine.test.js contains at least 3 `it(` or `test(` blocks
    - GrammarComboDetector.test.js contains at least 2 `it(` or `test(` blocks
  </acceptance_criteria>
  <done>All battle system tests pass. BattleStateMachine tests verify IDLE initial state, BATTLE_STATES export, and start() transition. GrammarComboDetector tests verify instantiation and export.</done>
</task>

<task type="auto">
  <name>Task 2: StatusEffectBar React component tests</name>
  <files>
    src/components/Battle/__tests__/StatusEffectBar.test.jsx
  </files>
  <read_first>
    src/components/Battle/StatusEffectBar.jsx
    src/components/Battle/StatusEffectBar.module.css
    src/data/statusEffects.js
    src/test/setup.js
  </read_first>
  <action>
Create `src/components/Battle/__tests__/StatusEffectBar.test.jsx`:

1. Import render + screen from @testing-library/react.
2. Import StatusEffectBar from `../StatusEffectBar.jsx`.
3. Mock `../../../data/statusEffects.js`:
   - `getStatusEffect` returns `{ arabic: 'نار', transliteration: 'nar', english: 'Burn', description: 'Deals damage', type: 'debuff' }` for 'burn', null otherwise.
   - `COMPOUND_EFFECTS` = empty object `{}`.
4. Mock `framer-motion` — replace motion.div with regular div, AnimatePresence with fragment. Pattern:
   ```
   vi.mock('framer-motion', () => ({
     motion: { div: (props) => <div {...props} /> },
     AnimatePresence: ({ children }) => <>{children}</>,
   }));
   ```
5. Mock CSS modules: vitest.config.js already has `css.modules.classNameStrategy: 'non-scoped'`.

6. Tests:
   - `it('renders nothing when effects array is empty')` — render with effects=[], container.firstChild is null or empty
   - `it('renders effect elements when effects array is non-empty')` — effects=[{ id: 'burn', remainingTurns: 2 }], container has child elements
   - `it('shows overflow badge when effects exceed maxVisible')` — pass 7 effects with maxVisible=5, check for '+2' text in document

Keep tests simple — test rendering output, not internal state.
  </action>
  <verify>Run `npx vitest run src/components/Battle/__tests__/ --reporter=verbose` — all tests pass.</verify>
  <acceptance_criteria>
    - File `src/components/Battle/__tests__/StatusEffectBar.test.jsx` exists and contains `describe('StatusEffectBar'`
    - `npx vitest run src/components/Battle/__tests__/` exits with code 0
    - StatusEffectBar.test.jsx contains at least 3 `it(` or `test(` blocks
    - Test file imports StatusEffectBar from `../StatusEffectBar`
  </acceptance_criteria>
  <done>StatusEffectBar component tests pass. Tests cover empty state, non-empty rendering, and overflow badge.</done>
</task>

</tasks>

<verification>
Run full test suite: `npx vitest run --reporter=verbose` — all tests pass, no regressions.
If coverage thresholds are exceeded, update vitest.config.js ratchet values upward.
</verification>

<success_criteria>
- 3 new test files exist under __tests__/ directories
- All tests pass with `npx vitest run`
- Zero production code changes
- Battle system contracts (states, exports, rendering) are now covered
</success_criteria>

<output>
After completion, create `.planning/phases/55-mini-games-content-polish/55-01-SUMMARY.md`
</output>
