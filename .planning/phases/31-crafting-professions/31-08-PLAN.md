---
phase: 31-crafting-professions
plan: 08
type: execute
wave: 4
depends_on: ["31-03", "31-04", "31-05", "31-06", "31-07"]
files_modified:
  - src/components/Router/GameLayout.jsx
  - src/store/slices/uiSlice.js
  - src/store/slices/npcSlice.js
  - src/store/slices/craftingSlice.js
  - src/store/middleware/craftingVocabMiddleware.js
  - src/data/npcsEnriched.js
  - src/data/companionDialogue.js
  - src/game/scenes/WorldScene.js
  - src/hooks/useKeyboardShortcuts.js
autonomous: true

must_haves:
  truths:
    - "RecipeBook and CraftingMiniGame overlays render correctly within GameLayout"
    - "Player can open RecipeBook via keyboard shortcut and HUD button"
    - "NPC dialogue includes profession teaching branches when profession is known"
    - "Crafting vocabulary words auto-added to FSRS when first encountered in recipes"
    - "Profession mastery contributes to zone reputation"
    - "GatheringSpotManager initialized in WorldScene for each zone"
    - "Companion dialogue includes profession-related lines"
  artifacts:
    - path: "src/components/Router/GameLayout.jsx"
      provides: "RecipeBook and CraftingMiniGame integrated into game overlay stack"
      contains: "RecipeBook"
    - path: "src/store/middleware/craftingVocabMiddleware.js"
      provides: "Middleware to sync crafting vocabulary with FSRS"
      exports: ["craftingVocabMiddleware"]
    - path: "src/store/slices/uiSlice.js"
      provides: "recipeBookOpen state and toggle"
      contains: "recipeBookOpen"
  key_links:
    - from: "src/components/Router/GameLayout.jsx"
      to: "src/components/Crafting/RecipeBook.jsx"
      via: "Conditional render based on uiSlice.recipeBookOpen"
      pattern: "recipeBookOpen.*RecipeBook"
    - from: "src/store/middleware/craftingVocabMiddleware.js"
      to: "src/store/slices/vocabularySlice.js"
      via: "dispatch addFsrsCard when recipe ingredients first viewed"
      pattern: "addFsrsCard"
    - from: "src/game/scenes/WorldScene.js"
      to: "src/game/systems/GatheringSpotManager.js"
      via: "GatheringSpotManager instantiation and create() call"
      pattern: "GatheringSpotManager"
---

<objective>
Wire all crafting components into the game: integrate RecipeBook/CraftingMiniGame in GameLayout, initialize GatheringSpotManager in WorldScene, add vocabulary middleware, update NPC dialogue for profession teaching, add keyboard shortcuts, and connect profession mastery to zone reputation.

Purpose: This is the "glue" plan that connects Plans 01-07 into a playable feature. Without this wiring, crafting exists as isolated components. After this plan, a player can actually: learn a profession → gather resources → open recipe book → craft items → see results.

Output: All crafting components wired into the game loop.
</objective>

<execution_context>
@/Users/theshumba/.claude/get-shit-done/workflows/execute-plan.md
@/Users/theshumba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-crafting-professions/31-01-SUMMARY.md
@.planning/phases/31-crafting-professions/31-03-SUMMARY.md
@.planning/phases/31-crafting-professions/31-04-SUMMARY.md
@.planning/phases/31-crafting-professions/31-05-SUMMARY.md
@.planning/phases/31-crafting-professions/31-06-SUMMARY.md
@.planning/phases/31-crafting-professions/31-07-SUMMARY.md
@src/components/Router/GameLayout.jsx
@src/store/slices/uiSlice.js
@src/hooks/useKeyboardShortcuts.js
@src/game/scenes/WorldScene.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire RecipeBook and CraftingMiniGame into GameLayout</name>
  <files>
    src/components/Router/GameLayout.jsx
    src/store/slices/uiSlice.js
    src/hooks/useKeyboardShortcuts.js
  </files>
  <action>
    **uiSlice.js** — Add crafting UI state:
    - Add to initialState: `recipeBookOpen: false`, `craftingMiniGameActive: false`, `craftingRecipeId: null`, `craftingProfessionId: null`
    - New reducers:
      - `openRecipeBook(state)` — sets recipeBookOpen: true
      - `closeRecipeBook(state)` — sets recipeBookOpen: false
      - `startCraftingMiniGame(state, { payload: { recipeId, professionId } })` — sets craftingMiniGameActive: true, craftingRecipeId, craftingProfessionId
      - `endCraftingMiniGame(state)` — resets all crafting mini-game state
    - Update `selectAnyOverlayOpen` selector to include recipeBookOpen and craftingMiniGameActive
    - Export new selectors: `selectRecipeBookOpen`, `selectCraftingMiniGameActive`, `selectCraftingRecipeId`, `selectCraftingProfessionId`

    **GameLayout.jsx** — Add crafting overlays:
    - Import RecipeBook, CraftingMiniGame, CraftingResult
    - Add after existing overlay imports (InventoryUI):
      ```jsx
      {recipeBookOpen && (
        <RecipeBook
          onClose={() => dispatch(closeRecipeBook())}
          onSelectRecipe={(recipeId) => {
            dispatch(closeRecipeBook());
            dispatch(startCraftingMiniGame({ recipeId, professionId }));
          }}
        />
      )}
      {craftingMiniGameActive && (
        <CraftingMiniGame
          professionId={craftingProfessionId}
          recipeId={craftingRecipeId}
          onComplete={(result) => {
            dispatch(endCraftingMiniGame());
            // Show CraftingResult
          }}
          onCancel={() => dispatch(endCraftingMiniGame())}
        />
      )}
      ```
    - Add CraftingResult as a post-craft modal (manage with local state)
    - Use AnimatePresence for enter/exit transitions (same pattern as BattleOverlay, MagicOverlay)

    **useKeyboardShortcuts.js** — Add crafting keyboard shortcut:
    - Add 'R' key (or 'C' if R is taken) to open RecipeBook: `dispatch(openRecipeBook())`
    - Only when no other overlay is open (check selectAnyOverlayOpen)
    - Add to the shortcuts help text
  </action>
  <verify>
    - `npx vite build` succeeds
    - `grep 'RecipeBook' src/components/Router/GameLayout.jsx` matches import and render
    - `grep 'recipeBookOpen' src/store/slices/uiSlice.js` matches state and reducers
    - `grep 'RecipeBook\|CraftingMiniGame' src/hooks/useKeyboardShortcuts.js` matches shortcut
  </verify>
  <done>
    RecipeBook opens via keyboard shortcut and renders within GameLayout. CraftingMiniGame launches from recipe selection. CraftingResult shows after crafting. All overlays managed via uiSlice state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire GatheringSpotManager, vocabulary middleware, NPC/companion integration</name>
  <files>
    src/game/scenes/WorldScene.js
    src/store/middleware/craftingVocabMiddleware.js
    src/store/store.js
    src/store/slices/npcSlice.js
    src/data/npcsEnriched.js
    src/data/companionDialogue.js
    src/store/slices/craftingSlice.js
  </files>
  <action>
    **WorldScene.js** — Initialize GatheringSpotManager:
    - Import GatheringSpotManager
    - In `create()` method: `this.gatheringSpotManager = new GatheringSpotManager(this);`
    - Call `this.gatheringSpotManager.create(currentZoneId)` after zone loads
    - In `update()`: call `this.gatheringSpotManager.update()`
    - On zone transition: destroy old manager, create new for new zone

    **craftingVocabMiddleware.js** — Redux middleware for FSRS integration:
    - Listen for `crafting/unlockRecipe` action
    - When recipe is unlocked, get all ingredient resourceIds
    - For each ingredient's `wordId`, check if it exists in vocabularySlice.fsrsCards
    - If not, dispatch `addFsrsCard({ wordId, card: newFsrsCard(), source: 'crafting' })`
    - This ensures all crafting vocabulary enters the FSRS review system automatically
    - Follow the same pattern as rootFsrsSyncMiddleware.js
    - Register in store.js middleware chain

    **npcSlice.js / npcsEnriched.js** — Add profession teaching:
    - Add a `professionTeaching` field to relevant NPCs in npcsEnriched.js
    - Format: `professionTeaching: { professionId: 'blacksmith', introRecipes: ['blacksmith_basic_nail', 'blacksmith_iron_ring'] }`
    - When NPC teaches a profession: dispatch `learnProfession()` and `unlockRecipe()` for intro recipes
    - Add 2-3 NPCs per zone that can teach professions (spread across zones)
    - Add dialogue lines for profession teaching (Arabic greeting + profession introduction)

    **companionDialogue.js** — Add profession-related companion lines:
    - Add 5-10 crafting-related dialogue lines per companion archetype
    - Lines triggered when: entering a zone with gathering spots, opening recipe book, crafting an item
    - Arabic text with English translation in comment

    **craftingSlice.js** — Add profession mastery → reputation integration:
    - New selector: `selectProfessionMasteryBonus(professionId)` — returns reputation bonus based on level
    - Level 1-3: +1 zone reputation per profession in that zone
    - Level 4-7: +2
    - Level 8-10: +5
    - This selector is consumed by npcSlice for relationship calculations

    IMPORTANT: Keep NPC data changes minimal — only add professionTeaching field, do NOT restructure existing NPC data. Companion dialogue additions should append to existing arrays, not replace.
  </action>
  <verify>
    - `npx vite build` succeeds
    - `grep 'GatheringSpotManager' src/game/scenes/WorldScene.js` matches import and usage
    - `grep 'craftingVocabMiddleware' src/store/store.js` matches middleware registration
    - `grep 'professionTeaching' src/data/npcsEnriched.js` matches at least 6 NPCs
    - No existing tests broken: `npx vitest run` passes
  </verify>
  <done>
    GatheringSpotManager runs in WorldScene. Crafting vocabulary auto-syncs to FSRS. NPCs teach professions via dialogue. Companions comment on crafting activities. Profession mastery contributes to zone reputation.
  </done>
</task>

</tasks>

<verification>
- Player can open RecipeBook via keyboard shortcut 'R' (or 'C')
- Player can select a recipe, launch mini-game, see result
- Gathering spots appear in world zones via GatheringSpotManager
- Crafting vocabulary auto-added to FSRS review queue
- NPCs can teach professions (at least 1 per profession)
- Companion dialogue includes crafting-related lines
- Profession mastery affects zone reputation
- All overlays managed by uiSlice (freeze/unfreeze player)
- `npx vite build` succeeds, `npx vitest run` passes
</verification>

<success_criteria>
- Complete end-to-end crafting flow works: learn profession → gather → browse recipes → craft → get item (CRAFT-01 through CRAFT-07)
- Vocabulary integration: crafting words in FSRS system (CRAFT-07)
- Profession mastery contributes to zone reputation and unlocks NPC dialogue (CINT-03)
- All crafting UI accessible from game via keyboard and HUD
</success_criteria>

<output>
After completion, create `.planning/phases/31-crafting-professions/31-08-SUMMARY.md`
</output>
