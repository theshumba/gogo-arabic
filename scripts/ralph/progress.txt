# Ralph Progress Log
Started: Thu  9 Apr 2026 01:01:20 BST
---

## Codebase Patterns

- **Middleware key for onboarding slice**: Store registers it as `onboarding2` but the slice selectors use `state.onboarding`. Pre-existing inconsistency. For middleware tests, mount the reducer at `onboarding` key to match selectors. Do NOT try to fix this inconsistency in passing.
- **Middleware test setup**: Use `configureStore({ reducer: { sliceName: reducer }, middleware: (gDM) => gDM().concat(myMiddleware) })`. Test helpers `_resetState` / `_getState` exported from middleware for state inspection.
- **Pre-existing test failures**: 30 tests were failing before batch-4 work began (forgettingCurveService, settingsSlice, datasetValidation, HUD, antiFrustrationMiddleware, zoneReviewMiddleware, battle.integration, spacedListeningService). Do not try to fix these — they are known pre-existing issues.
- **Extending slices**: Export constants (like `TUTORIAL_SEQUENCE`) alongside the slice so both the middleware and tests can import them without circular deps.

---

## 2026-04-09 01:06 - FEAT-031

- Implemented guided tutorial auto-trigger system
- Extended `onboardingSlice.js`: added `tutorialSteps`, `tutorialSkipped`, `completeTutorialStep()`, `skipTutorial()`, `selectNextTutorialStep()`, `selectTutorialComplete()`, `TUTORIAL_SEQUENCE` constant
- Created `tutorialMiddleware.js`: intercepts `onboarding/recordFeatureUse`, checks first-use (timesUsed===1) + prerequisites, dispatches `completeTutorialStep`
- Created `tutorialMiddleware.test.js`: 22 tests covering sequence, gate logic, skip, first-use guard, selectNextTutorialStep, selectTutorialComplete
- Wired `tutorialMiddleware` into `store.js`
- **Files changed**: `src/store/slices/onboardingSlice.js`, `src/store/middleware/tutorialMiddleware.js`, `src/store/middleware/__tests__/tutorialMiddleware.test.js`, `src/store/store.js`
- **Learnings for future iterations:**
  - Store registers onboarding as `onboarding2` but selectors use `state.onboarding` — test stores must use `{ onboarding: reducer }` to match
  - `recordFeatureUse` action type is `'onboarding/recordFeatureUse'` (slice name, not store key)
  - After `next(action)` is called, `featureUsage[featureId].timesUsed` is already updated — so `timesUsed === 1` correctly identifies first use
---
