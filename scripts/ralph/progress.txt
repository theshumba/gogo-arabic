## Codebase Patterns

- **Slice location**: `src/store/slices/` — all Redux slices live here
- **Middleware location**: `src/store/middleware/` — all custom middleware here
- **Slice tests**: `src/store/__tests__/` — tests for slices; middleware tests in `src/store/middleware/__tests__/`
- **Store registration**: Add reducer to `rootReducer` in `store.js`, add to `persistConfig.whitelist` for localStorage persistence, add middleware to `.concat(...)` chain
- **Pre-existing failures**: 8 test failures + 413 lint problems are pre-existing (not caused by batch-5 work)
- **Middleware blocking pattern**: return early without calling `next(action)` to silently block; expose `_reset*` functions for test isolation
- **Pure selector helpers**: Export standalone functions (e.g. `getLetterMastery`, `isAlphabetComplete`) in addition to Redux selectors so middleware can import them without circular deps

---

# Ralph Progress Log
Started: Thu  9 Apr 2026 19:09:00 BST
---

## 2026-04-09 19:20 - FEAT-041
- Implemented alphabet mastery progression engine
- Files created:
  - `src/store/slices/alphabetProgressSlice.js` — tracks 28 consonants + 6 vowels, 4 forms each; exports AFL_SEQUENCE, VOWEL_IDS, getLetterMastery, isAlphabetComplete, selectNextLettersToLearn
  - `src/store/middleware/alphabetGateMiddleware.js` — blocks world/enterZone until isAlphabetComplete; bypasses for A2/B1 placement test
  - `src/store/__tests__/alphabetProgressSlice.test.js` — 35 tests covering all acceptance criteria
- Files modified:
  - `src/store/store.js` — registered alphabetProgressReducer + alphabetGateMiddleware
- **Learnings for future iterations:**
  - AFL sequence IDs mapped from arabicPhonetics.js ARABIC_CONSONANTS: hamza=أ, ba=ب, … ya=ي
  - Vowels (fatha/kasra/damma/alif/ya_vowel/waw_vowel) tracked but not gated
  - Gate bypass uses placement.hasCompleted + assignedLevel !== 'A1' (not just hasCompleted)
  - Pre-existing lint/test failures: safe to ignore (152 errors, 8 test failures existed before batch-5)
---

## 2026-04-09 19:30 - FEAT-042
- Implemented core 100 high-frequency word set with explicit teaching data
- Files created:
  - `src/data/coreVocabulary.js` — 100 MSA words ordered by frequencyRank 1-100; categories: greetings(10), pronouns(8), basic_verbs(15), numbers(10), family(8), food(8), directions(8), time(8), common_nouns(15), adjectives(10); exports CORE_VOCABULARY array + getNextCoreWords(completedCount, batchSize)
  - `src/store/slices/coreVocabularySlice.js` — tracks per-word mastery (0-100); exports isCoreVocabComplete, getCoreVocabProgress, selectCoreVocabProgress, CORE_MASTERY_THRESHOLD=70, CORE_WORD_COUNT=100
  - `src/store/__tests__/coreVocabularySlice.test.js` — 29 tests covering data completeness, category counts, ordering, getNextCoreWords, reducer actions, completion logic, progress calculation, selectors
- Files modified:
  - `src/store/store.js` — registered coreVocabularyReducer + added coreVocabulary to localStorage persist whitelist
- **Learnings for future iterations:**
  - coreVocabulary.js uses wordId field to cross-reference vocabularyExpanded.js IDs (exp_a1_001, etc.) even though AC doesn't require it — useful for future integration
  - CORE_MASTERY_THRESHOLD=70 (vs alphabet's 80) — different gates for different content types
  - category field uses underscores (basic_verbs, common_nouns) not spaces — keep consistent for subsequent slices that filter by category
---

## 2026-04-09 19:50 - FEAT-043
- Implemented trilateral root system teaching engine
- Files created:
  - `src/data/rootTeachingSet.js` — 30 MSA roots; each has id, root (e.g. "ك-ت-ب"), coreMeaning, foundationOrder (1-5 for foundation roots, null otherwise), derivedWords (4-6 per root with arabic/english/pattern/form); exports PATTERN_TEMPLATES, getRootById, getRootByLetters, getFoundationRoots, predictWordFromRoot
  - `src/store/slices/rootKnowledgeSlice.js` — tracks knownRoots[] and wordScores{}; exports learnRoot, updateWordScore, resetRootScores, resetAllRootKnowledge, getRootMastery, getKnownRoots, selectKnownRoots, selectRootMastery, selectRootMasterySummary, selectRootKnowledgeProgress, ROOT_MASTERY_THRESHOLD=70
  - `src/store/__tests__/rootKnowledgeSlice.test.js` — 43 tests covering all acceptance criteria
- Files modified:
  - `src/store/store.js` — registered rootKnowledgeReducer + added rootKnowledge to localStorage persist whitelist
- **Learnings for future iterations:**
  - predictWordFromRoot uses two-phase substitution (private-use Unicode placeholders U+E001-E003) to prevent cross-contamination when a root radical is the same character as a template placeholder (ف, ع, or ل)
  - Foundation roots (foundationOrder 1-5): root_ktb, root_drs, root_3lm, root_qr2, root_klm — highest-yield for beginners
  - rootKnowledgeSlice.wordScores keyed by [rootId][arabic] (Arabic word string), not wordId — keeps it self-contained; FSRS integration can be wired via middleware later
  - getRootMastery averages scores across ALL derivedWords (including unscored ones at 0), so mastery only reaches 100 when every word in the set is scored
---

## 2026-04-09 19:55 - FEAT-044
- Implemented foundation phase orchestrator
- Files created:
  - `src/store/slices/foundationSlice.js` — 4-stage state machine (alphabet→coreVocab→rootIntro→complete); exports FOUNDATION_STAGES, getCurrentFoundationStage, isFoundationComplete, getNextStage, isStageComplete, getStageProgress, advanceFoundationStage, bypassFoundationWithPlacement, resetFoundation, selectFoundationProgress
  - `src/store/middleware/foundationMiddleware.js` — blocks world/enterZone until stage='complete'; reads isFoundationComplete from foundationSlice
  - `src/store/__tests__/foundationSlice.test.js` — 52 tests covering all acceptance criteria
- Files modified:
  - `src/store/store.js` — registered foundationReducer, added 'foundation' to localStorage persist whitelist, added foundationMiddleware to chain
- **Learnings for future iterations:**
  - foundationSlice imports AFL_SEQUENCE inline (not from alphabetProgressSlice) to avoid circular dep — foundationSlice already imports isAlphabetComplete, so re-importing the array too would be fine, but inline is safer
  - selectFoundationProgress uses createSelector with 4 input selectors (stage, letterMastery, wordMastery, knownRoots) then reconstructs minimal state object inside — avoids passing full state through reselect while keeping memoization tight
  - overallProgress formula: stageIndex * 25 + stageProgress * 0.25 (each of 4 stages = 25%, stage='complete' hardcoded to 100)
  - Placement bypass sets bypassedViaPlacement=true AND stage='complete' — foundationMiddleware only checks isFoundationComplete (stage==='complete'), so placement field in foundation state is just for audit trail
  - Stage completion criteria: alphabet=isAlphabetComplete(80%), coreVocab=first 50 words at 70%+, rootIntro=knownRoots.length>=5
---

## 2026-04-09 20:10 - FEAT-045
- Implemented zone vocabulary pre-teaching data
- Files created:
  - `src/data/zoneVocabIntros.js` — 8 zones × 12 words each (96 total); exports ZONE_VOCAB_INTROS, ZONE_IDS, ZONE_COUNT, getZoneIntroWords(zoneId), getAllZoneIntroWordIds(); zone IDs from zones.js: oasis_village, desert_marketplace, ancient_library, farmland, bedouin_camp, mountain_village, coastal_port, royal_palace
  - `src/store/slices/zoneVocabIntroSlice.js` — tracks reviewedByZone state; exports markWordReviewed, resetZoneIntro, resetAllZoneIntros, isZoneIntroComplete, getZoneIntroProgress, getNextUnreviewedWord, selectIsZoneIntroComplete, selectZoneIntroProgress, makeSelectZoneIntroProgress, selectAllZoneIntroProgress
  - `src/store/__tests__/zoneVocabIntroSlice.test.js` — 34 tests covering all acceptance criteria
- Files modified:
  - `src/store/store.js` — added zoneVocabIntroReducer import, registered reducer, added 'zoneVocabIntro' to localStorage persist whitelist
- **Learnings for future iterations:**
  - Zone IDs in zones.js differ from PRD theme descriptions: ancient_library (not sacred_library), farmland (not garden_district), mountain_village (not mountain_pass), royal_palace (not ancient_ruins) — always read zones.js for canonical IDs
  - makeSelectZoneIntroProgress(zoneId) factory pattern used for memoized per-zone selectors (avoids parameterized selector issues with reselect)
  - Each word's `root` field is an empty string for Arabic particles (لا، نعم) — tests must allow empty string for root field specifically
  - 96 total words (8 zones × 12) comfortably within 80-100 range; overlap allowed but avoided here since each zone has distinct vocabulary
---

## 2026-04-09 20:07 - FEAT-046
- Implemented zone intro teaching middleware
- Files created:
  - `src/store/slices/zoneIntroSlice.js` — active session state (activeZone, status, startedZones); exports startIntro, dismissIntro, clearIntro, resetZoneIntroSession, resetAllIntroSessions, completeIntroWord (plain action creator), presentNextIntroWord, selectActiveIntroZone, selectIntroStatus, selectIsIntroActive, selectIsIntroDismissed, selectActiveIntroProgress, selectZoneIntroShouldStart, selectZoneIntroProgress
  - `src/store/middleware/zoneIntroMiddleware.js` — intercepts world/enterZone (dispatches startIntro when intro incomplete) and zoneIntro/completeWord (dispatches markWordReviewed + addFsrsCard; clears intro when all words reviewed)
  - `src/store/middleware/__tests__/zoneIntroMiddleware.test.js` — 28 tests covering all acceptance criteria
- Files modified:
  - `src/store/store.js` — imported zoneIntroReducer, zoneIntroMiddleware; added 'zoneIntro' to localStorage persist whitelist; added zoneIntroMiddleware to concat chain
- **Learnings for future iterations:**
  - Immer freezes reducer initial state in dev mode — test mocks must use fresh plain objects, NOT spread from `reducer(undefined, { type: '@@INIT' })` output when those objects need to be mutated in dispatch mocks
  - Two-slice design: zoneVocabIntroSlice (persistent reviewed-word tracking, FEAT-045) + zoneIntroSlice (active session state, FEAT-046) — clean separation of concerns
  - addFsrsCard payload expects { wordId, card, source? } where card is a nested object — pass { wordId, card: { state, stability, ... }, source } not flat fields
  - completeIntroWord is a plain action creator (not a Redux Toolkit action), so it lives outside the slice definition; middleware intercepts 'zoneIntro/completeWord' by string match
  - Re-entry after dismissal works automatically: zoneIntroMiddleware checks isZoneIntroComplete on every world/enterZone; dismissed state is just UI chrome, not middleware state
---
