# Ralph Progress Log
Started: Tue  7 Apr 2026 02:38:13 BST
---

## Codebase Patterns

### Quiz Type Registration Pattern
To add a new quiz type:
1. Add entry to `QUIZ_TYPE_REGISTRY` in `src/data/quizTypes.js` — shape: `{ label, cluster, minLevel, cefrMin }`
2. Add `buildChoices` case in `useQuiz.js` (the `buildChoices` closure has access to `fsrsCards` and `vocabulary`)
3. Add grading case in `answer()` callback
4. Add `correctAnswer` case in the correctAnswer block
5. Update `src/data/__tests__/quizTypes.test.js` count assertion
- Non-word-based types (DialectIdentify, RootExpand, CulturalContext, sentence_build) attach data to `word` object via mutation: `word.myField = data`

### Pre-existing Test Failures (do not fix, not your bug)
- `forgettingCurveService.test.js` — 4 failures (getUrgentReviewList, getRetentionForecast, getRetentionHealth)
- `settingsSlice.test.js` — 1 failure (initial state)
- `HUD.test.jsx` — 1 failure (review due count)
- `antiFrustrationMiddleware.test.js` — full file failure
- `zoneReviewMiddleware.test.js` — full file failure
- `battle.integration.test.js` — full file failure
- `datasetValidation.test.js` — 2 failures (goldenAgeScholars, achievementChains)
Baseline: 8 test files failed, 9 tests failed before any changes.

---

## 2026-04-07 - WIRE-001
- Wired `sentenceGenerator.js` into the quiz system as a new `'sentence_build'` question type
- Added `'sentence_build'` to `QUIZ_TYPE_REGISTRY` (cluster: reading, minLevel: 5, cefrMin: A1)
- Imported `generateSentence()` into `useQuiz.js`; wired into `buildChoices()`, `answer()`, and `correctAnswer` block
- `sentence_build` uses player's mastered FSRS words as vocabulary pool (falls back to full vocabulary if <2 mastered)
- Generated sentence data stored on `word.sentenceBuild` for grading and feedback
- Updated `quizTypes.test.js`: count assertion now expects 21 types (was 20) — fixed 1 pre-existing failure
- Added 16 new tests in `src/hooks/__tests__/useQuiz.sentenceBuild.test.js`
- Files changed: `src/data/quizTypes.js`, `src/hooks/useQuiz.js`, `src/data/__tests__/quizTypes.test.js`, `src/hooks/__tests__/useQuiz.sentenceBuild.test.js`
- **Learnings for future iterations:**
  - `buildChoices` is a closure inside `useQuiz` — it has access to `fsrsCards` and `vocabulary` from outer scope, so no need to thread these as parameters
  - Mutating the `word` object (`word.sentenceBuild = ...`) is the established pattern for attaching question-specific context (same as `word.dialectItem`, `word.rootExpansion`, `word.culturalItem`)
  - Always update the `quizTypes.test.js` count assertion when adding new types
  - Lint has 420 pre-existing errors (tools/*.js node globals) — my changes add ≤1 warning, this is expected
---

## 2026-04-07 - WIRE-002
- Added `selectWordsAtRisk` selector to `vocabularySlice.js` — imports `getDecayingWords()` from `forgettingCurveService.js`, wraps it for Redux state, returns stability-sorted list (stability < 3 days)
- Added "Words at Risk" section to `ProgressReport.jsx` — shows top 5 at-risk words (Arabic + English + stability), with overflow count if > 5
- Added CSS styles for the at-risk list in `ProgressReport.module.css`
- Added 5 tests to `vocabularySlice.test.js` covering empty state, filtering by threshold, exclusion of zero-stability words, sort order, and result shape
- Files changed: `src/store/slices/vocabularySlice.js`, `src/components/Progress/ProgressReport.jsx`, `src/components/Progress/ProgressReport.module.css`, `src/store/__tests__/vocabularySlice.test.js`
- **Learnings for future iterations:**
  - `selectWordsAtRisk` uses `createSelector([selectFsrsCards], ...)` — single input selector is sufficient since service does all the logic
  - `getDecayingWords(cards, 3)` excludes zero-stability words (new, never reviewed), so selector naturally skips unreviewed cards
  - ProgressReport is the right place for retention health UI — already has `fsrsCards` and all the vocab stat context
  - Word lookup: import `vocabularyAll.js` directly and build a `useMemo` id→word map; vocabulary is already lazy-chunk-split so no extra bundle impact on main chunk
---

## 2026-04-07 - WIRE-003
- Added `filterDialogueForPlayer()` convenience wrapper to `npcVocabFilter.js` — single-line version of `filterDialogueBatch` for Redux state integration
- Created `FilteredArabicLine.jsx` — renders annotated Arabic dialogue: known words shown normally, unknown words get dashed-border highlight + transliteration hint below, tappable to add to FSRS learning queue via `addFsrsCard`
- Updated `DialogueBox.jsx` — after typewriter completes, shows `FilteredArabicLine` when `filteredLine.hasUnknownWords` is true; builds `knownWordIds` Set from `selectFsrsCards` via `useMemo`
- Added 6 tests for `filterDialogueForPlayer` in existing `npcVocabFilter.test.js`
- Files changed: `src/services/npcVocabFilter.js`, `src/components/NPC/FilteredArabicLine.jsx`, `src/components/NPC/FilteredArabicLine.module.css`, `src/components/NPC/DialogueBox.jsx`, `src/services/__tests__/npcVocabFilter.test.js`
- **Learnings for future iterations:**
  - DialogueBox uses `arabicTypewriter.isComplete` as gate — only show enhanced rendering AFTER typewriter completes (same pattern as `inlineVocab` highlights)
  - `filteredLine.hasUnknownWords` guards the `FilteredArabicLine` render — lines where the player knows everything render plain (no performance cost)
  - `addFsrsCard` is the correct action for tap-to-learn; `createNewCard()` from fsrs.js creates a fresh FSRS card
  - Arabic→wordId map needs both diacriticized and stripped versions for flexible matching
---

## 2026-04-07 - WIRE-004
- Created `src/services/tradeRouteIntegration.js` — maps shopId→zoneId and equipment slot/rarity→trade category; exposes `getZoneAdjustedBuyPrice`, `getZoneAdjustedSellPrice`, `getCheapestZoneForItem`, `getBestSellZoneForItem`, `getShopZoneInfo`
- Updated `ShopOverlay.jsx` — applies `getZoneAdjustedBuyPrice` to each shop item on inventory compute; shows zone name + specialty banner above buy grid
- Added `.zoneInfo` CSS to `ShopOverlay.module.css`
- Added 23 tests in `src/services/__tests__/tradeRouteIntegration.test.js`
- Files changed: `src/services/tradeRouteIntegration.js`, `src/services/__tests__/tradeRouteIntegration.test.js`, `src/components/Shop/ShopOverlay.jsx`, `src/components/Shop/ShopOverlay.module.css`
- **Learnings for future iterations:**
  - `ZONE_TRADE_PROFILES[zone].buy[category]` gives the multiplier directly — no need to call `getBuyPrice` and divide by reference price
  - Equipment items don't have a trade category — must map via slot (weapon→metals, armor→textiles, ring→gems) then fallback to rarity
  - ShopInventory already handles `item.basePrice` vs `item.price` for direction arrows (from existing ECON-04 implementation) — zone pricing changes `item.price` so arrows still work
  - `shopId` must be included in `useMemo` deps when used inside the memo callback
---

## 2026-04-07 - WIRE-005
- Added `completedChains: []` to `achievementSlice` initial state; added `completeChain` reducer + export
- Added `selectCompletedChains`, `selectAllChainProgress`, `selectChainProgress` selectors to `achievementSlice.js` (import `getChainProgress`/`getAllChainProgress` from achievementChainService)
- Updated `achievementMiddleware.js`: collects `newlyUnlockedIds` during the achievement check loop; after the loop, calls `checkChainCompletions` per newly unlocked achievement and dispatches `completeChain` + batches chain XP rewards; uses `Set` to guard against duplicate chain rewards within a single middleware pass
- Updated `AchievementPanel.jsx`: added "Chains" tab; renders chain progress cards with progress bar (X/Y), percentage, XP reward, and completion state
- Fixed `achievementSlice.test.js`: added `completedChains: []` to expected initial state
- Tests leveraged existing `achievementChainService.test.js` (8 tests already exist and cover the acceptance criteria)
- Files changed: `src/store/slices/achievementSlice.js`, `src/store/middleware/achievementMiddleware.js`, `src/components/Achievements/AchievementPanel.jsx`, `src/store/__tests__/achievementSlice.test.js`
- **Learnings for future iterations:**
  - Adding fields to initial state ALWAYS breaks the existing `should return the initial state` test — update immediately
  - Chain rewards must use a Set guard to prevent double-rewarding when multiple achievements unlock in one middleware pass
  - `checkChainCompletions` takes the newly unlocked achievement ID + full unlocked map; call it after ALL achievements are dispatched so the state is current
  - `achievementChainService.test.js` already existed with good coverage — check before creating duplicate test files
---

## 2026-04-07 - WIRE-006
- Added `getTodayPhrase(cefrLevel)` call in `MainMenu.jsx` via `useMemo` — phrase rotates daily and filters to player's CEFR level
- Added `selectCefrLevel` selector from `cefrProgressSlice`; falls back to `'A1'` when null
- Added phrase card JSX between title and nav (`phraseCard`, `phraseLabel`, `phraseArabic`, `phraseTranslit`, `phraseEnglish`)
- Added CSS for `.phraseCard` and child classes in `MainMenu.module.css` — pixel-art style with gold border and semi-transparent dark background
- Fixed pre-existing TDZ bug: `const hasCharacter` was declared AFTER the `useEffect` that referenced it in its dependency array — moved the declaration before the first `useEffect`
- Added 6 tests in `src/components/Menu/__tests__/MainMenuDailyPhrase.test.jsx`
- Files changed: `src/components/Menu/MainMenu.jsx`, `src/components/Menu/MainMenu.module.css`, `src/components/Menu/__tests__/MainMenuDailyPhrase.test.jsx`
- **Learnings for future iterations:**
  - `const` TDZ bugs in React components only surface when the component is rendered in tests (jsdom); they may not throw in a real browser due to Vite's transpilation. Always declare variables before the `useEffect` that references them in deps.
  - cefrProgress slice uses `currentLevel` (not `level`); placement slice uses `hasCompleted` (not `hasCompletedPlacement`) — match slice field names exactly in preloadedState
  - Mocking `dailyPhraseService.js` with `vi.mock` + fixed return value lets CEFR-level assertion use `toHaveBeenCalledWith` on the imported `getTodayPhrase`
---

## 2026-04-07 - WIRE-007
- Created `WeeklyDigestModal.jsx` — renders learning stats (wordsReviewed, wordsMastered, studyMinutes, streakDays, averageAccuracy) as a pixel-art overlay modal with dismiss button
- Created `WeeklyDigestModal.module.css` — overlay + modal styles matching game's pixel aesthetic
- Updated `MainMenu.jsx`: imports `isDigestReady`, `getDigestIfReady` from `weeklyDigestService`; reads `s.analytics` from Redux; on mount checks if digest is ready and sets `weeklyDigest` state; renders `WeeklyDigestModal` when digest is non-null and placement test is not active
- `markDigestShown()` called inside the modal's dismiss handler (not in MainMenu) to keep the modal self-contained
- Digest is suppressed while placement overlay is active (`!showPlacement` guard)
- Added 5 tests in `src/components/Menu/__tests__/WeeklyDigestModal.test.jsx`
- Files changed: `src/components/Menu/MainMenu.jsx`, `src/components/Menu/WeeklyDigestModal.jsx`, `src/components/Menu/WeeklyDigestModal.module.css`, `src/components/Menu/__tests__/WeeklyDigestModal.test.jsx`
- **Learnings for future iterations:**
  - `analyticsSlice` (Redux name: `'analytics'`) holds `dailyActivity`, `sessions`, `wordAccuracy` — exactly the fields `generateStudyReport` needs
  - `getDigestIfReady` returns null if activity is zero — no need to guard in MainMenu
  - Run-once effects for localStorage-based checks use `[]` deps with `// eslint-disable-line react-hooks/exhaustive-deps`
  - Mock `formatDigestSummary` in tests to avoid emoji assertion brittleness
---

## 2026-04-07 - WIRE-008
- Added `spacedListeningEnabled: false` to `settingsSlice.js` initialState + `toggleSpacedListening` reducer + `selectSpacedListeningEnabled` selector
- Added "Spaced Listening" toggle row in `SettingsMenu.jsx` Audio section — dispatches `toggleSpacedListening()`
- Wired `startListening` / `stopListening` into `GameLayout.jsx` via `useEffect([spacedListeningEnabled])` — starts on enable, stops on disable, stops on unmount
- Used `useStore()` from react-redux to get `store.getState` reference (service requires a `getState` fn, not hooks)
- Service internally handles pause during battles/mute via `shouldPause(state)` — no extra wiring needed
- Added 11 tests in `src/services/__tests__/spacedListeningService.test.js` covering start/stop, shouldPause conditions, settings toggle, speech synthesis support detection
- Files changed: `src/store/slices/settingsSlice.js`, `src/components/Menu/SettingsMenu.jsx`, `src/components/Router/GameLayout.jsx`, `src/services/__tests__/spacedListeningService.test.js`
- **Learnings for future iterations:**
  - `useStore()` from react-redux gives access to `store.getState` — use when a service needs direct store access rather than reactive hooks
  - `vi.useFakeTimers()` in service tests can produce IndexedDB teardown errors (known pre-existing artifact with fake-indexeddb) — tests still pass
  - `spacedListeningService.setInterval` shadows globalThis.setInterval — it calls `globalThis.setInterval` internally to avoid recursion; no action needed
---

## 2026-04-07 - WIRE-009
- Created `HandwritingPractice.jsx` — self-assessment practice mode showing letter forms, accuracy rating buttons (Miss/Poor/OK/Good/Perfect), stats bar (attempts/best/avg/trend), and weak letters panel
- Created `HandwritingPractice.module.css` — pixel-art styling matching AlphabetModule aesthetic
- Updated `AlphabetModule.jsx`: added `HandwritingPractice` import, `showPractice` state, renders `HandwritingPractice` when `showPractice` is true, added "Practice Writing" button in header
- Added `.practiceBtn` + `.headerProgress` flex layout to `AlphabetModule.module.css`
- Trend detection: compares last two `recentAttempts` — improving if +5 delta, declining if -5 delta, otherwise stable
- Added 8 tests in `src/components/Alphabet/__tests__/HandwritingPractice.test.jsx`
- Files changed: `src/components/Alphabet/HandwritingPractice.jsx`, `src/components/Alphabet/HandwritingPractice.module.css`, `src/components/Alphabet/AlphabetModule.jsx`, `src/components/Alphabet/AlphabetModule.module.css`, `src/components/Alphabet/__tests__/HandwritingPractice.test.jsx`
- **Learnings for future iterations:**
  - `getLetterStats` returns null for unseen letters — always guard with `stats && ...`
  - `alphabet.json` uses `forms.isolated` as the display character; `forms.initial/medial/final` for positional variants
  - Self-assessment (no canvas) approach satisfies "logic-only" requirement in PRD notes
---

## 2026-04-07 - WIRE-010
- Added `selectDialectVariants(wordId)` to `vocabularySlice.js` — plain function (not Redux selector) that looks up a word's English in `DIALECT_COMPARISON` via a pre-built Map at import time
- Added imports for `DIALECT_COMPARISON` and `vocabularyAll` to `vocabularySlice.js`; Map is built once at module level for O(1) lookup
- Created `DialectVariantsPanel.jsx` — shows MSA (marked with ★ as default), Egyptian, Levantine, Gulf forms (Arabic + transliteration); returns null if no dialect entry exists
- Created `DialectVariantsPanel.module.css` — compact grid layout, MSA cell highlighted with gold border
- Updated `ReviewSession.jsx`: imported `DialectVariantsPanel`, added `{answered && currentWord && <DialectVariantsPanel wordId={currentWord.id} />}` after answer reveal
- Added 6 tests in `src/store/__tests__/dialectVariants.test.js` — null checks, missing word, known word, field shapes
- Files changed: `src/store/slices/vocabularySlice.js`, `src/components/Review/DialectVariantsPanel.jsx`, `src/components/Review/DialectVariantsPanel.module.css`, `src/components/Review/ReviewSession.jsx`, `src/store/__tests__/dialectVariants.test.js`
- **Learnings for future iterations:**
  - DIALECT_COMPARISON is indexed by English string (lowercase) — not by word ID; need word lookup first
  - Build the Map at module-level import time, not inside the selector function — avoids O(n) scan on every call
  - `selectDialectVariants` is a plain function (not createSelector) since it uses static data, not Redux state
  - The real dialect data DOES have 'hello' — safe to use in tests without mocking dialectComparison.js
---

## 2026-04-07 - WIRE-011
- Created `loreEntriesBatch3.js` — converts CALLIGRAPHY_STYLES (6) and GOLDEN_AGE_SCHOLARS (30+) to lore entry format using `calligraphyToLore` and `scholarToLore` transform functions
- Updated `loreEntries.js`: imported LORE_ENTRIES_BATCH_3, spread into LORE_ENTRIES, added 'calligraphy' and 'scholars' to LORE_CATEGORIES, added their CATEGORY_META entries (icons: ✒️ and 📚, Arabic labels: الخط, العلماء)
- Updated `loreEntries.test.js`: changed "has 10 categories" to `>=10`, updated "25 entries per category" to allow 1+ for new extension categories
- Added 10 tests in `src/data/__tests__/loreEntriesBatch3.test.js`
- Files changed: `src/data/loreEntriesBatch3.js`, `src/data/loreEntries.js`, `src/data/__tests__/loreEntries.test.js`, `src/data/__tests__/loreEntriesBatch3.test.js`
- **Learnings for future iterations:**
  - `loreEntries.test.js` has strict assertions: category count (was 10), 25+ entries/category, valid trigger format — check before adding categories
  - Valid trigger prefixes: `zone_visit:`, `npc_talk:`, `quest_complete:`, `word_learn:`, `faction_tier:`, `level_reach:`, `battle_win:`, `object_interact:`, `collection` — use `collection:codex_browse` for always-browsable entries
  - `replace_all: true` on `'codex_browse'` won't catch backtick template literals `` `codex_browse` `` — check for both quote styles when replacing
  - `getLoreEntriesByCategory` from loreEntries.js handles filtering — no extra wiring needed in LoreCodex.jsx since it already reads from LORE_ENTRIES
---

## 2026-04-07 - WIRE-012
- Added `pronunciationAccuracy: {}` to `analyticsSlice.js` initialState with rolling-average similarity tracking per word
- Added `recordPronunciationResult({ wordId, correct, similarity })` reducer — updates `correct`, `total`, and `avgSimilarity` (rolling average)
- Exported `recordPronunciationResult` action and `selectPronunciationAccuracy` selector
- Updated `ReviewSession.jsx`: imported `PronunciationFeedback` and `recordPronunciationResult`; added `<PronunciationFeedback arabicWord={...} onResult={...}>` in `ar-to-en` quiz type — shown before answer choices so player can attempt pronunciation before guessing
- `PronunciationFeedback` already existed in `src/components/Arabic/` and was complete — only needed wiring and dispatch handler
- Added 7 tests in `src/store/__tests__/pronunciationTracking.test.js` — initialState, first entry creation, correct/total increment, rolling average, missing similarity fallback, selector, reset
- Files changed: `src/store/slices/analyticsSlice.js`, `src/components/Review/ReviewSession.jsx`, `src/store/__tests__/pronunciationTracking.test.js`
- **Learnings for future iterations:**
  - `PronunciationFeedback` gates itself on `state.settings.pronunciationPractice && isSpeechRecognitionSupported()` — safe to wire unconditionally, it self-hides when unavailable
  - Rolling average formula: `(prevAvg * (total - 1) + newValue) / total` — compute AFTER incrementing total
  - `onResult` receives `{ recognized, similarity, correct }` from `evaluatePronunciation` — similarity is 0–1 float
---

## 2026-04-07 - WIRE-013
- Added `selectQuranicCoverage` and `selectQuranicCoverageByCategory` memoized selectors to `vocabularySlice.js` using `createSelector([selectFsrsCards], ...)` — both call the quranicCoverage service with the full `vocabularyAll` static dataset
- Imported `calculateQuranicCoverage` and `getQuranicCoverageByCategory` from `src/services/quranicCoverage.js` into vocabularySlice
- Added Quranic Coverage section to `AnalyticsDashboard.jsx`: shows percentage mastered (large number), mastered/total/seen counts, and per-category breakdown (top 5, with colored progress bars)
- Section only renders when `quranicCoverage.total > 0` (hidden for players with no Quranic vocab in the dataset)
- Added 6 tests in `src/store/__tests__/quranicCoverage.test.js` — empty cards, mastery threshold (≥7 days stability), percentage calc, category grouping, non-Quranic exclusion, sort order
- Files changed: `src/store/slices/vocabularySlice.js`, `src/components/Analytics/AnalyticsDashboard.jsx`, `src/store/__tests__/quranicCoverage.test.js`
- **Learnings for future iterations:**
  - quranicCoverage.js filters by `w.source === 'quran' || w.quranRef` — both fields used
  - Mastery threshold = 7 days FSRS stability (same as forgettingCurveService pattern)
  - `getQuranicCoverageByCategory` uses word.category (not surah name) — "surahs" in PRD = categories in practice
  - AnalyticsDashboard uses inline style objects (not CSS modules) — add new style keys to the `styles` const at bottom
---

## 2026-04-07 - GROW-014
- Created `src/data/companionBackstories.js` with all 12 companions: each has 4-paragraph backstory, personality traits[], gift preferences {loved, liked, disliked} tied to shops.js item IDs, and 4 dialogue triggers {onLevelUp, onBattleWin, onQuestComplete, onLowHealth} with {arabic, english}
- `selectCompanionBackstory(id)` and `selectGiftReaction(companionId, itemId)` — plain functions using module-level Map for O(1) lookup; not Redux selectors (static data)
- 35 tests: 12 for field completeness per companion (via it.each), 12 for dialogue trigger structure, plus selector tests for known/unknown/null inputs and all 4 reaction tiers
- Files changed: `src/data/companionBackstories.js`, `src/data/__tests__/companionBackstories.test.js`
- **Learnings for future iterations:**
  - 12 companion IDs from companionSlice.js: companion_amira, _khalid, _zahra, _omar, _layla, _hassan, _fatima, _ali, _maryam, _samir, _nadia, _tariq
  - Item IDs from shops.js: prayer_beads, ancient_inkwell, compass_of_qibla, astrolabe, desert_bisht, burda_of_blessing, emerald_pendant, etc.
  - culturalItems.js is for quiz expressions (bismillah, alhamdulillah) NOT inventory items — use shops.js for gift itemIds
  - `it.each(ARRAY)('%s has ...', (id) => {...})` pattern works cleanly for testing all 12 companions without repetition
---

## 2026-04-07 - GROW-016
- Created `src/data/questChains.js` — 5 chains using existing quest IDs from quests.json (chain_oasis_scholar, chain_library_mastery, chain_desert_merchant, chain_mountain_wisdom, chain_port_navigator); each has chainReward {xp, dirhams, item}
- Created `src/services/questChainService.js` — pure functions: getNextQuestInChain, isChainComplete, getChainProgress, getChainReward, getChainByQuestId; module-level Map for O(1) quest→chain lookups
- Created `src/store/middleware/questChainMiddleware.js` — intercepts `quests/completeQuest`, auto-activates next locked quest in chain, dispatches addXP on full chain completion
- Added `activateQuest` reducer to `questSlice.js` — force-activates a locked quest (for chain middleware)
- Wired `questChainMiddleware` into `store.js` middleware array
- 20 tests in `src/services/__tests__/questChainService.test.js`
- Files changed: `questChains.js`, `questChainService.js`, `questChainMiddleware.js`, `questSlice.js`, `store.js`, `questChainService.test.js`
- **Learnings for future iterations:**
  - sideQuestChains.js already exists with 8 chains (oasis_mystery, etc.) but uses different IDs not in quests.json — use quests.json IDs for chains that wire into questSlice
  - questSlice has no direct "unlock" action — add activateQuest for targeted chain unlocking
  - getChainProgress falls back to locked quest when no active quest found in chain, so currentQuest is always the "next to tackle" quest
  - `store.getState()` in middleware returns state AFTER next(action) completes — use it post-next for accurate chain completion check
---

## 2026-04-07 - GROW-017
- Created `src/services/npcRelationshipService.js` — pure functions: getTier, getShopDiscount (0/0/5/15/25%), getAvailableQuests (filters by npcGiver and minRelationship, hostile returns []), getDialogueVariant (returns tier key), getBattleAssistChance (0/0/0/7/15%)
- RELATIONSHIP_TIERS exported constant: Hostile 0-19, Cold 20-39, Neutral 40-59, Friendly 60-79, Beloved 80-100
- 26 tests covering all tiers for each function, edge cases (null npcId, out-of-range values, no npcGiver on quest)
- Files changed: `src/services/npcRelationshipService.js`, `src/services/__tests__/npcRelationshipService.test.js`
- **Learnings for future iterations:**
  - Pure service + separate Redux middleware is cleaner than baking consequences into npcSlice
  - getAvailableQuests expects `allQuests` to have `npcGiver` matching the npcId string — not the full NPC ID from zones.js (which is e.g. `guide-amira`) but the npcGiver field from quests.json (e.g. `guide`)
  - Hostile NPCs refuse all quests AND no discount; Cold NPCs refuse discount but can give quests if minRelationship=0
---

## 2026-04-07 - GROW-018
- Created `src/data/grammarRules.js` — 60 rules (15 A1, 15 A2, 15 B1, 10 B2, 5 C1); categories: nouns, verbs, particles, sentence_structure, morphology, idafa, verb_forms
- Each rule: id, title, titleArabic, cefrLevel, category, explanation, examples[{arabic, english, breakdown}], commonMistakes[], practiceTemplates[]
- selectRuleById(id), selectRulesByLevel(cefr), selectRulesByCategory(cat) — plain functions with module-level Map for O(1) ID lookup
- 16 tests: field completeness, ID uniqueness, exact CEFR distribution per level, all 7 categories represented, selector correctness
- Files changed: `src/data/grammarRules.js`, `src/data/__tests__/grammarRules.test.js`
- **Learnings for future iterations:**
  - Grammar rule IDs use pattern: gr_[level]_[nnn] (e.g., gr_a1_001, gr_b2_010)
  - All 7 category strings: nouns, verbs, particles, sentence_structure, morphology, idafa, verb_forms
  - CEFR distribution is exact (15/15/15/10/5 = 60) — test checks exact counts per level
  - "verb forms" in PRD description maps to category "verb_forms" (underscore, no space)
---

## 2026-04-07 - GROW-019
- Created `src/data/loginRewards.js` — 30-day reward cycle; milestones at 7/14/21/30 with items prayer_beads/silver_ring/compass_of_qibla/astrolabe; getRewardForDay cycles after day 30; getNextMilestoneDay wraps across cycles
- Added to `playerSlice.js`: lastLoginDate, loginStreak, totalLogins, pendingLoginReward in initialState; processLoginReward (increments streak, applies XP/dirhams/item immediately), resetLoginStreak (resets to 1), dismissLoginReward; 4 selectors
- Created `src/store/middleware/loginRewardMiddleware.js` — fires on persist/REHYDRATE; same-day=noop, consecutive=processLoginReward, gap=resetLoginStreak; uses UTC date strings (YYYY-MM-DD)
- 21 tests covering dataset completeness, cycling behavior, milestone detection, reducer state changes, streak reset
- Files changed: `loginRewards.js`, `loginRewardMiddleware.js`, `playerSlice.js`, `store.js`, `loginRewards.test.js`
- **Learnings for future iterations:**
  - persist/REHYDRATE fires on every app start with redux-persist — safe hook for "app launch" logic
  - UTC date string 'YYYY-MM-DD' is the reliable format; avoid Date.toLocaleDateString() which is locale-dependent
  - processLoginReward applies XP/dirhams inline (no separate addXP dispatch) to keep state update atomic
  - getTodayUTC unused helper — remove immediately to avoid ESLint warning
---

## 2026-04-07 - GROW-020
- Created `src/data/loreEntriesBatch4.js` — 50 entries: history (10), mythology (10), science (10), scholars (10), world-lore (10); each includes PRD-extra fields: cefrLevel, relatedVocab[], unlockCondition
- Updated `loreEntries.js`: import + spread LORE_ENTRIES_BATCH_4; added 'world-lore' to LORE_CATEGORIES and CATEGORY_META (icon 🌍, labelArabic أساطير العالم)
- Updated `loreEntries.test.js`: added 'world-lore' to newCategories Set (allows <25 entries for extension categories)
- Updated `playerSlice.test.js`: added lastLoginDate/loginStreak/totalLogins/pendingLoginReward to initial state expectation (fields were added in GROW-019 but test wasn't updated)
- 11 tests in `loreEntriesBatch4.test.js`: 50-entry count, 10-per-category, unique IDs, required fields, keyTerm completeness, valid rarities, valid trigger prefixes, PRD-extra fields, world-lore pattern, scholars pattern, legendary presence
- Files changed: `loreEntriesBatch4.js`, `loreEntries.js`, `loreEntriesBatch4.test.js`, `loreEntries.test.js`, `playerSlice.test.js`
- **Learnings for future iterations:**
  - 'world-lore' is a new category — requires addition to LORE_CATEGORIES array, CATEGORY_META object, AND test's newCategories exception Set
  - loreEntries.test.js has a separate check for categories with <25 entries (newCategories Set) — any new extension category must be added there
  - playerSlice initial state test uses toEqual with a hardcoded object — any new initialState field MUST also be added to that test's beforeEach object
  - Batch files use id pattern: category_batch4_NNN (e.g., history_batch4_001) except world-lore which uses world_lore_NNN
---

## 2026-04-07 - GROW-021
- Added 5 new AI patterns to `EnemyAI.js`: berserker, turtle, trickster, scholar, healer
- Berserker: always attacks, floor(baseDamage*1.2) damage, defenseMultiplier:0.8 field
- Turtle: 60% block chance + counterattack (130% damage) next turn — uses _turtleLastBlocked instance state
- Trickster: random debuff (status_attack) every 3rd turn, 50% damage — uses _tricksterTurnCount instance state
- Scholar: uses optional vocabLookup (wordId→{category}) to find weakest category by avg FSRS stability; falls back to adaptive
- Healer: heals when bossHP < 40% maxBossHP, otherwise basic attack
- Added optional 3rd constructor param `vocabLookup` for Scholar pattern
- Module-level constant TRICKSTER_DEBUFFS = ['poison','burn','slow','confuse','silence','freeze']
- 21 tests in EnemyAI.test.js covering all 5 patterns + edge cases
- Files changed: `EnemyAI.js`, `EnemyAI.test.js`
- **Learnings for future iterations:**
  - EnemyAI is a plain class (not Redux-connected) — test by instantiating directly, no store needed
  - Pattern methods that don't use the `state` param must be named `_state` to satisfy ESLint no-unused-vars rule
  - Instance state for stateful patterns (turtle turn tracking, trickster counter) goes in constructor
  - Pattern map in `decide()` must be updated when adding new patterns
  - Real file is at `src/game/systems/battle/EnemyAI.js`, not `src/game/systems/EnemyAI.js` as noted in PRD
---

## 2026-04-07 - GROW-022
- Created `src/data/vocabCollections.js` — 10 themed collections, 961 total words
- Collections: greetings_social(90), food_drink(90), family_body(92), nature_animals(95), numbers_time_colors(92), trade_commerce(95), travel_directions(92), verbs_action(101), golden_age_science(110), advanced_discourse(100)
- Used range helper `r(prefix, start, end, pad)` to generate consecutive exp_* ID ranges; curated words listed explicitly
- Selectors: selectCollection(id), selectCollectionsByLevel(cefrLevel), selectWordCountByCollection(), selectCollectionProgress(collectionId, masteredWordIds)
- 22 tests covering structure, word reference validity, selectors, progress calculation
- Files changed: `vocabCollections.js`, `vocabCollections.test.js`
- **Learnings for future iterations:**
  - vocabulary.json has 8 INTERNAL duplicate Arabic texts: good_morning_1/sabah_al_khayr, goodbye_1/ma_a_salama, god_willing_1/inshallah, etc. — the PHRASE category versions (good_morning_1 etc.) appear EARLIER and win deduplication; GREETINGS category aliases (sabah_al_khayr etc.) are NOT in vocabularyAll
  - vocabularyAll.js default export is SYNC-ONLY (curated + vocabulary-final A1-A2) — expanded exp_* IDs require importing vocabularyExpanded.js directly
  - vocabularyExpanded.js uses DEFAULT export (not named VOCABULARY_EXPANDED) — import as `import VOCABULARY_EXPANDED from '../vocabularyExpanded.js'`
  - All exp_a1_* IDs are contiguous by category (greetings: 001-030, pronouns: 031-060, numbers: 061-085, etc.) — range helper works for all CEFR levels
  - Word reference validation test must check BOTH vocabularyAll AND vocabularyExpanded to cover all ID types
---

## 2026-04-07 - GROW-023
- Created `src/services/miniGameScaler.js` — 4 scaling functions using composite difficulty factor
- Difficulty factor = cefrNorm*0.6 + levelNorm*0.4 where cefrNorm=(CEFR score/5), levelNorm=(level/50)
- getWordSearchParams: gridSize 8-15, wordCount 5-15, allowDiagonals, timeLimit 120-300s
- getCrosswordParams: clueCount 5-20, gridComplexity 1-3, showLetterHints, timeLimit 240-600s
- getMemoryMatchParams: pairCount 4-12, timeLimit 60-120s, showPreview, previewDuration 1-3s
- getNumberChallengeParams: digitCount 1-4, operations (add→sub→mul→div unlocked progressively), questionCount 5-15, timePerQuestion 10-30s
- 24 tests covering all 4 functions, level boundaries, clamping, operation tier progression
- Files changed: `miniGameScaler.js`, `miniGameScaler.test.js`
- **Learnings for future iterations:**
  - C1 + level 50 gives difficulty 0.88 (not 1.0) — use C2 + 50 to reach exact maximum in tests
  - Player level 1 gives non-zero difficulty (1/50*0.4 = 0.008) — A1/level1 ≈ 299s not 300s
  - Test boundary values with `toBeGreaterThanOrEqual`/`toBeLessThanOrEqual` for computed clamp ranges; exact `toBe` only when formula is known-zero
  - Operations array uses progressive tiers (OPERATIONS_BY_DIFFICULTY) not linear scaling
---

## 2026-04-07 - GROW-024
- Added `playerSettings: { type: mongoose.Schema.Types.Mixed, default: {} }` field to `server/src/models/User.js`
- Added `playerSettingsSchema` (Zod `.strict()`) to `server/src/validation/userSchemas.js` — mirrors all 26 settingsSlice fields
- Added `getSettings` and `saveSettings` to `server/src/controllers/userController.js` — getSettings returns `playerSettings || {}`, saveSettings merges with last-write-wins, increments syncVersion, updates lastSyncedAt; `markModified('playerSettings')` required for Mixed fields
- Added `GET /settings` and `POST /settings` routes to `server/src/routes/user.js` — both behind authenticate; POST uses `validate(playerSettingsSchema)` middleware
- Created 11 tests in `server/src/routes/__tests__/userSettings.test.js`: empty GET, GET after POST, save+return, syncVersion increment, partial merge, unknown field rejection (strict), masterVolume >100 rejection, masterVolume <0 rejection, invalid battleSpeed, valid battleSpeed values, 401 without auth
- Note: ALL 14 server test files fail pre-existing with `Cannot find module '../../../src/data/items.json'` when run from `server/` directory — this is a pre-existing regression, not caused by this story
- Files changed: `server/src/models/User.js`, `server/src/validation/userSchemas.js`, `server/src/controllers/userController.js`, `server/src/routes/user.js`, `server/src/routes/__tests__/userSettings.test.js`
- **Learnings for future iterations:**
  - Mongoose Mixed fields require `markModified('fieldName')` before save — without it, Mongoose won't detect the change and won't persist
  - The `validate()` middleware calls `schema.parse(req.body)` and REPLACES `req.body` with validated output — controller should use `req.body` directly, not re-validate
  - playerSettingsSchema must use `.strict()` to reject unknown fields (the validate middleware calls schema.parse which throws ZodError for extras, returned as 400)
  - `syncVersion` and `lastSyncedAt` fields already existed in User model — no schema migration needed
  - Server tests must be run as a suite (not individually) due to `shopController.js` using `createRequire` for a path relative to the project root
---

## 2026-04-07 - GROW-025
- Created `server/src/utils/requestCounter.js` — module-level in-memory counter with `requestCounterMiddleware` and `getRequestCount()`
- Created `server/src/controllers/healthController.js` — `getHealth` (public) pings MongoDB via `mongoose.connection.db.admin().ping()`, returns {status, uptime, version, database, timestamp, responseMs}; `getDetailedHealth` (auth) adds {memory: {heapUsedMB, heapTotalMB, rssMB}, requestCount}
- Created `server/src/routes/health.js` — GET / (public) and GET /detailed (authenticate middleware)
- Updated `server/src/app.js`: imported healthRoutes + requestCounterMiddleware; registered `app.use('/health', healthRoutes)` and `app.use(requestCounterMiddleware)` BEFORE CSRF middleware so /health is always accessible
- 11 tests in `server/src/routes/__tests__/health.test.js`: 200 with required fields, valid ISO timestamp, no-auth accessible, responseMs field, <100ms response, DB connected (MongoMemoryServer), detailed 401 without auth, detailed with auth + memory + requestCount, all basic fields in detailed, requestCount increases per request
- Files changed: `healthController.js`, `health.js` (route), `requestCounter.js`, `app.js`, `health.test.js`
- **Learnings for future iterations:**
  - `mongoose.connection.readyState === 1` checks if connected; must guard before calling `.db.admin().ping()` or it throws
  - Register public endpoints BEFORE `app.use(validateCsrfToken)` to avoid CSRF blocking health checks from load balancers
  - `process.memoryUsage()` returns bytes — divide by 1024*1024 for MB
  - Request counter must be a module-level singleton (not per-request) — export a plain counter, not a class
---
