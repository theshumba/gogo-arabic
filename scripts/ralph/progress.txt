# Ralph Progress Log
Started: Wed  8 Apr 2026 04:37:33 BST
---

## Codebase Patterns

- **Middleware pattern**: Always call `next(action)` first, then dispatch side-effects. Pattern from `vocabRateMiddleware.js`.
- **Middleware test pattern**: Mock store with `{ getState: vi.fn(), dispatch: vi.fn() }`, mock `next = vi.fn((action) => action)`. Create middleware with `middleware = myMiddleware(store)(next)`. Check `store.dispatch` calls with `expect.objectContaining`.
- **Skill tree IDs**: `reading`, `writing`, `listening`, `speaking`, `grammar`, `culture` (from `SKILL_TREE_ORDER` in `data/skillTrees.js`).
- **Quiz completion hook**: `achievements/recordQuizTypeResult` — fires after every quiz session with `{ quizType, perfect }`.
- **Battle end hook**: `battle/endBattle` — payload `{ victory, accuracy, timeElapsed, rewards, bossId }`.
- **Conversation hook**: `conversation/completeScenario` — payload `{ scenarioId, score }`.
- **Pronunciation hook**: `phonetics/recordPracticeAttempt` — payload `{ id, score, type }`.
- **addSkillXP action**: `import { addSkillXP } from '../slices/skillTreeSlice.js'` — takes `{ treeId, amount }`.
- **Pre-existing test failures**: 7 test files fail before any changes (IndexedDB, dataset, HUD, antiFrustration, zoneReview, battle integration). Not caused by feature work.

---

## 2026-04-08 - FEAT-021
- Implemented `src/store/middleware/skillTreeXpMiddleware.js`
  - Listens for 4 action types: quiz, battle, conversation, pronunciation
  - QUIZ_TREE_MAP covers all 6 skill trees with 12+ quiz type mappings
  - Battle XP scales with reward XP (BATTLE_BASE_XP + 10% of reward XP, capped at BATTLE_MAX_XP)
  - Conversation XP scales with session score (CONVERSATION_BASE_XP + 0.2 * score)
  - Pronunciation awards fixed PRONUNCIATION_XP per attempt
  - All amounts in SKILL_TREE_XP_CONFIG constants
- Created `src/store/middleware/__tests__/skillTreeXpMiddleware.test.js` — 25 tests (14+ required)
- Files changed: 2 new files
- **Learnings for future iterations:**
  - `achievements/recordQuizTypeResult` is the right quiz completion hook (dispatched in useQuiz.js `next()`)
  - `addSkillXP` validates treeId against `SKILL_TREES` — must use one of the 6 valid IDs
  - "magic tree" in PRD maps to `grammar` (no magic tree exists; grammar is closest thematically)
  - "conversation tree" maps to `speaking`; "pronunciation tree" maps to `listening`
  - Mock the slice's action creator with `vi.fn(...)` returning the action object — lets dispatch calls be inspected by type/payload
---

## 2026-04-08 - FEAT-022
- Implemented `src/data/factionRewards.js`
  - `FACTION_TIER_REWARDS`: 30 reward sets (6 factions × 5 tiers: neutral/friendly/trusted/allied/revered)
  - Reward types: shopDiscount (5-25%), exclusiveRecipes (IDs), xpMultiplier (1.1-1.5x), zoneAccess (zone IDs)
  - `getFactionRewards(factionId, tier)` — accepts string label or FACTION_TIERS object
  - `getAccumulatedFactionRewards(factionId, score)` — cumulative rewards up to player's current tier
  - `checkFactionGate(factionId, requiredTier, playerState)` — gate check using Redux state
- Extended `selectFactionBonuses` in `src/store/slices/factionSlice.js`
  - Now takes `selectAlignment` as third input alongside primary/secondary faction IDs
  - Returns shopDiscount, zoneAccess, exclusiveRecipes, xpMultiplier in addition to existing XP multipliers
  - Secondary faction zone access/recipes unioned; discount takes max(primary, secondary*0.5)
- Created `src/data/__tests__/factionRewards.test.js` — 26 tests
- Files changed: 2 new, 1 modified
- **Learnings for future iterations:**
  - Existing FACTION_TIERS use labels Neutral/Friendly/Trusted/Allied/Revered (NOT the PRD notes' Novice/Initiate/Ally/Champion/Hero)
  - `getFactionTier(score)` already exists in factions.js — reuse it
  - Extending a `createSelector`: add to the input selectors array, update the result function signature
  - Edit tool requires Read first even for files created in the same session
---
